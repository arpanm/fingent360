# BEA-QUARANTINE-001 handoff

Authoring in codex/bea-quarantine-001 at f592e13. No tests/build/install/format/migration/services/provider requests/commit/push run. Parent integrates and verifies; all fixtures are authored only.

New files: contracts/src/bea-quarantine.ts; api/src/bea-quarantine.ts; web/src/BeaQuarantine.tsx; migration035_bea_quarantine.sql; API/browser/offline bea-quarantine.spec.ts; product/bea-quarantine.md; this handoff. Existing edits: API discovery.ts/research-providers.ts/migrate.ts, Operations.tsx, contracts index. No dependencies. Preserve SOURCE-REVIEW-DIFF and OPS-AUDIT changes during semantic merge. BeaQuarantine.onUnauthorized must use shared sessionExpired and its requests should use the shared guarded request wrapper at integration; no replacement of Operations wholesale.

Endpoints under /ops/discovery: GET bea-attempts (after cursor), GET bea-attempts/:id, GET :id/evidence, GET :id/history, GET bea-validations/:id, POST bea-attempts/:id/revalidate and POST bea-staging. Fixed source only, protected originals, strict request IDs and fingerprints. Source/body URLs cannot be provided. Migration035 creates immutable attempts/events/validations/staging receipts and a fixed admission gate; normal BEA ingestion and recovery staging both acquire the gate before sorted source heads. Revalidation accepted/rejected receipts persist and history is paginated. Run failure after a draft commit does not remove the atomic staged event.

API500 linked actual captured BEA response, validation and concurrent exact staging replay; API501 invalid retained XML and changed Mongo bytes; API502 changed source head prevents staging; API503 real gate wait with operator expiry401; API504 actual finalization-trigger failure after atomic staged draft commit. WEB500 protected evidence→Back→validation→stage and reload. OFFLINE510 connected-only and zero network. Test fixtures reuse captured BEA RSS through explicitly simulated transport to the actual ingestion store; no live fetch in authoring or these fixture helpers.

Manual parent/user: apply additive035 using existing migration command after contracts/API build; pnpm format and pnpm check; configured local PostgreSQL/MongoDB/operator key; pnpm dev; E2E_BROWSER=chrome pnpm e2e:ui select @BEA-QUARANTINE-001 api/desktop/mobile (watch off). Rebuild device web assets then offline510. Open /#ops → BEA recovery. Failure evidence: latest.md run ID/time/target/case, safe first assertion and isolated fixture annotation. Never share keys/cookies/private data. Root owns exact gate evidence and final commit hash.

Remaining external gates: physical-device acceptance, production source approval, broad provider adapters and role separation. Legacy raw blobs have no trusted attempt association and remain ineligible. A missing linked blob is unavailable, never replaced by a provider request. No public/offline snapshot content or financial data changes.

Additional authored coverage: API505 rejects invalid UTF8 and interrupted actual response streams without a retained link; WEB501 forces normal draft insertion rollback, then revalidates/stages the linked body and opens newly loaded Publishing review. The recovery component now requires request:typeof json and onReview:()=>Promise<void>; parent passes OPS-AUDIT guarded request, sessionExpired, and guarded load→Publishing. This refresh failure stays on the saved historical receipt with a retryable Open staged drafts action. Source bytes decode fatally with BOM preserved, avoiding lossy provenance. Monotonic event sequence preserves same-millisecond chronology. UUID canonicalization and strict page schemas prevent replay/query ambiguity.

WEB502 additionally loses an actual committed validation response, retries the exact request, checks one durable receipt, then expires the real operator session and verifies immediate protected recovery clearing. Existing parent tests must retain their isolated fixture scope; no credentials are written to artifacts by this feature.

Bounded-storage peer correction: Mongo retained reads and BEA persistence/read-back use driver-supported timeoutMS3000 plus maxTimeMS2500. Quarantine transactions and normal BEA staging use local PostgreSQL statement_timeout3s; explicit staging also checks a10s application deadline between candidate operations. A failed wait rolls back staging and leaves historical evidence/receipts intact. No simulated Promise.race leaves a background database operation holding admission locks.

Final UI acceptance additions: WEB500 reopens saved validation/staging history after full reload; WEB501 deliberately fails the post-stage list refresh503, verifies the historical staging receipt remains, then retries the actual list and enters publication review. WEB502 already covers a lost committed reply/exact replay and actual401 clearing. Deep history pagination and old-session401 across a later new login depend on the shared Operations barrier and remain useful additional parent regression selections; no pass is claimed here.

Final reserved cases: API506 creates31 actual rejected retained-response validations and traverses both bounded history pages without omissions/duplicates. WEB503 holds an actual401 evidence response, navigates Back, releases/drains it and requires the shared parent Operations request barrier to clear the same session. This case explicitly requires OPS-AUDIT integration; the older isolated base does not contain that barrier. Final authored selection: API500–506, WEB500–503 on desktop/mobile, OFFLINE510. No execution.

WEB503 timing correction before refreeze: evidence is held before fetching; Back occurs while the operator is valid, ordinary API reads drain, and only then is the owned session expired. The actual401 is held once more while asserting Publishing is still authenticated immediately before denial delivery. This prevents an unrelated list401 from satisfying the test.

Final peer correction: both API and browser BEA quarantine specs disable trace/video/screenshots because operator login uses the actual local credential. WEB502 now captures and runtime-validates both actual201 receipt bodies and compares exact equality across the deliberately lost first reply and replay, alongside identical request input and one durable database row. No execution.

## Parent integration state

Integrated with the shared guarded request, sessionExpired callback and guarded load→Publishing; source comparison and reading/audit registrations remain intact. Migration035 follows032 in the additive ledger. All code/cases are authored and unverified; no migration/provider/build/test/commit execution follows the latest instruction. User runs build/migrate prerequisites and pnpm sdlc. Legacy evidence is not retroactively linked, and the installed APK has not changed.

## REGRESSION-011 — responsive recovery and receipt semantics

Authored on main baseline b5cfcd0 after the user-reported run2026-09-14T08-28-33-631Z-98876 (started2026-09-14T08:28:34.851Z; API4103/web5175;534 selected attempts). WEB500/501 reported two simultaneous unnamed statuses; mobile WEB500–503 reported timeline paragraphs intercepting normal Inspect clicks. Those are failure observations, not passes for these edits.

Changed files: apps/web/src/BeaQuarantine.tsx, new apps/web/src/bea-quarantine.css, tests/e2e/cases/browser/bea-quarantine.spec.ts and this handoff. Recovery now has scoped content-sized rows, wrapping hashes/identifiers and wrapping action groups. Active requests announce their actual operation through the named Recovery progress status. Historical validation remains dated review context; only a validated staging response produces the named Draft staging receipt and publication-review action. Staging still does not publish. Existing guarded requests, exact replay, history and source review are reused; no API/contracts/database/dependency/provider/offline-content change.

WEB500–503 preserve stable IDs and add @REGRESSION-011. Every Inspect action checks region bounds and that all timeline content ends above its control, then uses an ordinary pointer click. WEB500 holds an actual201 staging response from the explicitly owned feedbackSandbox.apiOrigin, validates its schema, asserts progress plus historical context without a premature staging receipt, then releases/drains the response and asserts the saved receipt. Reloaded history remains historical context. WEB501 retains the actual queue503/retry/history/publication-review flow; WEB502/503 retain actual session-expiry and late-denial barriers. Sensitive automatic trace/video/screenshots remain disabled.

Manual next actions: dependencies and migrations are unchanged. Keep the configured PostgreSQL/MongoDB and API/web services available at http://127.0.0.1:4103 and http://127.0.0.1:5175. After user-run format/check gates (or the registered user pnpm sdlc workflow), select WEB500–503 for desktop and mobile: `pnpm e2e:run --project=desktop --project=mobile --grep='E2E-WEB-50[0-3]\b'` (eight project selections). Manually inspect /#ops → BEA recovery at normal desktop/mobile widths, tab through controls and confirm long hashes, retained RSS and history buttons remain readable and reachable. Expect normal Inspect clicks, accurate progress, dated receipt only after the actual response, persisted history, queue recovery and protected clearing on401. Physical-device and visual acceptance are separate. Report failures with the new run ID/time, targets, case/project, first assertion and error-context path; omit credentials and protected body text.

No execution, format/check, browser verification, services, DB actions or commit by this author. These edits await the user's gates; local commit remains b5cfcd0. Root owns concurrent README/TODO/status/catalog/coverage/regression documentation and database/OPSREAD corrections; the report comparison author owns its separate UI/cases. Those unrelated changes were preserved.
