# CONNECTION-REVIEWS-001 integration handoff

## Integrated parent result — 2026-09-14

The complete child now includes API310–314, WEB310–313 desktop/mobile and OFFLINE340–343. Root integrated all prerequisite receipts/privacy fields, applied027 after025, added the Research connections entry link, and verified54/54connected plus7/7packaged offline selections. Gates passed110units; documentation-inclusive gates precede the scoped commit. Desktop/mobile captures and keyboard/response races were checked. Main account/financial digests are unchanged and owned test resources are zero. See status for exact run IDs, logs, manual commands and APK/physical acceptance limits. The following records historical authoring and does not replace this verified integration result.

Authored in isolated worktree from0069aeb. No gates/install/build/migrations/services/commits executed. Dependencies: AUTH-WAIT and EVIDENCE-LINKS-001 must be integrated first. This branch references sibling research-connections contracts but intentionally does not copy those files.

New files: contracts/connection-reviews.ts, migration027_connection_reviews.sql, APIconnection-reviews.ts, webConnectionReviews.tsx, offlineconnection-reviews.ts, product spec, unit and API310/311 WEB310 OFFLINE340. Shared additions: contractsindex/privacy,APIapp/migrate/privacy,webApp/navigation/AccountGate,offlineindex/accounts. Preserve later comparisons/private-report/research-connection export fields during merge.

Integration UI route #connection-reviews is under More/My money. Parent must add a Review inbox link to the dependency's ResearchConnections header (file absent in this branch); inbox links to that screen for authoritative source/receipt context, never directly to potentially withdrawn source URLs. Current source availability is validated by that screen. No source text/title copied into notices.

Current notice state is coalesced by deterministic fingerprint. Saved operation receipt replay is not a current inbox response; UI marks old context unavailable until an authoritative GET succeeds. Same-state check preserves acknowledgements, changed states reopen, connection removal/current reaffirmation resolves. Acknowledgement never mutates connections or finances. Account lock precedes sorted source locks, then wall-clock authentication is rechecked. API head query excludes historical removed connections without unresolved retained notices and bounds evaluation400; offline iterates existing receipt history without materializing its entire latest set. No provider calls.

Retention is explicit in UI/spec:200 resolved notices/100 evaluations;1000 operation receipts within30days, replay after TTL starts a fresh operation. Export exposes retained notices/check/operation receipts. The request-receipt limit rejects rather than silently dropping unexpired IDs. Original connection/financial receipts are untouched. New migration is additive with account cascades.

Parent manual gates: pnpm format; pnpm check; pnpm db:migrate; pnpm dev. E2E_BROWSER=chrome pnpm e2e:ui select @CONNECTION-REVIEWS-001 API/desktop/mobile, watch off. E2E_BROWSER=chrome pnpm android:test:ui select OFFLINE340 after package rebuild. Needs isolated Postgres fixture privileges and real bundled permitted source. API310 tests copied actual source plus explicitly synthetic editions, ack/coalescing/reopen/removal/foreign/stale/privacy/unchanged finances. API311 tests actual concurrent checks/acks. WEB310 tests manual UI/filter/persistence/503 unknown context/mobile. OFFLINE340 tests dated bundle and local changedholding notices/noAPI/export/delete. Physical Android/TalkBack remain explicit later acceptance. Report actualrunID and artifacts/e2e/latest.md on failure.

Inbox notices also open the actual owned latest connection history receipt inline, validated by ResearchConnectionHistorySchema; focus moves to its heading and Close returns to the originating button. Receipt displays the user's note and minimal dated source receipt, never source article text. Current publication remains a separate authoritative connection-context navigation. WEB310 asserts this real owned receipt path.

Backend regression additions (authored, not executed): API312 constructs an isolated synthetic lifecycle with 200 resolved removed connections, 200 newly removed open connections, and 200 active changed-source connections. A real check must resolve/prune the old cohort and produce exactly 400 notices without changing financial records or 1,000 immutable connection revisions. The evaluation query includes active heads and unresolved notices only, preventing old resolved history from blocking future checks. API313 holds the actual final source lock, waits for the API transaction to block, expires the owned session, and verifies 401 with zero inbox/request writes after release. API314 exercises the 1,000-receipt boundary, retained replay at capacity, rejection without inbox mutation, 30-day pruning, fresh reuse after expiry, and cross-operation request-ID conflict. Source-lock expiry depends on integrated AUTH-WAIT clock_timestamp authentication.

Parallel UI regressions WEB311–313 and offline additions are owned by the designated reviewers; incorporate their final manifests before treating the complete feature as frozen. No local commit was made by this worker. Parent owns the scoped integration commit after authorized gates.
