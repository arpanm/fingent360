# Permitted watch of retained original financial filing URLs

## Specification — 2026-09-15

Reuse the25 primary NSE rendered IndAS URLs and identities independently researched in SRC-005-COHORT. Move only source identity/URL/parser into a server-owned registry, not test expectations or invented live values. This is a watch of specific historical originals for corrected bytes, not discovery of new releases or a claim of latest accounts. Dates in filenames do not become original publication timestamps.

An administrator selects registry entries, records actual NSE contributor retention/display/offline permission and explicitly enables a default-disabled gate. Existing research scheduling owns acquisition; a bounded batch of three sources rotates fairly across selected entries. Fixed HTTPS origins and exact source paths only, no redirects/user URLs. Keep all obtained original bodies including rejected layouts. Unchanged source/parser/rights reuse their existing edition; changed bytes create a new draft and retain previous publications until independent correction/review. Transport failures and unsupported layouts are recorded per-source without blocking other sources. Retrying rotates safely, with durable run/status evidence. A permission/schedule change while fetching prevents final admission; gate/schedule lock order is consistent.

Reuse existing equity edition/observation tables and company reader/offline payload. Draft author is the administrator who configured permission, so that actor cannot self-publish; named independent review is always required for these sources. Re-admit current source permission at publish and public-read time; revoked permissions hide connected source records, while immutable history remains inspectable privately. Do not automatically publish findings or replace an issued observation.

New127 tables store gate selections/version/cursor and bounded-run source attempts linked to existing equity editions. Operations shows selected original URLs, rights, worker control and per-source draft/quarantine/unavailable/unchanged outcomes; it links directly to existing financial-source review. Original-publication time remains unknown; capture time is actual. Native uses the same connected workflow; offline cannot configure/acquire and reads only downloaded admitted company records.

Cases1990–1999: actual research tick/configuration with upstream-only simulation; unchanged/corrected/unsupported/transport outcomes; permission withdrawal while awaiting source; independent review and actual company projection; browser gate/form/results/review route; offline refusal/provenance. All are authored without execution. No new source rights request is repeated: actual permission is a later operator input, not a blocker to code authoring.

## Authored delivery and manual acceptance

API1990 exercises the actual research-auto tick with only upstream transport simulated: partial results, exact raw retrieval, unchanged-body reuse, corrected-body draft, independent review and permission withdrawal removing company/list/snapshot admission. API1991 changes permission while acquisition awaits its source and rejects unregistered source selection. API1992 verifies keyset continuation with22 equal-timestamp attempts. WEB1990 submits the real gate and schedule controls, inspects and independently publishes the watched original, and follows its real company link. OFFLINE1990 preserves captured provenance and rejects Operations changes; an offline device remains a dated downloaded snapshot until refreshed.

Source rights are a separately administered input and no actual watch has been activated. Existing original-source research in [SRC-005-COHORT](../tasks/SRC-005-COHORT.md) supplies the fixed registry; these are historical source URLs. New-release registry discovery is still unimplemented. Original XBRL instance/taxonomy bytes and unsupported financial statement sections remain as recorded in [SRC-004](../tasks/SRC-004.md), rather than guessed parser support.

User-owned validation: no dependency changes; PostgreSQL, MongoDB and API/web development services are required. Apply `pnpm db:migrate`, start `pnpm dev` if needed, then `pnpm sdlc "Validate original filing watch" -- --grep "E2E-(API-199[0-2]|WEB-1990|OFFLINE-1990)"`. Use the printed web URL → Operations → Original filing watch / Automatic research. Expected: all selected API, desktop/mobile and offline cases pass. For a failure provide saved run ID, exact case and error excerpt. No formatting, checks, builds, migrations, services or tests were run by the agent; no commit was made pending the manual gates. Baseline commit remains `a2c53a0`; concurrent repository changes are preserved.

## Scoped WEB1990 repair — 2026-09-18

Automatic research now loads its schema-validated schedules on section entry instead of requiring an undocumented first click on Refresh research schedules. The refresh/recovery action remains available, and a generation guard prevents an older initial request from overwriting newer state. WEB1990 explicitly waits for the `equity-filing-watch` schedule heading before using its enable control, then retains the real schedule, worker, review and company-projection assertions. Saved run `1789723036823-6f833c8e-c99d-49f7-bc1c-b71fad7bd5fc` passed this repaired path on desktop. The supplied mobile failure points to committed pre-repair line 59, where the case clicked the absent control without first requiring loaded schedule state; the repaired mobile path remains unverified. No API, storage, source-rights or migration behavior changed.
