# Connected API audit — 2026-09-19

Scope: repair the 30 distinct API failures in the saved connected run `artifacts/sdlc/1789752953639-97020/06-pnpm-e2e_run.log`, without executing validation. The latest handoff is a later offline run (1789760139774-98edc942-99ee-445a-b31e-fb270cec37af, started 2026-09-18T19:35:39.774Z, 202 selected/completed, failed, web port61142); it does not supersede connected failure evidence. Saved logs are evidence only. Parent owns consolidated task indexes and browser/offline work.

Acceptance: retain real endpoint assertions, independent review, strict contracts, encrypted private fixtures, immutable source revisions and fail-closed source admission. Author repairs and regressions, preserve saved failures, and hand off selected manual validation. No new feature or source activation is authorized by these fixtures.

Initial failure groups: handler-level named Operations read denial; obsolete legacy publication fixtures; PostgreSQL JSON-array fixture serialization; encryption-era private fixture drift; bond receipt reconstruction; WhatsApp delivery/storage/immutable scheduling fixtures; report preparation classification. API1850/1851 Cleveland strict source-parser repair is coordinated with the parent.

## Authored repairs and saved case mapping

| Saved API cases               | Root cause and authored correction                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ----------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1960,1670,1624,1673,1900,1920 | `OperatorPermissionGuard` recognized `@OperatorRead` only on a controller, although these queues/evidence routes annotate their handlers. Authenticated GETs returned403; the rating test then compared an error response’s missing body with the PDF. The guard now checks handler and controller read metadata, still requires named read permission, and does not enable mutations. Unit regressions cover handler reads, denied identity and mutation denial. Rating evidence now asserts HTTP200 before examining source bytes.                                                      |
| 1850,1851                     | Retained Cleveland originals have known chart decoration keys absent from the strict provider schema. Parent owns explicit-key schema/regression repair; unknown fields remain rejected.                                                                                                                                                                                                                                                                                                                                                                                                  |
| 906                           | `publishIndas` used legacy bearer/self-publication although the source parser requires independent named review. The helper now creates named preparer/reviewer sessions, asserts self-review403, publishes through the separate reviewer and disposes its context. API906 and coordinated WEB906 run in a scoped named-mode describe block.                                                                                                                                                                                                                                              |
| 1620                          | Original BEA discovery setup called the explicitly blocked legacy publication route in named mode. Setup now creates a discovery publication proposal, rejects self-approval and uses independent approval before evaluating the original SPF/BEA comparison.                                                                                                                                                                                                                                                                                                                             |
| 1433                          | Historical flows setup called the blocked legacy event review route. It now uses the existing `publishNamedEvent` helper, including legacy denial, self-approval denial and exact independently published event-version assertions.                                                                                                                                                                                                                                                                                                                                                       |
| 1880                          | `pg` serializes a JavaScript array as a PostgreSQL array, not JSON. Calendar fixture insertion now serializes its events with `JSON.stringify` for the retained JSON column. Shared browser calendar setup uses the same corrected helper.                                                                                                                                                                                                                                                                                                                                                |
| 264                           | `payload=payload` on an encrypted revision is a no-op with a null plaintext field and is allowed by the encrypted-rewrap trigger. The test now actually changes the immutable content hash and still requires rejection; deletion rejection remains. No trigger is weakened.                                                                                                                                                                                                                                                                                                              |
| 276                           | The post-recovery database assertion expected plaintext from an encrypted revision. It now asserts the stored plaintext is null and decrypts through the actual owner/hash-checking helper with the sandbox’s keys before comparing the original immutable receipt. Session-revocation/401 assertions remain.                                                                                                                                                                                                                                                                             |
| 250,253,254,255,256,257       | Retention setup appended plaintext image/audio columns to a newly encrypted feedback row, violating the database exclusion constraint. It now submits an actual valid generated PNG through the API and changes only expiry. The existing API feedback PNG generator was extracted unchanged into `helpers/feedback-image.ts`, with no import-time actions. Cleanup assertions include null encrypted payload. Capacity setup uses explicitly labelled synthetic legacy rows rather than copying an encrypted envelope to a different ID; original encrypted feedback still participates. |
| 378                           | The in-process Assistance controller’s AccountStore omitted the sandbox encryption keys, so account identity decryption failed before the delayed-source check. It now receives the existing sandbox keys; actual delayed withdrawal and suppression assertions are unchanged.                                                                                                                                                                                                                                                                                                            |
| 1600–1604                     | The admitted authored glossary source has `sourceHash:null`, as permitted by the canonical FeedItem contract, while WhatsApp job payloads required a string. Zod rejection inside the account transaction surfaced as503. WhatsApp now reuses the canonical source-hash schema: null for authored glossary or an exact64-character hash. The existing source allowlist, exact version comparison, encryption, consent and withdrawal gates remain. API1600 explicitly records the null-hash glossary precondition.                                                                        |
| 1605                          | The concurrency fixture checked out the only connection from a pool configured with `max:1`, then tried polling/revoking through that same exhausted pool. A separate owned observer pool now inspects the real blocker and expires the session. No wait timeout or expected401 was relaxed.                                                                                                                                                                                                                                                                                              |
| 1690                          | The scheduling fixture updated `discovery_versions` in place, violating the append-only trigger. Its synthetic publication clock is now chosen before the first retained fixture insertion through an optional `whatsappSource` argument. The actual schedule/outbox assertions and source exclusion remain.                                                                                                                                                                                                                                                                              |
| 359                           | Parent-owned repair: malformed decoded report snapshots raised ZodError inside AccountStore’s transaction and were converted to storage503 before worker classification. The worker now preserves a typed preparation failure only for that schema error; API359 retains retry/reconstruction and added row-state assertions, with API360 as the real storage-failure companion.                                                                                                                                                                                                          |

The connected run began `2026-09-18T17:36:54.520Z`, selected1293 tests and used one worker. The table covers all30 distinct `[api]` failures in its final failure blocks. No failure record is marked passed by these edits.

Saved browser trace inspection additionally confirms WEB1901 failures are GET`/api/v1/ops/fund-mergers`, and WEB1921 failures are GET`/api/v1/ops/sovereign-bonds`; their capture/review methods already declare prepare/approve permissions. The saved corporate-rating browser trace is not a readable ZIP, so the evidence for that shared repair is its method-level read declaration and the API1960 evidence GET failure. WEB1256 is a separate frontend transport defect: omitted POST made `json` attempt GET with a body; the browser agent owns that correction. These distinctions avoid weakening mutation permissions to cure read failures.

## Layers and remaining work

Contracts: canonical WhatsApp hash shape and historical credit admission-window reconstruction repaired; source schemas stay strict. API/workflow: named GET admission fixed with identity checks retained. Database: no schema or migration changes; fixtures respect encryption exclusion and immutable revisions. UI/UX: shared helper effects are coordinated with the browser owner; this audit does not claim keyboard/mobile/visual acceptance. Real data: original source fixtures and explicit simulations remain labelled; no provider/source/channel activation. Automation: no new job or background execution. Documentation: this RCA and parent-owned SDLC-REPAIR-016/index/matrix records preserve authored versus verified status.

No diagnosed API cause remains deliberately unpatched across the coordinated authors. Every repair is unverified; later checks may expose additional downstream failures. No new private input, package installation, source permission or migration is required for these changes. Existing source activation gates remain separate.

## Manual handoff

Use the consolidated user command: `SDLC_AUTO_REPAIR=0 pnpm sdlc "Repair full audit failures" --story SDLC-REPAIR-016`. It owns formatting, checks, gated commit and the saved-failure matrix plus API360. API-only diagnosis after those gates can use `pnpm e2e:run --project=api --grep "E2E-API-(1982|1960|1670|1850|1851|906|1624|1673|1900|1620|1433|1880|264|276|250|253|254|255|256|257|378|1920|1600|1601|1602|1603|1604|1605|1690|359|360) "`. The unchanged PNG generator extraction also affects existing API191/192/193 attachment assertions; run `pnpm e2e:run --project=api --grep "E2E-API-(191|192|193) "` if separately validating that shared fixture.

Prerequisites: existing configured PostgreSQL/MongoDB, current migrated schema, and user-started API/web services from `pnpm dev`; no dependency changes. UI uses the printed web origin at `/#ops` for source queues/support and `/#account` for WhatsApp settings. Expect actual named review, exact source evidence, encrypted retention/deletion, reconstruction and race outcomes asserted by the selected cases. On failure report the saved run ID, case/project, full error and sanitized response/server diagnostic path; a generic503 alone does not establish a new root cause.

No tests, formatting, checks, builds, installation, migrations, service operations, SDLC, commit or push ran. HEAD inspected: `8b5d817` (`Audit all pending validation`). Pre-existing generated task/bug/validation changes and other agents’ coordinated edits are left uncommitted; this branch awaits the user’s gates. No saved pass was fabricated.
