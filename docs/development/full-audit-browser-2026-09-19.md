# Connected browser audit — 2026-09-19

Authoring scope: diagnose saved connected failures in `artifacts/sdlc/1789752953639-97020/06-pnpm-e2e_run.log` and its page snapshots; repair browser selectors/setup or demonstrated UI defects without removing assertions or bypassing authorization. No deterministic validation is authorized. The latest handoff is the later offline run1789760139774-98edc942-99ee-445a-b31e-fb270cec37af, started2026-09-18T19:35:39.774Z,202 selected/completed attempts, failed, on-device web61142; it does not replace connected evidence.

Acceptance remains actual source capture/review, private storage, consent, exact persisted values, navigation, desktop/mobile and recovery. API/contracts/database are reused except independently diagnosed repairs owned by the API agent. External integrations remain synthetic in tests; no live sends/provider activation. Existing task IDs and test IDs remain stable. Authoring is not a passing result; saved generated validation and bugs remain untouched.

## Root causes and authored repairs

| Cases (desktop and mobile)                           | Saved root cause                                                                                                                                                         | Authored repair / owner                                                                                                                                                                                                                                            |
| ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| WEB1542,1020,1260,1380,1390,1394–1396,1681,1710,1831 | Exact `getByLabel` on a wrapping select label includes option text, while saved accessibility snapshots expose the intended combobox name.                               | Browser specs select the exact accessible combobox; preserve exact option values, capture, publication, numerical, history, withdrawal and recovery assertions. Scenario-family/original-format selectors use the same role to prevent the next identical failure. |
| WEB1053                                              | Substring Calendar capture also matches Policy calendar capture.                                                                                                         | Exact combobox role/name, preserving history and injected failure/retry assertions.                                                                                                                                                                                |
| WEB1070                                              | Case seeds the isolated API but never routes calendar reads there; snapshot shows actual application calendar instead of its fixture.                                    | Route only research-calendar reads to that test's API; exact source/capture combobox names. Other public requests retain existing behavior.                                                                                                                        |
| WEB1360                                              | Operations tab was renamed Sector mappings; saved page has no Classification crosswalks button.                                                                          | Use actual Sector mappings navigation, preserving independent review/history assertions.                                                                                                                                                                           |
| WEB1730                                              | Reader correctly states board approval is not **its** publication timestamp; test expects old wording.                                                                   | Assert current exact caution while keeping ratio, source and withdrawal checks.                                                                                                                                                                                    |
| WEB1080                                              | Portfolio disclosures are now supported; the old global unsupported message is obsolete. Saved page explicitly reports no admitted mapping for the selected test scheme. | Assert that specific missing-mapping state in Fund portfolio disclosure, retaining NAV/source/back checks.                                                                                                                                                         |
| WEB1320                                              | Unscoped status text matches multiple elements.                                                                                                                          | Scope reconciliation assertion to the opened June2026 vintage details; retain exact index/publication-cutoff/recovery checks.                                                                                                                                      |
| WEB661                                               | Private account request handling deliberately normalizes503 to a safe temporary-unavailable message.                                                                     | Expect that current user-facing message; retain distinct injected outage, immutable saved receipt version2 and unavailable-current-context assertions.                                                                                                             |
| WEB1420,1450,1490                                    | Holdings did save; snapshots show Holdings saved / Saved revision1 and the exact saved ISIN, not the old combined sentence.                                              | Assert current saved revision and actual Saved holdings ISIN; secondary agent reviews storage values and reload.                                                                                                                                                   |
| WEB1600,1690                                         | `route.continue` cannot install browser cookies via a Cookie header, so real account endpoints return401.                                                                | Install the real account session in the browser cookie jar, preserving path/httpOnly/sameSite and browser origin; existing isolated account route is reused. Secondary agent owns final consent/selectors/persistence review.                                      |
| WEB906                                               | IndAS fixture attempted publication with legacy actor while parser requires named independent review.                                                                    | Browser case enables named operators only in its own describe block and passes playwright/sandbox to the API agent's repaired `publishIndas` helper; numerical assertions remain unchanged.                                                                        |

## API and fixture dependencies

The API/shared-helper agent owns the following roots; browser tests must retain their original checks and pass only after the corresponding real repair:

- WEB1622,1624,1672,1673,1850: saved page becomes Operations sign-in after protected expectation queue denial. Shared operator guard must honor method-level read metadata without widening write permissions.
- WEB1901,1921,1961: real import response says the operation is not enabled for named roles. Repair explicit route admission while preserving source review separation.
- WEB1256 is resolved at the frontend call site below; API1257 already supports POST.
- WEB250,252,253: retention setup writes plaintext attachment columns into encrypted rows, violating `feedback_encrypted_plaintext_exclusion`; repair fixture encryption, not the constraint.
- WEB1881: `reading-calendar.ts` passes a JavaScript array as PostgreSQL json rather than serialized JSON; fix the helper's source fixture.

A distinct production UI defect was identified for WEB1256: `FeedbackEncryption` called the JSON helper with a body but omitted its method, which defaults to GET. Browser fetch rejects GET-with-body before reaching the API. The component now explicitly submits POST; its browser case observes real POST201 and retains confirmation/result/reset assertions. See DEV-017 for full acceptance and manual handoff. No production UI change was justified by the other selector/copy failures. Public reading share WEB1255's existing denial test remains valid: its Share button renders for any canonical link, and the click handler reads `navigator.share` at click time; the capability override is not cached at mount. It is not among the new failed pairs.

## Remaining limitations

All repairs are authored, not executed. Saved bugs remain open until their exact case/project passes. The API-owned causes above and final broker/WhatsApp review are integrated by the parent; this document does not independently certify them. Source permissions, actual WhatsApp delivery, public deployment and physical-device behavior are not established by synthetic acceptance.

## Manual validation

Use current configured migrated databases/API/web services and user-invoked SDLC only. Dependencies unchanged in this browser slice. No tests, formatting, checks, builds, service changes, browser verification or commits were executed. The parent owns task/index reconciliation. The consolidated saved-failure matrix is `SDLC-REPAIR-016`; user runs `pnpm sdlc "Repair saved full audit failures" --story SDLC-REPAIR-016` once the full authoring batch is integrated. Connected UI uses the current `pnpm dev` printed URL (saved connected target http://127.0.0.1:5176); navigate through Operations, More, holdings and source readers as each case specifies. Expect every selected case/project to pass, with no silent skip counted as acceptance. On failure report case/project, run ID, redacted log and error-context path. Native/device activation remains separate.

## Combined static review follow-up

Scenario selectors after source choice match `EventScenarioOperations`: Scenario family is a combobox, editorial and review reasons are labelled textareas, extraction/save/review controls and notices retain their asserted text. A downstream isolation gap was found: the shared event fixture forwarded events but not the published scenario/detail/history API, so FED/RBI/BEA/company scenarios would query the unrelated dev database after publication. The event fixture now forwards both events and event-scenarios to its real isolated API. WEB1020 already had a case-local scenario route; that remains compatible. No new scenario skip/mock was introduced. Offline scenario fixtures now reconcile event publication and reviewed graph edges before schema parsing; offline calendar selectors use the actual combobox role.

WhatsApp browser session cookies preserve the API's `/api/v1/account` path, httpOnly and sameSite fields while mapping the domain to the web origin. Cookies are not port-scoped; the `app-fixture` account route continues the real request to the isolated API on the same hostname and cookie-bearing path. Existing API request context retains its own verified-owner cookie for synthetic webhook setup. No `Cookie` header override is used. Remaining broker/WhatsApp acceptance enhancements are owned by the secondary browser reviewer.
