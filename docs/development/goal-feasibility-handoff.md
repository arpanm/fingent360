# GOAL-FEASIBILITY-001 handoff

Authored only; no tests, format/check, builds, installation, migration, service, provider request or commit executed. Root registered the DEV-009 continuation before implementation. Existing unrelated working-tree changes remain preserved. Root confirmed base HEAD b5cfcd0; no commit is claimed by this child.

## Delivered layers

- Product contract: `docs/product/goal-feasibility.md` specifies explicit downside-capacity policy, missing inputs, exact assumptions, no-growth comparison and education limits.
- Strict contracts/calculation: `packages/contracts/src/goal-feasibility.ts`, exported through index. INR paise/BigInt, unknown versus0, interruption bounds, reserve deficit, ceiling shortfall, captured goal/version and independently reconciled response.
- Database/API: additive037, `apps/api/src/goal-feasibility.ts`, registered controller. Account-first goal locks, post-wait authorization, optimistic version validation, exact request replay,100 retained capacity with explicit deletion freeing capacity, immutable original payload/tombstones, account cascade. No goal/holding/allocation writes or provider calls.
- UI: `GoalFeasibility.tsx` embedded in `Goals.tsx`, explicit choose→unknown or entered rupees/months→review→consent→save→historical detail→remove. Back preserves inputs; uncertain save retries exact request; definitive conflict releases intent so user can reload goals and review again.401 clears private parent and child only for the mounted originating session; list and mutation generations prevent late overwrite. Reload/delete cannot run during an open draft. Confirmed save appears immediately without requiring anotherGET.
- Local: `offline/goal-feasibility.ts`, same exact calculation and real serialized persistence. SHA256 request fingerprints do not retain deleted private input JSON. Parent integrates dispatch, account cascade and privacy registration.
- Export: at most100 complete visible receipts plus explicit `deletedRequestCount`; internal anti-replay IDs/fingerprints are not exported as a purported full history. Deleted financial bodies removed, markers survive until account deletion. Root integrates API/local privacy helpers and strict export property.
- Tests: units exact82000 total/18000 gap, ceiling/reserve, unknowns, horizon endpoints, above-safe-integer arithmetic and tampered receipt. API600 ownership/replay/deletion/immutable trigger/account cascade; API601 strict input/unknown/stale/Origin; API602 actual capacity boundary with explicitly seeded owned valid receipts. WEB600 real keyboard/mobile review/back/save/reload/remove; WEB601503 recovery and unknown assessment. OFFLINE600 actual local save/reload/export/account deletion and zeroAPI traffic. New shared browser helper sets up only selected-test owned accounts. Cases authored, not executed.

## Manual acceptance

User prepares existing database/app services; migration037 must be applied after building the changed API. No new dependencies. Normal user workflow remains `pnpm sdlc "Complete goal downside capacity"` with its format/check gate before commit; then selected E2E projects as configured. Alternatively select `@GOAL-FEASIBILITY-001` API/desktop/mobile in `pnpm e2e:ui`, watch off; run offline600 with the existing packaged offline runner. Open the URL printed by `pnpm dev`, route `#my-goals`, and verify entered80INR ceiling/3months interruption/100INR protected savings against a synthetic goal with200INR saved/100INR monthly/1000INR target/12months gives820INR downside total and180INR gap. Saved goals and holdings must remain unchanged. Report safe first failing assertion, run ID and project; never attach real private inputs.

No subjective suitability, expected returns, probabilities, inflation forecasting, asset-liquidation values or advice is delivered. This child supplies the explicitly chosen contribution/horizon/downside-capacity feasibility, not regulated investment suitability. Physical-phone/design acceptance and user-run gates remain unverified. Root owns TODO/README/CATALOG/status updates and records final commit evidence.
