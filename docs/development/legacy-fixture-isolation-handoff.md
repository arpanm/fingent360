# Legacy source fixture isolation handoff

LEGACY-FIXTURE-ISOLATION-001 / DEV-021. Authored only; no deterministic execution or commit. Base commit `6f2b50821d68b76bd6356efce7a2db258c643fee`. The worktree was clean before this child; all six listed files belong to this scope. Root owns integration and shared tracker updates. The user owns format/check, builds, tests, services, migrations and the gated commit; no push.

## Exact manifest

1. `docs/development/legacy-fixture-isolation.md` — specification, boundaries and layer acceptance.
2. `docs/development/legacy-fixture-isolation-handoff.md` — this handoff and remaining suite inventory.
3. `tests/e2e/helpers/source-fixture.ts` — dedicated real isolated API/database fixture, exact public routes and normal source digest guard.
4. `tests/e2e/cases/api/sources.spec.ts` — API070 uses owned storage and checks all three retained revisions.
5. `tests/e2e/cases/browser/sources.spec.ts` — WEB070 keeps its full real UI flow and checks both owned persisted revisions.
6. `tests/e2e/cases/browser/source-journeys.spec.ts` — only WEB181 changes to the owned fixture, browser-cookie login/logout and actual glossary run/edition assertions. WEB180/182 retain their existing context.

No runtime product files, shared app/feedback fixture changes, migrations, dependencies, provider fixtures, new test IDs or offline changes. API070, WEB070, WEB181 are three stable IDs: one API selection plus four desktop/mobile selections, five total when explicitly selected. All three carry the additive `@LEGACY-FIXTURE-ISOLATION-001` tag and retain their old tags. They retain trace/video/automatic-screenshot suppression because they use the actual operator key.

## Integration interface and evidence

The source fixture extends `feedback-fixture`, so protected `/ops` and feedback routing, actual migrations/process, random PostgreSQL schema, matching Mongo namespace and exact owned teardown remain reused. Its only additional browser route matches are the two exact public paths `/api/v1/sources` and `/api/v1/discovery/catalog`; it does not capture feeds, item evidence/history, macro or private account routes. OPS-READ-ADMISSION has no dependency on this helper and edits different cases/runtime files.

The normal guard opens the configured loopback PostgreSQL URL, preserves its configured namespace, rejects the owned namespace, and compares six source-table row digest sets in separate read-only snapshots before/after the selected case. Raw fields and credentials never become expected-output artifacts. No normal cleanup/restore is attempted. This establishes only unchanged source/registry rows for the bounded selection, not every application table or an unrestricted suite. Normal PostgreSQL must already be migrated and correspond to the configured application; a storage failure is reported honestly. Concurrent normal source ingestion/publication makes the guard fail, even if another actor caused it.

WEB181 calls only the existing authored glossary synchronizer, whose branch builds local glossary items without an upstream fetch or Mongo raw write. It checks a single successful glossary source-run and the exact persisted edition count in its owned schema. No external rights, current feed or real-provider validation is asserted.

## Remaining global-suite inventory

This read-only inventory records concrete remaining gaps; they were not rewritten, skipped or executed by this child. No unrestricted full-suite safety claim is made.

| Existing cases/files                                   | Concrete remaining normal-storage or provider behavior                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| API181 in `api/research.spec.ts`                       | Actual normal glossary refresh writes discovery heads/versions/run receipts; separate from migrated WEB181.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| WEB130–132 in `browser/discovery.spec.ts`              | `ensureReading(page.request)` logs into normal Operations, may synchronize glossary, publishes selected terms, and adds withdrawal revisions in cleanup. WEB131 also creates account/library records.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| API122 in `api/library.spec.ts`                        | Publishes/corrects/withdraws an existing normal source and restores visibility through another immutable revision; account deletion does not undo source history.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| API160 / WEB160 media                                  | Generate/review normal stored media; API160 additionally changes source publication and withdrawal. These are actual generation/persistence assertions, not read-only public media checks.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| API123 in `api/operator.spec.ts`                       | Mutates normal operator sessions/login allowance and retains audit/limit effects; logout does not restore every record.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| API020, API040, API080, API120; WEB020, WEB040, WEB080 | Existing `@external` checks invoke genuine World Bank macro or Federal Reserve refresh against normal storage. API020's refresh is repeated. Their external labels are descriptive, not a default full-suite exclusion. They require a separately authorized provider/data context or a future captured-evidence isolated fixture preserving real ingestion assertions.                                                                                                                                                                                                                                                                                                                                   |
| Legacy account/private suites                          | Global imports remain in API account, goals, holdings, library, learning, privacy, inbox, alert-preferences, overview, assistance, journey and research files; browser account, goals, holdings, library, learning, learning-context, privacy, inbox, alert-preferences, overview, assistance, journey, goal-focus, holding-focus, loading-race and discovery-quality files. Their synthetic users/private records are created through the normal API, generally with owned-user cleanup; session/rate counters, failed cleanup and shared histories still need per-case isolation review. Some cases in these mixed files are read-only or simulations; the file list does not claim every case mutates. |
| Existing isolated identity cases with `@real-provider` | Owned storage does not prevent an explicitly real OpenFIGI request. Provider execution remains separately authorized even where database isolation already exists.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| API004 `@manual-outage`                                | Requires an explicitly prepared outage; this source fixture neither runs nor authorizes service shutdown.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

Public reading cases such as WEB180/182 depend on already published content but do not trigger ingestion themselves. UI/local-storage simulations such as WEB196/197 are not evidence of normal database writes merely because they import the global test. The default Playwright configuration has no global setup/server hook, but also no tag-based external exclusion. Importing the new helper has no setup effects: all storage/process work is inside selected fixtures.

## User-run next actions

No dependency installation is required by this change. After root integration, run `pnpm sdlc "test: isolate legacy source mutations"` for the configured user-owned format/check and gated local commit flow. Do not treat earlier child passes as verification of these edits.

For explicit bounded connected acceptance, the user starts the existing local PostgreSQL/Mongo services with `pnpm db:up`, builds with `pnpm build`, and starts the app using `pnpm dev` if it is not already running. The fixture requires the configured loopback databases, current application migrations, actual operator key and CREATE SCHEMA permission. Use the web URL printed by dev, routes `/#sources` and `/#ops`; default development web URL is `http://localhost:5173`, but selected ports take precedence.

Run:

```bash
pnpm e2e:run --project=api --grep='E2E-API-070\b'
pnpm e2e:run --project=desktop --project=mobile --grep='E2E-WEB-(070|181)\b'
pnpm e2e:report
```

The equivalent focused group is `pnpm e2e:run --grep='@LEGACY-FIXTURE-ISOLATION-001'` across the default API/desktop/mobile projects.

Or select exactly these cases/projects in `pnpm e2e:ui` at its printed URL (usually port9323). Do not run normal source refresh/publication concurrently: discovery refresh has a cross-schema advisory lock and the preservation guard detects any normal changes. Expected results are the five selections above, valid owned-schema annotations, successful real persistence and unchanged normal source digests. Physical phone/visual acceptance is separate; no automatic screenshots containing credentials are retained.

On failure report run ID/time, case/project, safe error and owned-schema annotation from `artifacts/e2e/latest.md`. Do not attach operator keys, session cookies, database URLs, private dumps or real source-body artifacts. A teardown failure must identify only the exact annotated owned namespace for manual investigation; never remove normal sources/databases as a workaround. Nothing was run by the author, and no new local commit exists.
