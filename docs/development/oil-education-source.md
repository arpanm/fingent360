# DEV010: real issuer fuel-cost educational walkthrough

Primary research dated2026-09-15. No services, migrations, provider captures, browser/API tests or deterministic validation were run.

## Researched evidence

The [IndiGo issuer disclosure dated1-Apr-2026](https://www.goindigo.in/press-releases/indigo-revises-fuel-charges-due-to-rising-atf-costs.html) discusses aviation turbine fuel costs and partial fare pass-through. The implementation retains one short exact anchor and the original submitted HTML. It does not turn the issuer's cited regional fuel statistic into an independently measured Brent/WTI change. The [earlier13-Mar-2026 issuer filing at NSE](https://nsearchives.nseindia.com/corporate/Indigo1_13032026195941_PressRelease13032026.pdf) independently establishes that the filer is InterGlobe Aviation/INDIGO and discusses fuel as an operating-cost input. The [official NSE identity filing](https://nsearchives.nseindia.com/corporate/ixbrl/INTEGRATED_FILING_INDAS_124405_04112025195024_iXBRL_WEB.html) identifies INE646L01027 and AIR TRANSPORT SERVICES. These are issuer observations and qualitative context, not evidence of a universal share-price response.

The issuer page supplies a date without an exact publication instant. The retained proof records `sourceDate:2026-04-01` and `datePrecision:date-only`. A derived editorial reading item's timestamp means its own source-review publication time; its label explicitly says the issuer release time is unknown. Historical evidence remains historical after recent capture/review.

Access is not a storage licence. No issuer permission or existing NSE agreement has been supplied. The source UI requires actual written retention/display/offline scope and a different named reviewer; it never silently enables background scraping. Source acquisition rights remain an activation input. The fixed original HTML format is verified from the public issuer page, so no user needs to research its format. Only a minimal20-word quote is embedded as the parser anchor; source fixtures reconstruct markup and identify their simulated admission. No original full copyrighted article, logo or private holdings are committed.

## Connected implementation

1. Operations → Oil education source accepts the original fixed-date issuer HTML and permission record. Unsupported date/anchor/layout or script-only content stays quarantined; raw bytes/capture envelope remain downloadable. PostgreSQL migration088 stores immutable editions/reviews and Mongo stores originals; no numerical or source-date rewriting.
2. A different named reviewer publishes. The source becomes an actual discovery item for Today/Explore with original evidence. Later source publication supersedes the earlier edition; withdrawal also removes the derived reading source from normal event admission.
3. Operations → Events cites the exact anchor, with reviewed Airlines sector and canonical InterGlobe Aviation identity. Original source remains retrievable and version-bound. Unknown effective time remains unknown. Publish through independent event review.
4. Research governance prepares the qualitative company mechanism, citing that event: fuel input cost versus partial fare offset, with demand/FX/fuel arrangements/competition explicitly limiting the interpretation. Release only after actual invariant simulation and independent review.
5. An authenticated user with that ISIN in their own recorded holdings selects a saved goal in Impact trace, enables the oil walkthrough, and chooses the released context. The exact source→factor→sector→company→holding→goal trace, source date/precision, mechanism and no-action comparator preview before explicit storage consent.
6. The receipt is stored through existing encrypted private trace storage, included in privacy export and deletion, and re-admitted on reading. Holdings/goals are unchanged. Shared web/Android code and offline handler reconstruct the same proof from bundled current source/event/context and local financial records. Missing context, wrong identity or changed/withdrawn source blocks a new oil receipt and flags old receipts for review. Stale/conflicting evidence cannot produce a confident financial action: `quantifiedImpact` is null and the explicit output is no trade/allocation.

No new private database table is needed beyond existing encrypted ImpactTrace receipts. The source table088 is public evidence metadata plus an opaque source hash; private holdings remain in their existing encrypted owner-bound storage. No LLM or notification operation is involved. No dependencies changed. The existing ActionCentre no-action comparison is linked, without moving financial assumptions implicitly between workflows.

## Scope and validation

This completes authoring of the original DEV010 **educational** six-step/no-action behavior, conditional on actual admitted source and identity inputs. It does not complete IMPACT-TRACE's broader numerical calibration/forecast scope, all oil-linked companies or current oil-news ingestion. Publication/permission and manual validation remain explicit separate statuses.

API1470 covers real isolated capture/review, actual minimal issuer quote with simulated provider admission, released mechanism, exact private comparator, idempotency/privacy, unchanged holdings and source withdrawal. API1471 covers retained quarantine. WEB1470 covers real API-backed preview/save/reload and keyboard/mobile-width flow. OFFLINE1470 covers the same proof and local storage with no network, then context removal. Contract unit cases cover exact date/anchor and script-only rejection. Finance values are synthetic user inputs; actual issuer/ISIN facts are researched, while fixture identity-provider responses and reconstructed HTML are explicitly simulations.

User next actions: configured PostgreSQL/MongoDB, `pnpm db:migrate`, user-started `pnpm dev`, then `pnpm sdlc "Add real issuer oil-cost educational walkthrough" -- --grep "@DEV-010"`. Use the printed web origin at `/#ops` and `/#impact-traces`; the reading source is `/#read/oil-education-<capture-id>`. Expected comparator in synthetic fixtures: holding cost10000 paise, no-growth goal projection2200 paise, gap97800 paise; no event-induced gain/loss or trade. For failure return selected case/project, saved run and exact error. No formatter/check/build/test/migration/APK/commit was executed. Rebuild and reinstall the APK manually for code/new snapshot changes; an old APK does not update itself.
