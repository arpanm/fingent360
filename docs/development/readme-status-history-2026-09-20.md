# README status chronology archived 20 September 2026

These are historical status notes copied before README cleanup. “Latest”, “pending”, “not run”, local ports and execution boundaries below describe their original revision, not the current run or continuing authorization. Current evidence is in [functional acceptance](functional-acceptance-2026-09-20.md), the [repair record](../tasks/SDLC-REPAIR-016.md) and generated [validation](../validation/README.md). Historical commands are retained for traceability; use current task prerequisites before running them.

The authorized initial acceptance run completed with1478 passes (1275 connected and203 offline),17 connected failures and one deliberately skipped outage case. The mobile commodity overlap has since been reproduced, diagnosed and passed its focused retry after a wrapping fix. Reviewed story matrices and targeted repairs/new coverage are now integrated for validation; no new full-suite pass is claimed. See [current acceptance review](../development/functional-acceptance-2026-09-20.md) and the generated [story results](../validation/README.md). Native-device certification and source/provider permissions remain separate.

The latest portfolio and saved-reading retries passed their connected and offline acceptance matrices (SDLC1789837057413-22117 and1789837362616-23249). The user has now authorized a complete agent-run validation and repair pass. Run1789837762812-24470 is in progress against healthy local API/web/databases; format/check passed. [Run diagnosis](../tasks/SDLC-REPAIR-016.md#authorized-complete-validation--19-september-2026). Generated validation remains authoritative; no full-suite pass is claimed yet.

Portfolio holdings, holdings reconciliation and saved reading/reminders now have reviewed completion matrices. The new guided offline portfolio case covers actual UI entry/save/reload/removal without a server. Successful current-revision scoped SDLC runs can now close these three bounded modules instead of leaving them at “acceptance matrix needed”. [Module scope and exact commands](../development/main-module-closure-2026-09-19.md). No new pass or native-device certification is claimed.

The full validation run `1789752953639-97020` recorded **104 connected and 15 offline failures**, with 1375 passes and one skip. Repairs address confirmed API permissions, report preparation, chart parsing, feedback maintenance, offline calendar errors and outdated test setup/navigation. [Root causes and scoped retry](../development/full-audit-2026-09-19.md) links each group to its saved evidence. Run `SDLC_AUTO_REPAIR=0 pnpm sdlc "Repair full audit failures" --story SDLC-REPAIR-016` to validate the failed-case selection plus the report-storage companion, without repeating the entire inventory. Code and regressions are authored, not executed; bugs remain open until passing evidence.

The scoped OFFLINE960/OFFLINE961/OFFLINE1700 repair now models the complete published evidence-graph transition in their shared synthetic impact-trace fixture: the event becomes published and every linked edge becomes reviewed before strict contract parsing. Shared regression assertions preserve the nonempty, fully reviewed graph invariant while the unchanged cases retain no-network receipt lifecycle, exact equity-conflict/edition-binding coverage and downloaded-mechanism binding/removal denial. The repair is authored but not run; see [IMPACT-TRACE-001](../tasks/IMPACT-TRACE-001.md#offline960offline961offline1700-shared-evidence-graph-publication-repair--2026-09-18).

The September18 run review found only WEB1702 and WEB1593 still failing on desktop/mobile in the three reported scopes. Governance selectors now expose stable accessible names, and WEB1702 reads its actual isolated event API. The brief run recorded all seven required cases passing after its own repairs, but their differing source revisions leave overall acceptance stale; no new brief code change is needed. [Targeted reruns and evidence](../development/core-journey-acceptance.md#september18-run-follow-up). No agent validation or commit was run.

The scoped WEB1593 repair gives its real retained-source/event setup and two-actor governance browser workflow an explicit 60-second whole-test budget, then requires the exact asynchronously loaded fixture event option before selection. All existing form, citation, simulation, independent release and withdrawal assertions remain. Production code and individual action timeouts are unchanged. The repair is authored, not run; see [DEV-015](../tasks/DEV-015.md#web1593-reviewed-event-readiness-repair--2026-09-18).

The scoped WEB1990 repair makes Automatic research load its schedule controls when the operator opens that section, while preserving the explicit refresh/recovery action and rejecting stale initial responses. The filing-watch browser regression now requires the `equity-filing-watch` schedule heading to appear before it enables the real schedule. The supplied mobile run used the committed pre-repair case and timed out because it searched for a control that the unloaded panel could not render. Saved run `1789723036823-6f833c8e-c99d-49f7-bc1c-b71fad7bd5fc` passed the repaired desktop selection only; the repaired mobile path remains unverified. See [SRC-004-WATCH](../tasks/SRC-004-WATCH.md#web1990-automatic-schedule-load-repair--2026-09-18).

The scoped WEB444 repair uses the catalogue's complete `Federal Reserve historical policy decisions` accessible name instead of a shortened label that was never rendered. The real save/reload regression still proves only `fed` is stored and selected while the distinct `fed-policy-history` choice remains unselected; first-read recovery and pending-control assertions are unchanged. Production behavior is unchanged. The latest saved evidence is desktop-only, so the supplied mobile failure remains pending a mobile retry. See [READING-FOLLOW-001](../tasks/READING-FOLLOW-001.md#web444-canonical-source-label-repair--2026-09-18).

The scoped OFFLINE1520 repair now models the complete published evidence-graph transition in its retained historical-source fixture: the event becomes published and every linked context edge becomes reviewed before strict contract parsing. A direct regression assertion preserves that invariant while the existing real offline handler still proves five-point admission, missing-event suppression and withdrawn-source suppression without replacements. Production contracts and offline behavior are unchanged; the repair is authored but not run. See [DEV-006](../tasks/DEV-006.md#offline1520-evidence-graph-publication-repair--2026-09-18).

The scoped API1521 repair publishes a corrected event edition through the required named proposal and independent-approval workflow instead of the intentionally blocked legacy review route. The shared event publication helper now supports noninitial revision heads and asserts the returned published version; brief version/history behavior and production permissions are unchanged. The repair is authored but not run. See [DEV-006](../tasks/DEV-006.md#api1521-corrected-event-publication-repair--2026-09-18).

The scoped API1330 repair follows the independently published event revision returned by the governance fixture instead of assuming draft revision 1. The case also proves that the preceding stale revision remains rejected before saving the current source-bound holding/goal trace. Production event/context admission is unchanged; the repair is authored but not run. See [IMPACT-TRACE-001](../tasks/IMPACT-TRACE-001.md#api1330-reviewed-event-version-repair--2026-09-18).

The scoped API1783 repair aligns each synthetic old/new equity edition's declared effective date with its June/July 2025 identity and boundary-price observations. The API's fail-closed rejection of observations after the declared source date remains unchanged; the helper now asserts the admitted edition preserves the aligned dates before independent review. Its fixture rows are immutable tuples so strict E2E compilation knows every required date is present. The repair is authored but not run; the supplied `pnpm check` stopped at the now-repaired tuple inference error. See [SRC-003-CONSOLIDATION](../tasks/SRC-003-CONSOLIDATION.md#api1783-edition-date-repair--2026-09-18).

The scoped API1982 repair now binds every attached historical corporate-rating observation to the complete verified source row for its ISIN. A receipt can no longer combine individually valid coupon, maturity or opinion fields from different instruments while retaining a selected ISIN. The existing encrypted receipt, withdrawal replay and source-version behavior is unchanged; the repair and focused contract regression are authored but not run. See [FUNDS-BONDS-001](../tasks/FUNDS-BONDS-001.md#api1982-exact-rating-row-repair--2026-09-18).

Your last scoped run passed all eight selected API/desktop/mobile cases (SDLC1789668919723-57996, commit `bee7f02`). The next authored functional batch repairs evidence-to-portfolio selection/recovery, current brief admission, complete action-comparison controls, report-worker failure isolation and reading/share acceptance. [Completed scope and all manual commands](../development/core-journey-acceptance.md) includes the full API/browser/offline story matrices. These new changes have not been run by the agent. Live source activation and production release are separate from functional acceptance.

The scoped Axis repair accepts a valid empty workbook shared-string table when all cells are inline strings, while continuing to reject a missing or renamed XML root. API1720 passed the direct parser rerun; API1721 then showed the connected capture API returning its specified quarantined receipt because the running API had not admitted the same original. API1721 now validates the entire edition contract and requires a parsed draft with no error before mapping, so a future parser/runtime mismatch is explicit instead of a null dereference. Exact quantities, displayed weights, yield, reconciliation, independent review and withdrawal behavior are unchanged. The connected repair is authored but not run; use the exact command and API-process prerequisite in [SRC-016-AXIS](../tasks/SRC-016-AXIS.md#scoped-api1721-capture-repair--2026-09-18).

AUTH-WAIT-001 API304's real recovery race fixture now allows the reset's bounded pre-transaction password derivation before requiring its owned account-row wait, and detects that wait through `pg_blocking_pids` rather than exact SQL text. Allocation read/write401, unchanged-private-state and recovered-session controls remain intact. The repair is authored, not validated; use the exact scoped command in [AUTH-WAIT-001](../tasks/AUTH-WAIT-001.md#scoped-api304-fixture-repair--2026-09-17).

ACCOUNT-001 is now **Done for its reviewed scope**: user-run SDLC1789569346007-35322 recorded all required API, desktop/mobile and offline cases passing, resolving registration bug BUG-99cc743e4a8bae69. The gated implementation commit is d1dd7e3; generated result documents remain uncommitted. No key setup needs repeating on this installation.

The user-started all-validations run also exposed a stale research-governance fixture: it used a legacy publication endpoint blocked in named mode. The fixture now uses real proposal creation and independent approval, retaining legacy/self-approval rejection checks. API1503/1504 rerun evidence is still pending.

The scoped API1503/API1504 repair additionally checks that rejected self-approval leaves the stored proposal pending, the approval receipt identifies the intended event and independent reviewer, and the public event exposes the independently published second revision. Validation remains pending; see [DEV-016's exact API1504 command](../tasks/DEV-016.md#scoped-api1503api1504-prerequisite-repair--2026-09-17). No production permissions changed.

GOALS-001 is **Done for its accepted scope** in user-run commit846e990 and the saved story acceptance. The final offline handoff1789667443808-7529f3d3-5122-4e95-9a8a-d0eb21ce16e1 passed OFFLINE010/068. This includes removal/draft cleanup and stale-list protection; native release certification remains separate.

Functional completion now uses story-specific acceptance: `pnpm sdlc "Complete account workflow" --story ACCOUNT-001` runs gates, a gated local commit, that story's connected cases and its rebuilt offline cases. It then updates [story validation](../validation/README.md), each observed task and TODO's validation column, and the separate [bug tracker](../bugs/README.md), including when a repair fails. For reviewed completion scopes in the acceptance manifest, complete passing acceptance automatically marks them Done; failure or stale coverage reopens it. Other stories need their completion scope reviewed before opting in. No push occurs. Story mode exits nonzero if required acceptance remains incomplete, even when selected tests alone exited zero. Post-test result documents remain uncommitted. Account, goals, standard XLSX and worker-health matrices are defined initially; other tagged stories receive selected-result status until their complete acceptance matrix is reviewed.

Historical account setup: the earlier documentation-only run218f3a5 selected zero E2E tests. The later complete account story run above supersedes that pending state; registration and watchlist acceptance passed.

Earlier E2E-API-030 registration evidence recorded a configuration blocker: local `.env` lacks the three private identity/payload key settings required by registration. The supplied503 alone does not confirm the running API’s cause. See [ACCOUNT-001 recovery and exact rerun](../tasks/ACCOUNT-001.md#registration503-blocker--2026-09-16). Restore existing keys when encrypted data already exists; use [first-time key setup](../development/private-key-setup.md) only for a new installation. No runtime changes or validation were performed in this scoped attempt.

Account deletion unit fixtures now exercise real cookie/session admission before checking the owner rate limit, including malformed-cookie and post-lock revocation regressions. The password-salt guard now explicitly returns its throwing assertion to satisfy ESLint, with a direct guard regression. These repairs are authored, not validated; see [ACCOUNT-001](../tasks/ACCOUNT-001.md#getter-lint-repair--2026-09-16). Smallest lint validation: `pnpm exec eslint apps/api/test/account-security.test.mjs`.

[SDLC-REPAIR-015](../tasks/SDLC-REPAIR-015.md) fixes retry classification after a code repair: new formatting failures now use scoped Prettier before another agent, even after the agent budget is exhausted. The saved run successfully launched Codex; repeated formatting failures exhausted attempts before E2E. Regressions are authored, not run. Install the previously added dependency with `pnpm install --frozen-lockfile`, then rerun `pnpm sdlc "Complete source workflows" --affected`. No additional CLI skill is required for this repair path.

The task index now explicitly marks SDLC-REPAIR-005, WORKER-HEALTH-001 and XLSX-001 **Done for their recorded scope**, and SDLC-DOCS-001 **Done for documentation**. These are bounded existing deliveries, not new feature claims or a passing result for the current changes. Broader source/broker coverage remains in its own task records.

[SDLC-REPAIR-014](../tasks/SDLC-REPAIR-014.md) repairs the supplied E2E compiler errors, preserves workflow assertions and shares native bridge declarations. Root test helpers now declare the already locked `fflate` dependency. User prerequisite: `pnpm install --frozen-lockfile`; smallest compiler validation: `pnpm e2e:typecheck`. The subsequent formatting-only repair adjusts two offline assertion layouts and TODO table padding; its focused manual regression command is in the task record. No validation or commit was performed; the parent owns the exact `pnpm check` retry.

Implementation and validation are tracked separately in [TODO](../../TODO.md). Completed code is marked **Completed implementation; validation pending** until the user runs the relevant gates. Completed source workflows now have individual child rows so their delivery is visible even when a broader source family remains partial. This batch has not run tests, applied migrations, rebuilt a phone package or committed; the baseline remains `a2c53a0`.

| Completed implementation scope              | What is now authored                                                                                                                                                                                        |
| ------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DEV-006 / DEV-016                           | Independently reviewed five/six-point intelligence briefs, corrections and progressive source/context/scenario reading.                                                                                     |
| DEV-010 / DEV-015 / DEV-019                 | Educational oil-to-goal trace, research governance, FIFO/purchase/rebalance comparisons, restricted tax policies and unchanged/no-action alternatives.                                                      |
| DEV-011 / RESEARCH-AUTO-002                 | Durable report workers, scheduled evidence capture/publication controls, selected verified calendars, original GDP vintages and dated CPI model history.                                                    |
| DEV-020 / EVENT-SCENARIOS-001               | Seven source-backed educational event families, reviewed mechanisms and original GDP/CPI expectation comparisons, including a dated historical CPI model golden.                                            |
| DEV-018 / IMPACT-TRACE-001                  | Watchlist/follow controls with exact BEA calendar context and evidence-to-holding/goal traces with uncertainty, conflicts, stale-data handling and no-action receipts. Numerical forecasts are unavailable. |
| DEV-017                                     | Supported private-record encryption, identity lookup, MFA and owner lifecycle.                                                                                                                              |
| DEV-021                                     | Shared API monitoring plus independent uptime observations, persisted incidents and a read-only viewer.                                                                                                     |
| DEV-029                                     | Consented one-off/scheduled WhatsApp public summaries and Android/iOS shared workflows, native feedback, broker return and private-file sharing.                                                            |
| SRC-003                                     | Initial action taxonomy, consolidation bridges, independently reviewed rights/stock-swap terms and explicit theoretical comparisons.                                                                        |
| SRC-009                                     | Daily WTI/Brent source capture/review and scheduled drafts, monthly oil/metals and daily reference FX.                                                                                                      |
| SRC-016                                     | Initial SBI/Axis holdings and Kotak factsheet fee/fund-size workflows with independent plan mapping and offline evidence.                                                                                   |
| SRC-005                                     | Four initial financial statement schemas and a25-company source-backed revenue/PAT acceptance cohort; original-source validation remains manual.                                                            |
| SRC-007 / 008 / 010 / 011 / 012 / 014 / 015 | Initial India/global macro, institutional flows, participant positioning, company-news onboarding, regulatory-source registry and AMFI current/history workflows.                                           |

Original SPF GDP survey expectations and Cleveland Fed CPI model snapshots now support retained, independently reviewed comparisons against original releases. Seven-family qualitative transmission mechanisms are bound to reviewed contexts and reconstructible holding/goal receipts; they do not establish numerical causal forecasts. Additional SBI/Axis fund disclosures, reconciled banking/general-insurance/life-insurance statements, monthly commodity capture, India GDP vintages, retained price-history pages and reviewed share-consolidation bridges are authored. Their exact coverage is tracked in [current delivery](../tasks/current-delivery.md), alongside remaining exchange, broker, fund/bond and historical-source gaps. A completed child never silently completes those broader parents.

New fund/bond flows include original announced fund-merger lineage, independently mapped AMC fee/fund-size factsheets with paginated history, and a four-original historical sovereign auction example with exact clean/accrued/dirty calculations. See [funds and bonds](../tasks/FUNDS-BONDS-001.md), [factsheets](../tasks/SRC-016-FACTSHEET.md) and [sovereign sources](../tasks/SRC-017.md). Migrations119–123/125–128 and API/browser/offline1900–2003 cases are authored; these are not current bond quotes, investor conversion instructions or executed acceptance.

CCIL curve originals have a separate disabled server gate (`CCIL_ZERO_ENABLED`, `CCIL_ZERO_PERMISSION_REFERENCE`), reviewed source-point/parameter history and offline support. Their original labels leave numerical conventions unresolved; the app does not use them to value holdings. [Source scope and manual acceptance](../tasks/SRC-017-CURVE.md).

Daily oil is available at More → Daily oil observations (`#daily-oil`), with a separate contributor-permission control in Operations → Daily oil source. The source schedule remains disabled until configured by the operator. Corporate ratings use an exact attributed ICRA original and separate agency withdrawal from editorial withdrawal. Saved bond comparisons can explicitly attach an admitted historical ISIN/source version, with stale/withdrawn denial and immutable receipt reconstruction; description-only evidence remains valid while attached observations must match an exact verified source row. See [credit source scope](../tasks/SRC-018.md).

The [original filing watch](../tasks/SRC-004-WATCH.md) monitors selected verified historical financial-result URLs, retains corrections and stages drafts for independent review. Its permission control and schedule start disabled. The separate [official RSS discovery inbox](../tasks/SRC-004-DISCOVERY.md) retains newly advertised original/revised filing pointers with explicit identity and parsing gaps. Source revocation also affects fresh company and downstream fundamental admission.

Provider/source permissions, public deployment, signing and physical-device acceptance remain explicit activation inputs. WhatsApp and regulatory retention are disabled by default. Installed offline Android apps require a new snapshot/build/reinstall to receive web changes. Native background push and generated story video are not claimed implemented.

Use [WhatsApp configuration](../development/whatsapp.md), [iOS setup](../development/ios.md), [native feedback](../development/ios-feedback.md), [broker return](../development/ios-broker-return.md), and [regulatory-source requirements](../tasks/SRC-014.md) for exact setup and focused acceptance. Independent monitoring starts only when the user runs `pnpm uptime:monitor /absolute/operator/uptime.json /absolute/persistent/monitor/state.json`; edit the example targets first and read the [monitor runbook](../development/independent-uptime.md).

**Server setup changed:** configure `PRIVATE_DATA_ACTIVE_KEY` and `PRIVATE_DATA_KEYS` in ignored `.env` before using connected feedback, private financial records or authenticator enrollment. These features now require server encryption keys, independently of optional AI history. Keep old keys until every retained record/history and required backup has been rewrapped or expired; a seven-day AI retention window does not cover financial history. Missing keys fail protected operations closed. Never put these keys in VITE variables or an APK. See [private storage](../development/private-financial-storage.md), [authenticator](../development/account-authenticator.md) and [action comparisons](../development/action-plan-completion.md).

For local setup, run `pnpm privacy:keys`; use `pnpm privacy:keys --status` for identifiers/counts only or `pnpm privacy:keys --rotate` to add an active key while preserving old ones. Restart the API afterward. First setup also creates the stable identity lookup key. Existing pre-identity installations use `pnpm privacy:keys --add-identity-key` once; a lost identity key must be restored, not regenerated. These commands never print keys or rewrap the database themselves. [Options, safeguards and manual acceptance](../development/private-key-setup.md).

The pickup-queue batch now adds support-access history, private AI-history encryption, durable deployment monitoring, corporate-action/IndAS imports, BLS calendars, current AMFI Plan/Option support, historical Fed scenario extraction and safe public-link sharing. See [exact authored scope, remaining work and manual activation](../tasks/DELIVERY-TEAM-005.md). These changes are unvalidated and uncommitted; parent tasks remain partial where specified. Shared web changes require an Android rebuild/reinstall to reach an installed APK.

E2E-API-120 still needs its underlying503 cause. New [reader diagnostics](../tasks/READER-DIAGNOSTICS-001.md) preserve capture and return a safe incident/category/phase for the next user-run failure; no cause or repair success is guessed.

[Task pickup readiness](../tasks/pickup-queue.md) separates active records into agent-ready work, research-first work, parent rollups and validation/activation evidence. Existing user answers are recorded in each task; the next independent steps need no new questionnaire. Source/format/regulatory research belongs to the agent. Readiness does not authorize test execution, provider spending or regulated activation.

For a clear implemented-versus-pending breakdown, read [Current delivery](../tasks/current-delivery.md). **The batch still has functional gaps, listed individually; completed implementation and manual validation are tracked separately.** [TODO.md](../../TODO.md) is now a concise task index. Every task links to its own specification, reusable prompt, history, evidence and remaining actions under [docs/tasks](../tasks/README.md). Update the task record and its index row together after every implementation; do not add long prompts or summaries back to TODO.

SDLC-REPAIR-008 updates the media reservation unit fixture for pool-level evaluation recording and adds recording lifecycle/replay assertions. Production behavior is unchanged; validation is pending. See [focused validation](../development/media-reservation-repair.md).

SDLC-REPAIR-007 replaces inconsistently padded Markdown tables with equivalent labelled lists and removes a surplus blank line after the supplied README/SDLC Prettier warnings. Validation is pending; see [manual regression acceptance](../development/sdlc.md#sdlc-repair-007--markdown-formatting). No dependencies or services are needed.

SDLC-REPAIR-006 repairs E2E-OFFLINE-1021 callback type narrowing with a stable numeric model binding and retains the rejection assertions. Verification is pending; smallest manual validation: `pnpm e2e:typecheck` (no services or dependency changes). The parent retries `pnpm check`. See [scenario regression coverage](../product/event-scenarios.md#sdlc-repair-006--callback-narrowing).

SDLC-REPAIR-005 is closed for its recorded formatting failure: saved user-run1789457453614-67049 passed formatting/checks and advanced to staging. This is historical evidence only; the current working tree has not passed gates. See [evidence](../tasks/SDLC-REPAIR-005.md).

SDLC-REPAIR-004 corrects assertion wrapping in feedback delivery E2E-WEB-195 after the reported Prettier failure. Assertions are preserved; validation is pending. Smallest manual check: `pnpm exec prettier --check tests/e2e/cases/browser/feedback-delivery.spec.ts`. The parent retries `pnpm check`; no services or dependency changes are needed for the formatting check.

SDLC-REPAIR-003 fixes feedback list trace rendering to use each item’s context while no review is selected. E2E-WEB-195 now covers initial inbox rendering and closing/reopening a saved review. Validation is pending; rerun `pnpm check` for the reported compiler failure. No dependencies or migrations changed.

DELIVERY-TEAM-004 adds connected company evidence, owned impact traces, educational change comparisons, fund/cash-flow tools, automatic research controls, release-calendar captures, source evaluation and reviewed story images. See the [delivery handoff](../development/team-004/README.md) and top of TODO.md for exact scope and remaining gaps. This batch is authored but not executed; existing test results apply only to their recorded revisions. Shared web changes reach an installed Android app after rebuilding and reinstalling its package.

New reader destinations are **More → Indian companies**, **Evidence and my plans**, **Explore a change**, **Funds and bonds** and **Release calendar**. Operations adds **Indian equity data**, **Fund data**, **Automatic research** and **Research evaluation**. Existing Publishing → Prepare visual summary can request a configured image, then approve that exact image. Stories show reviewed images without the previous arrow placeholder; gesture help is a dismissible one-time overlay.

Eligible existing discovery sources refresh automatically while the API runs; `RESEARCH_AUTO_ENABLED=false` disables the worker globally and Operations can pause each source. Captures retain source data. An approved per-source automatic-publication policy adds qualifying official news to Today/Stories; other drafts retain individual review. Policies expire and named mode requires independent approval. `STORY_IMAGE_PROVIDER` defaults to `off`; deliberate image generation uses a configured OpenAI/Gemini image model and matching server key. No provider calls were made while authoring these features.

Privacy now offers a separate opt-in seven-day private AI request history with owner-only export/deletion; Operations evaluation joins public source/model/composite-view records with submitted feedback.

Complete Indian equity history, the five named broker export parsers, complete causal impact policies, remaining RBI calendar/backfill and additional original numerical vintages and fund look-through/bond market coverage remain distinct pending requirements. Zerodha account connectivity is authored separately from its still-missing export parser; Upstox connectivity is also authored; Eligible Angel publisher connectivity is also authored. All require provider activation and their own acceptance. Generic mapped imports and normalized evidence ingestion are not labelled complete source adapters.

A failed manual SDLC stage starts a scoped Codex repair attempt. For E2E, it receives only one failed case's error/location; the script rebuilds and reruns that exact file, title and project, never the full suite. Other command failures retry that command. Agents edit only. Default repair budget:3 attempts per invocation (`SDLC_REPAIR_LIMIT`, range1–10); opt out with `SDLC_AUTO_REPAIR=0`. Formatting-only check failures use scoped Prettier retries without an agent (maximum two). CLI discovery checks `SDLC_CODEX_BIN`, then PATH, then installed macOS Codex/ChatGPT app binaries; an invalid explicit override stops with a clear error. Codex authentication still uses your configured CLI login. Post-commit repairs remain uncommitted until new format/check gates pass. Launcher changes are authored; execution validation is pending.

Use `pnpm sdlc --affected-plan` to preview conservative E2E selection without running anything, then `pnpm sdlc "Describe change" --affected` to run all check gates, commit and execute the selected E2E files. Add `--base HEAD~1` for changes already committed. The default baseline is HEAD captured before the run, plus all working changes. Web changes select desktop/mobile and freshly rebuilt offline web tests; shared API/contracts/migrations and unmapped files fall back to full coverage. This does not select unit tests: `pnpm check` remains complete. See [impact selection](../development/sdlc.md#impacted-e2e-selection-sdlc-affected-001) for limitations. Authored; execution pending.

For a user-operated format/check/gated commit without E2E: `pnpm sdlc "Describe change" --checks-only`. For a scoped SDLC run: `pnpm sdlc "Describe change" -- --grep TASK-ID`. The existing unfiltered command still runs all E2E cases. Agents author changes and affected test IDs; deterministic execution remains user-operated. The new checks-only option is authored and awaits validation.

REGRESSION-015 recorded an earlier, explicitly authorized SDLC validation run. The current feature batch returns deterministic execution to the user. Web/E2E typing corrections and UTF-16 excerpt validation are implemented. Additive migrations037–048 and runtime grants were applied locally; existing data was preserved. Validation results are recorded in docs/development/regression-015.md. No push is authorized.

REGRESSION-014 corrects provider receipt URL typing and named-mode cursor signing after format/lint passed. Named audit/publishing cursors use a random server-only key and require resetting the page after an API restart; bootstrap cursor compatibility is preserved. Four codec regression cases are authored. Rerun the original `pnpm sdlc` invocation with its existing filters; typechecks, tests and commit remain pending.

REGRESSION-013 addresses the11 lint errors reported after formatting passed: dead assignments, provisioning error cause, CSV control validation and two test-code declarations/branches. Rollback, authorization and redacted CLI output are preserved. CSV control-range regression coverage is authored. Rerun the original `pnpm sdlc` invocation and filters; checks and commit remain pending.

REGRESSION-012 corrects wrapping in the four files rejected by the reported format:check stage. The runner still requires format and check to pass before committing. Rerun the same `pnpm sdlc` command with its original filters; this correction has not been executed or committed. No runtime behavior, test selection or formatting exclusions changed.

Remaining roadmap implementation is active under ROADMAP-COMPLETE-003. The current delivery adds saved goal downside assessments, mapped and attested-cost imports, progressive explanations, reviewed events, named operator approvals, numerical ECB policy and reference exchange rates, monthly oil benchmarks, material-change monitoring, purpose-consent controls and Operations diagnostics. Implementation and verification are tracked separately; the earlier regression corrections remain in the working tree. No new APK, migrations applied, test passes or commit is claimed for this batch.

Latest user-run SDLC committed `b5cfcd0`, then reported 522 passed, 11 failed and 1 skipped. The current correction addresses owned database lock observations, BEA mobile recovery/progress states and report-comparison loading states. [Causes and focused rerun](../development/regression-011.md). These edits await user validation and a new gated commit.

The three audited delivery items are implemented in the working tree. The recorded local database activation enabled owner/runtime separation, applied migrations035/036 and started the runtime API on4103 (web5175), with readiness200 at that time. It does not establish current process health or apply the newer migrations. Format/check/E2E and the gated commit remain user-run; this is not a claim of full test or production acceptance. [Current activation and exact next command](../development/local-database-activation.md). Installed APKs do not update until rebuilt/reinstalled.
