# REGRESSION-011 — integration failures

User-run baseline: commit b5cfcd0; run folder2026-09-14T08-28-33-631Z-98876, manifest1789374514851-64529e8e-bd30-4b1e-983a-65cbc2348982 started2026-09-14T08:28:34.851Z.534 selected/completed attempts:522 passed,11 failed,1 intentionally skipped. API4103/web5175. These are user-run results, not passes for the corrections below.

| Failure                               | Cause and correction                                                                                                                                                                                                                                                                                                         | Preserved acceptance                                                                                                                                                                                                 |
| ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| API586                                | Table ACCESS EXCLUSIVE did not block GRANT's ACL catalog write. Hold an uncommitted SELECT revoke on only the owned fixture's app_users ACL instead.                                                                                                                                                                         | Observe exact provisioning SQL/blocker PID, cancel actual query, roll back the blocker, compare original ACL/runtime SELECT, absent candidate role and unchanged holdings; marker-guarded fallback teardown remains. |
| WEB521 desktop/mobile                 | Long queue-query observation required its LIMIT tail, which pg_stat_activity may truncate. Match the opening join plus actual waiting relation and blocker identity.                                                                                                                                                         | Only expire sessions after real storage blocking is observed; eventual sign-in and recovery assertions remain. No timeout increase.                                                                                  |
| WEB421 desktop/mobile                 | Refresh displayed a stale empty report count alongside its loading status. Suppress empty/one-report state until loading finishes.                                                                                                                                                                                           | Hold an actual isolated options200 after real first-report issuance, assert one loading status and then the real one-report result; later error/retry/comparison remains.                                            |
| WEB500/501 desktop; WEB500–503 mobile | Ambiguous progress/validation statuses and timeline overflow over the Inspect button. Scoped grid flow wraps retained hashes; operation-specific Recovery progress is separate from historical validation context and the saved Draft staging receipt. WEB500 holds/parses a real201 before checking the receipt transition. | Saved staging receipt must be explicit, every Inspect remains an ordinary pointer click, and real replay/staging/auth/history flows remain.                                                                          |

No production grant, data or provider behavior is changed by the lock-test correction. PostgreSQL16 GRANT implementation uses ACL catalog updates; its source is https://github.com/postgres/postgres/blob/REL_16_STABLE/src/backend/catalog/aclchk.c . The permission change exists only inside the user's selected synthetic fixture test transaction and is always rolled back.

No tests, format/check, builds, database/service actions or commit were executed for this correction. Existing services/migrations and installed APK are unchanged. Exact user-run focused workflow:

```bash
E2E_BROWSER=chrome pnpm sdlc "Fix integration regressions" -- --grep 'E2E-API-586|E2E-WEB-(421|500|501|502|503|521)'
```

Expected selection: one API case plus six desktop/mobile cases =13 instances (includes the two previously passing desktop BEA paths). Format/check must pass before the script's local commit; then selected E2E runs, with no push. Use the existing PostgreSQL/MongoDB and configured app at the launcher's printed URL. Keep test watch/eye mode off. On failure share artifacts/e2e/latest.md and the first safe assertion/project/case; the older run cannot verify a corrected file. Physical-phone BEA layout acceptance requires a later rebuilt APK/connected setup and is not inferred from mobile browser emulation.
