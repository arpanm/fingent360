# SOURCE-REVIEW-DIFF-001 author handoff

Authoring only in codex/source-review-diff-001, base30c5ca2. No tests, builds, format/check, services, installation, provider calls, migration, commit or push executed. No dependency or schema change.

## Integration

New contracts/source-review.ts exports strict SourceReviewQuerySchema, SourceReviewComparisonSchema, sourceReviewDifferences. GET /api/v1/ops/discovery/items/:id/comparison?expectedVersion=N uses actual current source FOR SHARE, post-wait operator auth, exact-version409 and nearest preceding non-draft original. Existing PUT publication already checks expectedVersion after its exclusive lock. No public endpoint/projection changed.

SourceReview.tsx replaces only the old Operations publication Dialog. Component props id/version/onClose/onDenied/onSaved. Merge its onDenied callback into OPS-AUDIT's shared protected-session barrier and preserve that agent's guarded list loader; do not copy older Operations over sibling changes. Successful mutation acknowledges its returned edition before refreshing list. New receipt is historical and action buttons disappear. Evidence drill-down uses existing retained operator endpoint and exact edition. Current and predecessor show explicit state/version; a withdrawn predecessor is never labelled published.

Modified: apps/api/src/discovery.ts, apps/web/src/Operations.tsx, packages/contracts/src/index.ts. New: apps/web/src/SourceReview.tsx, packages/contracts/src/source-review.ts, packages/contracts/test/source-review.test.mjs, tests/e2e/cases/{api,browser,offline}/source-review.spec.ts, docs/product/source-review-diff.md and this handoff.

## Authored acceptance

API460 actual stored source + retained Mongo fixture, absent predecessor, publication/withdrawal/republication, stale review409, strict input and immutable old rows. API461 actual source-row wait then operator expiry401 with no retained body. WEB460 real operator/source comparison, protected evidence/Back, concurrent edition change409, explicit reload then actual publication and saved receipt; escaping supplied through synthetic script-like title. WEB461 actual operator expiry denies evidence and clears private dialog/list. OFFLINE470 connected-only Operations notice and zero API requests. Contract unit rejects differences inconsistent with originals and foreign predecessor IDs.

Parent should run format/check and @SOURCE-REVIEW-DIFF-001 in api/desktop/mobile, then rebuild device web assets and select OFFLINE470 in offline UI. Existing PostgreSQL/MongoDB and research operator configuration required. No provider connectivity needed for these synthetic fixtures. Typical route printed by pnpm dev: /#ops. Keep watch/eye off. Record latest.md run/time/project/first failure and safe trace; never disclose operator cookies/keys.

Four-eyes workflow, actor roles, broad source rights/production/physical-device acceptance remain open. This feature reads already retained originals and does not ingest, delete evidence, infer finance or change account data. Parent owns shared trackers, integration evidence and scoped gated commit.

Peer corrections: existing WEB321 now acknowledges the actual saved publication/withdrawal receipt and closes it explicitly before its unchanged public-state assertions. Include tests/e2e/cases/browser/bea.spec.ts in the modified manifest. API460 also inserts two actual synthetic draft editions after republication and proves the comparison skips both as predecessor candidates. WEB462 holds an actual isolatedAPI200 comparison, closes with Escape, then releases/drains it and asserts the reader remains closed. Peer found no API locking/baseline blocker. These remain authored cases, not passes.

Release regression addition: API and browser source-review specs explicitly disable automatic trace, video and screenshot capture because they use an actual configured operator credential. WEB460 deliberately saves `source-review-before-publication.png` at the normal selected desktop/mobile project viewport, before publication/receipt, with controlled synthetic source content and document/dialog overflow assertions. This is the sole new explicit visual capture; physical/device and user design acceptance remain separate.

WEB463 holds retained evidence **before** its actual API fetch, closes the review while the session remains valid, settles ordinary page requests, expires only the owned browser session, fetches an actual401 and holds its delivery. The case proves the protected parent is still authenticated immediately before delivering that401, then drains all held routes and requires sign-in with no restored source title or dialog. Parent must pass its epoch-guarded Operations request function into SourceReview for this after-Close boundary; no implementation changes were made in this regression addition.

WEB464 retains an actual401 for the closed reader, explicitly signs out and signs into a new real browser operations session while delivery remains held, checks the new cookie differs and the actual session endpoint is authenticated, then releases/drains the old denial. The new publishing view and session must remain authenticated. Parent owns the complementary implementation fixes: normalize failures from an old Operations session generation to `StaleOperationsRead` before child callbacks, and return early from `SourceReview.failure` when its reader is no longer live. No implementation edits are included in this test checkpoint.

API462 holds `discovery_versions` with an actual ACCESS EXCLUSIVE lock, observes the exact comparison version-read waiter after its source-head admission, expires the owned operator session, releases the table lock and requires401 with no original title/body. Parent adds the corresponding final authorization after the retained-version read; the earlier source-head check alone does not cover this second wait. API/browser automatic private capture remains disabled.

Final selection: API460–462, WEB460–464 and OFFLINE470 — **13 connected project selections** (3 API +5 browser cases in each desktop/mobile project), plus **1 offline**;9 distinct new E2E IDs. Retain existing WEB321 in adjacent regression selections. Manifest additions remain only the existing API/browser specs and this handoff; no new test file/dependency/migration. This checkpoint edits only those three files, preserves all pre-existing source-review authoring and does not execute any checks or commit. HEAD remains30c5ca27c6942f28198be724686a6088bc8795f4 pending parent gates.

## Parent integration state

All HTTP calls use the shared Operations guarded request, onDenied=sessionExpired and guarded list refresh. Unmounted readers ignore local callbacks; old-generation request failures become StaleOperationsRead before propagation. Final authorization now follows the potentially waiting versions read. API462/WEB464 cover those added boundaries. Connected23/23 passed in run2026-09-14T04-28-05-781Z-88756; gates passed141units before later edits. OFFLINE470 now uses the existing accessible on-device label instead of nonexistent visible text; that correction is authored and unexecuted. Latest user instruction stops further agent gates/tests/commits; use pnpm sdlc manually. No new dependency/migration for this child.
