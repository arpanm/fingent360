# Current implementation and verification status

## Continued implementation — 2026-09-15

DEV-006 public briefs, DEV-010 educational source-to-goal flow, DEV-015, DEV-016 progressive evidence reading, DEV-017 supported server privacy, SRC-012, ACTION-CENTRE-001/DEV-019 and initial SRC-007/SRC-008/SRC-010/SRC-011/SRC-015 are now implementation-complete for their stated scope. Authored acceptance is API/WEB/OFFLINE1280–1283,1290–1291,1300–1301,1320–1323 and1350 as applicable; see the case catalogue for project-specific IDs. Broader equity/complex-action coverage, complete causal impact policies, AMC look-through variants and bond price/credit sources remain explicitly open. Private-storage lifecycle and simple action/classification support are authored; their manual acceptance remains. Source activation, deployment and manual checks remain distinct. No new gate pass, applied migration, APK or commit is claimed.

Server encryption keys are now required for connected feedback/private financial records/MFA, not only optional AI history. Keep historical keys until all required records and backups have been rewrapped or expired. Registered migrations are listed in apps/api/src/migrate.ts; agents have not applied them. Baseline remains a2c53a0.

## Pickup queue batch — 2026-09-15

[DELIVERY-TEAM-005](../tasks/DELIVERY-TEAM-005.md) records authored source, privacy, monitoring and reader-diagnostic slices with exact functional gaps and manual commands. New additive migrations058,059,061,062 are registered but not applied by agents. No format/check/test/build/provider jobs/service changes/commit or push were run. Baseline HEADa2c53a0; current changes do not inherit any historical pass. The saved check log1789457453614-67049 closes only the older SDLC-REPAIR-005 formatting incident.

## Current task-status navigation — 2026-09-15

Use [Current delivery](../tasks/current-delivery.md) for the nine-workstream implemented/missing breakdown and [TODO](../../TODO.md) for the task index. Detailed records live in docs/tasks; the current-delivery rows separately identify implemented scope and remaining functional gaps. The previous TODO was preserved under docs/archive. This documentation reconciliation does not apply migrations, run tests or establish a new commit/pass. Historical entries below retain their original scope.

## SDLC-REPAIR-004 — feedback delivery formatting

The supplied `pnpm check` failure stopped at Prettier before later gates. Manually corrected saved review-status assertion wrapping in E2E-WEB-195, preserving its value and all existing real-path cases. Manual regression acceptance in TODO.md requires a targeted Prettier pass and preservation of every assertion in the repair diff. No behavior change requires new executable cases or fixtures. No formatter, check, test, service, SDLC or commit was run; verification remains pending. Existing HEAD is efd28d3; prior pending changes remain uncommitted. The full suite report was not inspected under this scoped repair instruction.

## DELIVERY-TEAM-004 — current authored batch

Nine workstreams are integrated across shared web/Android, API/contracts, additive migrations049 and051–057, offline readers/private storage, test authoring and documentation. [Exact implementation and remaining gaps](team-004/README.md) is the current handoff. It includes verified-schema NSE UDiFF parsing, approved automatic news publication, source/model/composite-view/feedback lineage, opt-in private AI history and transient story gesture guidance. Parent source/policy requirements remain partial where specified; five named broker parsers remain0/5.

No format/check/build/test/migration/provider jobs/service changes/APK generation/commit/push were executed for this batch. HEAD at authoring was efd28d3; pending changes do not inherit earlier test passes. User-run pnpm sdlc owns validation and gated commit. Historical entries below retain their original revision context.

## SDLC-COST-001 — scoped manual validation

User restored the manual execution boundary. Explicit checks-only SDLC mode and gate/order regression cases are authored, not executed or committed. Full-run behavior remains unchanged; selected E2E filters are preferred for feature validation. Measured full-run timing and commands are in development/sdlc.md. REGRESSION-015 fixes are committed through5fc0907; its fresh31-case affected rerun passed in2.3minutes, after the full run exposed26 failures (739 passed,1 skipped). This is combined full-run plus targeted correction evidence, not a second full-suite run. SDLC-REPAIR-001 adds failure-only CLI handoffs and bounded parent-script exact-case retries (no whole E2E rerun); the launcher changes remain unexecuted and uncommitted.

## REGRESSION-014 — API typecheck and cursor signing

User reports format/lint/shared-contract checks passed; API typecheck stopped on six fixed-URL receipt errors and two HMAC key errors. Provider construction now uses each existing receipt shape to retain literal URLs without casts or relaxed schemas. Audit and Publishing preserve bootstrap signing compatibility and use independent random32-byte controller keys when server authorization is the named-mode internal Symbol. The Symbol is unchanged and never converted into a public signing secret. Named cursors require Reset after an API restart or switch to another API instance; multi-instance shared-key deployment remains separate scope. Four actual-codec unit cases cover round-trip/tampering/public-Symbol forgery/cross-instance refusal and bootstrap compatibility/domain separation. Existing provider transport and API pagination/auth cases remain applicable. No deterministic checks, requests, builds, migrations, services or commits executed; HEAD remains b5cfcd0 and previous changes are preserved. User reruns the original pnpm sdlc command and filters; report its first remaining failure. Existing latest.md is the prior534-case E2E run, not this stopped pre-commit invocation.

## REGRESSION-013 — lint gate after successful formatting

User confirms format:check now passes. Lint stopped on11 issues across numerical ingestion, event extraction, named provisioning, CSV validation and consent/material test helpers. Corrections remove only unused catch resets and overwritten initializers, preserve all rollback and post-COMMIT state tracking, attach the original provisioning error as cause without changing the CLI's fixed redacted message, preserve CSV control rejection through character-code checks, and use an explicit worker exit branch/const receipt where appropriate. A contract case covers forbidden C0/DEL characters and allowed tabs, Unicode, quoted newlines and normalized CRLF. Authored only: no formatter/lint/typecheck/tests/services/migrations or commit executed. HEAD remains b5cfcd0 and all earlier pending changes are preserved. Rerun the same user-owned pnpm sdlc command with its original filters; report the first remaining failure. Existing API/browser/offline cases are unchanged apart from the two lint-only test corrections. The previous534-case E2E report is historical, not evidence for this pre-commit run.

## REGRESSION-012 — formatting gate correction

User reports format completed but format:check rejected apps/api/src/events.ts, apps/api/src/operator-permissions.ts, tests/e2e/helpers/mapped-import.ts and tests/e2e/helpers/supplemental-holdings.ts. Authored whitespace-only wrapping corrections; runner/configuration are unchanged and sequential. The precise original divergence has not been reproduced, and no formatter/check/test was run. The user's invocation stopped before later checks, commit and E2E. HEAD remains b5cfcd0; the entire prior roadmap and regression batch remains uncommitted. Manual acceptance: rerun the original pnpm sdlc invocation with its existing filters; these four warnings should clear, and all remaining gates must pass before a commit. The old latest.md (2026-09-14T08:28:34.851Z,534 selections,API4103/web5175) is not this failure's test evidence. No dependencies, migrations, services or browser acceptance changes are required for this whitespace-only correction.

## ROADMAP-COMPLETE-003 — current authored implementation

The user requests completing remaining Partial/Planned tasks, then explicitly authorizes choosing broker order and researching export formats from official documentation. Current complete authored children: GOAL-FEASIBILITY-001 (migration037, owned receipts/private export/local parity), EVIDENCE-LAYERS-001 (exact admitted edition and private note layers), QUALITY-OVERVIEW-001 (bounded stored quality plus safe request correlation/process counters). MAPPED-IMPORT-001 with researched platform help, NAMED-OPERATORS-001 and MATERIAL-ALERTS-001 are also authored. ECB-RATES-001 is implemented for its selected numerical family. EVENT-REVIEW-001 and BROKER-DIALECTS-001 supplemental-cost reconciliation are authored. EIA-BENCHMARKS-001 implements World Bank monthly Brent/WTI after primary licence/format research. MATERIAL-AUTO-001 adds explicit daily stored-observation checks with durable due state, privacy permission and app-open offline parity. CONSENT-LIFECYCLE-001 and ECB-FX-001 are authored; EVENT-LINEAGE-001 and IDENTITY-ADJUDICATION-001 are authored; EVENT-EXTRACTION-001 is also authored with migration047, API850–862, WEB850–855 and OFFLINE850–851. Identity adjudication uses migration048, API870–875, WEB870–873 and OFFLINE870–871. Further named broker format research continues. The ongoing roadmap handoff records exact cases and remaining boundaries. These are not verified or committed. Baseline remains b5cfcd0; all previous REGRESSION-011 edits remain uncommitted and preserved. Agents have not executed gates, tests, migrations, providers or builds. Source-format documentation research is read-only and does not establish provider ingestion rights. Installed APK remains the previous build.

## Current — REGRESSION-011

User evidence for b5cfcd0: run2026-09-14T08-28-33-631Z-98876, API4103/web5175,534 attempts,522 passed/11 failed/1 skipped. The prior format/lint/typecheck gates completed before that commit. [Correction scope and selected rerun](regression-011.md) cover API586, WEB500–503, WEB521 and WEB421. Current corrections are unverified and uncommitted; no deterministic execution or DB/service changes by agents. Historical entries below retain their original revision context.

## DB-LEAST-PRIVILEGE-001 — E2E fixture type correction

Latest user output confirms format, lint and application typechecks passed; E2E typecheck stopped at API586 TS2347 (untyped observer queries) and TS18048 (optional provisioning promise). Reused ownedRetentionDatabase for a typed, guarded observer; added an explicit lazy module contract and a definite provisioning promise shared with optional teardown state. Marker checks, cancellation assertions, lazy imports and final connection closure remain. No type suppressions or non-null assertions. Correction authored without executing checks/tests or database actions; same user pnpm sdlc command/filters is the next step. No commit/E2E stage was reached by the reported invocation; HEAD remains6f2b508 and earlier pending changes are preserved. The older latest.md is unrelated offline470 evidence.

## DB-LEAST-PRIVILEGE-001 — reported lint failure corrected

User reports format passed and check stopped at ESLint no-unsafe-finally in API586 (lines435/445 before this edit). No commit or E2E stage was reached by that invocation. Extracted the owned-role cleanup into a local async helper outside finally, retaining marker/identifier refusal before grants/drop, pending-query settlement and unconditional pool closure. No lint suppression, production grant change or database operation. Correction is authored only; rerun the same pnpm sdlc command and filters. Current HEAD remains6f2b508; all preceding pending integration remains uncommitted. The older latest.md offline470 run is unrelated to this pre-E2E failure.

## Current — DELIVERY-RECONCILE-002 local activation

[Local activation record](local-database-activation.md) supersedes earlier unapplied/not-provisioned handoff statements: runtime role enabled in the ignored .env, migrations035/036 applied with grants refreshed, and this repo’s development session restarted on web5175/API4103. Readiness returned200 and actual PostgreSQL API connections use fingent360_runtime. This was the user-authorized local setup exception; no format/check/E2E/sdlc or commit was run. Normal development compiler watchers resumed with the service. Previous run results below remain historical.

All visible ad hoc requests are indexed in [session request register](session-request-register.md), including restored ASSIST-001/MEDIA-001 TODO aliases. DOMAIN-CONTRACTS-002 is executable schema/data-invariant code with goldens; its persistence/consumer mapping explains why it is not a deployed event/policy engine. PUBLISHING-QUEUE-001 includes applied indexes and the complete filter/page/retry/existing-review flow. Newly authored regressions remain awaiting user gates.

## ROADMAP-AUDIT-001 — implementation and status reconciliation

The audit distinguishes completed implementation from Partial parents with concrete remaining acceptance. DEV001/002 documents were brought up to date; DOMAIN-CONTRACTS-002 supplies the missing DEV003 schema/golden categories; PUBLISHING-QUEUE-001 is integrated with the existing source-review and BEA receipts. DB-LEAST-PRIVILEGE-001 is integrated with API580–586 and four units; migration035 remains registered, unapplied. No roles or credentials were provisioned/changed. [Exact gaps](roadmap-gaps.md) and [current manual commands](roadmap-audit-handoff.md) supersede stale “In progress” descriptions. Real event/company/policy features and broader source/platform coverage are not declared complete.

No deterministic commands or commits were run for this audit. Current HEAD is 6f2b508; earlier pending integration remains uncommitted together with this work until user gates pass. New contract cases and queue/API/browser/offline cases are authored only. WEB484/521 follow the new queue; WEB501 covers retry plus durable BEA recovery history, and API520 preserves all stable public feed fields while treating evaluatedAt as a read timestamp. Prior recorded passes below do not verify these changes.

## Current handoff — user-run validation only

The latest user instruction stops agent-run format/check/build/migration/E2E execution and supersedes earlier authorization throughout this log. Earlier passes remain historical evidence for their recorded versions. No new gated commit is claimed for the current working tree. The user invokes `pnpm sdlc "Complete pending workflow integration"` for format → check → local commit → E2E; optional Playwright filters follow `--`.

SOURCE-REVIEW-DIFF connected run2026-09-14T04-28-05-781Z-88756 passed23/23 in1.6minutes before this instruction. This includes13feature instances and10 existing publication/audit/withdrawal regressions; format/check had passed141units. Its exact handoff is artifacts/source-review-connected-handoff.md. Desktop1280×720/mobile390×844 controlled screenshots were inspected. Initial selected offline run passed5/6; OFFLINE470 incorrectly searched visible text for the on-device banner's accessible label. A subsequent attempt still used that incorrect text locator. The authored correction now uses getByLabel, matching existing passing offline cases; it has not been rerun. Source review's final storage authorization, shared guarded requests, unmounted callback guard and old-session failure normalization are included.

BEA-QUARANTINE code/contracts/UI/migration035 and API500–506/WEB500–503/OFFLINE510 are integrated as authored, unverified work. Migration035 has not been applied, and no new provider refresh or native APK build has been run. Existing applied ledger remains28 migrations including032. Existing installed/native artifact remains code5; changing web source does not update an installed APK. OPS-READ-ADMISSION and the bounded legacy fixture isolation checkpoint are integrated under the same authoring-only boundary. Existing app/account/financial data and services are preserved.

User prerequisites for this batch: existing PostgreSQL/MongoDB and configured app; after reviewing changes, `pnpm build && pnpm db:migrate` applies035, then run `E2E_BROWSER=chrome pnpm sdlc "Complete pending workflow integration"`. For selected feature checks use `-- --grep 'SOURCE-REVIEW-DIFF-001|BEA-QUARANTINE-001|OPS-READ-ADMISSION-001|LEGACY-FIXTURE-ISOLATION-001'`. A separate user-run `pnpm android:build` rebuilds the offline APK; packaged offline cases use `E2E_BROWSER=chrome pnpm android:test`. No commands in this paragraph have been run after the latest instruction. Watch mode stays off; failures should include artifacts/e2e/latest.md and the first assertion. No push.

## BEA-QUARANTINE-001 — retained-response inspection and recovery

Integrated immutable additive035 attempt/events/verified response links, parser-versioned validations and exact staging receipts. Normal fixed BEA ingestion records the real run and complete verified retained bytes; parsed drafts and staged event commit together. Later finalization failure leaves that success evidence intact. Mongo operations have3second driver deadlines and2.5second server bounds; staging uses3second statements, a10second operation deadline, fixed gate and sorted heads. Strict UTF8 preserves byte provenance; interrupted/invalid bytes never gain a retained link. Legacy evidence without a trusted link stays ineligible.

Protected Operations list/detail/evidence→explicit stored-byte revalidation→candidate/current-baseline review→draft staging→guarded Publishing gives full loading/empty/failed/unavailable/retry/uncertain/historical/conflict/401/Back/keyboard/mobile states. History pages/reopen preserve original rejected or accepted receipts. Lost replies retry exact request IDs; stale heads conflict. Shared Operations request/session guard handles denial even after Back. No raw editing/arbitrary URL/hash, force acceptance, auto-publication, source-rights expansion or financial mutation. Device mode explains connection requirements without API traffic.

Authored API500–506, WEB500–503 desktop/mobile and OFFLINE510. Actual captured permitted BEA RSS enters isolated storage through labelled transport fixtures, including invalid bytes/stream faults, altered/missing evidence, replay, genuine lock expiry, rollback and crash-after-commit. Current integration is authored; further verification and commit await the user-run SDLC command.

Manual review: existing PostgreSQL/MongoDB, configured operator, API4103/web http://127.0.0.1:5175/#ops. Apply035 using `pnpm db:migrate` after build; `E2E_BROWSER=chrome pnpm e2e:ui`, select @BEA-QUARANTINE-001 plus existing BEA/provider regressions, watch off. Rebuild with `pnpm android:web`, then OFFLINE510 in `E2E_BROWSER=chrome pnpm android:test:ui`. Expect verified retained originals, no-network revalidation, exact draft receipts and explicit publication review. Report artifacts/e2e/latest.md/run/project/redacted first assertion, never keys/cookies. Physical phone/TalkBack, broad adapters, roles and production source acceptance remain separate. No push.

## SOURCE-REVIEW-DIFF-001 — inspect actual publication changes

Integrated strict comparison contracts and protected exact-head read, current source SHARE admission and post-wait wall-clock authorization. The baseline is the nearest preceding non-draft public state, explicitly labelled published or withdrawn; first publications have no predecessor. Original snapshots/evidence remain unchanged. Existing exclusive version-checked publication/withdrawal writes are reused.

The extracted review dialog provides readable field differences, retained evidence/Back, review note, changed-head Reload and historical saved receipt. Its requests use the parent Operations generation/denial barrier, including401 after Close; late successful comparisons cannot reopen the dialog. Successful mutation keeps its receipt while refreshing the guarded list. No new data, provider, background task, migration or dependency. Connected-only device notice is reused.

Authored API460–462, WEB460–464 desktop/mobile, OFFLINE470 and1 contract unit. WEB321 now closes the successful review receipt explicitly before continuing unchanged publication/withdrawal assertions. Current integration is authored; further verification and commit await the user-run SDLC command.

Manual review: existing PostgreSQL/MongoDB/API4103, web http://127.0.0.1:5175/#ops. `pnpm build`, `E2E_BROWSER=chrome pnpm e2e:ui`, select @SOURCE-REVIEW-DIFF-001 and WEB321, watch off. `pnpm android:web` then `E2E_BROWSER=chrome pnpm android:test:ui` OFFLINE470. Expect exact dated differences, protected evidence, stale409 reload, saved decisions and sign-out clearing. Report artifacts/e2e/latest.md/run/project/first assertion without keys/cookies. Physical phone/TalkBack, named operators/four-eyes and production acceptance remain open. No push.

## OPS-AUDIT-001 — protected request activity

Integrated strict safe audit projections, fixed module/event/time filters and50-row keyset pages with microsecond ordering and signed filter/upper/before cursors. Final operator-session SHARE admission followed by wall-clock authentication covers waits after the audit read. Queries never select raw targets or actor/session hashes. Existing immutable table/trigger is reused; no new dataset, migration, dependency, provider or background task.

Operations adds a keyboard/mobile audit browser, applied-filter recovery, page/error/empty/Retry/Reset states and accurate request-versus-completion labels. Its shared request/session-generation barrier protects parent publication/evidence and source registry/refresh reads plus audit responses returning after Back. Same-session401 clears protected parent state; earlier success cannot restore it or sign out a newly authenticated generation. Independent older operator modules retain their existing handling. Device bridge returns connected-only503 and preserves owned finances with no network.

Authored API480–487, WEB480–486 desktop/mobile, OFFLINE490 and4 units. Parent verification: connected run2026-09-14T04-18-51-933Z-86121 passed30/34 including all8 API cases,10 selected existing publication/cleanup/worker/evidence regressions and12 other audit browser instances. WEB481 on desktop/mobile used an exact text locator that omitted the displayed section prefix; it was scoped to the actual paragraph while retaining the request-only semantics and row-count checks. The shared session guard initially replaced the cleanup-specific sign-in recovery message; restored that guidance while retaining the shared invalidation barrier and unchanged cleanup test. Final four-case rerun passed4/4; all34 selected scenarios have passing evidence across these runs. Exact handoffs: artifacts/ops-audit-connected-initial-handoff.md and ops-audit-connected-handoff.md. Rebuilt device assets passed4/4 selected offline cases in4.7seconds (see artifacts/ops-audit-offline-handoff.md). WEB480390px captures from both projects were inspected; no overflow and keyboard pages/filter/navigation passed. Initial typecheck found optional filter values in URLSearchParams; explicitly omitted absent values and reran gates. Format/check passed140 units (37contracts,91API,12tooling); documentation-inclusive final logs are artifacts/ops-audit-final-format.log and ops-audit-final-check.log. Resource audit2026-09-14T04:24:18.511Z found zero temporary PostgreSQL/Mongo resources, exact original account/financial digests and28 migrations preserved. READING-FOLLOW-001 was committed99be437. Shared device assets include the change; final native packaging/physical-device acceptance remain separate. Source-review integration follows this shared session guard. A fixed upper timestamp/ID is a chronological boundary, not a retained database snapshot or complete audit export; later commits of earlier recorded requests may appear.

Manual review: configured PostgreSQL/MongoDB/API4103 and web http://127.0.0.1:5175/#ops. `pnpm build`, `E2E_BROWSER=chrome pnpm e2e:ui`, select @OPS-AUDIT-001 api/desktop/mobile (22instances), watch off. `pnpm android:web` then `E2E_BROWSER=chrome pnpm android:test:ui` OFFLINE490. Expect strict bounded activity, accurate request-only labels, no private body/hash disclosure, consistent paging/expired-session recovery and noAPI device notice. Share artifacts/e2e/latest.md/run/project/first assertion. Physical phone/TalkBack and named roles/four-eyes/production acceptance remain open; no push.

## READING-FOLLOW-001 — explicit subscriptions and change receipts

Integrated source/topic settings, atomic initial baselines and explicit stored-edition checks, coalesced notices, precise overlapping-follow/mute/unmute/acknowledgement rules, durable historical replay and complete account-owned event export. Migration032 adds three owned cascading tables and an immutable ledger. Scans use sorted200-item batches with post-wait authorization,3second statement bounds and a10second operation deadline; failed checks roll back. Current/history pages are bounded100-row reads without a lifetime cutoff. Withdrawn provider text is never stored in the notices, and current-reading links require actual publication admission.

Shared UI includes review/consent/draft discard, all loading/error/retry/uncertain/historical/current states, page filters, history closure during pending requests, disabled pending settings, private401 clearing and late-response guards. Complete privacy collects both reading and schedule pages before download and rechecks the owner. Actual serialized device storage preserves the same transitions/deletion without network access. No provider, LLM, background worker, financial mutation or new dependency.

Authored API440–445, WEB440–445 desktop/mobile, OFFLINE450–451 and5 domain units. Main integration preserves later schedules/source-withdrawal/holdings exports and report comparison routes. Parent verification: migration032 applied successfully;28 migrations and exact original account/financial digests preserved. Connected run2026-09-14T04-09-57-863Z-82773 passed25/25 in51.4seconds (18 feature instances plus7 privacy/source-wait regressions). Earlier failing assertions exposed lexical sequence pagination, disappearing vanished-topic choices and an ambiguous source/topic locator; the numeric ordering, retained editor choices and scoped locator were corrected without weakening completeness checks. Readable current titles are admitted separately from text-free history. Navigation now keeps Saved active; final WEB440 desktop/mobile run2026-09-14T04-12-47-995Z-83946 passed2/2 in8.7seconds and1280×720/390×844 screenshots were inspected. Rebuilt shared assets passed10/10 selected offline scenarios; final navigation rebuild passed OFFLINE450–451 again2/2 in3.4seconds. Exact handoffs: artifacts/reading-follow-connected-handoff.md, reading-follow-offline-initial-handoff.md, reading-follow-navigation-handoff.md and reading-follow-offline-navigation-handoff.md. Audit2026-09-14T04:11:41.482Z found zero temporary PostgreSQL schemas/Mongo databases. Format/check passed136 units (33contracts,91API,12tooling); documentation-inclusive final gates are artifacts/reading-follow-final-format.log and reading-follow-final-check.log. REPORT-COMPARE-001 was committed locally as f592e13. APKcode5 still predates this batch; packaging and physical-device acceptance remain separate.

Manual review: existing PostgreSQL/MongoDB and API4103/web http://127.0.0.1:5175/#reading-follow. Apply additive032 using `pnpm db:migrate`, build, then `E2E_BROWSER=chrome pnpm e2e:ui`, select @READING-FOLLOW-001 (18 connected instances), watch off. Rebuild shared assets with `pnpm android:web` and use `E2E_BROWSER=chrome pnpm android:test:ui`, OFFLINE450–451. Expect baseline/no-backlog, explicit checks/ack/reopen, preserved overlap, complete export and noAPI locally. Share artifacts/e2e/latest.md with run/project/assertion. Device knowledge is limited to the bundle; physical phone/TalkBack and external delivery remain separate. No push.

## REPORT-COMPARE-001 — compare actual issued originals

Integrated strict metadata-only choices and read-only comparison contracts/API, exact BigInt/scaled changes across saved goals/contribution-only projections/holdings/allocation bindings, additions/removals/unknowns and dated v1/v2 research receipts. Deterministic capture chronology is independent of input selection. Account SHARE then sorted owned job SHARE locks and final wall-clock authentication prevent foreign/deleted/unissued/revoked original disclosure. No new table, migration, dependency, worker, private dataset or report-capacity use.

Responsive UI covers choose/review/cancel/compare/original/Back/reload/deep links, fewer-than-two/failed/unreadable/current-missing/401 states, independently versioned reads and immediate denial. Reports now keys by the full route so a changed original ID cannot keep an older reader mounted. Existing original dialog closes on first Escape/Back before navigation. Local serialized comparison reads actual issued originals and never issues queued work; export/deletion policy is reused. Authored API420–426, WEB420–425 desktop/mobile, OFFLINE430–432 and five exact-domain units. Connected run2026-09-14T03-53-41-415Z-77355 passed33/37 with four browser harness failures (WEB421/423 on both projects); all API, original/deletion/schedule and other comparison checks passed. WEB421 now scopes status to its comparison region, excluding the footer. WEB423 holds an active explicit refresh after the initial route settles, preserving the actual late-result assertions. Corrected run2026-09-14T04-00-47-203Z-79696 passed4/4 in30.3seconds. All37 selected scenarios now have passing evidence; production code was unchanged by the harness corrections. Hand-offs: artifacts/report-compare-connected-initial-handoff.md and report-compare-connected-handoff.md. Format/check passed with131 units (28contracts,91API,12tooling). Rebuilt shared assets passed20/20 selected packaged offline cases, including all3comparison cases and17 report/schedule regressions; exact run identity is in artifacts/report-compare-offline-handoff.md. The WEB420 screenshots were inspected at390px in both project runs (the case intentionally narrows the viewport); ordinary desktop behavior is exercised before that resize. Final resource audit found zero temporary PostgreSQL/Mongo resources and exact original account/financial digests preserved,27 migrations. Final gates: artifacts/report-compare-final-format.log and report-compare-final-check.log. Installed code5 APK predates the feature; batch rebuild remains pending. HOLDINGS-RECONCILE-001 was committed locally as30c5ca2. Registration prompts for the next two TEAM007 children and correction of stale parent roadmap statuses are included in this delivery.

Manual review: existing PostgreSQL/MongoDB/API4103 and web http://127.0.0.1:5175/#report-compare; `pnpm build`, `E2E_BROWSER=chrome pnpm e2e:ui`, select @REPORT-COMPARE-001 api/desktop/mobile (19 executions), watch off. `pnpm android:web` then `E2E_BROWSER=chrome pnpm android:test:ui`, OFFLINE430–432. Expected exact changes between two actual issued reports, preserved originals, truthful missing/deleted/sign-out states, keyboard/mobile/Back and zero API traffic on device. Share artifacts/e2e/latest.md with run/project/assertion on failure. Physical phone/TalkBack and real-user design acceptance remain separate; no provider request, financial mutation or push.

## HOLDINGS-RECONCILE-001 — exact replacement review

Integrated strict reconciliation contracts and exact scaled quantity/integer-cost differences, durable owned baseline in existing preview JSONB, server recomputation before confirmation, explicit removal acknowledgement and unchanged successful replay. Legacy confirmed receipts remain replayable; unconfirmed legacy/corrupt/expired reviews require a fresh preview. Account/preview/holdings waits retain wall-clock authorization and expiry checks, including after expired-preview cleanup DELETE.

Manual/CSV/XLSX UI shares complete review, dependency counts, saved/conflict/error states and explicit draft-preserving baseline refresh. All initial and later reads use generations so stale responses cannot overwrite a newer baseline or input;401 invalidates pending reads. Actual allocation/connection dependencies are dated counts only. Local serialized storage, privacy export and deletion preserve parity; no raw workbook retention, prices, new migration or dependency.

Authored API400–403, WEB400–402 desktop/mobile, OFFLINE410 and exact-arithmetic units, plus explicit-consent compatibility updates to API090/240/242/268, WEB090 and offline research connections. API403 observes the actual blocked cleanup DELETE; WEB402 holds every initial read to cover development effect replay and production mount. Connected run `2026-09-14T03-49-50-574Z-75463` passed31/31 in1.6minutes: all10 feature executions plus21 workbook, removal and post-wait authorization regressions. Hand-off: artifacts/holdings-reconcile-connected-handoff.md. Desktop1280×720 and mobile390×844 WEB400 review captures were inspected; no horizontal overflow, with keyboard consent and subsequent confirmation/reload verified by the actual UI cases. Authored contract typing/lint issues were corrected before execution; both format and check passed with126 units (23contracts,91API,12tooling). Rebuilt shared device assets passed14/14 selected offline cases in10.1seconds, including OFFLINE410 and all selected workbook/connection/ownership regressions. Exact offline run identity: artifacts/holdings-reconcile-offline-handoff.md. The installed code5 APK still predates this feature; batch rebuild follows after integration. Final gates: artifacts/holdings-reconcile-final-format.log and artifacts/holdings-reconcile-final-check.log. Audit2026-09-14T03:51:47.915Z found zero temporary PostgreSQL schemas/Mongo databases, exact original account/financial digests preserved and27 migrations. SOURCE-WITHDRAWAL-001 was locally committed as4c814ae.

Manual review: current PostgreSQL/MongoDB/API4103 and web http://127.0.0.1:5175/#holdings; `pnpm build`, `E2E_BROWSER=chrome pnpm e2e:ui`, select @HOLDINGS-RECONCILE-001 api/desktop/mobile and compatibility cases, watch off. `pnpm android:web` then `E2E_BROWSER=chrome pnpm android:test:ui`, OFFLINE410 plus workbook/research-connection regressions. Expected four exact groups, consent before removal, stale review recovery, unchanged dependent records and zero network on device. Share artifacts/e2e/latest.md/run/project/assertion on failure. Physical phone/spreadsheet/TalkBack acceptance remains separate; no real financial documents are test fixtures and no push.

## SOURCE-WITHDRAWAL-001 — consistent current-publication disclosure

Integrated sorted source admission and strict projection across current/history/evidence/media/context/personalized feed, saved reading, reminder replay/edit/cancel/notifications and private export. Withdrawn public histories become dated tombstones; selected-edition evidence prevents a still-published sibling from exposing withdrawn shared RSS. Protected original history/raw evidence/media remain available to authenticated operators after final waits. Immutable stored snapshots, own notes, exact finances and issued report copies remain intact. No new migration, dependency, cleanup or source-rights claim.

Private operations use account-before-source admission and post-wait wall-clock authorization. Coherent repeatable-read export locks account/session before sources and denies a changed session snapshot. Async assistance revalidates sources and authentication after provider completion. The existing reminder worker now selects bounded candidates, locks sorted eligible accounts and sources, then claims only still-due eligible reminders; root worker-control admission remains first and health observations/tick/shutdown are preserved.

Reader/Saved/Operations provide withdrawn controls, escaped protected originals, Refresh/Retry/history/Back and late-response invalidation. Actual local public/private handlers resolve highest non-draft feed/history editions. Snapshot collection obtains a final admitted manifest after reads, removes withdrawn assets and rejects changed current editions before atomic replacement. Old disconnected bundles cannot discover later withdrawals. Authored API370–383, WEB370–374 desktop/mobile, OFFLINE390–393 and three projection units. Existing API122 intentionally expects projected withdrawn text; OFFLINE353 removes BEA from every bundle collection when simulating its absence. Selected verification passed. Connected run **2026-09-14T01-56-48-000Z-71915**, handoff **1789351009131-1292a0fd-5a9c-404c-a8c6-8e749d737a7f**, started01:56:49.131Z: **27/27 passed,1.8minutes**, all24 new feature executions plus existing API/desktop/mobile media160. Initial run **2026-09-14T01-50-56-534Z-71432**, handoff recorded in artifacts/source-withdrawal-connected-initial-handoff.md, passed48/51, including all23worker-health and4source/connection regressions. API375 expected200 from an actual201 review-check creation; WEB374's exact text locator included the adjacent Retry button. Corrected the expected existing status and targeted the real alert without weakening disclosure assertions. Logs: artifacts/source-withdrawal-connected.log, source-withdrawal-connected-fixed.log and source-withdrawal-connected-handoff.md.

The genuine public snapshot was rebuilt at **2026-09-14T01:59:50.228Z**: **179 published items,52 annual histories,22 reviewed visuals,5 security identities**, with only public loopback GET routes and a final publication manifest. No synthetic source fixture entered the app. Scoped evidence replaces shared raw RSS in the distributable bundle; protected originals remain stored. Packaged offline initial handoff **1789351195620-ac5830b8-da60-4bf7-88a2-419d10fccf55**, started01:59:55.620Z at web58389, passed25/26 in12.4seconds. OFFLINE392 incorrectly expected the check receipt to contain a current notice; it now validates the dated receipt and fetches the actual inbox for the open withdrawal reason. Final feature handoff **1789351316572-85435855-1bd1-4fe0-8998-a7dbbafc29c9**, started02:01:56.572Z at web58970: **4/4 passed,2.7seconds**. All26selected scenarios have passing evidence across runs, covering BEA, connections/review inbox, source reading, issued receipts and finances. Logs: source-withdrawal-snapshot.log, source-withdrawal-offline-build.log, source-withdrawal-offline.log, source-withdrawal-offline-initial-handoff.md, source-withdrawal-offline-fixed.log and source-withdrawal-offline-handoff.md.

WEB370 captures inspected at1280×720 and390×844 show the dated unavailable heading, preserved source identity/history/Back and absent original/evidence/media/connection CTAs with no horizontal overflow. At02:00:21.083Z, source-withdrawal-resource-audit.json records zero temporary PostgreSQL schemas/Mongo databases; team2-records-source-withdrawal-after.json exactly preserves pre-TEAM003 account/financial counts/digests and27migrations. The existing reminder unit fixture now supplies full admitted editions and tests pause/ineligible/no-longer-due behavior while preserving current-title/withdrawal assertions. Format/check passed124units (21contracts,91API,12tooling); documentation-inclusive final logs are artifacts/source-withdrawal-final-format.log and source-withdrawal-final-check.log. REPORT-SCHEDULES-001 was committed as0e109ab. Installed APKcode5 still predates these changes; shared assets have been rebuilt and final APK packaging remains required.

Manual review: current PostgreSQL/MongoDB/API4103 and web http://127.0.0.1:5175; `pnpm build`, `E2E_BROWSER=chrome pnpm e2e:ui`, select @SOURCE-WITHDRAWAL-001 api/desktop/mobile (24 executions), watch off. Use owned synthetic source lifecycle records in tests; do not withdraw real app content merely to verify. After genuine public snapshot collection, `pnpm android:web`, then `E2E_BROWSER=chrome pnpm android:test:ui` OFFLINE390–393 and reading/report/library regressions. Expected unavailable public text/CTAs, preserved protected originals/own records, correct admission and zero network on device. Share artifacts/e2e/latest.md with run/project/assertion. APK rebuild/reinstall, physical phone/TalkBack and prior downloaded copies remain separate. No push.

## REPORT-SCHEDULES-001 — explicit scheduled record capture

Integrated strict schedule/configuration/receipt/export schemas, additive029, owned immutable editions and occurrence uniqueness, five-live-schedule policy, actual existing financial snapshots and the ordinary report issuance worker. The report-control admission is first in each schedule transaction, before account/schedule locks, preserving committed Pause across instances. Account deletion and configuration writes serialize on account ownership. Source research receipts require a separate explicit report selection and are not automatically scheduled.

Daily/weekly IANA wall time resolves DST folds to the first occurrence and gaps to the next valid minute within three hours. Create/edit/resume uses the next future date. An active schedule missed during downtime captures only the latest due date at the actual execution time, preserving skipped counts and capacity/validation outcomes. Immutable configuration/operation/occurrence history has no lifetime shutdown: bounded keyset pages and frozen upper cursors provide complete privacy/schedule downloads, with final account confirmation and no partial download on failure/401.

UI covers review/consent/Back, weekday/timezone, pause/resume/edit/delete, historical receipt versus authoritative state, response loss/exact retry, conflict draft discard/reload, issued-report deep links and missing/deleted recovery. Privacy clears private controls on typed401. Actual serialized device storage materializes due work on eligible reads, performs no API traffic and includes owned export/deletion. Authored API330–335, WEB330–335 desktop/mobile, OFFLINE360–361 plus timezone units. Selected verification passed. Connected feature run **2026-09-14T01-38-44-157Z-68081**, handoff **1789349925151-54205c9c-3bd6-40e3-92ab-81c9d7837747**, started01:38:45.151Z: **18/18 passed,43.9seconds**. The initial44-case run **2026-09-14T01-36-55-773Z-67689**, handoff **1789349816830-90164c84-ce6a-44c0-a823-8eab85cb1ea3**, started01:36:56.830Z, passed41 and failed3 date-aging fixtures before worker execution. Explicit text-to-timestamp casts fixed conflicting PostgreSQL parameter inference, preserving the original ISO payload. All23 health and3 privacy regressions passed in that initial run. WEB050 now validates the complete download contract rather than the API page envelope. Logs: artifacts/report-schedules-connected.log, report-schedules-connected-initial-handoff.md, report-schedules-connected-fixed.log and report-schedules-connected-handoff.md.

Packaged offline handoff **1789350029979-767ea67a-62fd-45f0-bb65-19e62739c4e0**, started01:40:29.979Z at web51122: **20/20 passed,46.9seconds**, including both schedules, original reports/research/deletion, health and financial regressions. A subsequent visual review corrected Next run to display the schedule's timezone; occurrence history labels the viewing device's timezone. WEB330 then passed **2/2,8.4seconds**, run **2026-09-14T01-43-05-219Z-69207**, handoff **1789350186025-5bfa8c0a-82a3-4b6c-8744-ba225d3db705**, started01:43:06.025Z, with explicit09:00 America/New_York assertions. Both1280×720 and390×844 captures were inspected: readable cadence/zone/next time, wrapped actions, persistent navigation and no horizontal overflow. Rebuilt offline schedules also passed2/2; exact handoff/time is in artifacts/report-schedules-offline-timezone-handoff.md. Logs: report-schedules-offline.log, report-schedules-offline-handoff.md, report-schedules-visual-fixed.log, report-schedules-visual-handoff.md, report-schedules-offline-timezone-build.log and report-schedules-offline-timezone.log.

At01:40:38.932Z the resource audit found zero temporary PostgreSQL schemas/Mongo databases; the pre-TEAM003 account/financial counts/digests remain exactly unchanged, with27 applied migrations including029/030. Evidence: artifacts/report-schedules-resource-audit.json and team2-records-report-schedules-after.json. Stable format/check passed118units (18contracts,88API,12tooling); the first format pass needed one further stable pass on the new browser test. Documentation-inclusive final gates: artifacts/report-schedules-final-format.log and report-schedules-final-check.log. WORKER-HEALTH-001 was committed asa18010b. Installed APKcode5 predates schedules and requires batch rebuild/reinstall. Root registered READING-FOLLOW-001's complete prompt before parallel isolated code authoring; integration still waits for source withdrawal.

Manual review: `pnpm build`, `pnpm db:migrate`, current PostgreSQL/MongoDB/API4103 and web http://127.0.0.1:5175/#report-schedules. `E2E_BROWSER=chrome pnpm e2e:ui`, select @REPORT-SCHEDULES-001 api/desktop/mobile, watch off (18 executions). `pnpm android:web` then `E2E_BROWSER=chrome pnpm android:test:ui`, OFFLINE360–361. Expected: explicit consent, truthful next occurrence/history, one actual capture across workers, exact owned records and preserved deletions, full export and zero network in device mode. Share artifacts/e2e/latest.md with run/project/assertion on failure. Physical phone/TalkBack, storage pressure and real server uptime remain separate. No push or main-account schedules created as verification.

## WORKER-HEALTH-001 — aggregate worker visibility and explicit controls

Integrated two existing workers with minimal aggregate heartbeat/last-success/safe-failure observations, bounded queues/ages and immutable operator control receipts. Additive030 is independent of029: missing report-schedule storage yields dueSchedules:null/not enabled. Stale/unobserved/storage unavailable are distinct; counts cap10000 with moreAvailable and no private snapshots/IDs/titles/raw exceptions. Reports and reminders retain their existing intervals, leases/uniqueness and actual execution semantics.

Pause/resume uses expected version/UUID/confirmation, global/control locking and post-wait wall-clock operator authorization. Every new work transaction first holds the control row FOR SHARE; committed Pause fences later claims across instances. Already committed report leases may finish. Control receipts retain1000per worker; monotonic versions prevent an evicted old intent applying anew. Historical receipt replay must reload current state. UI covers initial error/retry/conflict/401/keyboard/mobile; local mode returns a connected-operator explanation with no API calls.

Peer review corrected finish persistence failures classified as preparation, and added a wall-clock lease check after the actual report row lock. API360 exercises a trigger-rejected persistence write with safe storage classification and real recovery; API362 holds a claimed row while the lease expires, rejects stale completion, then proves one successful reclaim. Authoring includes API350–362 (13), WEB350–354 (5 each desktop/mobile), OFFLINE380 and contract cases. Fixture manualWorkers defaultsfalse and only selected health cases drive actual owned workers by IPC; early validated schema annotations improve failed-setup cleanup evidence. Parent verification completed for the selected feature and regressions. Connected run **2026-09-14T01-30-27-929Z-65369**, handoff **1789349428993-b96f017f-3e1c-46fa-9ad7-fd6772d844c3**, started01:30:28.993Z: **39/39 passed,2.3minutes**. This includes all23 health executions, actual library/reminder and account/reading/retention regressions. The earlier74-case run **2026-09-14T01-21-10-483Z-63390** passed73 and failed only API358's synthetic md5 UUIDs; replacing them with valid gen_random_uuid fixtures corrected the test without weakening history validation. Its51 existing report regressions already passed. Logs: artifacts/worker-health-connected.log, worker-health-connected-initial-handoff.md, worker-health-connected-fixed.log and worker-health-connected-handoff.md.

Peer review additionally corrected shared HTTP handling: a received401 now cancels its response body and throws immediately, before JSON parsing. WEB353 preserves an actual server401 while holding only its body reader, confirms zero JSON reads and immediate private-screen clearing, then releases an older actual200 and proves no restoration. Both browser projects passed. Shared account/reading/retention regressions cover normal callers.

Packaged offline handoff **1789349588605-453c7d46-1f3d-43f3-9968-cec3732d8c06**, started01:33:08.605Z at web63863: **19/19 passed,47.1seconds** after rebuilding shared assets; OFFLINE380 plus report/research/reading/financial regressions. Evidence: artifacts/worker-health-offline-build.log, worker-health-offline.log and worker-health-offline-handoff.md. Both WEB350 captures were inspected at390×844: readable scrollable controls, visible paused/current distinction and no horizontal overflow; desktop workflow also executed, but these captures intentionally switch to mobile width.

At01:33:13.378Z, artifacts/worker-health-resource-audit.json records zero temporary PostgreSQL schemas/Mongo databases. artifacts/team2-records-worker-health-after.json exactly preserves the pre-TEAM003 account/financial counts/digests, with26 applied migrations including030. No main worker was paused as a test. Format/check passed116 unit/tooling cases; documentation-inclusive final gates are artifacts/worker-health-final-format.log and worker-health-final-check.log. BEA-001 was committed as4318237; WORKER-HEALTH-001 was subsequently committed as a18010b after its documentation-inclusive gates. Installed APKcode5 predates this feature; batch rebuild/reinstall remains required.

Manual review: `pnpm build`, `pnpm db:migrate`, current PostgreSQL/MongoDB/API4103/web http://127.0.0.1:5175/#ops → Worker health with an operator session. `E2E_BROWSER=chrome pnpm e2e:ui`, select @WORKER-HEALTH-001 api/desktop/mobile, watch off (23executions). `pnpm android:web`, then `E2E_BROWSER=chrome pnpm android:test:ui` OFFLINE380. Expected: truthful fresh/stale/unavailable counts, explicit versioned pause/resume, preserved running work, no private data in health, recovery after failure and noAPI on device. Share artifacts/e2e/latest.md on failure. Physical phone/TalkBack/user-design and real production monitoring remain separate; no push/provider fetch or main-app pause as verification.

## BEA-001 — official release metadata

Integrated fixed official RSS adapter, strict bounded parsing, retained raw Mongo evidence, independent durable ingestion outcomes and explicit immutable editorial publication. Only first-party modern release headline/link/publication fields become reading items; known legacy archives are skipped, numerical extensions and media/descriptions are not interpreted. Existing source catalogue/Operations/filter/Scan/Stories/reader/context/learning and owned research connections are reused end to end. Public evidence is a selected-release excerpt whose hash explicitly identifies the original full RSS. Withdrawal projects BEA tombstones and denies evidence under source locks; protected originals and minimal private report/inbox receipts remain.

Primary access/rights evidence: parent received ordinary HTTP200 text/xml at2026-09-13T23:47:04.397755Z,88601bytes, SHA256446cb4078349673864407af824f8a6f54c1ec9fc54d10d076451ae5c179915f7 from https://apps.bea.gov/rss/rss.xml, and checked BEA's FAQ147/current releases. Exact capture/provenance accompany parser fixtures; synthetic faults are labelled. Metadata-only scope excludes third-party material and inferred financial effects. No new dependency/migration028.

Verification has passing evidence for all 19 connected feature cases, 11 source/discovery regressions and all 19 selected offline scenarios across correction runs.

- Connected run **2026-09-14T01-12-48-403Z-60459**, handoff **1789348369411-1abc4ce0-3d68-4d0e-8c14-15d7f138659f**, started01:12:49.411Z: **19/19 passed,45.2seconds**, API320–328 and WEB320–324 desktop/mobile. The ordinary provider case passed; all successful APIs/storage were actual. Source/discovery regression **2026-09-14T01-13-49-051Z-60720**, handoff **1789348429954-ab56c515-c947-49a6-bddf-10897938b0ea**, started01:13:49.954Z: **11/11 passed,15.5seconds**. Logs/handoffs: artifacts/bea-connected-fixed.log, bea-connected-handoff.md, bea-source-regressions.log and bea-source-regressions-handoff.md.
- Initial connected run **2026-09-14T01-09-26-183Z-59545**, handoff **1789348167260-ed88d953-cba9-484c-a647-92bdb48de37b**, started01:09:27.260Z: **15 passed/4 failed,2.1minutes**. Root's simultaneous normal refresh correctly triggered the shared database advisory lock in API320; the successful rerun ran without overlapping refresh. API328 needed fresh pg_stat_activity snapshots while observing its real lock wait; WEB322 used a nonexistent Retry label, corrected to the actual Refresh reading control. No production timeout/assertion was weakened. Evidence: artifacts/bea-connected.log and bea-connected-initial-handoff.md.
- Packaged offline initial handoff **1789348472701-a1515a31-e958-4fad-8ad6-1e83a323f71f**, started01:14:32.701Z at web55675: **18 passed/1 failed,12.5seconds**, BEA plus source/learning/connections/financial regressions. OFFLINE351 used a malformed goal-update envelope, then incorrectly expected201 from the existing local inbox200 response. The intermediate 4-case run retained3passes; the final corrected actual receipt workflow **1789348592411-fa568d22-d8c7-46c8-9191-ddb6cb9b79eb**, started01:16:32.411Z at web56310, passed **1/1,2.5seconds** with zero API traffic. All19 selected scenarios have passing evidence across these runs, not a single clean19-case run. Evidence: artifacts/bea-offline.log, bea-offline-initial-handoff.md, bea-offline-fixed.log, bea-offline-status-handoff.md, bea-offline-receipts.log and bea-offline-handoff.md.
- Parent normal refresh retrieved the actual RSS at01:09:29.002Z, created43drafts, and explicitly reviewed/published43 canonical first-party headlines at01:12:01.642Z. A later ordinary refresh checked43items with zero new drafts/failures. The original RSS includes one scheme-less BEA modern release URL; narrowly normalize that admitted host/path to HTTPS while retaining raw bytes and rejecting lookalikes/protocol-relative/credential forms. Evidence: artifacts/bea-refresh.log, bea-drafts-for-review.json, bea-reviewed-drafts.json, bea-published.json and bea-refresh-repeat.log. No synthetic news enters the app.
- Public snapshot **2026-09-14T01:12:49.652Z** contains179published items, including43BEA releases,52annual histories,22reviewed visuals and5security identities. Source editions retain original publication/retrieval dates, including older archive releases. Both WEB320 viewport captures were inspected at1280×720/390×844: wrapped long headline, visible source/date, scrollable context, keyboard Back/disclosures and no horizontal overflow. Installed APKcode5 predates this snapshot; batch packaging remains required.
- At01:16:39.432Z artifacts/bea-resource-audit.json records zero temporary PostgreSQL schemas/Mongo databases. artifacts/team2-records-bea-after.json exactly preserves pre-TEAM003 account/financial counts/digests, ledger25migrations. Format/check passed114units after correcting a fixture parenthesis and the captured scheme-less URL. Documentation-inclusive final gates: artifacts/bea-final-format.log and artifacts/bea-final-check.log; local commit is conditional on success.

CONNECTION-REVIEWS-001 was committed locally as **953d3e7** after its final gates. TEAM006 registers the next isolated holdings reconciliation and report comparison children; their authoring does not imply verification.

Manual actions: existing PostgreSQL/MongoDB/API4103/web http://127.0.0.1:5175; `pnpm build`, `E2E_BROWSER=chrome pnpm e2e:ui` select @BEA-001 api/desktop/mobile, watch off. Use #ops for explicit source refresh/review, #explore?source=bea for reading, #connections/#reports/#connection-reviews for owned flows. `pnpm android:web` then `E2E_BROWSER=chrome pnpm android:test:ui` selects4offline cases after a genuine BEA snapshot exists. Expected19 connected selections plus4offline; share artifacts/e2e/latest.md on failure. Installed APK needs rebuild/reinstall, physical phone/TalkBack/user design acceptance is separate. SOURCE-WITHDRAWAL-001 and broader SRC-008 market data gates remain open; nothing pushed.

## CONNECTION-REVIEWS-001 — private review inbox

Integrated strict contracts, additive migration027, protected bounded check/acknowledgement/history/export storage, responsive inbox and owned receipt reading, connected navigation and serialized local parity. Checks explicitly evaluate actual owned connection/source/record versions without provider requests or financial mutations. Same-state checks preserve acknowledgement, changed states reopen, and removal/current reaffirmation resolves. Account then sorted-source locks and post-wait wall-clock authorization protect writes.

Current inbox state is distinct from historical check/ack receipts. Failed refresh disables current-state actions;401 clears all private state and fences delayed responses. Closing a pending receipt read invalidates its response and restores focus. Historical removed connections with resolved notices do not block later cohorts: evaluation considers at most200 active plus200 unresolved removed heads. Retention keeps200 resolved notices,100 evaluations and1000 operation receipts within30days; unexpired capacity rejects new operations, and an expired request ID starts a new operation. Original connection/financial histories remain untouched. Tests cover actual600-head turnover, source-wait expiry,1000-receipt capacity/TTL, real committed-response loss and stale-response races.

Integration verification passed all selected cases. API310–314 (5), WEB310–313 (4 each desktop/mobile), OFFLINE340–343 (4), plus relevant research/privacy regressions use actual stored evidence/bundle and persistence; synthetic source/fault/time states are confined to fixtures.

- Connected run **2026-09-14T01-02-29-949Z-56494**, handoff **1789347750999-bc422fda-c97b-465b-bc3e-8208c043bcfd**, started01:02:30.999Z: **54/54passed,2.4minutes**, @CONNECTION-REVIEWS-001/@EVIDENCE-LINKS-001/@PRIVACY-001 across api/desktop/mobile. All13new executions passed alongside41regressions, with one worker and no retries. Evidence: artifacts/connection-reviews-connected.log and artifacts/connection-reviews-connected-handoff.md.
- Packaged offline: **7/7passed,6.6seconds**, OFFLINE340–343 plus010/011/240, using actual local accounts/storage/bundle with no API/database. Exact run/time/target are in artifacts/connection-reviews-offline-handoff.md; log artifacts/connection-reviews-offline.log. Desktop1280×720 and mobile390×844 captures from WEB310 were inspected: readable notices, wrapped controls, connected entry/return navigation and reachable scroll content. Keyboard receipt Close/focus/late-response assertions passed in both projects.
- Additive027 applied after025, ledger25migrations, preserving earlier checksums. At01:05:38.215Z artifacts/connection-reviews-resource-audit.json records zero temporary PostgreSQL schemas/Mongo databases; artifacts/team2-records-connection-reviews-after.json exactly matches the pre-TEAM003 account/financial counts and digests. No user-data/service reset or provider call.
- Initial gates caught an offline helper's overly narrow inferred UUID string type; explicit string annotation corrected it. Format/check then passed110units (14contracts/84API/12tooling). Documentation-inclusive final gates are artifacts/connection-reviews-final-format.log and artifacts/connection-reviews-final-check.log; scoped commit follows only on success. Shared offline assets are verified, installed code5APK predates this feature until batch packaging.

REPORTS-003 was committed locally as **ae3dcd4** after its documentation-inclusive gates.

Manual review: `pnpm build`, `pnpm db:migrate`, current local databases/API4103/web http://127.0.0.1:5175/#connection-reviews. `E2E_BROWSER=chrome pnpm e2e:ui` selects @CONNECTION-REVIEWS-001 api/desktop/mobile,13executions, watch off. `pnpm android:web` then `E2E_BROWSER=chrome pnpm android:test:ui` selects4offline cases. Expect repeated checks to coalesce, acknowledge to remain distinct from reaffirmation, current context to recover explicitly, and private export/deletion to persist correctly. Share artifacts/e2e/latest.md on failure. No dependency, external notification, source fetch or private synchronization. Physical Android/TalkBack/user design acceptance remains separate; installed code5APK needs batch rebuild/reinstall. Nothing pushed.

## REPORTS-003 — optional immutable research receipts

Integrated optional canonical selection, exact v1/v2 runtime unions and immutable source/target/reason receipts with capture-time review context. Existing report JSONB storage, leases/retries, capacity/hourly limits, deletion tombstones and late-worker fences are reused; no migration026 or dependency. Up to20 owned connection revisions are captured under account/sorted-source locks with final wall-clock authentication. Source withdrawal never copies article text; reports never present capture-time publication as current availability or infer financial impact.

Web supports selection→review→capture→status/open/JSON/print/HTML, explicit current-connections navigation and typed401 handling. A session denial immediately clears private report/selection/review/pending data and invalidates older responses; it does not wait for another poll. Device mode uses actual local connection records/public bundle, captures once and issues from that snapshot. Existing v1 report shape/policy/calculations remain unchanged. The31 authored definitions are API290–299, WEB290–299/301 and OFFLINE330–339, plus relevant existing report/auth regressions. Integration verification has passing evidence for all54 selected connected scenarios across correction runs, plus15/15 packaged offline cases.

- Combined report/auth regression **2026-09-13T23-48-13-949Z-52140**, handoff **1789343294996-3a0c484b-9856-48e8-85b8-9103835da586**, started23:48:14.996Z: **53passed/1failed,22.6minutes**. All32 new selections passed. Final mobile WEB221 timed out during isolated API setup before its UI assertion. Mac power logs show maintenance sleep during the run; one empty owned schema remained. Evidence: artifacts/report-research-connected.log and artifacts/report-research-connected-initial-handoff.md.
- Disclosure/print/keyboard run **2026-09-14T00-11-12-626Z-53459**, handoff **1789344673442-f818cb69-4c5d-482c-af5d-82eddd784a9e**, started00:11:13.442Z: **5passed/1failed,17.6minutes**. WEB221 passed both projects, while mobile WEB297 crossed another17-minute system-sleep interval waiting for report preparation. Evidence: artifacts/report-research-disclosure.log and artifacts/report-research-disclosure-initial-handoff.md; actual system sleep/wake events in artifacts/report-research-sleep-events.log. No timeout/assertion was weakened.
- Unchanged focused rerun with temporary sleep prevention **2026-09-14T00-53-06-501Z-53664**, handoff **1789347187252-d31f3053-97c7-4390-9491-e669b5babf1c**, started00:53:07.252Z: **6/6passed,33.2seconds**, WEB290/297/221 desktop/mobile. Receipt hashes/URLs and connection identifiers now use accessible disclosures; printed copies expand every field. Source dates/versions/reasons remain visible, with primary Print and secondary close/download actions. Both viewport captures were inspected at1280×720/390×844, with wrapped controls and scrollable reader;360px keyboard/Back passed in the combined run. Evidence: artifacts/report-research-disclosure-awake.log and artifacts/report-research-disclosure-handoff.md. This is passing evidence across runs, not one clean54-case run.
- Packaged offline handoff **1789347237876-e4ab8fd2-ae66-427c-93ad-4f79b132321d**, started00:53:57.876Z: **15/15passed,47.3seconds**, OFFLINE330–339 plus five existing report regressions at http://127.0.0.1:62948 with no API/database. Actual local capture, v1/v2 reconstruction, limits/replay/deletion/export and printable HTML passed. Evidence: artifacts/report-research-offline.log and artifacts/report-research-offline-handoff.md.
- At00:54:40.063Z, artifacts/report-research-resource-audit.json records zero temporary PostgreSQL schemas/MongoDB databases. Root removed only the earlier empty owned schema after confirming no relations/namespace locks and no surviving owned process; artifacts/report-research-fixture-cleanup.json records it. artifacts/team2-records-report-research-after.json exactly matches pre-TEAM003 account/financial counts and digests; migration ledger remains24. No main data/service reset or cleanup.
- Format/check passed109 unit tests after fixing one test-helper conditional-expression lint error. Final documentation-inclusive gates are artifacts/report-research-final-format.log and artifacts/report-research-final-check.log, required before local commit. No dependency/schema change; installed APKcode5 remains historical pending batch packaging.

Manual review: existing local databases/web http://127.0.0.1:5175/#reports/API4103, `pnpm build`, and prerequisite migrations through025. `E2E_BROWSER=chrome pnpm e2e:ui` selects @REPORTS-003 api/desktop/mobile (32executions), watch off. `pnpm android:web` then `E2E_BROWSER=chrome pnpm android:test:ui` selects10 offline cases. Expect opt-in exact dated receipts, unchanged plain reports, duplicate-safe replay and preserved deletion/cancellation/private export. Share artifacts/e2e/latest.md on failures. Android HTML saving reuses the native picker; physical phone/printing/TalkBack/user-design outcomes remain separate. No pushes/provider calls or private cloud sync.

GOAL-SCENARIOS-001 is committed as **843b975** after final format/check passed109 unit tests.

## GOAL-SCENARIOS-001 — exact contribution-only comparisons

Integrated strict shared schemas/calculations, migration025, owned immutable comparison/adoption receipts and conflict/replay-safe API, responsive compare/review/save/reopen/adopt UI, private export/account cascade and serialized on-device parity. All amounts use integer paise;100saved comparisons and up to3alternatives, with maximum1200months. Captured baseline plus monthly/horizon changes assume no growth, inflation, tax or market value. Saving does not change goals; separately confirmed adoption appends the ordinary goal revision and triggers existing allocation-review semantics.

AUTH-WAIT's shared wall-clock check is reused after account/final-goal locks; duplicate custom token/expiry SQL was removed. UI initial503/unreadable responses have Retry. Successful save/replay and adoption receipts are historical until authoritative goal refresh succeeds; failed refresh or changed/deleted baseline disables adoption. Integration verification is complete for API280–282 (3), WEB280–283 (4 each desktop/mobile), OFFLINE320 and exact arithmetic unit cases.

- Combined connected run **2026-09-13T23-37-10-259Z-49263**, handoff **1789342631227-1d1f423f-399b-4516-ad8b-8280b1dd23a6**, started23:37:11.227Z: **25passed/2failed**,27 selections including existing goals/allocations,2.5minutes. Both WEB282 failures came from a first-request-only simulated outage being consumed by React development effect replay. The test now maintains its explicit outage until Retry, then forwards actual API responses; no successful response or retry count is fabricated. Evidence: artifacts/goal-scenarios-connected.log and artifacts/goal-scenarios-connected-initial-handoff.md.
- Corrected comparison browser run **2026-09-13T23-40-20-075Z-49742**, handoff **1789342821059-c558c24f-210e-4c68-be5f-e3b6d23e947b**, started23:40:21.059Z: **8/8passed,22.8seconds**, WEB280–283 desktop/mobile. All27 selected connected scenarios therefore have passing evidence across runs, not one clean27-case run. Saved comparison/adoption, uncertain acknowledgment, stale baseline, initial failure/retry and keyboard/Back passed. Evidence: artifacts/goal-scenarios-browser-fixed.log and artifacts/goal-scenarios-browser-handoff.md. Review captures from that run were inspected at1280×720/390×844: readable exact amounts, unchanged-plan comparison, distinct Save/Back/Cancel, vertically scrollable mobile cards and no horizontal overflow. The100-comparison limit is explained alongside its disabled create action and privacy export link.
- Packaged offline handoff **1789342867093-3c9df742-b965-487a-8c2d-87b1c08d42da**, started23:41:07.094Z: **4/4passed,6.8seconds**, OFFLINE010/011/240/320 at http://127.0.0.1:56611 with no API/database. Evidence: artifacts/goal-scenarios-offline.log and artifacts/goal-scenarios-offline-handoff.md.
- Migration025 is applied after024, preserving all earlier checksums. At23:44:12.155Z, artifacts/goal-scenarios-resource-audit.json records zero temporary PostgreSQL schemas/MongoDB databases. artifacts/team2-records-goal-scenarios-after.json exactly matches pre-TEAM003 user/financial counts and digests; the ledger contains24 migrations. No user data or service was reset.
- Initial format/check passed109 unit tests. Final documentation-inclusive gates are artifacts/goal-scenarios-final-format.log and artifacts/goal-scenarios-final-check.log; the scoped local commit is conditional on their success. Code5APK predates this feature; the shared offline build has been tested, with batch APK packaging still pending.

Manual review after gates: `pnpm build`, `pnpm db:migrate`, current web http://127.0.0.1:5175/#comparisons/API4103/local databases, then `E2E_BROWSER=chrome pnpm e2e:ui` @GOAL-SCENARIOS-001 api/desktop/mobile, watch off. Expect11 connected selections; device test via `pnpm android:web` and `E2E_BROWSER=chrome pnpm android:test:ui` OFFLINE320. No dependencies added. Confirm comparison save preserves the original goal, adoption changes only the reviewed goal once, stale/replayed receipts never masquerade as current and private export/delete works. Share artifacts/e2e/latest.md on failures. Existing installed APK requires rebuild/reinstall; physical phone/screen-reader/user-design acceptance remains separate. Nothing pushed.

EVIDENCE-LINKS-001 is committed as **e875bd0** after final gates passed107 unit tests.

## EVIDENCE-LINKS-001 — explicit personal research connections

Integrated strict contracts, migration024, owned API/revision/idempotency storage, source-reader entry and responsive connections workspace, privacy/account deletion and the on-device equivalent. Actual permitted stored publications provide minimal dated receipts; private holdings/goals and reasons are user-selected. No source fetch, financial inference, calculation change or synchronization is introduced. Note edits preserve original bindings; explicit reaffirmation checks current versions under account/source locks. Current-context readiness clears on reload and after saved/replayed receipts; failed refreshes preserve receipts without claiming current source availability. Session authorization is rechecked after blocking account/source locks.

Integration verification passed:

- Full corrected run **2026-09-13T23-12-07-341Z-47194**, handoff **1789341130870-1932bc58-f2bc-4869-83b2-0ed975d40588**, started23:12:10.870Z: **41/41passed,4.8minutes**, @EVIDENCE-LINKS-001 plus @PRIVACY-001 across api/desktop/mobile. All12 new API cases and26 browser executions passed, alongside3privacy regressions. Evidence: artifacts/evidence-links-connected-fixed.log and artifacts/evidence-links-connected-handoff.md.
- Earlier run **2026-09-13T23-09-05-657Z-46756**, handoff **1789340946629-f552d35d-d31a-4eeb-9f5d-c50fd6b3cd19**, was interrupted after browser edit/Back failures exposed an unstable textarea label association. Root changed the personal-reason field to an explicit label/ID, retaining draft assertions and behavior. AllAPI cases had passed. Evidence: artifacts/evidence-links-connected.log and artifacts/evidence-links-connected-initial-handoff.md. Only the owned test runner was interrupted; development services were preserved.
- Final disclosure/keyboard run **2026-09-13T23-33-32-568Z-47581**, handoff **1789342413384-77a55ec3-89c7-4a56-a0ff-85519688d75d**, started23:33:33.384Z: **4/4passed,10.8seconds**, WEB260/262 desktop/mobile. Technical item/hash details now sit behind a keyboard-accessible Verify source receipt disclosure while source/edition/dates remain visible. Captures were inspected at1280×720/390×844; native focus, retained drafts,360px no-overflow and Back assertions passed. Evidence: artifacts/evidence-links-disclosure.log and artifacts/evidence-links-disclosure-handoff.md; screenshots are in that run's results.
- Rebuilt packaged offline assets: handoff **1789342478390-f36b6bc3-b53c-4c84-a793-9a51e8655be4**, started23:34:38.390Z: **12/12passed,8.5seconds**, OFFLINE300–309 plus010/011 at http://127.0.0.1:53740, no API/database. Actual dated bundle, local owned storage, replay/version/withdrawal/privacy and legacy financial behavior passed. Evidence: artifacts/evidence-links-offline.log and artifacts/evidence-links-offline-handoff.md.
- Initial format/check passed107 unit tests; final documentation-inclusive gates are artifacts/evidence-links-final-format.log and artifacts/evidence-links-final-check.log, required before commit. Peer-found fixture correction kept expiry/count queries on the already-held single-pool client, avoiding a self-deadlock; actual source-wait401 API277 passed without weakening assertions.
- Migration024 applied additively; ledger001–022 plus024 preserves earlier checksums. Read-only artifacts/team2-records-evidence-links-after.json matches pre-TEAM003 record counts/digests exactly. At2026-09-13T23:34:57.012Z artifacts/evidence-links-resource-audit.json recorded zero temporary PostgreSQL schemas/MongoDB databases. No user-data cleanup/provider calls occurred. Code5APK still predates this feature; final batch packaging follows. Physical TalkBack/native Back/user-design acceptance is separate.

Manual review: existing web http://127.0.0.1:5175, API4103 and local databases; `pnpm build`, `pnpm db:migrate`, then `E2E_BROWSER=chrome pnpm e2e:ui`, select @EVIDENCE-LINKS-001 on api/desktop/mobile and Run with watch off. Expect38 connected cases. `pnpm android:web` then `E2E_BROWSER=chrome pnpm android:test:ui` selects10 packaged offline cases. No installation changes. On phone a rebuilt/reinstalled APK is required; physical TalkBack/native Back/user-design acceptance remains separate. Share artifacts/e2e/latest.md on failure. No push or app-data reset.

AUTH-WAIT-001 was completed in gated local commit **af85fb9**; its actual evidence remains below.

## AUTH-WAIT-001 — authorization after database waits

The 2026-09-17 API304 rerun exposed a fixture admission timeout before the allocation request began: recovery derives its new password before opening the account-locking transaction, while the observer allowed only2.5 seconds to see the database wait. The authored repair gives recovery admission a bounded eight-second window and uses its owned `pg_blocking_pids` relationship instead of exact SQL text; private-operation observation remains2.5 seconds. API304 still exercises real allocation GET/PUT,401, unchanged digests and recovered-session controls. This repair has not been run and does not extend the earlier passing evidence below.

Authored correction covers holdings preview/confirm, goal create/update/delete, allocation current/save and report request/delete/replays. Each reauthenticates after the existing account lock and subsequent relevant preview/goal/advisory/job locks, including early replay paths; report cancel/retry follows account → report lock order with rechecks after both. Account session expiry uses clock_timestamp rather than the transaction-start time. Background workers, exact financial calculations, ownership, Origin and immutable histories retain their existing semantics. No schema/dependency/local-handler change.

Integrated verification has passing evidence for all32 selected authorization/recovery/allocation/report-deletion scenarios across correction runs. API300–309 use actual password recovery and observed owned PostgreSQL lock waits; API309 includes seven natural-expiry waits. WEB300 proves revoked-save sign-in recovery and a legitimate save on desktop/mobile. No fabricated successful responses or user-data blockers.

- Combined run **2026-09-13T23-02-54-334Z-44695**, handoff **1789340575333-e9dd8304-0477-45ab-adcd-b2957ff14d34**, started23:02:55.333Z: **30passed/2failed,3.4minutes**. All ten new API cases and existing regressions passed. Both WEB300 projects failed because Chromium could not retrieve the actual forwarded401 response body through Network.getResponseBody. Root retained the real upstream APIResponse and forwarded it unchanged, preserving response/status/body and UI assertions. Evidence: artifacts/auth-wait-connected.log and artifacts/auth-wait-connected-initial-handoff.md.
- Corrected browser run **2026-09-13T23-06-36-021Z-45342**, handoff **1789340796733-84e0a73f-7681-424a-a9bd-895ab7208239**, started23:06:36.733Z: **2/2passed,10seconds**, desktop/mobile. Rejected saves clear private state; keyboard sign-in restores the retained plan and later authorized save/reload works. Evidence: artifacts/auth-wait-browser-fixed.log and artifacts/auth-wait-browser-handoff.md. This is passing evidence across runs, not one clean32-case run.
- Initial gates caught unsafe throws in fixture finally blocks. Cleanup now always releases/drains resources while preserving the primary assertion failure. Application checks/builds and107 unit tests passed; final documentation-inclusive gates are artifacts/auth-wait-final-format.log and artifacts/auth-wait-final-check.log, required before commit.
- Read-only records in artifacts/team2-records-auth-wait-after.json exactly match pre-TEAM003 counts/digests; migration ledger remains001–022. At2026-09-13T23:06:57.677Z artifacts/auth-wait-resource-audit.json showed zero temporary PostgreSQL schemas/MongoDB databases. No existing service was stopped, no user data reset. This server-only change adds no local handlers or APK changes; physical-phone acceptance is not claimed.

Manual review after gated integration: current web http://127.0.0.1:5175, API4103/local databases, `pnpm format`, `pnpm check`, and `E2E_BROWSER=chrome pnpm e2e:ui`, select @AUTH-WAIT-001 with watch off. Expect revoked or expired waiters to return401 with unchanged private records, then new-session saves to succeed. Share artifacts/e2e/latest.md on failure. No migration or APK rebuild is required for this server-only correction; existing offline recovery coverage remains relevant. No push.

RETENTION-001 is committed as **0069aeb** after final format/check passed107 unit tests; connected/offline evidence and its corrected failures are recorded below. Main was clean before this next task.

## RETENTION-001 — operator-controlled expired data cleanup

Integrated strict contracts, migration022, protected preview/confirm/results/history API and responsive Operations workflow. Each preview fixes a server cutoff and eight allowed categories; execution is bounded, serialized, idempotent and atomic with count-only saved results/audit. Failed batches roll back all category changes. No scheduled timer or arbitrary financial deletion policy. Offline Operations explains connected access and leaves local records/network untouched. Expired confirmed holdings receipts are preserved across new previews on server/device, including XLSX metadata.

Migration022 is applied locally with unchanged prior checksums and financial record digests. Cases use actual isolated APIs/databases and explicitly synthetic expiry/fault rows; no application-data cleanup was invoked.

- Initial combined @RETENTION-001/@XLSX-001/@PORTFOLIO-001/@PRIVACY-001 run, handoff **1789338673519-ef9bacc7-d5e7-4405-9cfc-c6498069e842**, started2026-09-13T22:31:13.519Z: **33 passed,1 failed**. API255 exposed an actual unhandled checked-out pg client connection error after terminating its owned backend. The database rolled back, but the API exited rather than returning503. Added a lifetime client error listener: queries still reject, transaction rollback/retry semantics remain, and the pool discards dead connections. No success response is fabricated. Log/handoff: artifacts/retention-connected.log and artifacts/retention-connected-initial-handoff.md.
- Corrected cleanup run **2026-09-13T22-35-33-978Z-42460**, handoff **1789338937484-6e59aa3d-696c-425f-8dd6-ebe76ae8df34**, started2026-09-13T22:35:37.484Z: **17 passed,1 browser-launch failure**. All eight API cases passed, including actual interrupted-connection rollback,503 and same-preview retry. Mobile WEB250 did not reach the application because Chrome launch timed out; the remaining mobile cases passed. Log/handoff: artifacts/retention-connected-fixed.log and artifacts/retention-connected-fixed-handoff.md.
- Unchanged WEB250 mobile rerun **2026-09-13T22-53-48-259Z-42777**, handoff **1789340029025-95772bef-1908-410d-b523-2d22f1815fa1**, started2026-09-13T22:53:49.025Z: **1/1 passed,4.8seconds**. Thus all18 cleanup scenarios have passing evidence across the correction runs; this is not one clean18-case run. Desktop/mobile confirmation captures were inspected: clear fixed scopes/counts, scrollable content, reachable Cancel/Confirm and no horizontal overflow; keyboard/Escape/focus/result/reload assertions passed. Physical screen-reader/user acceptance remains separate.
- Packaged offline handoff **1789340044056-196c9497-8f15-4be2-a0bf-7bb6cf9968c6**, started2026-09-13T22:54:04.056Z: **5/5 passed,5.8seconds**, OFFLINE010/011/280/281/290 at http://127.0.0.1:61078 without API/database. Confirms local-mode explanation/links, retained confirmed receipts/draft capacity and existing workbook/financial/privacy behavior. Logs/handoff: artifacts/retention-offline.log and artifacts/retention-offline-handoff.md.
- Read-only snapshot artifacts/team2-records-retention-after.json matches the pre-TEAM003 financial/account record counts and digests exactly. Migration ledger now001–022. The crashed initial API255 left its exact owned schema; root removed only that annotated fixture after verifying it held no relation locks. At2026-09-13T22:54:26.156Z the final audit showed **zero temporary PostgreSQL schemas and MongoDB databases**. Evidence: artifacts/cleanup-retention-fixture.mjs and artifacts/retention-resource-audit.json. No user service or data was reset.
- Initial check caught a helper's overly narrow inferred UUID type; annotating its string input fixed E2E type checking, and builds/migration succeeded. Final commit gates are recorded in artifacts/retention-final-format.log and artifacts/retention-final-check.log; commit is conditional on both passing. Parent-only execution remains scoped to this authorized implementation.

Manual actions after gated integration: `pnpm build`, `pnpm db:migrate`, then the current web/API/local databases. Operations is http://127.0.0.1:5175/#ops. `E2E_BROWSER=chrome pnpm e2e:ui` selects @RETENTION-001, watch off; expect18 connected cases. `pnpm android:web` then `E2E_BROWSER=chrome pnpm android:test:ui` selects two offline cases. Preview/Cancel on your own app is sufficient for review; deletion acceptance belongs to isolated fixtures. Share artifacts/e2e/latest.md for failures. No dependency added; APKcode5 predates this feature. Physical-device/operator acceptance remains separate. Final local commit follows successful gates; nothing pushed.

XLSX-001 is committed as **dec6d47** after final format/check passed **107 unit tests**, plus the documented connected/offline passes below. Its main tree was clean before this integration.

## XLSX-001 — standard workbook import

Implemented API/web/on-device parsing, fixed two-sheet template and declared-total reconciliation, owned preview/confirmation/history and privacy-compatible parser metadata. Pinned fflate0.8.3/fast-xml-parser5.11.1 and lockfile added. Existing preview arrays remain readable/exportable; no migration. Explicit review/consent and exact paise/quantities apply to every input path. A real synthetic workbook saved by openpyxl3.1.5 is retained in the contracts fixtures; qualified inert metadata/rooted internal relationships now round-trip without relaxing active-content rejection. Numeric default/row/column date/scientific formats, hidden rows/columns, formulas and external relationships are rejected.

- Connected regression **2026-09-13T22-24-18-339Z-39575**, handoff **1789338259236-2d131faa-cc6b-4cc2-a809-3a1e7ce3a542**, started2026-09-13T22:24:19.236Z: **16/16 passed,45.1seconds**, API/desktop/mobile, @XLSX-001/@PORTFOLIO-001/@PRIVACY-001. Actual targets web http://127.0.0.1:5175/API4103; new private cases use isolated actual APIs/schemas. Log/handoff: artifacts/xlsx-connected-fixed.log, artifacts/xlsx-connected-handoff.md.
- Strengthened WEB240 now keyboard-activates both actual template downloads, parses their bytes, rejects a bad upload, saves the independent 16-digit-text round trip and reloads. Handoff **1789338329653-4b6e32f2-f575-4a70-877c-528c43915add**, started2026-09-13T22:25:29.653Z: **2/2 desktop/mobile passed**. artifacts/xlsx-downloads-connected.log and corresponding handoff. Review captures in the full run show readable controls and no horizontal overflow at1280×720/390×844; physical screen-reader/user-design acceptance remains separate.
- Rebuilt packaged offline assets: handoff **1789338357147-f1a635d6-f046-4828-bd37-44b13420b068**, started2026-09-13T22:25:57.147Z: **3/3 passed,3.8seconds**, OFFLINE010/011/290 at http://127.0.0.1:56175 without API/database. Includes real downloads, local workers, exact persistence/export and owned account deletion. artifacts/xlsx-offline.log and artifacts/xlsx-offline-handoff.md.
- Earlier connected run **2026-09-13T22-22-23-284Z-39306** was interrupted after six API passes and browser load failures: the existing Vite session had cached unresolved newly installed parser dependencies. Added explicit nested dependency pre-bundling using [Vite's documented configuration](https://vite.dev/config/dep-optimization-options); the server reloaded its config, module reads returned200, then the corrected run passed. No assertions or dependency protections were weakened. Historical failure: artifacts/xlsx-connected.log and artifacts/xlsx-connected-initial-handoff.md.
- The first complete format/check run passed **106 unit tests**, before the additional hidden-row/column regression. Final pre-commit gates are recorded in artifacts/xlsx-final-format.log and artifacts/xlsx-final-check.log; the local commit is conditional on their success. Actual disposable worker cases cover timeouts, termination, malformed/absent replies, constructor errors and capacity release.
- Read-only main record counts/digests exactly match the pre-TEAM003 snapshot: three users and unchanged goal/holdings tables; migrations remain001–021. At2026-09-13T22:26:16.994Z, **zero temporary PostgreSQL schemas/MongoDB databases** remained. Evidence: artifacts/team2-records-xlsx-after.json and artifacts/xlsx-resource-audit.json. No existing service was stopped or user data cleaned.

Manual review: `pnpm install --frozen-lockfile`, then `pnpm format`, `pnpm check` and the running web/API/local databases. Open http://127.0.0.1:5175/#holdings. Select @XLSX-001 plus @PORTFOLIO-001/@PRIVACY-001 in `E2E_BROWSER=chrome pnpm e2e:ui`, watch off. Device cases use `pnpm android:web` then `E2E_BROWSER=chrome pnpm android:test:ui`. Expect exact confirmed rows, matching declared totals and failed imports preserving drafts; share artifacts/e2e/latest.md on failure. No new migration. Code5 APK predates this feature and needs a later batch rebuild/reinstall. Excel/LibreOffice and physical Android picker acceptance are not inferred from the independent-library/browser evidence. No push/deployment.

## REPORTS-002 — individual report deletion

Implemented across strict contracts, migration021, actual API/storage, responsive Reports confirmation/capacity, offline persistence, privacy and regression cases. Normal polls omit lifetime deletion receipts; explicit privacy export retains metadata only. Permanent deletion removes snapshot/output bytes, reclaims capacity and refuses original create-ID replay. Running work must first be cancelled. The actual late worker method is tested in an isolated plain Node process. Usable earlier poll responses are accepted while newer requests remain pending; applied newer responses/mutations fence stale reads.

Verification:

- Connected report regression `2026-09-13T22-04-34-824Z-35078` completed19 cases,17passed and2 WEB225 test-harness failures. Actual API224–226, WEB223–224 and existing reports passed. Initial run `2026-09-13T22-00-22-459Z-33402` had16passes and a Playwright ESM-loader failure in API225; the real worker check now runs through the isolated plain Node helper and passes. No business assertion or validation was weakened.
- Final WEB225 desktop/mobile run **1789337486916-d4c0841e-9d63-42b2-9219-938fcac9e665**, started **2026-09-13T22:11:26.916Z**, passed **2/2** in25.6seconds with normal fixture shutdown. Combined with the connected regression, all19 selected report scenarios have passing evidence. WEB225 now delays active mounted polls, then drains real responses after leaving Reports; earlier harness versions intercepted a discarded StrictMode mount and released requests during teardown. Their failures remain in `artifacts/reports2-*handoff.md`. The two leftover schemas/databases from the failed cleanup were removed by exact recorded fixture IDs; no main application data or services were reset.
- Packaged offline report run **1789337513388-a2f486f2-042a-4aed-9ca0-10ef8667e42a**, started **2026-09-13T22:11:53.388Z**, passed **5/5** in17.8seconds, no skips/failures, at http://127.0.0.1:51466 with no API/database. Includes receipt/replay/foreign-ID/privacy, delayed response and previous populated report/reload cases. `artifacts/reports2-offline-handoff.md`.
- Format/check passed with94 unit tests; final documentation and test-harness adjustments are included in the pre-commit repeat. `artifacts/reports2-format.log`, `artifacts/reports2-check.log`. Migration ledger20→21 and existing user/financial counts **and row digests** remained identical: `artifacts/team2-records-team3-before.json`, `artifacts/team2-records-reports2-after.json`. Final read-only resource audit found zero temporary PostgreSQL schemas/MongoDB databases at22:12:45UTC: `artifacts/reports2-resource-audit.json`.
- Desktop1280×720 and mobile390×844 confirmation captures inspected: distinct neutral Keep/red Delete actions, readable scope/download-copy warning, wrapped buttons, no overflow. WEB223 covers cancel/Escape/focus return and successful deletion focus on Report history. Physical Android/TalkBack and user design acceptance remain separate.

No dependency added. Migration021 is applied locally; another checkout runs `pnpm build`, `pnpm db:migrate`, then `pnpm dev`. Open http://127.0.0.1:5175/#reports. Manual regression: `E2E_BROWSER=chrome pnpm e2e:ui`, select @REPORTS-001/@REPORTS-002 in API/desktop/mobile with watch off; device cases use `E2E_BROWSER=chrome pnpm android:test:ui` against freshly built assets. Expect deleted content to stay absent while goals/holdings/allocations remain. Share `artifacts/e2e/latest.md` for failures. The existing code5 APK predates this feature; the shared offline assets are tested, and a rebuilt APK will follow the integrated feature batch. No push/deployment.

## TEAM-002 — connected planning, recovery, reports and identities

Started from clean commit `f0eed9a`. Implemented ALLOCATIONS-001, RECOVERY-001, REPORTS-001 and IDENTITY-001 across strict contracts, additive migrations017–020, real PostgreSQL/MongoDB/API, shared web/device workflows, privacy, tests and feature specifications. This integrated batch shares contract exports, application registrations, account locks, privacy exports and the offline router; it was committed atomically as `0a91aff` after gates. Broad DEV/SRC and physical/user-design acceptance stay open where their remaining requirements are not delivered.

- Allocations save exact owned quantities and proportional acquisition cost in immutable revisions; they reject oversubscription and stale writes and flag changed goals/holdings for review. Cost is never added to already-entered goal savings or treated as market value.
- Recovery uses explicitly generated256-bit single-use codes, hash-only storage, password confirmation, rotation, generic failures, capped durable abuse counters and full account-session revocation on reset. Global admission prevents blocked requests creating new username counters. Device accounts retain their records and remain separate from server accounts.
- Record reports capture actual owned inputs and deterministic contribution-only calculations. PostgreSQL jobs use leases, retry bounds, cancellation and idempotency; issued snapshots/downloads do not change after later edits. Device reports resume on the next Reports visit. Scheduled market research/notifications and per-report retention remain separate work.
- Source audit2026-09-13 established OpenFIGI open-metadata access/usage through official primary sources. Actual operator run `0b0915b4-1de2-4017-a779-01e8f5c31704`,20:28:31–20:28:43UTC, stored matched edition1 records for Reliance, Infosys, TCS, HDFC Bank and ITC with immutable raw evidence. The public directory/detail/history and offline bundle use these actual responses. Evidence and precise NSE EOD/corporate-data gates: [identity specification](../product/security-identities.md). No prices, exchange-listing validation or private holdings transmission is implied.

Verification:

- Full connected run **2026-09-13T20-32-29-686Z-27492** passed **177**, failed **0**, with one intentional API004 database-outage skip:178 selected/completed. Handoff **1789331550530-7e3c00e9-3b8e-4bb8-ba64-a9eb50599591**, started2026-09-13T20:32:30.530Z, web5175/API4103,6.5minutes. Includes real isolated source/recovery/report/allocation API and desktop/mobile flows and the complete existing feedback/learning/account/reading regression. `artifacts/team2-full.log`, `artifacts/team2-full-handoff.md`, `artifacts/e2e/2026-09-13T20-32-29-686Z-27492/report`. After-full record digests still match the pre-migration snapshot exactly. Subsequent displayed-code screenshot masking receives separate focused verification.
- `pnpm format` and `pnpm check` passed, including94 unit tests (2contracts,80API,12tooling), lint, app/E2E types and builds. Logs: `artifacts/team2-format.log`, `artifacts/team2-check.log`. Final documentation changes are included in the pre-commit gates.
- Migrations017–020 applied; before/after row counts **and aggregate row digests** matched for existing users/goals/goal revisions/holdings/holding revisions (3users,0financial rows). Ledger advanced16→20. Evidence: `artifacts/team2-records-before.json`, `artifacts/team2-records-after-migration.json`, `artifacts/team2-migrate.log`. No user data was reset.
- Initial focused run `2026-09-13T20-25-00-845Z-25943` passed22/28. Invalid test-only GDP indicator fixtures were corrected to the actual World Bank identifier and setup statuses asserted; Goal/Holding select labels now use explicit id/for associations. All6 failed executions subsequently passed in `artifacts/team2-focused-retry.log` and its saved handoff. No assertion or production validation was weakened.
- Post-review screenshot masking is covered by WEB210 on both desktop/mobile and OFFLINE253. Focused run `2026-09-13T20-41-33-092Z-28615` completed8 passing cases (WEB190/200/210/222), including actual opaque-mask pixel checks and no feedback upload. One finished Playwright worker stalled during shutdown after6 cases; parent stopped only that owned worker after graceful termination failed, then remaining cases completed. This intervention is recorded rather than presented as an unattended clean run. Main services were unchanged. Logs/handoff: `artifacts/team2-final-focused.log`, `artifacts/team2-final-focused-handoff.md`.
- Initial offline run passed23/26; recovery tests issued direct requests after reload before asynchronous local transport initialization, reaching the intentionally rejecting test asset server. Tests now await the visible On-device mode at navigation boundaries. A following run passed25/26 but one case reported15.5minutes elapsed against a120second timeout while the ready UI was present; this timing anomaly is retained in `artifacts/team2-offline-timing-handoff.md`. Final complete offline verification is recorded below.
- Independent connected recovery-mask run **1789333840230-b762873d-bd62-4361-86eb-e0de1877732d**, started2026-09-13T21:10:40.230Z, passed **2/2** desktop/mobile in8.8seconds and exited normally without worker intervention. `artifacts/team2-recovery-verified.log`, `artifacts/team2-recovery-verified-handoff.md`.
- Final packaged offline run **1789333771403-889ac67a-e773-4f7e-8380-af24d3e29f0d**, started2026-09-13T21:09:31.403Z, passed **26/26**, zero failures/skips, in53.4seconds at http://127.0.0.1:59267 with no API/database. It includes all existing device workflows, actual five-identity evidence, exact allocations/reports, retained recovery state, capped counters across reload and screenshot masking. `artifacts/team2-offline-verified.log`, `artifacts/team2-offline-verified-handoff.md`. The code5 APK contains this tested bundle; native hardware remains a separate acceptance gate.
- Resource audit after full connected verification found zero temporary PostgreSQL schemas and zero temporary MongoDB databases. `artifacts/team2-resource-audit.json` records the read-only counts; cleanup was scoped to each selected test's owned resources.
- Parent visually inspected the actual populated public directory at390×844 and1280×900, plus mobile identity detail, evidence and revision actions. Build-time API restarts produced a retryable error; retry succeeded after the API was ready. API correctness and viewport review are distinct from physical-device/screen-reader/user-design acceptance.

APK **0.4.0-planning / code5** built2026-09-13T20:48:36.885Z,3,128,774bytes. SHA256 **942489b9c08473345cd35e49f7f298a94a7d95c59c7e03ec5d717d071f42108e**; bundled web SHA256 **dfc0f147dc489b8cc15b7b850f5992ed93538edb38d703d2e250c8f6b2e1a18d**. Signature/package inspection passed (Android8/API26 minimum). The public snapshot captured2026-09-13T20:29:39.159Z has136published items,52annual histories,22visuals and5actual source identities with evidence. Logs: `artifacts/team2-android-build.log`, `artifacts/team2-apk-verification.log`, `artifacts/team2-snapshot.log`; output `artifacts/android/fingent360-debug.apk`. This rebuild preserves the application ID/signing key; an installed offline app needs the updated APK. Native bridge code is unchanged; new physical-phone acceptance has not been performed.

Manual review after integration: existing databases/web/API at http://127.0.0.1:5175 and http://127.0.0.1:4103; another checkout must apply `pnpm db:migrate` after installing/building dependencies. Open `#allocations`, `#reports`, Account/Privacy recovery and `#securities`; Operations → Security identities supports explicit refresh. Run `E2E_BROWSER=chrome pnpm e2e:ui` and select @ALLOCATIONS-001/@RECOVERY-001/@REPORTS-001/@IDENTITY-001 in API/desktop/mobile, or Run all, with watch off. Offline: `E2E_BROWSER=chrome pnpm android:test:ui`. Expect owned persistence, exact amounts, meaningful failures and no offline API traffic. Share `artifacts/e2e/latest.md` for failure evidence. Install code5 over the same-key prior APK without clearing storage. No new dependency, push or deployment.

## FEEDBACK-TEST-001 — isolated, repeatable feedback integration

User run `2026-09-13T17-22-11-309Z-16823` completed147 cases with139 passes,7 feedback failures and one intentional API004 skip. All seven failures involved the real shared hourly quota returning429; cleanup of reports that never arrived also tried to create cancellation tombstones and masked the original failure. The earlier focus/readiness correction remains, but focused passing runs did not establish repeat-run isolation.

Connected feedback cases now launch the actual compiled application API on an OS-assigned loopback port with a per-test random PostgreSQL schema, applying the existing migrations with no public search-path fallback. Browser forwarding is limited to feedback/operations; web content still comes from the running app. Rate limits, payload validation, SQL persistence, screenshots/audio, operator review and receipt/deletion behavior are unchanged. Each fixture closes only its own API and drops its own schema, including settled startup cancellation. Retention setup uses only the fixture URL. Discovery starts no processes/connects to no databases. No app migration, dependency, production code or APK change was required.

- Feedback run **`2026-09-13T17-39-43-757Z-18148`** passed **21/21**; immediately following run **`2026-09-13T17-41-07-366Z-18359`** also passed **21/21**, zero skips/failures. Both include API190–194 and WEB190–197 desktop/mobile. Real quota test API194 verifies20 accepted reports, rejection/rollback of21st, idempotent retry and owned deletion at the limit. Logs and retained handoffs: `artifacts/feedback-isolation-run1*`, `artifacts/feedback-isolation-run2*`.
- Read-only snapshots before/after those runs matched exactly: **55 main-app feedback rows, unchanged address/global quota rows at20, zero remaining temporary schemas**. Evidence: `artifacts/feedback-isolation-before.json`, `artifacts/feedback-isolation-after-repeat.json`. No quota was reset or raised.
- Discovery listed **22 feedback cases** after API195 was added, even with both database URLs set to unreachable local endpoints; no fixture side effects occurred. Log: `artifacts/feedback-isolation-discovery.log`. API195 exercises cancellation at the schema-created boundary, verified in the full run below.
- Full API/desktop/mobile run **`2026-09-13T17-43-32-847Z-19192`** completed149 cases: **148 passed, zero failed, one intentional API004 outage skip**, in4.5minutes. Handoff **`1789321413617-89339a51-1a94-47b6-943c-934fc43f8236`**, started **2026-09-13T17:43:33.617Z**. All seven reported failures and new quota/cancellation cases passed. Configured targets: web **http://127.0.0.1:5175**, API **http://127.0.0.1:4103**; feedback's actual temporary targets are recorded per case. Log/report/handoff: `artifacts/feedback-isolation-full.log`, `artifacts/e2e/2026-09-13T17-43-32-847Z-19192/report`, `artifacts/feedback-isolation-full-handoff.md`.
- Final read-only comparison still matched the original55 main feedback rows and unchanged quota rows exactly, with **zero temporary PostgreSQL schemas or MongoDB databases** remaining. Evidence: `artifacts/feedback-isolation-after-full.json`. Fixtures did not reset data or enlarge quotas. API195 confirmed startup cancellation removed only its own schema while preserving its sibling fixture.
- Format/check passed including **85 unit tests**; the reporter regression now verifies isolated target annotations while omitting unrelated annotations. Final documentation is included in the pre-commit gates. Logs: `artifacts/feedback-isolation-format.log`, `artifacts/feedback-isolation-check.log`. Native/offline testing was not repeated because no app/native code or bundled assets changed.

Use the existing running web/database services and `pnpm build` after code changes, then `E2E_BROWSER=chrome pnpm e2e:ui` with Run all or @FEEDBACK-001, watch off. No app migration or new dependency is needed. The local role must create schemas; default local Compose configuration supports it. Reports retain configured app targets plus each feedback case's actual temporary API/schema. The code4 APK and its prior physical-device limitations are unchanged. No push or deployment.

## UI-RACES-001 — corrected delivery settings and financial-form timing

The user's run `2026-09-13T17-05-17-513Z-13848` completed 139 cases with 136 passes, WEB194/060 failures and one intentional API004 outage skip. WEB194 acted on an unchecked loading placeholder before the actual enabled setting arrived. Playwright's `uncheck()` returns immediately for an already unchecked control, even while disabled, so disabling alone was insufficient. Feedback now mounts settings only after reading stored values, and storage-open failure provides explicit Retry without claiming an empty history. WEB194 also waits for readiness. WEB060's snapshot contained monthly `1000.0112` and horizon `120`: an uncancelled animation callback moved focus after the user selected the months field. Goals and the analogous holdings step now focus synchronously in a layout effect, before interaction.

No API contract, schema, dependency, financial calculation, consent or synchronization policy changed. Existing migrations and user records were preserved. Timing fault fixtures use real local storage and actual owned API data, then delete only their own test accounts/reports.

- Before-fix failures were reproduced deterministically: WEB067 goal focus in `2026-09-13T17-12-59-569Z-14642`; WEB196 placeholder settings in `2026-09-13T17-14-06-415Z-14740`; WEB095 holding focus in `2026-09-13T17-15-56-628Z-14825`.
- Corrected desktop/mobile run **`2026-09-13T17-17-23-232Z-14992`** passed **30/30**, zero failed/skipped. Cases: WEB060,062–067,090,092–095,194,196–197. This includes both reported failures in both projects, exact saved financial values, real lost-acknowledgment receipt/retry, delayed/failed local settings reads and neighboring Back/loading/draft workflows. Targets: web **http://127.0.0.1:5175**, API **http://127.0.0.1:4103**. Retained log/handoff: `artifacts/ui-races-regression.log`, `artifacts/ui-races-connected-handoff.md`.
- Packaged offline run **`1789319973827-a0096fe2-64b9-454c-86ce-ba2e03d8166a`**, started **2026-09-13T17:19:33.827Z**, passed **16/16**, zero failures/skips, against http://127.0.0.1:55521 with no API/database. These assets are the rebuilt APK bundle. Logs/handoff: `artifacts/ui-races-offline.log`, `artifacts/ui-races-offline-handoff.md`. This is focused regression evidence, not a fresh full-suite pass. The real feedback quota remains enabled; no data or quota reset was used.
- `pnpm format` and `pnpm check` passed: formatting, lint, application/E2E type checks, builds and **85 unit tests** (2 contracts, 71 API, 12 tooling). Final documentation is included in the pre-commit gates. Logs: `artifacts/ui-races-format.log`, `artifacts/ui-races-check.log`.

Current APK: **`artifacts/android/fingent360-debug.apk`**, **2,819,412 bytes**, **`0.3.1-feedback` / code4**, built **2026-09-13T17:18:48.163Z**. APK SHA256 **`dffb9b43cb31c8647b770fc163084f1f68a5a60674c8d6f2e198e112bbe80dda`**; complete bundled web SHA256 **`7ce12d1570d5966f6f5f756ba9550ce50d1d07b4c0bc2a9443131c43707c686f`**. Android apksigner v2 verification passed; package inspection confirmed code4, Android8/API26 minimum and target35. The existing public snapshot and signing key are retained. Native bridge behavior was unchanged; this correction did not rerun native emulator/hardware acceptance. Build log/metadata: `artifacts/ui-races-android-build.log`, `artifacts/android/build.json`.

Manual next actions: reload the app; run WEB060/194 and @UI-RACES-001 using `E2E_BROWSER=chrome pnpm e2e:ui`, desktop/mobile, watch off. Existing services suffice; no install/migration is required for this patch. Install the rebuilt code4 APK over code3 with the same key and check the forms and saved feedback settings in airplane mode. Report `artifacts/e2e/latest.md` for failures. Physical-phone microphone, native HTTPS delivery and TalkBack acceptance remain as documented below; this patch does not claim new native-hardware verification. Commit locally only after format/check pass; never push.

## FEEDBACK-001 — historical initial screenshot, voice and offline delivery

Implemented the supplied pointing-finger launcher with two connected choices, viewport capture/preview/crop/privacy covers/retake, text and recorded audio, explicit consent, persistent history, receipt/retry/deletion states and operator review. Shared contracts validate the actual data through browser/native storage, API and additive PostgreSQL migration016. The feedback queue is separate from financial accounts; Android starts without an enabled server. Delivery uses its explicitly configured destination while open/online/resuming, with idempotency, leases, backoff, monotonic review receipts, cross-tab invalidation and durable cancellation tombstones. No transcript provider, external ticket/email or closed-app background delivery is claimed.

Current executed evidence, 2026-09-13:

- `pnpm format` / `pnpm check` passed with **85 unit tests** (2 contracts, 71 API, 12 tooling), lint, application/E2E types and builds. Final documentation gates run again before the local commit. Migration016 applied successfully; previous checksums and financial records were preserved (`artifacts/feedback-migrate.log`).
- Full API/desktop/mobile run **`2026-09-13T15-00-03-286Z-10289`** completed139: **136 passed, two failed WEB195 assertions, one intentional API004 outage skip**. The two review updates succeeded; a test selector matched the same notice in the inbox and open dialog. The test now targets the dialog notice and closes it before navigation.
- Focused run **`2026-09-13T15-04-42-973Z-11023`** passed **12/12**: WEB195 plus WEB191/133/136/065/066 on desktop/mobile. It also verifies the corrected browser Back ordering for nested discard dialogs; native Back already used the innermost-dialog policy. Combining latest results gives **138 passing unique web/API/project cases**, one intentionally unexecuted destructive outage scenario, no unresolved failures. This is evidence across both runs, not a clean first full run. Machine aggregation: `artifacts/feedback-verification.json`; retained handoffs: `feedback-full-handoff.md`, `feedback-targeted-handoff.md` under artifacts.
- Final packaged offline run **`1789311960134-d531a4c5-6a9b-45a3-8e2b-470f29ab8404`**, started15:06:00.134Z, passed **16/16** against http://127.0.0.1:49250, with no API/database. It covers all existing offline finances/research and new feedback230–235, including concurrent deletion, version ordering and cross-tab visibility. Log: `artifacts/feedback-offline-delivery.log`.
- Connected targets were web **http://127.0.0.1:5175** and API **http://127.0.0.1:4103**, with existing PostgreSQL/MongoDB. Actual screenshot/crop/cover pixels and real MediaRecorder bytes were uploaded to this API and privately read/deleted. Synthetic microphone input, permission denial, lost acknowledgment and offline transport failures are explicitly labelled simulations; the successful browser receipts and operator updates used the real database. Existing account/body parsing, source, holdings, goals and reading regressions passed.

Integration corrections included mounting the normal JSON parser alongside bounded feedback uploads, stable accessible field labels after retake, settings-loading protection, microphone encoding/cancel generation guards, recovery after lost/terminal POST responses, nondecreasing receipt versions and payload-free cross-tab updates. Repeated development runs reached the real hourly upload quota once; it remained enabled, and later verification used the next hour. Only exact owned failed-run fixtures were removed; no financial data or quotas were reset.

### Android artifact and limits

Delivered **`artifacts/android/fingent360-debug.apk`**, **3,102,185 bytes**, Android8/API26+, package `com.fingent360.app`, version **`0.3.0-feedback` / code3**, built **2026-09-13T15:05:34.901Z**. APK SHA256 **`7a6967556e3f2198511bad4d603056db23d9a59c2fe1034b8f460073e054ea7d`**; complete packaged web SHA256 **`b2bbe38ac3d42a3af816a49d7255ddfeb4292223da31413cf1c0885e1d3de772`**. The final offline run exercised these assets. Build metadata/log: `artifacts/android/build.json`, `artifacts/feedback-android-final-build.log`. The prior public snapshot is retained:136 reading items,52 annual histories,22 reviewed visuals, captured08:33:53.105Z.

Owned Android35 emulator testing verified app-only native capture/crop, text plus synthetic WebAudio→MediaRecorder/playback, offline submission, force-stop persistence, failed CDN-mode recovery, native revision conflicts and same-key update preservation. Stored goal/holding hashes stayed unchanged. The actual audio-only Android permission prompt was exercised; physical microphone input could not be checked because this emulator uses `-no-audio`. Actual native HTTPS delivery was not exercised because no Android-trusted local certificate was available; no TLS bypass or external deployment was introduced. Browser/API delivery is verified separately. Full chronological identities, sanitized queue summaries and screenshot evidence are in [native feedback](feedback-native.md).

Physical-phone microphone, trusted HTTPS delivery, TalkBack/large-text breadth and user design acceptance remain manual. Delivery retries while the app is open/resumes, not while closed. Form fields are hidden for capture, but ordinary rendered financial text still needs the user's preview/cover/consent. Server report content expires after30days and is scrubbed on the next feedback operation; local history stays until explicitly deleted. Native feedback is global to this app while financial records remain isolated by workspace origin. Account deletion does not silently delete separately owned feedback.

Next actions: reload the current web URL, use the floating finger and More → Your feedback, then review a submitted report at Operations → Feedback inbox. Transfer/install the APK over the previous build with the same key, without uninstalling, and try both choices in airplane mode. Configure the dedicated HTTPS feedback origin when a compatible reachable API exists and enable delivery; confirm Received only after acknowledgment. For another checkout install dependencies, start databases, apply migration016 and start dev; exact commands and manual test IDs are in [feedback guide](feedback.md), [Android guide](android.md) and TODO. Local format/check-gated commit only; no push or deployment.

## SOURCES-002 — delivered free-source research and Android update

Implemented the complete accepted source pack through strict shared contracts, fixed official adapters, bounded fetch/validation, original MongoDB evidence, immutable PostgreSQL editions/review, durable per-source outcomes, labelled deterministic topic context, web/offline UI and tests. Source/topic/region filters and selection survive Scan/Stories/reader/Back; Today balances sources within 24 items, while Explore exposes the full corpus. Evidence/history, related reading and learning links work with source versions. Seven quizzes and one poll share the same catalogue/rubrics online and offline. Overview connects recorded goals/watchlists to educational topics with a visible reason; Learning lab stays explicitly fictional. This does not introduce live equity valuations, comprehensive Indian company news or automatic background ingestion.

Actual published inventory captured 2026-09-13T08:33:53.105Z:

| Collection                    | Published items | Latest source date / scope                                            |
| ----------------------------- | --------------: | --------------------------------------------------------------------- |
| Federal Reserve               |              20 | 2026-09-11T14:00Z                                                     |
| ECB press                     |               2 | 2026-09-10T12:15Z; accepted press paths only                          |
| ECB statistics                |              13 | 2026-09-02T08:00Z                                                     |
| PIB economic releases         |               2 | 2026-09-13T06:36Z; official release times retain IST basis            |
| World Bank India              |              88 | Annual GDP/CPI plus unemployment/trade/FDI; observations through 2025 |
| Original educational glossary |              11 | Authored 2026-09-13; conceptual definitions, not current market facts |

These are **136 items in six collections**, representing four external institutions plus editorial glossary. The two ECB feeds are separate adapters from one institution. The snapshot also retains 52 annual histories and 22 reviewed visuals. Publication/effective dates remain distinct from retrieval, dataset update and packaging dates. Three additional World Bank series retain exact source decimal lexemes and annual units. Additive migration 015 was applied with existing migration checksums/data preserved; repeat refresh checked 136 items, created 0 duplicate drafts and reported 0 failed sources. A failed source retains earlier accepted editions and does not cancel others. Fully received bounded raw responses are stored before parsing; malformed payloads remain evidence. Oversized/incomplete transport responses are rejected before full capture.

Executed verification on 2026-09-13:

- Format/check passed: formatting, lint, application/E2E types/builds and **82 unit tests** (2 contracts, 68 API, 12 tooling). Captured provider fixtures retain recorded hashes/provenance. Final gates include documentation and the corrected offline test helpers before commit.
- Full API/desktop/mobile run `2026-09-13T08-35-48-579Z-99326`: **122 passed, 0 failed, 1 intentional E2E-API-004 database-outage skip**, 123 selected/completed. Handoff `1789288549329-d1e97d7f-4a4b-4678-9baa-45430c135556`, started 08:35:49.329Z. Targets: web http://127.0.0.1:5175; API http://127.0.0.1:4103. Covers new API180–182, WEB170/180–182 and existing gesture, save/reaction/Undo/reminder, media, financial, privacy and navigation regressions.
- Final offline run `1789288944685-f1813848-713a-4d6b-b68d-9f7a91846f89`, started 08:42:24.685Z: **10/10 passed**, no skips, static assets http://127.0.0.1:59883 with **no API/database server**. Covers OFFLINE010–012, 201–204 and 220–222. Earlier offline runs exposed test requests racing asynchronous initialization after initial load/reload; all API helpers now wait for the visible on-device ready state. No production behavior, validation or failure expectations were weakened. The temporary asset server closed after the run.
- Browser visual review at 390px covered collapsed Today filters, real PIB reading, source/edition layout and sticky actions; desktop review covered filtered Explore and Sources. WEB182 verifies PIB Stories→reader→actual evidence hash/history→related learning→Back with selection retained in both desktop and mobile projects. Existing viewport, keyboard, touch/long-press and recovery cases passed in the full run. This is not a claim of completed physical-device or user-design acceptance.
- Built **`artifacts/android/fingent360-debug.apk`**, **2, 719, 416 bytes**, package `com.fingent360.app`, version **`0.2.0-sources` / code 2**, minimum Android 8/API26. Built 08:34:40.704Z; APK SHA256 **`a7629f7f9ec3142da5df44031b682d19e6574b7c3dac1758c9ef6418f501021e`**. Complete web bundle SHA256 **`582fba7fb1da5b0a27f4560f170bae3fea5831d3575573f11078dc98cfd40f44`**. The final offline run exercised these dedicated assets. Later changes affected test readiness and documentation only. Metadata: artifacts/android/build.json; build log: artifacts/sources-android-build.log.
- Actual owned Android 35 ARM64 emulator: installed the new same-key APK over version 0.1.0-offline, confirmed installed version 2, airplane mode enabled, newest PIB item visible, Today→PIB reader→native Back worked. Existing owned fixture account/holding survived the update (1 holding, ₹100.00 total purchase cost before/after). Evidence: artifacts/android/sources-money-before.xml, sources-money-after.xml and sources-installed.png, sources-reader-final.png, sources-back-final.png. Emulator stopped after validation; no existing AVD or physical phone was changed. Earlier native picker/export/connection checks below remain historical coverage.

Initial full-run failures came from old assertions that all news was Fed-only, Explore Back had no query state and the page had only one status region. Those assertions now express the actual product requirements. Corrected targeted 11 executions passed before the final full suite. No unrelated user records were reset or committed. Generated toolchains/build outputs/signing material and runtime test artifacts are ignored; the checked-in bundle contains public records only.

RBI/SEBI/BoE reuse permission, unverified MoSPI endpoints and BLS403 are documented individually in [source evidence](free-sources.md). No paid provider calls, permissions email, hosted deployment or automatic push was performed. Broader source families remain open in TODO/delivery matrix. Physical-phone usability, TalkBack/large-text breadth and user feedback remain manual. Installed offline APKs need a rebuild/reinstall to receive new web code or data; future CDN/API mode depends on an actual deployment and performs no automatic private-data sync.

Next user actions: reload http://127.0.0.1:5175/#today and follow Sources→PIB→Scan/Stories→reader/evidence/history/learning→Back, then Saved/Learning lab/My overview. Install the new APK over the previous build without uninstalling; repeat in airplane mode and report screen/steps/expected versus actual behavior. Optional manual regression: `E2E_BROWSER=chrome pnpm e2e:ui` with @SOURCES-002 and existing reading/library/media cases, or `E2E_BROWSER=chrome pnpm android:test:ui` for offline; leave watch off and report artifacts/e2e/latest.md for failures. No dependencies changed; existing PostgreSQL/MongoDB and migration 015 are ready locally. Refresh future source data explicitly with `pnpm research:refresh --sources=fed,ecb-press,ecb-statistics,pib,world-bank,glossary`, then review in #ops; rebuilding device data follows [Android guide](android.md).

## ANDROID-001 — historical initial offline Android feedback APK

The current investor app is packaged as a native Android feedback APK with durable on-device accounts, goals, holdings/imports, overview, reading/library/preferences/reminders, learning/query assistance, privacy controls and the explicitly fictional learning lab. The bundled public snapshot contains 75 published items, 52 annual observation histories and three reviewed visuals, captured at 2026-09-13T04:51:19.110Z. Existing server data/migrations are preserved; Android creates IndexedDB schema 1. HTTPS CDN/API configuration is implemented separately from device storage, with explicit native confirmation and no automatic data sync.

Delivered artifact: `artifacts/android/fingent360-debug.apk`, **2,641,660 bytes**, package `com.fingent360.app`, version `0.1.0-offline`, minimum Android 8/API26 and current Android System WebView. Development signed, signature verified with Android apksigner (v2). APK SHA256: `11253f3563798b1087420b34bae4cf77bd80f24d915cb0aebb563bb9ee1edbdf`. Complete bundled web SHA256: `ad0d7a3632e78c2c91b17d0defa4e2079f9b50b50c8367aecc5f09df11459af6`. Build metadata is retained in `artifacts/android/build.json`. Package inspection found no .env, signing keys, local.properties, source maps or matches for configured private credentials in packaged web assets.

Executed verification on 2026-09-13:

- Format/check passed: formatting, lint, application/E2E types/builds and **69 unit tests** (2 contracts, 55 API, 12 tooling).
- Final dedicated offline run `1789275930383-42670c82-5394-4c1a-abb8-ce676cba8cdf`, started05:05:30.383Z: **7/7 passed**, zero skips, web assets http://127.0.0.1:57041 and **no API/database server**. Covers OFFLINE010–012 and201–204, exact large values, ownership/expiry/throttle, revisions/replay, reading/reminders/learning, actual navigation and no network API requests. The runner stopped its temporary asset server.
- Existing-web regression at web http://127.0.0.1:5175/API http://127.0.0.1:4103: **8/8 passed** (API006,011–013 and desktop WEB001–004), handoff `1789275733519-6f792131-4bc8-47cb-8fd4-609c4c02853e`. Additional download regression run `2026-09-13T05-04-55-435Z-90607`: **3/3 passed** (desktop WEB050,090,160), including actual JSON/CSV files and downloaded WebM decoding. These are targeted regressions, not a rerun of the full prior111-case suite.
- Actual Android35 ARM64 emulator, isolated task AVD: installation, airplane-mode cold launch, account creation/sign-in, saved goal, force-stop/reopen persistence, reader/system Back, keyboard dismissal, Android CSV picker→preview→confirmed holding, native JSON destination picker/file write/parsed export, unavailable connected-host confirmation→offline recovery, and final same-key APK update preserving goal/holding/session. Final native bridge reports the bundled hash above. Screenshot `artifacts/android/emulator-delivery-today.png`; export was 2274 bytes with the owned synthetic goal present. Emulator/isolated adb were stopped after validation; no physical device or existing AVD was changed.

Physical-phone usability, TalkBack/large-text/rotation breadth and Android WebM export remain manual acceptance. A real CDN/API deployment is not supplied or tested; its failure/recovery path is tested. Device reminders appear when the app opens/runs, not as background push notifications. Research is a dated snapshot, cloud AI is unavailable offline and query suggestions remain usable. The local password check is not a production identity service/encrypted vault; uninstall/clear-storage deletes device records. See [installation and rebuild guide](android.md), [native detail](android-native.md) and [native acceptance matrix](../../tests/e2e/plans/android-acceptance.md). No push or deployment is performed.

## UX-002 — integrated mobile reading and planning experience

The approved plan is implemented against actual APIs and persisted data: five destinations, Scan/Stories, contextual macro/article/source readers, owned saved/reaction/preferences/reminders, guided goal/holding forms, three optional assistance providers with query fallback, sourced quiz/poll and reviewed SVG/caption/WebM. The [CTA inventory](ux2-cta-inventory.md) maps screens and actions to contracts, API and tests. Migrations010–014 add these domains without replacing user records. Public information comes from reviewed official Fed RSS metadata, existing World Bank annual data and sourced authored glossary/learning; no synthetic market feed or valuation is presented.

Full API/desktop/mobile run `2026-09-13T04-24-09-866Z-83652` passed **110**, failed **0**, and intentionally skipped **E2E-API-004** (the destructive database-outage scenario): 111 selected/completed executions. Targets: web http://127.0.0.1:5175; API http://127.0.0.1:4103. Format/check passed, including 68 unit tests, lint, application/E2E types and builds. Migrations010–014 were applied without resetting existing records. Actual downloaded WebM files were saved and decoded on desktop/mobile. Live paid-provider calls, physical-device/screen-reader checks and field/user-design acceptance remain unverified. Saved handoff ID: `1789273450604-fce06d92-1cc8-4d37-aeba-6e72cd979a60`, started2026-09-13T04:24:10.604Z. Report: artifacts/e2e/2026-09-13T04-24-09-866Z-83652/results.json. Earlier integration failures were corrected before this run; earlier UX-001 evidence below is historical.

Additional viewport run `2026-09-13T04-32-24-890Z-84690` passed both WEB135 desktop/mobile executions after making each screenshot a fresh document visit. Captures verify top/bottom controls at 320,360,390,430 and640px, plus large text. Mobile390 Today/reader images were visually inspected; desktop/mobile WebM files decode as960×640 video. Artifacts: `artifacts/ux2-viewport-*`, `artifacts/ux2-visual-*`, `artifacts/ux2-caption-*.webm`. This is emulated-browser evidence.

Open release gates: actual iOS/Android and VoiceOver/TalkBack checks, field performance, representative user design/affinity evaluation and live paid-provider calls with supplied keys/models. Existing broader parent tasks for verified identities/prices/allocations and more content sources remain open. The working media path is sourced template visuals and a real caption clip; optional AI selects only validated source excerpts, not invented scenes.

## UX-001 — historical implemented baseline

Implemented and verified for the existing account workflows. The new authenticated overview read model joins saved goals, holdings, watchlist and inbox under one repeatable-read transaction. Responsive navigation and guided forms connect overview, account, holdings, goals, research/inbox and privacy. Runtime schema validation, exact money, ownership and provenance remain enforced. Migrations001–009 are reused; no dependency installation or new migration was required. Documentation specifies all delivery layers and audits every DEV/SRC parent's remaining scope.

`pnpm format` and `pnpm check` passed, including 40 unit tests, lint, type checks and builds. Full `pnpm e2e:run` artifact ID `2026-09-12T17-03-42-614Z-70343` (handoff ID `1789232623309-12af3cff-d986-401e-b6ae-5361b97a0c11`, started 2026-09-12T17:03:43Z): 69 selected/completed executions, 68 passed, zero failed, one intentional E2E-API-004 outage skip. Projects: API, desktop and mobile. Targets: web http://127.0.0.1:5175, API http://127.0.0.1:4103. Reports remain under artifacts/e2e; latest.md is overwritten by future runs. No hosted CI or deliberate database outage was run for UX-001.

Browser inspection covered guest/populated overview, goal cards/editor, holdings, macro context and account at desktop and narrow mobile widths. WEB111 verifies keyboard/menu/skip-link/route-focus/header clearance and viewport containment. Integration testing found and corrected stale initial-load overwrites and delayed goal autofocus; WEB063/093 hold real API responses to test those races. WEB110 verifies real registration → goal → holdings → saved overview. These checks establish the tested behavior, not full WCAG certification or user design acceptance. Broader roadmap work remains explicitly open.

## Implemented baseline before UX-001

Source modules provide NestJS/React contracts and persistence for accounts/watchlists, real World Bank annual India GDP/CPI observations and raw evidence, inbox receipts/mutes, user-entered holdings/CSV, saved contribution-only goals, privacy exports/sessions, source registry revisions and a neutral offline fallback. Migrations001–009 and a checksum ledger support their relational data. The separate synthetic journey remains explicitly fictional. API health/readiness and user-invoked SDLC/E2E tools exist.

Historical TEAM-001 run `2026-09-12T16-09-40-894Z-64656`: 51 passed, zero failed, one intentional manual database-outage skip; format/check passed with 40 unit tests. Migrations001–009 and repeat ledger application were verified in that batch. This evidence predates current UX edits and cannot verify them. Historical BUG-006 run `2026-09-12T15-42-10-408Z-58293`: 32 passed, zero failed, one outage skip, covering the earlier responsive macro-table correction. Source/task history in TODO retains earlier authored and user-reported states; those entries are not current runtime assertions.

## Remaining scope

See [current delivery](../tasks/current-delivery.md) and [TODO](../../TODO.md) for exact authored versus pending scope. Remaining implementation includes verified named broker export formats, BSE/broader exchange and sector/index history, complex corporate-action adjustments, additional industry financial statements, broader AMC disclosures and bond price/credit originals, additional publication calendars/vintages, and any numerical causal forecasts beyond the explicitly qualitative/empirical methods. Source/operating-model permissions, provider/signing configuration, actual deployment and manual validation are separate gates. Do not describe already authored jobs, educational policies, privacy lifecycle or monitoring as unimplemented simply because their validation is pending.

## Verification rules

A committed change is not proof of tests or user acceptance. Format/check must pass before commit. Command execution is manual; the latest user instruction leaves format/check/build/migrations/E2E and the gated commit to user-run SDLC. Earlier execution exceptions do not apply. Handoffs state exactly what ran and did not run, run IDs/cases/targets, migration impact, commit scope and remaining uncommitted work. Do not carry old localhost ports forward: use current launcher output. No automatic push.

## Historical foundation context

The initial foundation was tested locally on Node26.7.0/pnpm11.23.0 before later SDLC changes. That included bootstrap preservation, health/readiness and basic desktop/mobile connectivity; it was not evidence for current feature completeness. CI's Node24 baseline and workflow definition are not proof of a hosted CI run. The repository was created from reviewed requirements rather than importing the earlier chat's blocked bundle. Historical market figures were never accepted as live facts.

OPS-READ-ADMISSION-001 is integrated as authored, unexecuted work: four final wall-clock authorization checks after existing legacy discovery/source storage reads. API520–523 exercise exact actual table waits with expiry/revocation and nonempty stored controls; WEB520–522 cover settled initial reads, sign-in recovery and drained late200 responses; OFFLINE530 checks unchanged connected-only behavior/noAPI. Existing contracts/UI/data are reused; no migration/dependency/provider. The user runs pnpm sdlc for gates/commit/selected E2E. Broader parent security acceptance is open.

LEGACY-FIXTURE-ISOLATION-001 is integrated as authored/unverified work in six files. API070/WEB070/WEB181 use the actual owned API/schema/Mongo fixture, exact additional public source/catalogue routes and read-only normal-source digest checks. Full successful API/UI/revision/history assertions remain, with private traces/video/screenshots disabled. Five connected selections use @LEGACY-FIXTURE-ISOLATION-001; no new migration/dependency/offline behavior. The remaining global mutator/provider inventory is recorded in docs/development/legacy-fixture-isolation-handoff.md; an unrestricted suite is not claimed isolated. No commands were executed for this child.

## SDLC-REPAIR-003 — feedback selection (2026-09-15)

Authored correction for user-reported TS18047 at FeedbackInbox.tsx:114/118: list cards use item context, while selected-report access remains inside the guarded dialog. E2E-WEB-195 adds populated initial rendering and close/reopen saved-status assertions using the existing real API sandbox. No execution or commit performed; parent/user must rerun `pnpm check`. Browser acceptance remains pending: `pnpm e2e:run --grep E2E-WEB-195` with existing E2E prerequisites and app services; use the launcher’s configured web URL. Check keyboard close/reopen and mobile layout separately. On failure report the command, case/project and first error; do not include credentials or private feedback.

Source Operations submission coverage now includes actual form-to-API cases WEB1590–1596: India CPI, institutional flows, positioning, governance, company news, classification and normalization. Source prerequisites remain explicitly synthetic; the mutations and independent named reviewer checks use isolated real storage/API. Static fixes invalidate changed permission attestations, cancel stale file reads on authorization denial, clear changed-company mappings and retain selected governance events across pagination. Manual validation only: `pnpm sdlc "Validate source Operations submissions" -- --grep "E2E-WEB-159[0-6]"`. No execution or broad UI pass is claimed.

SBI archive support now covers verified July/August original URLs with structural section discovery, variable positions, option premiums, tiny-weight bounds and cached-total reconciliation without formula execution (API1533–1534, WEB1531, OFFLINE1531). Old V1 receipts remain reconstructible. NSE identity reconciliation adds paid-up value/market lot, strict row widths and dated symbol/series ambiguity, with independent V2 review and shared reader/offline history (API1580–1581, WEB1580, OFFLINE1580). BSE acquisition and other AMC formats remain explicitly open. Source transport limits now match accepted larger originals at dedicated upload routes; API1630 covers large valid capture and retained generic/domain rejection bounds. All acceptance is authored, not run.

SDLC-REPAIR-012 (2026-09-16): supplied API compiler boundary repairs and regression cases are authored. No validation or commit was performed. See the task record for exact scope and user-run commands; broader feature verification remains pending.
