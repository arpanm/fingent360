# WORKER-HEALTH-001 handoff

Authored in artifacts/task-worktrees/worker-health-001 from843b975. No tests, formatting, type checks, builds, installs, services, migrations, provider calls, commits or pushes were executed by this agent. Parent owns root trackers, serial integration, execution evidence and the gated commit. Other worktrees were not edited.

## Behavior and layers

Specification: docs/product/worker-health.md defines aggregate observations,30second freshness, bounded counts/history and exact admission semantics. Contracts: strict worker IDs, count/control/receipt/overview/history schemas and exact-boundary unit cases. Migration030: fixed two controls/two observation rows, immutable control receipts bounded to latest1,000 each, queue-age index. API: authenticated overview/history and Origin-checked versioned/idempotent explicit control, current-time session authorization after lock waits, atomic control+audit rollback. No account/report/reminder contents enter operator responses. Real data: actual existing job/reminder state and server times only; no market/provider integration needed. Automation: existing real workers record heartbeat/success/safe failure and consult transaction-level control before new admission. UI/UX: Operations tab, initial loading/Retry, stale versus unavailable, queue counts, review/cancel/confirm, immutable receipt versus current observation, conflict/dismiss/reload, paged history,401 clearing/late-read invalidation, keyboard and mobile. Offline: explicit connected-server notice and local503 rejection with no API traffic. Tests below author actual isolated database/worker/API operations with labelled temporal/storage/load fault fixtures. Documentation: this handoff and product spec; parent updates README/TODO/catalog/coverage/status.

## Integration order and shared files

Integrate this feature independently before report schedules. Migration030 does not reference029. Health checks only the fixed report_schedules relation in current_schema(); when absent, dueSchedules:null and the UI says scheduling is not enabled, never a fabricated zero or a health503. When029 later exists, the real due-schedule count appears. Current ReportWorker.tick records heartbeat and issues saved reports only. The schedule feature later injects ReportSchedulesStore as its second dependency and calls workOne before ordinary issuance. Both scheduling and claim transactions must call `admitWorker(c,'reports')` FIRST, before account/job/schedule locks. The helper holds FOR SHARE until commit; control uses FOR UPDATE. Existing claims finish using original leases. No write may update worker_controls from inside an admitted transaction, avoiding shared-lock upgrades.

Shared ReportsStore merge: preserve REPORTS-003 v1/v2 snapshot/publication behavior and every AUTH-WAIT account/advisory/job reauthorization. Add only gate at claim start, success observation beside successful finish, safe preparation failure observation for an affected lease, and observe helper. Distinguish ServiceUnavailableException from finish persistence as storage, while pure validation/issuance failures are preparation. Finish must recheck the original lease against clock_timestamp() after acquiring the job row; transaction-start now() is insufficient after a wait. Preserve later source/research/report-schedule code rather than replacing the file wholesale. Shared app/migrate/Operations/offline/index registration must retain all intervening features.

The isolated feedback fixture gains `manualWorkers`, defaultfalse. Only selected worker tests stop timer callbacks on their owned API after startup and drive independent actual worker classes through IPC. Other test cases retain normal worker timers. The child validates an owned loopback e2e schema and returns booleans only. Each test closes/drains its harnesses. The fixture additionally annotates validated schema-created IDs before ready so startup failure retains safe cleanup ownership evidence; no credential-bearing URL is recorded and no timeout is increased. The helper optionally loads the schedule class only when its owned schema contains029.

## Exact manifest

- docs/product/worker-health.md; docs/development/worker-health-handoff.md
- packages/contracts/src/worker-health.ts; packages/contracts/src/index.ts; packages/contracts/test/worker-health.test.mjs
- infra/migrations/030_worker_health.sql
- apps/api/src/worker-control.ts; apps/api/src/worker-health.ts; apps/api/src/reports.ts; apps/api/src/report-worker.ts; apps/api/src/library-worker.ts; apps/api/src/app.ts; apps/api/src/migrate.ts
- apps/api/test/library-worker.test.mjs (existing source-publication tests now admit the worker)
- apps/web/src/WorkerHealth.tsx; apps/web/src/Operations.tsx; apps/web/src/RetentionOperations.tsx; apps/web/src/offline/worker-health.ts; apps/web/src/offline/index.ts
- tests/e2e/helpers/worker-health.ts; tests/e2e/helpers/worker-health-process.mjs; tests/e2e/helpers/feedback-fixture.ts; tests/e2e/helpers/feedback-api-process.mjs
- tests/e2e/cases/api/worker-health.spec.ts; tests/e2e/cases/browser/worker-health.spec.ts; tests/e2e/cases/offline/worker-health.spec.ts

## Authored cases

API350: guest/Origin/unknown field/confirmation/initial aggregate boundaries. API351: retained replay after later resume, immutable history and conflicts. API352: two actual claimants, committed pause, restart persistence, already-claimed completion and new work fencing. API353: actual reminder creation, due-time fault, paused batches, concurrent unique delivery. API354: actual table-unavailable storage failure, safe classification, stale heartbeat and recovery. API355: actual claimed lease expiry/reclaim and late-worker result preservation. API356: observed database lock wait followed by operator expiry prevents control/audit. API357: actual audit insertion failure rolls back control; same-request retry succeeds. API358: capped aggregate load and bounded audit eviction/pagination, evicted old request cannot resurrect mode. API359: actual preparation validation failure, safe classification and original valid snapshot recovery. API360: actual report-finish INSERT storage failure is classified separately and retries without a partial issued report. API361: exact financial revisions, queued snapshots and source evidence remain unchanged through pause/resume. API362: actual held finish row lock with lease expiry after transaction start but before wall clock prevents stale issuance; a fresh claimant then recovers exactly once.

WEB350: real sign-in, empty/history, review/Escape/keyboard, confirmed pause/resume, receipt/reload/mobile and safe screenshot. WEB351: initial503/unreadable response Retry, later failed refresh preserves historical observations and disables controls. WEB352: actual committed pause response loss, actual other-operator resume, identical replay and failed current read versus historical receipt. WEB353: actual operations401 with an old successful read held; immediate clearing and no late restoration. WEB354: concurrent-operator conflict, dismiss/reload and navigation cancellation.

OFFLINE380: connected requirement, local endpoint503, keyboard/settings/Back/mobile and zero API traffic. Shared contract unit tests cover precise freshness threshold and strict/private-field rejection. Existing library-worker unit tests preserve current-publication and withdrawn-source behavior.

## Parent verification recipe

No new dependencies. After serial integration run parent-authorized `pnpm format`, `pnpm check`, then build/migrate as required (`pnpm build`, `pnpm db:migrate`) against existing preserved local databases. This feature adds030 and does not require029. PostgreSQL/MongoDB and the development web/API are needed for connected fixtures; fixtures own temporary schemas/Mongo databases and never change main app queues or controls. Do not use worker-control tests against a shared app directly.

Use `E2E_BROWSER=chrome pnpm e2e:ui`, printed URL (normally9323), select @WORKER-HEALTH-001: API350–362 and WEB350–354 in desktop/mobile. Keep watch off. Expect13 API cases and10 browser executions across two browser projects, all actual isolated success responses. Also run existing report/reminder cases to verify normal default-false fixture behavior and worker outcome preservation. Rebuild offline assets through the documented parent Android workflow, then `E2E_BROWSER=chrome pnpm android:test:ui`, select OFFLINE380; no API/database dependency for that case. Shared physical Android changes require a rebuilt/reinstalled APK; no previous APK is claimed updated here.

Manual app URL: the printed `pnpm dev` web origin plus `/#ops` → Worker health. On a disposable owned test workspace, inspect empty/fresh/stale and unavailable states; review/cancel with Escape and navigation; confirm pause and check server receipt then current mode; resume; inspect older history. Verify keyboard focus,360–390px layout and physical screen-reader wording independently of API tests. Existing app workers need not be paused just to verify implementation.

For failures report artifacts/e2e/latest.md, run time/targets/case/project, original assertion and safe worker screenshot. Startup failure should include feedback-owned-schema annotation; parent must verify ownership/no live processes before cleanup. Never attach keys, connection strings, user report snapshots or reminder titles. Local commit remains843b975 until parent gates and creates a scoped commit.

## Parent integration and verification

Integrated over4318237, preserving all earlier report capture/auth behavior. API358 load fixtures now generate RFC-valid UUIDs. Shared net.ts handles an actual401 before body parsing; WEB353 tests delayed body/older200 denial. Final selected connected39/39 and packaged offline19/19 passed; earlier73/74 includes51 passing report regressions, with the one invalid fixture corrected in the final run. Exact run IDs, gates, preserved-record/resource audits and mobile visual evidence are in docs/development/status.md. Shared offline assets rebuilt; installed APK still requires batch replacement. No push.
