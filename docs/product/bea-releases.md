# BEA-001 — official release headlines

Status: integrated with actual connected/offline verification and43reviewed releases; see development/status.md for exact runs and physical-device limits. Parent ordinary access and primary terms checked2026-09-13. Parent captured HTTP200 text/xml, 88,601 bytes at 2026-09-13T23:47:04.397755Z; SHA256 446cb4078349673864407af824f8a6f54c1ec9fc54d10d076451ae5c179915f7. Primary terms: https://www.bea.gov/help/faq/147; releases: https://www.bea.gov/news/current-releases. The 2026-09-13 research in `docs/development/free-sources.md` records public-domain terms and ordinary access to the BEA RSS feed. The parent rechecked those terms and the current releases page; the agent made no provider requests. This is not production approval.

## Source and display boundary

Use only `https://apps.bea.gov/rss/rss.xml`; refuse redirects, incomplete responses, invalid XML, entities/DTD and responses over 1,000,000 bytes. The captured feed contains one scheme-less modern BEA link; normalize only the exact `www.bea.gov/news/YYYY/slug` dialect to HTTPS, keeping original evidence unchanged. Reject lookalike hosts, protocol-relative links and credentials. A release must have one title, link and publication date, an HTTPS first-party `www.bea.gov/news/YYYY/...` URL without credentials/port/query/fragment and a bounded title. Bound the feed to 200 items and 24 XML nesting levels. The same actual RSS contains old apps.bea.gov/newsreleases/ archive entries: exclude only that recognized first-party legacy path; reject arbitrary external links. Exclude descriptions, media, enclosures and extension fields from public content; do not render source HTML. Reject unknown unnamespaced item fields and malformed required fields; recognized optional RSS/BEA numerical-extension fields are ignored, never instructions. Preserve titles as source text without extracting economic values or inferring reference periods, company exposure or Indian investment impact. Dates show publication and retrieval separately; effective context says to consult the release for the reference period.

Retain original bounded RSS bytes, URL, retrieval time and full-document hash in Mongo before parsing. Public evidence returns only the selected release's title/link/publication XML fields, explicitly labelled a release-metadata excerpt. Its hash identifies the original stored RSS, not the excerpt. The source catalogue states the limited headline scope and links the primary terms. Original pages open externally; no third-party article/media fetching.

## Publication and historical behavior

Reuse fixed-source selection and independent discovery run outcomes, PostgreSQL immutable draft/edition storage, URL-derived stable IDs, material-content fingerprints, review/version conflicts and explicit publish/withdraw. Repeated identical source content, including a changed retrieval time, adds no draft. Updated title/publication creates a draft and leaves the previous publication visible until review. A source fetch/parser failure records a failed source run and preserves existing editions; missing feed entries never imply withdrawal.

For BEA, withdrawal preserves original internal editions and minimal dated personal receipts but redacts public headline/summary/body and old public history text while withdrawn. Public evidence/context/media are unavailable after withdrawal. Source-row shared locks keep evidence/history publication checks coherent with editorial writes. A historical withdrawal entry remains redacted even after later republication. No source text is silently copied into a personal receipt or issued report.

## Connected investor and operator experience

Existing Operations → Publishing source checkbox/status/review uses the added BEA descriptor. Investors find BEA via Our sources → published items, Today/Explore source/global filters, Scan/Stories → reader → stored evidence/version history/related terms or learning → Back. The reader explains headline-only coverage and the absence of financial impact claims. Existing loading/empty/error/retry, keyboard, responsive and Back behaviors are reused and covered by BEA-specific cases.

Published hashed first-party BEA editions are eligible for “Your research connection.” The user chooses an actual owned holding/goal and personal reason with review/consent. Existing exact source/target versions, edit/reaffirm/remove/history, changed/withdrawn review flags and account privacy apply. REPORTS-003 and CONNECTION-REVIEWS-001 consume these same minimal receipts; no report/inbox schema change is needed. Dates and personal reasons never assert economic impact.

## Device and privacy

The parent captures actual reviewed BEA editions into the existing public bundle using `pnpm android:snapshot`; this agent does not manufacture bundled news. The same source classification, enrichment and connection eligibility apply offline. Source catalogue and reader disclose the bundle's own date. No automatic refresh or API/network request in local mode. A pre-BEA bundle remains usable and truthfully has no BEA items. A newer bundle lacking an edition gives unavailable/review context through existing connection semantics. Explicit synthetic withdrawal fixtures test local redaction without claiming a live source withdrawal. No private records enter the bundle; existing connection/report/inbox privacy export and account deletion apply unchanged.

## Delivery and acceptance

No migration028 or dependency is needed: reuse discovery tables/raw collection, run records, strict shared schemas, approved parser infrastructure and owned connection storage. No timer is added; provider refresh and publication are explicit operator actions. Author API320–329, WEB320–329 and OFFLINE350–359 only as needed, using real isolated API/PostgreSQL/Mongo fixtures and genuine parent-captured BEA evidence; deliberate faults are labelled synthetic. No discovery/import side effects. Validate no duplicate drafts, exact source hashes, rejected hostile inputs, independent failures, stale review, withdrawal preservation, private unchanged finances, report/review receipt compatibility, keyboard/Back/empty/retry and persisted zero-network device behavior. Physical phone, screen-reader/user design acceptance, production rights and broader SRC-008 remain separate. Parent owns provider validation, source capture, execution, root trackers and gated commit.
