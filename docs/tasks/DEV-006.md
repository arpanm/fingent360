# DEV-006 — Public intelligence slice

- **Status:** Needs repair
- **Implemented / recorded:** Exactly5/6 actual reviewed event-point preparation, independent issue, immutable brief/review history, source/identity re-admission, event/sector/company/source navigation, corrections/stale/unavailable states, shared web/Android/offline and API/browser/offline1520–1522 are authored. List refresh now clears prior admission cards; WEB1520 covers failed refresh, retry and restored-card navigation.
- **Pending:** Resolve outstanding bugs and complete the current acceptance matrix; see generated validation below.
- **Next action / inputs:** User runs the story acceptance command after resolving recorded bugs.
- **Verification:** The preserved evidence below applies only to its recorded scope/revision. This tracker migration did not run validation.

Current implementation detail: [IMPACT-TRACE-001](IMPACT-TRACE-001.md).

See [current delivery summary](current-delivery.md) for the batch-wide distinction between code, missing functionality and validation.

## Reviewed functional closure — 2026-09-17

The complete required API/browser/offline matrix is recorded in `docs/tasks/acceptance.json`. Implementation of its supported functional scope is complete; the new repairs remain unvalidated. User command: `pnpm sdlc "Complete DEV-006" --story DEV-006`. SDLC may mark the accepted functional scope Done only after all required current cases and checks pass. Live-source/editorial activation and native production release remain separate operational prerequisites, not permissions inferred from these tests. See [consolidated commands and completed scope](../development/core-journey-acceptance.md). Existing failure records stay open until observed passing reruns.

## Source-to-reader acceptance repair — 2026-09-17

Saved API1520/1521 and WEB1522 prerequisites were still calling the legacy event publication route. Oil and historical brief fixtures now use actual named proposals and independent approval, check legacy/self approval rejection, and require the public published revision. The browser brief suite explicitly starts named mode. No source approval, current market fact, API permission or database model was changed. Existing API/browser workflow assertions remain. Authored, not run; no saved bug is marked resolved. Validate API1470–1471/1520–1521/1860–1866 and WEB1472/1522/1590–1596 with the targeted command in the E2E catalogue.

## Reader admission recovery — 2026-09-17

Static inspection found that a failed brief-list refresh retained the previous cards, including their “currently admitted” counts. Clear the old list when starting a fresh admission read, consistently with detail/history clearing. Acceptance: failed refresh shows an error without stale admission cards; retry restores the admitted list; opening a restored card and Back retain working navigation. Extend WEB1520 with this recovery path on desktop and mobile. This is a shared web/Android reader state change; existing runtime schemas, API re-admission, migration091 and offline source handling are reused unchanged. No new data, source integration or automated background work is needed.

Existing preparation, independent issue, corrected-version reissue, withdrawal, source-linked reading and offline re-admission were inspected and remain authored. API1520/1521, WEB1520/1522 and OFFLINE1520 form the functional matrix; browser coverage also needs human keyboard/mobile/visual review. Live source/editorial activation remains external, not inferred from historical test fixtures. No additional private input is needed.

Manual validation: with configured PostgreSQL/MongoDB, migrated schema including091 and the existing API/web services running, use `pnpm sdlc "Repair brief admission recovery" -- --grep "@DEV-006"`. No dependencies or migrations changed. UI: printed web origin `/#intelligence-briefs` and `/#ops` → Intelligence briefs. Expect the complete matrix above plus failed-refresh suppression and successful retry. Report failing case/project, saved run ID and diagnostic path. No tests, format/check/build, services, migrations, SDLC or commit were run; changes await user gates. Existing saved evidence is preserved and does not verify this edit.

## Implementation handoff rule

After each change, update the summary above and the matching [TODO row](../../TODO.md). Keep prompts, detailed scope, remaining work, verification evidence and handoff commands in this file. Follow [task maintenance](README.md); a parent stays partial while a child requirement is missing.

## Preserved specification, prompts and history

The entries below are migrated records, not new execution instructions or current test-pass claims. The current summary above takes precedence where older statuses differ.

### DEV-006 — Public intelligence slice

- **Implementation:** Partial
- **Verification:** Selected child evidence exists; full parent acceptance is incomplete
- **Dependencies:** DEV-005
- **Context:** README.md sections 19–25; docs/product/decisions.md
- **Scope and acceptance:** Five/six verified points; event, sector and company details; supporting sources, corrections, stale/unavailable states
- **E2E cases:** Planned scenarios in tests/e2e/plans/product-coverage.md#dev-006; add stable executable IDs when implemented
- **Evidence / gaps:** Real reviewed releases, annual macro and EVENT-REVIEW-001 now provide source-bound event, editorial sector and retained security context, immutable review/history, correction/withdrawal admission and offline navigation. Remaining: accepted real multi-point editorial coverage and causal-policy context; source-bound extraction is authored and separate editorial candidate adjudication is authored; source-linked editorial relationships do not establish numerical impact. Evidence: docs/development/event-review-handoff.md.
- **Manual next actions:** After implementation, user runs relevant checks and selected E2E cases listed in the handoff; attach evidence before marking verified.

**Codex prompt**

> Read AGENTS.md, README.md, TODO.md task DEV-006, README.md sections 19–25; docs/product/decisions.md, and the linked E2E coverage plan. Follow the standard task prompt contract in TODO.md. Work on Public intelligence slice. Scope and acceptance: Five/six verified points; event, sector and company details; supporting sources, corrections, stale/unavailable states Dependencies: DEV-005. Do not invent missing domain/provider fields or claim future functionality. If a product decision is unresolved, record it as a blocker and progress independent authored work. Add/update API and/or browser acceptance cases (or manual document-review scenarios if there is no executable behavior), including prerequisites and meaningful failures. Update this task, test catalogue and relevant README details. Do not run installation, formatting, lint, builds, tests, browser checks, service changes or migrations. Leave the local commit to the user-run pnpm sdlc format/check gate; never push. Give exact manual next actions.

<a id="dev-007"></a>

## Reusable task prompt

Read AGENTS.md, the task-maintenance guide and this task’s current summary. Work only on DEV-006 unless the user expands the scope. Treat the preserved specification/history as context; current user instructions take precedence. Implement the listed remaining acceptance end to end, or reconcile recorded completion evidence if no implementation remains. Keep tests, documentation and the root index consistent. Record exact remaining work and who needs to act. Do not execute deterministic validation or commit without the user-authorized gates.

## Input and pickup decision — 2026-09-15

- **Readiness:** Follow child tasks
- **User input needed now:** No for the independent next step.
- **Decision:** This is a rollup. Advance the linked incomplete children rather than duplicating their code or requesting a parent-level approval.
- **Recorded answer / authority:** Existing user instruction: agent owns research/implementation decisions within scope; user owns deterministic validation.
- **Question status:** None now. Do not ask for a repeat of existing answers.
- **Later input trigger:** If a concrete private input or external authorization becomes necessary, record the exact evidence and question before asking.
- **Next action:** Developer: establish evidence-backed causal rules and golden outcomes.
- **Research/evidence:** See [dated source checks and existing answers](input-research-2026-09-15.md). Source-specific permissions, complete parser layouts and legal classification are not claimed resolved by triage.
- **Completion boundary:** This updates readiness only, not test passes, live activation or full feature completion. On later pickup, refresh saved evidence and update this record plus the TODO row.

## Public brief completion specification — 2026-09-15

Complete the original five/six-point public intelligence scope with one immutable editorial brief of exactly5 or6 distinct currently published reviewed fact events. Keep each point bound to its exact event version and supporting sources; require actual sector/company context across the chosen brief. No LLM-generated filler, invented facts or causal price forecast. Preparation selects actual admitted candidates; independent named review issues the brief. Retain full issued snapshots privately to Operations and expose public per-point current/changed/unavailable state after re-admitting sources, identities and event lineage. Changed or withdrawn evidence suppresses the old point text and links to current event review instead of silently rewriting an issued point. Stale/unknown time remains visible. Reader links event detail, sector filtered events, company detail and original evidence; includes loading, empty, error/retry, history and keyboard/mobile navigation. Shared web/Android reader and offline projection re-admit against installed source/event state with explicit snapshot limits. PostgreSQL migration091 stores immutable brief revisions/reviews and head; no new private financial data, LLM or notifications. Author API/browser/offline1520+ and docs. Source/editorial activation and all validation remain separate from authored scope.

## Authored completion evidence — 2026-09-15

[Public brief implementation](../development/intelligence-brief.md) closes the stated5/6-point public reading scope. Contracts require exact current reviewed fact-event editions and distinct source points; server preparation and independent publication retain snapshots in migration091. Reader/API/offline distinguish unchanged versus corrected/withdrawn/unavailable content and do not silently rewrite an issued point. Sector and company context link to existing actual event filters, identity and equity evidence. Freshness/unknown times, loading/empty/retry, history and keyboard/narrow-screen behavior are authored. Real historical FOMC/BEA/IndiGo facts support the acceptance fixture with simulated source/resolver admission explicitly labelled.

The old generic “numerical sensitivities” blocker exceeded this task's original public intelligence requirement. Those broader causal/portfolio functions remain in IMPACT-TRACE/DEV016. DEV006 is authored for the accepted supported scope, not verified or live-populated. Publication requires five/six real admitted fact events; the application never invents fillers when fewer exist. Source permission gates remain in their source tasks.

Manual next action: configured PostgreSQL/MongoDB, user `pnpm db:migrate` and `pnpm dev`, then `pnpm sdlc "Add public intelligence briefs" -- --grep "@DEV-006"`. Cases API1520/1521, WEB1520 desktop/mobile and OFFLINE1520 should show exact count, independent issue, source links, correction history, withdrawal suppression and offline re-admission. UI printed origin `/#ops` → Editorial briefs; `/#intelligence-briefs`. No dependency change. Return saved failing case/project/error path. No tests/format/check/build/migration/source jobs/services/APK/commit ran; baseline remains a2c53a0 and shared uncommitted work awaits manual gates. Nothing pushed.

## Operations transport correction — 2026-09-15

Static review found the source/brief Operations mutations passed fetch-style options to the shared `json(path, body, method)` helper, so the intended POST/PUT payload was not sent. Corrected both components to use the actual helper contract. Authored WEB1472 and WEB1522 now submit the real oil capture/review and brief prepare/issue/correct/withdraw forms against isolated actual APIs using distinct named operators. Earlier reader-only browser cases did not establish these Operations submissions. No execution or pass claim. Manual affected command: `pnpm sdlc "Fix oil and brief Operations requests" --grep "E2E-WEB-(1472|1522)"`; API/database/migrations088/091 and dev server required. Report failing ID/run path/error context.

<!-- sdlc-validation:start -->

## Automated validation

Failed — unresolved bug. [Evidence](../validation/README.md); [bugs](../bugs/README.md). Latest reconciliation: 1789709592634-72179.
<!-- sdlc-validation:end -->
