# DEV-017 — Privacy, security and consent lifecycle

- **Status:** Completed supported implementation; manual validation and operational activation pending
- **Implemented / recorded:** Supported server privacy/consent lifecycle, administrator-only audited feedback reads, owner-visible history, encrypted identity/financial/AI/feedback/anonymous financial payloads across read/write/export/deletion consumers, stable blind identity lookup and encrypted TOTP setup/sign-in/disable/recovery across web and connected Android. Migrations058/061/063/066/070–075/079–081 plus parent067/069; API1251–1257, API1400–1412 and connected browser cases are authored.
- **Pending:** User-owned server-key activation, migration/backfill rollout and acceptance of the authored security controls; deployment/backup/device security and broader abuse/injection acceptance. Sensitive financial payloads and account usernames now have authored encrypted consumers; minimum queryable preference/activity metadata remains owner-controlled as justified in the domain inventory. Historical rows only upgrade when admitted/read, so full conversion and key retirement are not yet verified.
- **Next action / inputs:** User configures payload and stable identity keys using the documented manual command, applies migrations, restarts the API and runs selected security E2E cases. No private research input is required. Root reconciles residual parent threat-model acceptance without treating authored controls as tested.
- **Verification:** The preserved evidence below applies only to its recorded scope/revision. This tracker migration did not run validation.

## Implementation handoff rule

After each change, update the summary above and the matching [TODO row](../../TODO.md). Keep prompts, detailed scope, remaining work, verification evidence and handoff commands in this file. Follow [task maintenance](README.md); a parent stays partial while a child requirement is missing.

## Preserved specification, prompts and history

The entries below are migrated records, not new execution instructions or current test-pass claims. The current summary above takes precedence where older statuses differ.

### DEV-017 — Privacy, security and consent lifecycle

- **TEAM-002 delivery:** RECOVERY-001 consumes hashed codes and revokes sessions without deleting records; allocations/reports participate in own-data export/account deletion and device isolation. Granular retention/MFA/key management and production abuse/support review remain open.

- **TEAM-001 delivery:** PRIVACY-001 adds own-data export and session revocation; encryption/key management, production retention, recovery/MFA and support auditing remain open.

- **Implementation:** Partial
- **Verification:** Selected child evidence exists; full parent acceptance is incomplete
- **Dependencies:** DEV-002, DEV-004, DEV-007
- **Context:** README.md section 16
- **Scope and acceptance:** Tenant isolation; granular consent expiry/revocation; PII encryption/retention/export/deletion; broker token vault and least privilege; isolated document parsing; untrusted-document injection tests; audit support access. Never use real private holdings as test fixtures.
- **Current delivery:** Account ownership/consent, recovery codes, paged complete exports, account deletion, cleanup and protected session guards have bounded child evidence. Final legacy read-admission changes await user validation; full threat-model/production/abuse acceptance remains open.
- **E2E cases:** Planned scenarios in tests/e2e/plans/product-coverage.md#dev-017; add stable executable IDs when implemented
- **Evidence / gaps:** Ownership/consent, recovery/session revoke, complete export/deletion, cleanup, isolated parsing and final read admission. CONSENT-LIFECYCLE-001 now supplies four purpose grants with expiry/revoke/renew, actual dispatch/worker admission, immutable export and local parity. Remaining: PII encryption/key handling, support-access roles/audit and remaining injection/security controls. Broker vault only when broker scope is selected. Evidence: docs/product/threat-model.md.
- **Manual next actions:** After implementation, user runs relevant checks and selected E2E cases listed in the handoff; attach evidence before marking verified.

**Codex prompt**

> Read AGENTS.md, README.md, TODO.md task DEV-017, README.md section 16, and the linked E2E coverage plan. Follow the standard task prompt contract in TODO.md. Work on Privacy, security and consent lifecycle. Scope and acceptance: Tenant isolation; granular consent expiry/revocation; PII encryption/retention/export/deletion; broker token vault and least privilege; isolated document parsing; untrusted-document injection tests; audit support access. Never use real private holdings as test fixtures. Dependencies: DEV-002, DEV-004, DEV-007. Do not invent missing domain/provider fields or claim future functionality. If a product decision is unresolved, record it as a blocker and progress independent authored work. Add/update API and/or browser acceptance cases (or manual document-review scenarios if there is no executable behavior), including prerequisites and meaningful failures. Update this task, test catalogue and relevant README details. Do not run installation, formatting, lint, builds, tests, browser checks, service changes or migrations. Leave the local commit to the user-run pnpm sdlc format/check gate; never push. Give exact manual next actions.

<a id="dev-018"></a>

## Reusable task prompt

Read AGENTS.md, the task-maintenance guide and this task’s current summary. Work only on DEV-017 unless the user expands the scope. Treat the preserved specification/history as context; current user instructions take precedence. Implement the listed remaining acceptance end to end, or reconcile recorded completion evidence if no implementation remains. Keep tests, documentation and the root index consistent. Record exact remaining work and who needs to act. Do not execute deterministic validation or commit without the user-authorized gates.

## Input and pickup decision — 2026-09-15

- **Readiness:** Agent-ready
- **User input needed now:** No for the independent next step.
- **Decision:** No user input is needed for independent implementation/diagnosis. Use documented requirements; do not run deterministic gates under the existing execution boundary.
- **Recorded answer / authority:** Existing user instruction: agent owns research/implementation decisions within scope; user owns deterministic validation.
- **Question status:** None now. Do not ask for a repeat of existing answers.
- **Later input trigger:** If a concrete private input or external authorization becomes necessary, record the exact evidence and question before asking.
- **Next action:** Developer: complete PII encryption/key handling, support access audit and security controls.
- **Research/evidence:** See [dated source checks and existing answers](input-research-2026-09-15.md). Source-specific permissions, complete parser layouts and legal classification are not claimed resolved by triage.
- **Completion boundary:** This updates readiness only, not test passes, live activation or full feature completion. On later pickup, refresh saved evidence and update this record plus the TODO row.

## Active specification — support access transparency (2026-09-15)

Close the concrete feedback-support read gap: named viewers/researchers/publishers must not read private feedback; only administrators (or existing bootstrap administration) can. Successful inbox/detail reads must commit a minimal access audit before any content is returned. Lock and recheck the same session after storage waits. If audit storage fails, return no content. Receipt-capability holders can inspect the latest twenty access events and total count, without operator identities or credentials. Deleted/expired reports retain only existing minimal audit metadata and remain inaccessible.

Web and Android reuse the existing owner receipt refresh/native transport. Save access history in the local receipt for offline display, explicitly dated; never label an unavailable history as no access. An unsent device report has no server support access. No additional provider, user decision, personal data fixture or external permission is needed. No database credentials or operator identities enter browser payloads.

Acceptance: admin read creates an immutable event; a non-admin or revoked session cannot read or create successful-access events; wrong receipt capability cannot read history; support history is visible after Check delivery and retained after reload/offline; failed refresh preserves the dated previous history. E2E cases are authored, never run by the agent. This slice does not complete encryption/key management, MFA or full security acceptance.

## Delivery — 2026-09-15

Support access slice is implemented in code; validation pending. [Detailed behavior, research and exact manual commands](../development/feedback-support-access.md). API1251–1253 and WEB1251 author actual role/access/failure/offline scenarios. Migration058 adds a history index; existing native receipt transport/storage covers Android without a new bridge route. No install, build, migration, format, check, test, service change or commit was executed. Baseline HEAD a2c53a0; concurrent unrelated feature changes remain uncommitted under the user-run gates. Parent remains Partial for the named remaining gaps.

Recorded input decision: no question needed; existing admin permission and feedback receipt ownership define this scope. PostgreSQL primary locking reference is in the handoff. No regulatory approval or source rights are inferred.

## Active specification — encrypted private AI history (2026-09-15)

Encrypt persisted private AI instructions, input and model outputs with AES-256-GCM before database writes. Server-only versioned key ring and active key ID; unique random nonce and authenticated owner/record binding. Missing/invalid keys or authentication failures return a safe unavailable error, never plaintext fallback. Existing plaintext rows are upgraded transactionally on owner access; rotate old-key rows on the same path and retain old keys until remaining rows expire or are upgraded. Preserve consent, seven-day retention, owner-only export and deletion. Additive migration061; no automatic schema/backfill execution. Existing owner History UI and Android connected mode consume decrypted authorized API responses; secrets never enter the browser. Device-only history remains its separate boundary and must not claim database encryption. This is a bounded sensitive-payload improvement, not encryption of all profile/goal/holding columns.

## Additional delivery — encrypted private AI history (2026-09-15)

[Implementation, configuration/rotation, limitations and user-run acceptance](../development/private-history-encryption.md). New provider-history writes and completion outputs are encrypted before persistence; authenticated legacy export upgrades/clears plaintext; owner/record-bound authentication prevents row substitution; configured old keys permit rotation. Existing Privacy UI/connected Android/own export/delete reuse unchanged response contracts. Migration061 is authored, not applied. API1254/WEB1254 are authored and existing API1115 retains its consent/isolation checks with fixture-only keys.

No new user question is needed to implement this bounded local-key design. Operator configuration is required only to activate private history; no secret is requested in chat. Parent remains Partial for encryption of other PII, broader lifecycle/KMS/MFA/abuse/injection and production acceptance.

## Active specification — encrypt submitted feedback and finish its access boundary

Encrypt report text, screenshots, voice recordings and contextual fields before persistence. Keep only non-content receipt/rate/status metadata plus optional public source ID for evaluation linkage. Legacy reads rewrap transactionally; deletion/expiry/retention must erase ciphertext as well as legacy fields. Evaluation access to feedback obeys the same administrator-only/audit rule, avoiding an alternate read path. Reuse server key ring, purpose/receipt-bound authentication, safe unavailable errors, native transport and owner UI. Add a bounded administrator batch action to upgrade/rotate remaining active feedback without reading bodies into the UI; never run it as an agent. No plain fallback when encrypted data cannot authenticate. Missing keys hold locally queued feedback for later retry. Additive migration063; exact bodies stay out of audit metadata.

## Feedback encryption implementation — 2026-09-15

Submitted feedback text/context/images/audio now use authenticated encrypted envelopes; server reads, support/evaluation read auditing, owner receipt, native/local retry, retention/deletion and confirmed fifty-report rewrap batches are integrated. Migration063, API1256/1257 and WEB1256 are authored. Existing public source linkage is kept separate from encrypted private content. Server key activation and all validation are user-owned; no run or claim of applied encryption is made. Remaining domains and MFA still require implementation; full parent is not marked complete by this delivery.

## Active specification — account authenticator second factor

Optional TOTP authenticator setup requires the current password and an existing unused recovery code. Generate a short-lived enrollment secret, encrypted with the server ring; confirm a current six-digit code before enabling. Enabled accounts require password plus a valid non-replayed TOTP at login. Setup/confirmation/disable are owner-scoped, origin checked, session-rechecked, and separately rate-limited; a failed attempt cannot roll back its counter. Disable requires password plus current TOTP. Enabling/disabling revoke other sessions. Recovery with the existing single-use high-entropy recovery code resets password, second factor and all sessions, so users are told to keep it offline before enrollment. No SMS/email/provider or extra dependency. Device-only accounts do not claim server MFA; connected Android shares the account/API flow. Shared login optional code is backward compatible for accounts without MFA. Migration066, API/browser cases and recovery instructions required. No tests or migrations run by agents.

Implementation: migration066, strict account/MFA/recovery contracts, encrypted authenticator helper, setup/status/confirm/disable routes, required login code for enabled accounts, replay tracking, recovery-code rotation step-up and reset integration are authored. Privacy settings and account sign-in share the connected web/native behavior, and device-only settings explain the boundary. API1400/1401/1402 and WEB1400 are authored; [authenticator guide](../development/account-authenticator.md) records exact manual acceptance. Primary research: RFC6238, retrieved 2026-09-15. No user secret/input is required for coding. The user configures the server key ring and tests a real authenticator after migrations; no such activation is claimed.

### Remaining private-data inventory (2026-09-15)

Next implementation specification: goal revisions use separate authenticated envelopes bound to owner, goal UUID and revision. Keep head/version/deletion metadata queryable; old payload is cleared only after successful encryption, in the same transaction. Every goal consumer must obtain the same schema-validated clear payload through the shared storage accessor; unknown/missing keys fail closed. New goal creation/edit/scenario adoption write ciphertext only. Privacy export includes all historical/deleted revisions through that accessor; goal deletion remains a soft-delete until the established user deletion/retention rules. Add migration070 and tests covering database plaintext absence, real read/edit/history/export and failed-key integrity. This changes server storage, preserving existing web/native/offline goal contracts and UI. Stored derived reports/comparisons remain separately tracked until their own envelope migration.

Holdings implementation specification: migration071 encrypts revision and import-preview JSON independently, binding owner plus revision/preview ID. Preserve expected-version/expiry/confirmation metadata for idempotency and cleanup. Upgrade legacy JSON on admitted reads, clearing plaintext transactionally; all revision consumers and privacy export use a shared accessor. Preview confirmation decrypts only an admitted unexpired preview; confirmed replay reads the encrypted revision. No broker/source document is committed. Existing exact-decimal parser/reconciliation workflows and device-only storage behavior remain unchanged; API1404 and existing browser import journeys cover the server storage boundary. Derived report/connection/allocation copies are separate inventory entries.

Goal derivative specification: migration072 covers immutable comparison/adoption and deletable feasibility receipts. Encrypt complete payloads with owner/receipt-purpose AAD; preserve fingerprints and public request metadata. A content hash binds every successful envelope rotation to the original decoded receipt. The database still rejects metadata/content-hash mutation, permitting first legacy rewrap, same-hash key rotation and existing owned deletion only. Export and replay decrypt through the same accessor; deletion clears ciphertext without requiring a functioning key. Existing UI contracts and offline calculations remain compatible. Author actual API persistence/replay/delete coverage and update existing mutation fixtures if applicable.

Goal revision payloads and holdings revisions/previews now have encrypted writes and admitted legacy rewrap/rotation across privacy export, allocations, reports/schedules, learning, assistance, overview, goal feasibility/scenarios, impact traces, connections/reviews and action-centre. Authored migrations070/071 preserve queryable versions/expiry/head metadata and disallow duplicate plaintext/ciphertext. API1403/1404 cover real persistence, historical reads, legacy compatibility and export. Existing workbook/reconciliation/retention fault cases were adapted to explicitly inject legacy synthetic payloads, preserving their original assertions. The consent worker wait matches the changed actual goal query. No validation or migration execution occurred.

Encryption must also cover derived persisted copies. Report jobs/issued outputs/schedules, impact-trace receipts and connection revisions/review payloads now have authored envelope migrations074/075 and connected consumers. Usernames remain lookup identifiers; passwords/recovery/session credentials already use one-way hashes. Broker credential vault is not an active consumer requirement while all imports are file-based; do not introduce a fake connected broker API. These are developer-ready gaps, not requests for user research or approval, and keep DEV-017 Partial until implemented. Full deployment storage/backups and physical Android acceptance remain separate from authored code.

Goal derivative implementation: migration072, private-goal-records accessor, comparison/adoption/feasibility writes, replay/list/export/deletion, content hashes and immutable rewrap restrictions are authored. API1405 covers actual DB/API behavior and reuses existing scenario/feasibility browser journeys. No execution occurred. Root owns matching task-row/catalogue updates.

Allocation specification: migration073 encrypts each versioned allocation snapshot with owner/version AAD. Update save/current/history/export and report/schedule readers; holdings removal review decrypts allocation rows to preserve exact dependency counts. Legacy rewrap clears plaintext atomically; missing keys yield no partial result. UI/offline contracts stay stable and existing allocation/removal E2E flows must remain valid. Author an at-rest API assertion through a real saved allocation, including revision history and import dependency count.

Allocation implementation: migration073 now encrypts all allocation revisions, with admitted rewrap across current/history/export and report/scheduler readers. Holdings import removal reviews decrypt row counts instead of inspecting plaintext JSON in SQL. API1406 checks actual saved encryption/history/export and preserved dependency count. Tests not run.

Report specification: migration074 encrypts job label plus captured financial snapshot, issued reports and schedule configuration/receipts. Preserve only lease/status/version/owner/time/idempotency metadata for worker admission and retries. All UI reads, owner export, report comparison and background claim/finish use the same authenticated envelope helpers. Legacy data upgrades without overwriting issued content; key failures cannot silently publish plaintext, and deletion remains possible without decrypting. Schedules retain SQL due-time fields and exact consent rechecks. Existing queue/retry/cancel/compare/export UX remains connected; cases cover actual ciphertext plus idempotent issuance and explicit unavailable recovery.

Schedule detail: active receipt choice/saved-at/version metadata used by legacy consent admission moves to explicit columns before payload encryption. Head saved-at remains sortable without revealing label/configuration. Every edition/request/occurrence envelope is bound to its own stable database identity and owner; immutable content hashes permit only rewrap while preserving metadata. Consent renewal updates the encrypted head next-due value and existing SQL due-time together. Source jobs are encrypted first-write. No background work is run by agents.

Connection and trace specification: migration075 encrypts connection revision content, review inbox/receipts and old evidence-impact trace payloads. Preserve request fingerprints, versions and a single target-kind metadata field used for exact holdings removal-review counts. Every connection list/history/replay, report-research capture, review evaluator and owner export decrypts with owner plus stable record/revision context. Immutable version/trace receipts retain clear-content hashes during rotation. Explicit trace deletion clears ciphertext without keys; existing review receipt retention/account cascades remain intact. Author actual saved-payload and export tests; existing connected/offline UX remains unchanged.

## Manual local key lifecycle specification — 2026-09-15

Add `pnpm privacy:keys` for user-invoked first setup, `--status` for safe metadata and `--rotate` for a new active key while retaining every previous key. Never print key values, mutate secrets during imports/test discovery, discard old keys, rewrite unrelated environment entries or accept malformed/ambiguous existing key settings. Reject symlinked files, serialize command writers, use restrictive atomic writes and refuse detected concurrent edits. Rotation changes the writer key after API restart; admitted reads perform existing rewrap, not this command. Deployment secret management and backup retirement remain explicit operator responsibilities. Author unit filesystem regressions; existing encrypted-record API cases cover runtime interoperability. Agents do not run this command or validation.

Report and connection implementation: migrations074/075, API1407/1408/1409, connected report comparison/capture/export/worker paths, schedule consent metadata, immutable receipt rewrap, connection removal-count metadata and key-independent deletion are authored. Existing synthetic due/corruption/load/browser receipt fixtures were updated to reflect encrypted storage. No deterministic execution occurred. Remaining classified inventory: app_bond_comparisons now has authored encrypted receipt migration079; library/reading-follow/learning/watchlist/alert activity is user-linked personal preference metadata; anonymous journey workspaces/previews/reviews retain token-owned portfolios. Do not mark these encrypted or mark the parent complete without their implementations.

Bond receipt specification: migration079 encrypts saved bond/deposit scenario inputs and deterministic outputs, using owner+comparison ID authenticated context. Preserve request fingerprint, timestamps and deletion metadata; writes, idempotent replay, list and privacy export use the same decrypt/rewrap accessor. Explicit removal clears the envelope without keys. Current responsive/native-connected comparison UI stays unchanged, with existing browser cases plus new database privacy assertions. Device-only calculations retain their separate private-device store. No fund/provider source data is changed.

Manual key setup implementation is authored in `scripts/privacy-keys.mjs`: first setup, safe status and additive rotation, restrictive atomic storage, ambiguous configuration refusal and no printed secrets. Filesystem unit cases are authored in `tests/unit/privacy-keys.test.mjs`; existing encrypted-record API/browser cases remain the application acceptance. [Command options and exact manual sequence](../development/private-key-setup.md). No secrets were generated in the user's environment, no setup/check/test was run and no commit was made by the agent.

Bond receipt implementation: migration079_bond_receipt_encryption.sql and shared encrypted read/write accessor cover saved bond/deposit inputs/outputs, replay, list/export, legacy upgrade and deletion without a readable key. API1410 adds actual ciphertext/legacy/unknown-key/delete coverage. All code remains authored, unexecuted. [Per-domain privacy inventory and key lifecycle](../development/private-domain-inventory.md) identifies the precise remaining username, user-linked preference/activity and anonymous virtual financial payload boundaries; no blanket claim that all PII is encrypted. Parent key setup/status/rotate command is independent and user-run.

Anonymous journey specification: migration080 encrypts virtual workspace portfolios, import previews, mutation/import responses and review snapshots with token-hash owner plus record identity and purpose authenticated context. Keep revision, expiry, request/content hashes queryable. Upgrade admitted legacy records atomically; duplicate-import detection reads only presence. Deletion verifies token ownership directly and cascades even without readable keys. Connected web/native contracts remain unchanged and device-only data retains its device boundary. Author actual API ciphertext, replay, wrong-key failure and key-independent deletion coverage.

Anonymous financial storage implementation: migration080_virtual_journey_encryption.sql, private-journey accessor and all JourneyStore writes/read/replay/review paths now use authenticated ciphertext. Token ownership and record purpose/identity bind every envelope; legacy reads clear plaintext atomically, unavailable keys preserve ciphertext and workspace removal cascades without decrypting. API1411 authors actual persistence, idempotent replay, legacy rewrite, unreadable-key and removal coverage. Existing virtual journey API/browser cases retain public response shapes. No execution. Historical import-only duplicate receipts remain until workspace deletion unless explicitly read/rewrapped; old keys cannot be retired based only on active-workspace access.

Identity specification: migration081 adds encrypted username and stable HMAC blind lookup index; existing usernames remain admissible only through dual legacy lookup until authenticated conversion. Serialize username registration on the blind index to prevent mixed legacy/encrypted duplicates. Register/current/login decrypt only after admission; store login limit identifiers as blind hashes. Dedicated PRIVATE_IDENTITY_LOOKUP_KEY is retained across payload-key rotation and never logged. Missing keys fail closed; password plus session account deletion can remove records without decryption. Recovery locates by blind index without requiring payload keys. Root owns key-command/config additions; this agent owns identity consumers and synthetic fixture key injection. No display-name/email/phone model exists to invent. Author actual encrypted registration/login/recovery/legacy and duplicate tests.

Identity implementation: migration081_account_identity_encryption.sql stores encrypted username plus deterministic keyed lookup; registration serializes duplicate checks across legacy/encrypted records, login/session reads upgrade admitted legacy identity, rate limits store blind keys while preserving live legacy attempts, recovery uses blind lookup, and password/session deletion does not decrypt. API1412 covers actual encrypted registration, duplicate legacy identity, login upgrade and deletion with an unknown payload key. Existing recovery-wait fixtures track the revised SQL. The stable lookup key is independently generated by the root-owned manual key command and never rotated with payload keys; missing keys fail closed. No email/phone/freeform profile fields exist in the current account contract. Named operator usernames remain administrative audit identities under restricted operator access, not private customer profiles. No deterministic validation or commit occurred.

Final implementation scope audit: the remaining queryable library/reading-follow/learning/watchlist/material-alert fields are bounded public references, choices, progress, due/timezone data and macro percentage-point thresholds; no arbitrary private financial note/amount is present in their schemas. Their owner/consent/export/account-cascade controls are the intentional protection, not an unsupported claim of anonymity or ciphertext. Supported parser isolation/injection rejection and provider no-tool boundaries already exist and remain under their specific authored cases. Broker token vault is not applicable until an actual credential-based broker connector exists. Server supported implementation is therefore authored; user-run key activation, migrations/historical conversion, selected validation and physical/deployment acceptance remain explicitly pending. Device-only storage retains the documented sandbox boundary rather than falsely claiming a new hardware vault.

Later DEV028 integration: a gated Zerodha connector now has an actual encrypted broker-token/capture vault under migration083, owner-bound callbacks, expiry/revoke, token-free export and account deletion cascade. This supersedes the earlier vault-not-applicable statement only for Kite; other file-only brokers still accept no credentials. See DEV028 activation permission and manual acceptance; no live broker call occurred.

Upstox connectivity added under DEV-028 uses the same owner/purpose-bound encrypted vault and raw-capture controls (migration086), expiry scrub, rotation-on-read, private export and account-delete cascade. Shared offline code refuses broker authorization. The application restricts itself to holdings operations; token privilege and permission activation are not inferred. See [Upstox connection](../development/upstox-connection.md).

Angel One Publisher uses the same encrypted owner-bound broker vault/capture lifecycle under DEV-028 migration089. Optional feed credentials and profile contact/name fields are discarded; broker client ID needed for logout remains inside the encrypted vault. Restricted deployment network identity is server configuration, never end-user input. See [Angel connection](../development/angel-connection.md).

## Feedback encryption submission repair — 2026-09-19

Saved WEB1256 desktop/mobile failures in full run1789752953639-97020 show the maintenance failure message. Static diagnosis: FeedbackEncryption supplies a JSON body to `json` but omits its method; `net.ts` defaults to GET, and browser fetch rejects GET with a body before reaching the API. Existing API1257 exercises the supported POST endpoint.

Specification/acceptance: retain explicit confirmation and disabled pending/subsequent-submit controls; send `{ confirm: true }` only through POST to the existing protected endpoint, parse the bounded result and show actual processed/remaining counts. WEB1256 must observe an actual POST201 response and retain the zero-count/status/reset assertions. No contract, storage, keys, source or API permission change is needed. Offline has no Operations maintenance capability; existing offline denial remains applicable. Shared web UI covers desktop/mobile; physical native acceptance is separate.

Reusable repair prompt: correct the request method at this call site, preserve confirmation and bounded server behavior, and verify response method/status in the existing browser case. Do not broaden authorization or replace the real operation with mocked success.

Implementation: explicit POST and actual201 response assertion authored. User runs the parent consolidated `pnpm sdlc "Repair saved full audit failures" --story SDLC-REPAIR-016`, or the smaller `pnpm sdlc "Repair feedback encryption submission" -- --project=desktop --project=mobile --grep "E2E-WEB-1256"`. Existing configured migrated databases/API/web required; no dependencies or migrations changed. Open the current printed dev URL (latest saved http://127.0.0.1:5176) → Operations → Feedback inbox → confirmation → Upgrade next feedback batch. Expect counts and confirmation reset. Report case/project/run ID and redacted response/error evidence if it fails. No tests, formatting, checks, builds, browser execution, services or commit run; initial inspected HEAD `8b5d817`. Existing working-tree changes remain preserved and uncommitted pending user gates.

## Saved full-inventory repair — 2026-09-19

The saved full run1789752953639-97020 includes failed cases tagged to this task. Confirmed causes, scoped authored repairs and remaining verification are recorded in [the full-audit RCA](../development/full-audit-2026-09-19.md). User runs `SDLC_AUTO_REPAIR=0 pnpm sdlc "Repair full audit failures" --story SDLC-REPAIR-016`. No new passing evidence or automatic bug resolution is claimed; this bounded repair does not remove broader source/device/functional requirements recorded above.

## Current acceptance review — 20 September 2026

Reviewed scope: Supported server and shared/offline privacy controls: own-account/session authorization and wait rechecks, granular purpose consent, coherent complete export and explicit cascade/removal, encrypted supported customer identity/financial/AI/feedback/anonymous/broker payloads, stable blind lookup and key lifecycle, TOTP/recovery, administrator audited support, bounded isolated import parsing/provider authority, runtime DB privileges and neutral private-cache exclusion. Intentional queryable preference/admission metadata remains personal under owner/consent/export/delete, not claimed anonymous/ciphertext. Parent is not production/physical/live-provider certification.

The required API, browser-project and offline case IDs are now explicit in [acceptance.json](acceptance.json). Only actual current receipts plus successful normal gates can close this scope. Existing API/contracts/database/source workflows are reused; a matrix correction itself adds no migration or source permission. Physical-device and deployment claims require their separate evidence. The user authorized this validation/repair run; older manual-only handoff wording is historical for this run.

Remaining gates:

- User-controlled key/migration/historical rollout: configure payload key ring plus stable identity lookup key; apply relevant additive migrations and prove complete historical inventory/conversion before old-key retirement. Admission-time rewrap and bounded feedback maintenance do not establish all dormant/historical rows or backups converted. Never replace a missing identity lookup key or infer deployment activation from fixture keys.
- Deployment/security operational acceptance remains: HTTPS cookie/transport configuration, least-privilege production role grants, secret/file/process access, backup retention and tested restore, operational abuse budgets/monitoring, and review of the documented residual threat model. Local fixture tests and local DB activation are not a deployment security certification.
- Physical native privacy acceptance remains separate: local/connected account isolation, source/import/export and private screenshot/microphone controls, installed build/update and OS storage boundaries on Android/iOS. Shared browser tests and encrypted server payloads do not establish physical-device security or a general offline hardware vault.
- Broker activation stays independently gated for Kite, Upstox and Angel: registered provider apps, applicable permissions/retention and deployment identity, actual token privileges and configured live authorization/revoke. Synthetic transport tests prove application vault/owner/callback/expiry behavior only; trading-capable provider tokens do not become least-privilege tokens because this app calls only holdings routes.
- Agent-actionable supported browser privacy coverage remains: WEB1400 covers enrollment/confirmation, while API1402 covers disable/recovery step-up; complete keyboard/narrow/error/recovery acceptance of MFA management and private-history controls is not established by those cases. Keep safe synthetic evidence distinct from actual authenticator/device acceptance.
- Carry the feedback child remaining acceptance: matching submitted source/model context, destination/lease/backoff transitions, failed support-history refresh and full keyboard/narrow feedback controls. These are documented coverage gaps, not claims of an observed product defect. Do not auto-complete the privacy parent while these required boundaries remain unreviewed.

<!-- sdlc-validation:start -->

## Automated validation

Passed — automated acceptance. [Evidence](../validation/README.md); [bugs](../bugs/README.md). Latest reconciliation: final-scoped-1789857073654.
<!-- sdlc-validation:end -->

## Remaining completion gates — 20 September 2026

The complete current automated matrix passed. The reviewed manifest retains these separate requirements; rerunning passing cases does not satisfy them:

- User-controlled key/migration/historical rollout: configure payload key ring plus stable identity lookup key; apply relevant additive migrations and prove complete historical inventory/conversion before old-key retirement. Admission-time rewrap and bounded feedback maintenance do not establish all dormant/historical rows or backups converted. Never replace a missing identity lookup key or infer deployment activation from fixture keys.
- Deployment/security operational acceptance remains: HTTPS cookie/transport configuration, least-privilege production role grants, secret/file/process access, backup retention and tested restore, operational abuse budgets/monitoring, and review of the documented residual threat model. Local fixture tests and local DB activation are not a deployment security certification.
- Physical native privacy acceptance remains separate: local/connected account isolation, source/import/export and private screenshot/microphone controls, installed build/update and OS storage boundaries on Android/iOS. Shared browser tests and encrypted server payloads do not establish physical-device security or a general offline hardware vault.
- Broker activation stays independently gated for Kite, Upstox and Angel: registered provider apps, applicable permissions/retention and deployment identity, actual token privileges and configured live authorization/revoke. Synthetic transport tests prove application vault/owner/callback/expiry behavior only; trading-capable provider tokens do not become least-privilege tokens because this app calls only holdings routes.
- Agent-actionable supported browser privacy coverage remains: WEB1400 covers enrollment/confirmation, while API1402 covers disable/recovery step-up; complete keyboard/narrow/error/recovery acceptance of MFA management and private-history controls is not established by those cases. Keep safe synthetic evidence distinct from actual authenticator/device acceptance.
- Carry the feedback child remaining acceptance: matching submitted source/model context, destination/lease/backoff transitions, failed support-history refresh and full keyboard/narrow feedback controls. These are documented coverage gaps, not claims of an observed product defect. Do not auto-complete the privacy parent while these required boundaries remain unreviewed.
