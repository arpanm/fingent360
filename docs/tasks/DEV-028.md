# DEV-028 — Broker and account connectivity

- **Status:** Partial — Zerodha, Upstox and Angel One Publisher pathways authored; activation/validation pending
- **Implemented / recorded:** Gated Zerodha, Upstox and Angel One Publisher connections, same-owner consent callback, encrypted capture/vault, reviewed import/confirmation, revocation/export/deletion and shared web/app UI; tests authored.
- **Pending:** User activates registered broker apps and retention permission; selected validation. Groww partner OAuth wire contract/eligibility and Breeze multi-user callback correlation remain unavailable inputs, not implemented connectors awaiting activation. CAS/registrar/eligible Account Aggregator pathways remain separate implementation gaps.
- **Next action / inputs:** Supply app credentials privately in server configuration and record applicable retention/deployment permission; user runs documented migrations/SDLC. Research other pathways independently.
- **Verification:** The preserved evidence below applies only to its recorded scope/revision. This tracker migration did not run validation.

### 2026-09-15 Groww / ICICI follow-up research

[Current protocol assessment and exact input checklist](../development/groww-breeze-connectivity-research.md): official Groww material confirms a partner OAuth offering, but inspected public guides do not supply its authorization/state/token/revocation wire contract. Its documented personal API token is a different product flow, not an implemented delegated connector. Breeze account-session and signed holdings requests are documented; safe multi-user callback correlation and eligibility are not established. Obtain the official partner specification/eligibility and retention terms; no secret needs to be shared in chat. No provider question was sent and no answer received. These two connectors remain unimplemented, rather than merely waiting for activation. No runtime placeholder or migration108 was added. Documentation acceptance and the manual checks-only command are in the linked assessment; no gates or commit ran.

## Implementation handoff rule

After each change, update the summary above and the matching [TODO row](../../TODO.md). Keep prompts, detailed scope, remaining work, verification evidence and handoff commands in this file. Follow [task maintenance](README.md); a parent stays partial while a child requirement is missing.

## Preserved specification, prompts and history

The entries below are migrated records, not new execution instructions or current test-pass claims. The current summary above takes precedence where older statuses differ.

### DEV-028 — Broker and account connectivity

- **Implementation:** Planned
- **Verification:** Not run
- **Dependencies:** DEV-008, DEV-017; SRC-019, SRC-021
- **Context:** README.md sections 6, 16 and Gate 6
- **Scope and acceptance:** Add approved broker OAuth, CAS/registrar and eligible Account Aggregator pathways as separate tasks; preserve consent/revocation, sync reconciliation, provider entitlement and format versions. Never collect broker passwords or OTPs.
- **E2E cases:** Planned scenarios in tests/e2e/plans/product-coverage.md#dev-028; add stable executable IDs when implemented
- **Evidence / blockers:** No implementation or verification evidence yet; dependencies and required product/source approvals remain open.
- **Manual next actions:** After implementation, user runs relevant checks and selected E2E cases listed in the handoff; attach evidence before marking verified.

**Codex prompt**

> Read AGENTS.md, README.md, TODO.md task DEV-028, README.md sections 6, 16 and Gate 6, and the linked E2E coverage plan. Follow the standard task prompt contract in TODO.md. Work on Broker and account connectivity. Scope and acceptance: Add approved broker OAuth, CAS/registrar and eligible Account Aggregator pathways as separate tasks; preserve consent/revocation, sync reconciliation, provider entitlement and format versions. Never collect broker passwords or OTPs. Dependencies: DEV-008, DEV-017; SRC-019, SRC-021. Do not invent missing domain/provider fields or claim future functionality. If a product decision is unresolved, record it as a blocker and progress independent authored work. Add/update API and/or browser acceptance cases (or manual document-review scenarios if there is no executable behavior), including prerequisites and meaningful failures. Update this task, test catalogue and relevant README details. Do not run installation, formatting, lint, builds, tests, browser checks, service changes or migrations. Leave the local commit to the user-run pnpm sdlc format/check gate; never push. Give exact manual next actions.

<a id="dev-029"></a>

## Reusable task prompt

Read AGENTS.md, the task-maintenance guide and this task’s current summary. Work only on DEV-028 unless the user expands the scope. Treat the preserved specification/history as context; current user instructions take precedence. Implement the listed remaining acceptance end to end, or reconcile recorded completion evidence if no implementation remains. Keep tests, documentation and the root index consistent. Record exact remaining work and who needs to act. Do not execute deterministic validation or commit without the user-authorized gates.

## Zerodha read-only connection specification — 2026-09-15

Primary evidence: [Kite login/token lifecycle](https://kite.trade/docs/connect/v3/user/), [holdings fields](https://kite.trade/docs/connect/v3/portfolio/), [API terms](https://kite.trade/terms/), [free Personal plan scope](https://support.zerodha.com/category/trading-and-markets/general-kite/kite-api/articles/historical-data-and-live-market-data-payment-plan). Custom request-token/checksum exchange uses documented redirect_params for a one-time state; do not label this standard OAuth. Tokens expire the next day at06:00; no general refresh-token entitlement is assumed. The application calls only token exchange/revocation and GET holdings. Provider credentials may permit more than reading, so UI must describe our restricted use rather than a broker-issued read-only scope.

Activation requires privately configured app key/secret, exact registered callback and an operator-held permission reference covering encrypted retention of account responses. Public terms restrict permanent copies absent permission and other-client platforms require applicable approvals; no permission is inferred. Default disabled. No agent contacts live brokerage APIs, installs anything or runs cases.

Bounded supported import: settled CNC Indian-equity holdings with no T1/used/collateral/short/MTF quantities, no price discrepancy and consistent opening/realised/quantity fields. Reject ambiguous rows and duplicates; never silently discard them. Preserve numeric JSON lexemes before parsing, multiply integer quantity by broker average cost using integer arithmetic, round each cost half-up to paise and disclose that average-price-derived cost is broker-reported, not verified tradebook/tax cost. Retain encrypted raw response and normalized capture with hashes/time/parser version, generate an actual baseline-bound holdings preview, then use existing explicit confirmation/removal workflow. Revoke stops local reads immediately, cancels pending callbacks, scrubs tokens and attempts provider invalidation; remote failure is explicit. Export omits token/secret values; account deletion cascades all broker records. Offline mode explains that broker login needs connected mode; shared web/native holdings UI reuses the component.

Implementation update: first Zerodha pathway authored across runtime contracts, exact-decimal provider parsing, encrypted token/capture migration083, same-owner callback completion, manually consented fetch, existing preview/removal/reconciliation/confirmation, revocation, export/delete, shared responsive web/connected Android UI and offline denial. API1420–1425, WEB1420 and OFFLINE1420 are authored with synthetic provider boundary and actual application/database execution when user-selected. [Detailed implementation and manual activation](../development/kite-connection.md). Parent task remains Partial because other broker/CAS/registrar/Account Aggregator pathways are separate unimplemented requirements. Activation inputs: actual privately supplied app credentials, registered redirect and documented retention/deployment permission; no live call or permission approval was performed.

Native completion is authored: strict bounded custom-scheme handback passes ephemeral state/request token once through the existing trusted main-frame bridge, with no persistent token storage or arbitrary navigation. The WebView explicitly completes against the owner-bound server callback using its existing account cookie. The public GET callback itself never exchanges credentials; wrong-account and signed-out completions are covered by API1424. Custom scheme uniqueness is not assumed. First-party web confirmation remains available. All validation remains manual.

Incomplete/denied callback pages provide a safe return and an explicit signed-in cancellation form; GET performs no mutation. Owner-bound cancellation cannot cancel a replaced connection. Pending expiry clears the saved state capability on status read and displays reconnectable expiry. API1425 authors incomplete callback, cancellation, replaced-state refusal and actual expiry cleanup. Remote revocation is called confirmed only after a successful broker response with true data; otherwise local-only revocation is explicit.

## Upstox connection specification — 2026-09-15

Implement the verified authorization-code flow from [official authentication docs](https://upstox.com/developer/api-documentation/authentication/), [token response/expiry](https://upstox.com/developer/api-documentation/get-token/), [holdings](https://upstox.com/developer/api-documentation/get-holdings/) and [logout](https://upstox.com/developer/api-documentation/logout/). Registered redirect must match exactly; state remains one-time and owner-bound before exchange and after the network wait. Access-token lifetime ends at the next03:30 IST, including the same morning for issuance before03:30. Do not use undocumented refresh or extended-token entitlements. No passwords/OTP or trading operations.

Require explicit server activation plus credential/retention permission reference. [Website terms/privacy](https://upstox.com/terms-of-use-and-privacy-policy/) and public developer access do not establish this deployment's right to retain or redistribute account responses; no approval is claimed. Supported D-product NSE/BSE rows require consistent ISIN/instrument key, zero CNC-used/T1/collateral quantities and positive quantities. Reject unsupported row shapes and duplicates rather than quietly excluding them. Parse raw number lexemes, calculate cost with integer multiplication and half-up paise rounding, label broker-average-price basis unverified. Preserve encrypted raw capture, existing baseline/removal review and explicit confirmation; independent vault/capture IDs and expiry/revoke/export/deletion. Reuse connected holdings UI and native signed-in handback pattern; device-only mode refuses provider access. Author API1450–1459 as needed, browser/offline cases and docs; no deterministic execution.

### 2026-09-15 Upstox implementation handoff

- Spec implemented: OAuth2 same-owner code/state exchange, strict documented settled-D holdings parsing, exact per-row cost, encrypted token/raw capture, existing reconciliation/explicit confirmation, expiry/cancel/revoke/export/deletion, shared Holdings UI and native return bridge, explicit offline denial.
- Migration086_upstox_connection.sql; API1450–1456, WEB1450–1451, OFFLINE1450 authored with a synthetic provider boundary and actual persistence/confirmation. No execution/pass/commit claimed.
- [Full research, activation inputs, layer acceptance and manual commands](../development/upstox-connection.md). No new user question is needed to author this implementation. Private app registration/credentials/retention permission and manual validation remain activation inputs; no approval is guessed.
- Remaining parent scope: other named brokers and CAS/registrar/eligible Account Aggregator connections. The two implemented API pathways do not verify missing CSV export formats or complete all connectivity.

### 2026-09-15 next connector research and Angel specification

- Groww's [official authentication guide](https://groww.in/trade-api/docs/curl) documents manual token, daily-approved user API key/secret and TOTP. No delegated redirect/state contract was found. Do not collect passwords/TOTP or invent OAuth; a verified third-party delegated contract is the input needed for this path.
- ICICI's [Breeze reference](https://api.icicidirect.com/breezeapi/documents/index.html) documents API_Session browser return and signed GET holdings, but does not document state echo. The [session article](https://www.icicidirect.com/ilearn/stocks/articles/how-to-generate-session-key-and-install-sdk-for-breeze-api) describes form-post return. Secure multi-user callback correlation remains a specific research/provider input, not permission to match the latest pending user.
- Angel One's [official User docs](https://smartapi.angelone.in/docs/User) and [official SDK](https://github.com/angel-one/smartapi-python/blob/main/SmartApi/smartConnect.py) document publisher-login, state echo, profile, holdings and logout. Public forum reports new-login app incompatibility; it is not proof of eligibility or a supported fallback. Operator must obtain publisher-app eligibility and profile/holdings/retention permission for this deployment.
- Implement only gated publisher browser auth, same-owner one-time completion, profile token verification, settled DELIVERY exact-cost parser, encrypted capture/vault, actual existing reconciliation/confirmation, revoke/export/delete and connected native handback. No broker PIN/password/TOTP, token refresh or order endpoints.
- Inputs: ANGEL_API_KEY/REDIRECT_URL/PERMISSION_REFERENCE and truthful deployment CLIENT_LOCAL_IP/CLIENT_PUBLIC_IP/MAC_ADDRESS in server config. Disabled by default. Migration089; API1490–1496, WEB1490–1491, OFFLINE1490 authored acceptance. No tests/gates/activation run.

### 2026-09-15 Angel Publisher implementation handoff

Spec through encrypted API/store/migration089, actual reconciled holdings confirmation, shared web/native UI, offline denial, tests API1490–1496/WEB1490–1491/OFFLINE1490 and [research/manual acceptance](../development/angel-connection.md) authored. Provider eligibility, registered app/deployment headers, retention permission and user-run validation remain explicit activation inputs. No commands/services/live provider calls or commit executed. DEV028 parent remains partial for the other named pathways; API connector schemas do not complete broker CSV parser verification. Kite API1426 also authors changed-app-key refusal before any holdings network request.

## Saved full-inventory repair — 2026-09-19

The saved full run1789752953639-97020 includes failed cases tagged to this task. Confirmed causes, scoped authored repairs and remaining verification are recorded in [the full-audit RCA](../development/full-audit-2026-09-19.md). User runs `SDLC_AUTO_REPAIR=0 pnpm sdlc "Repair full audit failures" --story SDLC-REPAIR-016`. No new passing evidence or automatic bug resolution is claimed; this bounded repair does not remove broader source/device/functional requirements recorded above.

<!-- sdlc-validation:start -->

## Automated validation

Stale — rerun required. [Evidence](../validation/README.md); [bugs](../bugs/README.md). Latest reconciliation: 1789927529684-81206.
<!-- sdlc-validation:end -->
