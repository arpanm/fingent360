# DEV-029 — WhatsApp and mobile application shells

- **Status:** Completed initial implementation; validation and activation pending
- **Implemented / recorded:** Opt-in verified-recipient WhatsApp explicit and recurring public-summary delivery with encrypted schedule/outbox/occurrence receipts, Android shell/shared application, canonical public-reading share/copy, restricted native chooser and an authored iOS local-asset/HTTPS preview with native masked capture, AAC voice, encrypted feedback queue and foreground delivery, restricted three-broker native return, protected native file export/share; user-owned Xcode generation.
- **Pending:** User-run functional/provider/device acceptance, Meta approvals, signing/distribution and public deployment. Native background push/universal links remain separate enhancements, not claimed implemented.
- **Next action / inputs:** User validates authored channel/native flows; deployment owner provides actual Meta approvals and Apple signing/public deployment inputs before activation.
- **Verification:** The preserved evidence below applies only to its recorded scope/revision. This tracker migration did not run validation.

## Implementation handoff rule

After each change, update the summary above and the matching [TODO row](../../TODO.md). Keep prompts, detailed scope, remaining work, verification evidence and handoff commands in this file. Follow [task maintenance](README.md); a parent stays partial while a child requirement is missing.

## Preserved specification, prompts and history

The entries below are migrated records, not new execution instructions or current test-pass claims. The current summary above takes precedence where older statuses differ.

### DEV-029 — WhatsApp and mobile application shells

- **Implementation:** Partial
- **Verification:** Selected child evidence exists; full parent acceptance is incomplete
- **Dependencies:** DEV-012, DEV-017, DEV-018
- **Context:** README.md Gate 7
- **Scope and acceptance:** Implement WhatsApp summaries/deep links and Android/iOS webview shells only after channel acceptance. Preserve secure session boundaries, notification preferences, evidence links and jurisdiction/consent controls across channels.
- **E2E cases:** E2E-OFFLINE-010–012, 201–204; E2E-API-006; native matrix in tests/e2e/plans/android-acceptance.md.
- **Evidence / gaps:** Android webview/native bridge, local accounts/finance/readers and packaging/test tooling. Remaining: WhatsApp/iOS remain separate unimplemented channels; physical-device and connected deployment acceptance remain user/external work. Evidence: android.
- **Manual next actions:** After implementation, user runs relevant checks and selected E2E cases listed in the handoff; attach evidence before marking verified.

**Codex prompt**

> Read AGENTS.md, README.md, TODO.md task DEV-029, README.md Gate 7, and the linked E2E coverage plan. Follow the standard task prompt contract in TODO.md. Work on WhatsApp and mobile application shells. Scope and acceptance: Implement WhatsApp summaries/deep links and Android/iOS webview shells only after channel acceptance. Preserve secure session boundaries, notification preferences, evidence links and jurisdiction/consent controls across channels. Dependencies: DEV-012, DEV-017, DEV-018. Do not invent missing domain/provider fields or claim future functionality. If a product decision is unresolved, record it as a blocker and progress independent authored work. Add/update API and/or browser acceptance cases (or manual document-review scenarios if there is no executable behavior), including prerequisites and meaningful failures. Update this task, test catalogue and relevant README details. Do not run installation, formatting, lint, builds, tests, browser checks, service changes or migrations. Leave the local commit to the user-run pnpm sdlc format/check gate; never push. Give exact manual next actions.

<a id="dev-030"></a>

## Reusable task prompt

Read AGENTS.md, the task-maintenance guide and this task’s current summary. Work only on DEV-029 unless the user expands the scope. Treat the preserved specification/history as context; current user instructions take precedence. Implement the listed remaining acceptance end to end, or reconcile recorded completion evidence if no implementation remains. Keep tests, documentation and the root index consistent. Record exact remaining work and who needs to act. Do not execute deterministic validation or commit without the user-authorized gates.

## Input and pickup decision — 2026-09-15

- **Readiness:** Agent-ready
- **User input needed now:** No for the independent next step.
- **Decision:** No user input is needed for independent implementation/diagnosis. Use documented requirements; do not run deterministic gates under the existing execution boundary.
- **Recorded answer / authority:** Existing user instruction: agent owns research/implementation decisions within scope; user owns deterministic validation.
- **Question status:** None now. Do not ask for a repeat of existing answers.
- **Later input trigger:** An iOS signing/developer account or WhatsApp business setup may be needed for live distribution. Do not let this block platform-neutral code.
- **Next action:** Agent: prepare channel contracts and offline/connected boundaries; ask for provider/deployment accounts only at integration time.
- **Research/evidence:** See [dated source checks and existing answers](input-research-2026-09-15.md). Source-specific permissions, complete parser layouts and legal classification are not claimed resolved by triage.
- **Completion boundary:** This updates readiness only, not test passes, live activation or full feature completion. On later pickup, refresh saved evidence and update this record plus the TODO row.

## Active specification — safe public reading share (2026-09-15)

Replace copying the current browser URL (which can include query context or point to local/offline app assets) with an explicit canonical public-reader link. Strict contract: public HTTPS origin, validated discovery ID, `/#read/ID` only, no credentials/query or private route. Local/offline editions show that a shareable deployment link is unavailable rather than copying an unusable device URL. Reader More connects Share and Copy to this payload; cancelled/unsupported share preserves fallback. Android connected mode uses a restricted native chooser; local mode never claims a remote link. Sharing does not include private holdings, goals, feedback tokens or reading history.

Reuse existing public discovery reader/API and consent boundaries; no new numerical model, DB table, migration or server job is needed for user-directed public-link sharing. Share action never auto-selects a recipient or sends an external message; native/browser success only confirms handoff. WhatsApp business automation, approved templates, webhook/signing, iOS shell and verified deployment remain separate pending scope. No provider account/input is needed for this platform-neutral slice. Web Share primary API evidence: https://www.w3.org/TR/web-share/ .

## Delivery — 2026-09-15

Public reader sharing is authored end to end across shared contracts, web Reader and Android native chooser, with explicit offline/unavailable boundaries and existing public reader APIs. [Implementation/research/manual acceptance](../development/public-reading-share.md). WEB1255/OFFLINE1255 and contract tests authored; no execution, external message, new dependency or migration. Parent remains Partial: WhatsApp business automation and iOS shell remain unimplemented; real deployment/device acceptance pending. No user input needed for this slice, and no question/answer is invented.

## iOS shell specification — 2026-09-15

Author a standalone Swift/WKWebView shell and deterministic user-owned Xcode project/asset packaging tool. Reuse shared offline React/API emulation, persisted local accounts/receipts/consent and connected HTTPS deployment. Restrict asset reading to bundled public web directory; disallow traversal, arbitrary native commands, file access and unapproved in-view navigation. Persistent WebKit storage is per app; local and connected origins remain separate, with no silent migration of private data. No universal/custom URL callback registration: broker OAuth callbacks remain unavailable on this initial shell. External source links open only after an explicit user link action and HTTPS validation. Report native feedback, background sync, push and callback capabilities as unavailable; shared text feedback/local storage remains. Native permissions must not be requested without a supported purpose. Offline bootstrap checks actual WebCrypto and IndexedDB, showing an actionable error rather than silently weakening cryptography if the OS WebView lacks them. Physical-device acceptance remains mandatory; authoring is not a built or signed IPA.

Research: Apple WKWebView navigation delegates and bounded local-resource access documentation; https://developer.apple.com/documentation/webkit/wkurlschemehandler; https://developer.apple.com/documentation/webkit/wkwebsitedatastore/default(); https://developer.apple.com/documentation/webkit/wknavigationdelegate. Existing React contracts/API/database are reused; no new DB model or migration is needed for a packaging-only channel. Manual generator creates a local Xcode project; simulator builds require Xcode and physical distribution requires user's signing account. No account is needed to author the shell.

## iOS preview authoring handoff — 2026-09-15

Authored native Swift WKWebView shell, loopback-only static assets, strict offline/HTTPS configuration, persistent origin boundaries, external-link confirmation, rejected callbacks/native commands, visible failure/retry, runtime cryptographic capability check and user-owned Xcode project generation. Details: [iOS preview](../development/ios.md). Custom-scheme design was replaced with stable loopback to preserve a trustworthy origin; no private/native endpoints exist. API/DB/shared offline implementation reused. E2E-OFFLINE1560/1561 plus manual IOS1562–1569 authored. This is an initial preview implementation, not complete native media/feedback/push/callback parity; those remain explicit work. No device behavior or built IPA is claimed. No user signing input was needed for authoring; actual signing/distribution later requires it.

Manual: user runs `node scripts/ios-package.mjs`, opens generated `artifacts/ios-preview/Fingent360.xcodeproj`, chooses simulator or signing team/device in Xcode. Existing dependencies required; no new package dependency/migration. For authored selected cases use `pnpm sdlc "Add iOS preview shell" --grep "E2E-OFFLINE-156[01]"`; then follow native acceptance matrix. Report failing ID/artifact path or Xcode error/iOS version. No tests, build, install, format/check or commit executed; baseline a2c53a0, shared changes pending user gates.

## WhatsApp public-summary channel specification — 2026-09-15

Initial supported scope is an explicit owner-requested admitted public-item summary, not automated bulk subscriptions. Owner enters their number and explicit channel consent; a high-entropy fifteen-minute challenge must arrive from that same number through Meta's signed webhook before delivery is enabled. App disconnect or inbound STOP disables future deliveries and cancels queued jobs. Recipient/challenge and message receipts use existing private-key encryption and owner export/deletion; no plaintext phone/body log. Account deletion cascades channel records. Original source admission/version/hash and an operator-configured allowed-source policy are rechecked before dispatch; no private holdings/goals/feedback or unpublished article body can enter the template. Cloud accepted is distinct from delivered/read. A timeout or lost acknowledgment becomes uncertain and is not blindly retried; explicit provider failure is retryable through an owner action. Raw-body HMAC webhook verification, configured business-number identity, duplicate event receipt and monotonic status transitions prevent fabricated or replay-regressed delivery status. Code/worker are default disabled until actual business credentials, three-text-parameter template approval, origin and source redistribution permission are configured. No external message is sent during authoring.

Primary API research: Meta-maintained current Postman collection https://www.postman.com/meta/whatsapp-business-platform/documentation/wlk6lh4/whatsapp-cloud-api documents message endpoint/template envelope and webhook subscription assets. Meta SDK reference https://whatsapp.github.io/WhatsApp-Nodejs-SDK/api-reference/messages/template/ confirms approved template body text parameters (SDK archived; no SDK dependency added). Developer-portal pages returned429 in this research; exact live provider/version approval remains an activation input. No guessed automatic subscription, opt-in approval, delivery receipt or account credentials are asserted.

## iOS native feedback implementation — 2026-09-15

[Specification, security boundaries and manual acceptance](../development/ios-feedback.md). Authored native screenshot, voice stop/cancel/permission recovery, Keychain/AES-GCM per-origin feedback queue, bounded exact HTTPS receipt transport, foreground signals and shared web/native capability adapters. API1610, WEB1610–1612, physicalIOS1612–1619 authored. Existing feedbackAPI/DB/consent/retry/deletion reused, no migration or dependency. No signing, project generation, build or deterministic validation executed; manual SDLC owns gated commit. Push/background delivery and broker callbacks remain separately unsupported.

## 2026-09-15 iOS broker return specification

Reuse the existing provider-registered HTTPS callback and explicit `fingent360://broker-kite|broker-upstox|broker-angel` return link. The native shell accepts only a prepared, unexpired state from its current trusted connected main frame; no cold-start/reload recovery of credentials, arbitrary navigation or token logging/storage. Shared UI must register a pending return after consent, expose a second completion confirmation and clear it on discard/revoke/unmount. The existing API remains authoritative for signed-in owner, one-use state and session/version re-admission. This first native return supports same-origin API deployments (HTTPS reverse proxy); separate API origins fail visibly rather than trust a web-supplied destination. No new provider authorization contract or database schema is needed. Custom schemes are not exclusive ownership and carry existing provider credentials, not opaque receipts; do not claim otherwise.

Acceptance: three provider returns, unsolicited/wrong-state/duplicate/query injection/expired/cold-start returns refused; offline denied; reload clears memory; native never exchanges credentials; existing owner mismatch API denial retained. Author shared browser simulations and physical acceptance1640–1649; user runs gates/device validation separately. Signing, provider registration, universal links and background push remain external/separate.

## WhatsApp authoring handoff — 2026-09-15

Connected explicit public-summary delivery is authored across runtime contracts, encrypted PostgreSQL098 recipient/outbox/replay/throttle tables, signed inbound verification/status, bounded manual retry, consent cancellation, owner export/deletion and shared responsive web/app settings. Sources are re-admitted under publication locks before external dispatch; unknown or changed receipts cannot silently send. Browser mutations use actual net.json body/method semantics. Offline API refuses recipient/delivery storage and retains existing public-link sharing. API1600–1604, WEB1600 and OFFLINE1600 author actual storage/API/browser workflows plus explicitly simulated provider transport/status; no real Meta messages were sent. [Configuration, limits, remaining inputs and manual acceptance](../development/whatsapp.md).

Remaining: actual Meta business account/template/version/source redistribution approval and public HTTPS webhook activation; manual account/provider/device validation. Recurring digests/subscriptions are not part of this explicit one-summary request implementation and remain unimplemented if broader automation is required. Historical encrypted channel rows require old private-data keys retained until rewrapped or deleted. No tests, checks, migrations, services, builds, dependency operations or commit were executed. Root owns concise TODO/README/catalog reconciliation; current tree also contains unrelated parallel feature work.

Implementation: iOS broker return authored across native main-frame bridge, shared three-connector UI and existing server callback labels. Browser1640–1643 and physical IOS1644–1649 authored; no execution. See [scope and manual actions](../development/ios-broker-return.md). API/database/provider authorization contracts reused; same-origin HTTPS limitation explicit. That delivery initially left native export/share; the later export implementation below closes this code gap. Release/device work still prevents full parent closure.

## 2026-09-15 iOS private export specification

Connect existing account/financial/report/feedback export buttons and download anchors to a bounded native share sheet. Accept structured bytes only, never a file path or remote URL. Require trusted current main frame, exact safe basename/allowed MIME, 12MB size limit and one sheet at a time. Write an unpredictable temporary directory with complete file protection and backup exclusion; remove only that owned export on completion/cancel/navigation/timeout and clear abandoned export directories on next launch. Share cancellation must surface as cancellation, not saved. The user-selected destination receives a copy outside app retention control. Offline exports reuse local contracts; connected exports still use owner-bound APIs. No schema/provider/network or npm changes. Author WEB1660–1662 bridge cases plus native IOS1663–1669; no deterministic execution.

2026-09-15 private export implementation: shared saveDownload and native UIActivityViewController now connect real API/offline exports with protected bounded temporary files, accurate cancellation and cleanup. WEB1660–1662 and physical IOS1663–1669 authored. See [private export scope/manual actions](../development/ios-private-export.md). No schema changes or execution. Accepted preview shell implementation now includes feedback, broker return and private export; full DEV029 stays partial for release/push/channel acceptance.

## Recurring public-summary specification — 2026-09-15

Add one explicit owner-consented daily/weekly schedule with editable local time/IANA timezone, weekday, admitted source filters, maximum item age and up to three summaries. Persist encrypted versioned configuration, idempotent write receipts and unique schedule-version/due-time occurrence receipts. Reuse existing calendar-time DST rules (fold:first instant; gap:next valid minute). Late occurrences over ten minutes are skipped with a receipt; never catch up missed digests. Each occurrence retains queued/empty/unavailable/skipped outcome and exact outbox IDs. Source selection uses actual current public receipts, configured redistribution filters and maximum age, then existing dispatch re-admits exact source/version. Pause/delete, STOP/disconnect and recipient replacement cancel queued scheduled messages and future scheduling. Recurring quota is distinct from ten explicit daily requests. Shared connected web/app exposes preview, next due, pause/resume/delete and outcomes; offline refuses configuration and scheduling. Owner export/delete includes these records. No provider activation, credentials, external send or deterministic execution during authoring.

## Recurring WhatsApp delivery handoff — 2026-09-15

The earlier recurring-subscription gap is now authored: migration102 encrypted schedule heads/versions/request receipts/occurrences, runtime schemas, actual occurrence selection/publication admission, distinct three-per24-hour recurring quota, optimistic/idempotent edits, pause/delete/STOP/recipient-change cancellation, timezone rules and skip-not-catchup outcomes. Shared web/app has source/time/frequency/age/consent configuration and visible next due/history; offline refuses unsupported scheduling. Dispatch additionally checks active schedule version and source freshness deadline. Owner export/delete includes actual records. API1690–1691, WEB1690 and OFFLINE1690 authored, not run. [Manual commands, limitations and deployment inputs](../development/whatsapp.md#recurring-public-summaries-migration102). Parent remains activation/manual acceptance pending for this channel; no Meta sends, migration, gates, services or commits performed.

## Initial-scope reconciliation — 2026-09-15

The accepted initial scope (WhatsApp summaries/deep links and Android/iOS shells preserving session boundaries, evidence links, consent/jurisdiction and notification choices) is authored. Shared readers/account consent and preferences are reused by both shells; canonical public links and explicit/recurring WhatsApp opt-in preserve public-only boundaries. Native feedback, broker returns and protected export additions are documented above. Code completion is separate from actual Meta/channel approval, signing, physical-device acceptance and deployment validation, none of which is claimed. OS background push and universal-link enhancements remain explicitly unsupported; the original initial-shell acceptance does not require claiming those mechanisms. Root reconciles TODO/README to completed initial implementation, validation/activation pending.

Channel admission follow-up: user-facing channel and recurring-schedule transactions recheck account session after storage work, including replay returns. API1605 authors actual outbox lock wait followed by session expiry and401 response; scheduled worker/provider webhook paths keep their distinct authenticated-machine/consent checks. No execution.

## Public sharing saved-failure repair — 2026-09-17

Authored: one GET-only network-reset retry in the synthetic host fixture, plus share-denial, clipboard-denial and selected canonical-link recovery assertions. Validation remains pending.

Scope/specification: BUG-14bc3236ae60219f records a reset of the synthetic public-host proxy while loading a CSS GET in WEB1255 mobile, before reader interaction. Desktop passed in that same run; this does not establish a production share defect. Retain real reader/API and canonical public-host simulation, but permit bounded transport reset retries only for safe GET requests. HTTP error responses and mutations must not be retried. Extend the same case to verify failed share and clipboard-denied recovery leave the canonical selectable public link available. Existing OFFLINE1255 continues proving no local/device share URL is offered. UI/contracts/API/database/source/native/automation behavior is reused unchanged; no runtime change is justified by this saved evidence.

Reusable prompt: retain exact canonical URL/private-context exclusion, successful handoff and cancellation assertions; do not replace real content with mocks or mark the saved bug resolved before user evidence. Keep synthetic host/OS capability substitution explicit.

Manual command: `pnpm sdlc "Repair public reading share acceptance" -- --project=desktop --project=mobile --grep "E2E-WEB-1255"`. Existing migrated databases/API/web are required; no dependency/migration changes. Use the current printed web URL → reader → More item actions (latest saved web target http://127.0.0.1:5176); localhost intentionally reports public sharing unavailable. Public-host behavior is simulated only inside the selected case. Public deployment/native physical acceptance remain separate activation gates. No checks/tests/services/browser/commit executed; baseline `bee7f02`, changes await user gates. Report case/project/run ID and redacted failure evidence. Existing generated failures are preserved.

## Saved full-inventory repair — 2026-09-19

The saved full run1789752953639-97020 includes failed cases tagged to this task. Confirmed causes, scoped authored repairs and remaining verification are recorded in [the full-audit RCA](../development/full-audit-2026-09-19.md). User runs `SDLC_AUTO_REPAIR=0 pnpm sdlc "Repair full audit failures" --story SDLC-REPAIR-016`. No new passing evidence or automatic bug resolution is claimed; this bounded repair does not remove broader source/device/functional requirements recorded above.

<!-- sdlc-validation:start -->

## Automated validation

Stale — rerun required. [Evidence](../validation/README.md); [bugs](../bugs/README.md). Latest reconciliation: 1789847379056-38783.
<!-- sdlc-validation:end -->
