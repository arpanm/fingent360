# FEEDBACK-001 — Submitted public context acceptance appendix

## Authoring record — 20 September 2026

The user requested the remaining non-deferred work, while retaining manual-only deterministic execution. This appendix covers only the submission-context gap in [FEEDBACK-001](FEEDBACK-001.md). The new cases are authored, not executed. No production change or migration was required by static inspection. The parent task still retains its separate delivery, accessible UI and physical native acceptance requirements.

Reusable scope: verify what the browser actually submits from the displayed reader/story, what the real feedback API retains, and how the retained source/media references link to provider evidence. Do not substitute sessionStorage inspection, mocked successful application responses or a provider call using live keys for that journey.

## Reviewed cases and layers

| Case / projects                | Concrete acceptance                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| E2E-WEB-1121 / desktop, mobile | Generate captions and an image through the actual MediaStore in an isolated owned schema, with explicitly synthetic external provider transport confined to a child process. Publish through the authenticated real API. Display the reader and story, submit each through the actual composer, and compare the real POST with the decrypted operator report. Require exact displayed source edition, media asset, image attempt and hash. Follow those references through the real evaluation API to the retained view/composition, caption provider/model/instructions/input/raw output/text output and image attempt; compare the image attempt's retained prompt/provider output/image with the synthetic transport and displayed media. |
| E2E-WEB-1122 / desktop, mobile | Navigate from a reader to a real synthetic account's private goal; submit and require no publicView or private goal/holding contents. Hold an actual reader response and submit while loading; require no stale publicView. Advance the immutable source and publish the newer media through real storage/API while retaining the old reader response. Require the crossed-version warning and exclusion of mismatched media from actual submitted context. Reload and submit the revised exact source/media references. Confirm only source-bearing submissions appear in the source's retained evaluation linkage.                                                                                                                         |

Implementation evidence: `apps/web/src/eval-lineage-view.ts`, `Discovery.tsx`, `MediaSummary.tsx`, `Feedback.tsx`; contracts in `packages/contracts/src/feedback.ts` and `eval-lineage.ts`; actual stores in `apps/api/src/feedback.ts`, `media.ts`, `eval-lineage.ts` and `eval-lineage-recording.ts`. Test ownership is `tests/e2e/cases/browser/feedback-context-acceptance.spec.ts`.

- Specification and provenance: the visible old source edition remains a legitimate historical view; only media from a different edition is excluded. No arbitrary snapshot age limit is invented. Feedback context remains a client-reported view, not authenticated proof of what a person saw.
- UI/UX and workflow: real reader, story, private navigation, loading, mismatch and reload recovery use the existing composer and automatic delivery. These cases do not replace separate keyboard, visual or physical-device acceptance.
- API/contracts: strict schema parsing of real submissions, receipts, operator reports, media and evaluation detail; actual consent and transport delivery are required.
- Database: existing fixture-owned PostgreSQL/MongoDB isolation and real encrypted feedback storage are reused. No new table, migration, retained production data or cleanup outside the fixture is introduced.
- Media/model evidence: only the external OpenAI transport is simulated, using labelled synthetic models and image bytes. Actual application generation, review, publication, browser delivery, lineage recording and feedback retention execute when the user runs the cases. No live provider quality, cost, permission or production integration is certified.
- Automation/resources: subprocess generation starts only inside the selected test, has a bounded timeout and closes its pool. The subprocess receives only explicit synthetic provider configuration, does not inherit live provider credentials, and rejects other fetch destinations. The fixture verifies its owned database schema before generation. Trace/video/screenshots are disabled to avoid capability and private fixture capture.
- Offline: these new cases specifically address connected submission and retained server/provider linkage. Existing offline feedback queue and native cases remain required; no claim is added for connected provider generation while offline.

The delayed transport reuses the first real response for duplicate mount requests, so React development remounts cannot accidentally replace the retained old edition with the newly revised one. No response payload or successful feedback receipt is fabricated.

## Manual handoff and remaining gates

No dependencies changed. The user must provide configured PostgreSQL/MongoDB with current migrations and current API/web services (`pnpm db:up`, `pnpm db:migrate`, `pnpm dev` when needed, following README). The SDLC launcher does not start services or apply migrations; it owns format/check, the gated commit and selected validation:

```bash
pnpm sdlc "Validate feedback submission context" -- --project=desktop --project=mobile --grep 'E2E-WEB-112[12] '
```

Use the web URL printed by the launcher (`/#read/<fixture-source>`, `/#explore`, `/#feedback`; the fixture data exists only during its selected test). The two IDs carry `@FEEDBACK-001 @EVAL-LINEAGE-001 @TEST-SIMULATION`. Expected new acceptance: both cases pass in both browser projects, with real stored reports and exact evidence comparisons. The parent must include these requirements in its reviewed matrix and retain existing API1110/1111/1114 and browser companions; these four new receipts alone do not close the parent.

For a failure, provide the run ID/date, selected IDs/projects, saved report path and sanitized error. Do not send receipt tokens, DB connection strings, private fixture captures or provider credentials. No tests, format, check, build, services, migrations or commit were run by the author. Worktree baseline was `c7874a5`; other agents' existing changes were preserved.

After actual current passes, the submission-context authoring gate can be replaced with those receipts. Until then it remains validation pending. Physical Android/iOS microphone/capture/storage/HTTPS lifecycle acceptance remains separate; the parent task's durable-delivery and full keyboard/mobile/history gates are unaffected by this appendix.
