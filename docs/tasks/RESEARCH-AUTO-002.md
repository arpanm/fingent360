# RESEARCH-AUTO-002 — Automatic source ingestion, release calendar and historical vintages

- **Status:** Completed initial implementation; manual activation and validation pending
- **Implemented / recorded:** Automatic source capture, schedule controls, approved news publication policies and source-isolated BEA/BLS calendar history, day-precision FOMC capture/history, permission-gated RBI MPC originals, generalized BEA original GDP acquisition and bounded operator-selected PIB monthly quarterly-GDP archive pickup into drafts/quarantine and exact dated Cleveland CPI model/first BLS actual comparisons are authored.
- **Pending:** User-run validation and operator-controlled worker/source activation with actual required permissions. Complete worldwide historical backfill, additional numerical series and RBI calendars beyond the verified MPC family are coverage expansions, not missing initial implementation. No live data/worker/device acceptance is inferred from authored code.
- **Next action / inputs:** User validates the linked worker, schedule, calendar and original-vintage cases; operator records source rights and deliberately enables eligible schedules/publication policies. No new private input is needed to finish the initial authored scope.
- **Verification:** The preserved evidence below applies only to its recorded scope/revision. This tracker migration did not run validation.

See [current delivery summary](current-delivery.md) for the batch-wide distinction between code, missing functionality and validation.

## Implementation handoff rule

After each change, update the summary above and the matching [TODO row](../../TODO.md). Keep prompts, detailed scope, remaining work, verification evidence and handoff commands in this file. Follow [task maintenance](README.md); a parent stays partial while a child requirement is missing.

## Preserved specification, prompts and history

The entries below are migrated records, not new execution instructions or current test-pass claims. The current summary above takes precedence where older statuses differ.

### RESEARCH-AUTO-002 — Automatic source ingestion, release calendar and historical vintages

- **Status:** Partial: automatic enabled-source capture, schedule controls, approved automatic news-publication policies and BEA calendar capture history authored. Other calendars and original numerical vintages pending. Verification not run; migration054 authored.
- **Scope:** Spec → shared web/app UI/UX → API/contracts → durable data/provenance → offline behavior → test cases → documentation. Record missing external access/format evidence explicitly; implement all independently possible layers.
- **Reusable prompt:** Deliver automatic source ingestion, release calendar and historical vintages with real-source evidence, strict versioned data, complete navigation/recovery and authored API/browser/offline tests. Follow DELIVERY-TEAM-004 boundaries and provide precise integration notes.

## Reusable task prompt

Read AGENTS.md, the task-maintenance guide and this task’s current summary. Work only on RESEARCH-AUTO-002 unless the user expands the scope. Treat the preserved specification/history as context; current user instructions take precedence. Implement the listed remaining acceptance end to end, or reconcile recorded completion evidence if no implementation remains. Keep tests, documentation and the root index consistent. Record exact remaining work and who needs to act. Do not execute deterministic validation or commit without the user-authorized gates.

## Input and pickup decision — 2026-09-15

- **Readiness:** Research-ready
- **User input needed now:** No for the independent next step.
- **Decision:** No user input needed for the next step: research primary documentation, record evidence and implement only verified source/domain behavior. Research-ready is not a claim that all inputs or permissions are already available.
- **Recorded answer / authority:** User: use free sources first and have the agent determine regulations, source usage and formats; do not ask the user to discover them.
- **Question status:** None now. Do not ask for a repeat of existing answers.
- **Later input trigger:** If a concrete private input or external authorization becomes necessary, record the exact evidence and question before asking.
- **Next action:** Initial implementation reconciled below; user validates existing cases, operator approves eligible source activation. Additional calendar/archive coverage is a separate future source task.
- **Research/evidence:** See [dated source checks and existing answers](input-research-2026-09-15.md). Source-specific permissions, complete parser layouts and legal classification are not claimed resolved by triage.
- **Completion boundary:** This updates readiness only, not test passes, live activation or full feature completion. On later pickup, refresh saved evidence and update this record plus the TODO row.

## BLS release calendar specification and research — 2026-09-15

No user input is needed. [Official BLS subscription instructions](https://www.bls.gov/help/hlpical.htm) identify `https://www.bls.gov/schedule/news_release/bls.ics`. [BLS copyright terms](https://www.bls.gov/bls/linksite.htm) permit public-domain publication reuse with attribution, excluding previously copyrighted imagery; no logo or imagery is used here. Read-only format research retrieved HTTP200 `text/calendar`, 80,672 bytes, 313 events on 2026-09-15. Its immutable research fixture and SHA256 provenance live in packages/contracts/test/fixtures/bls-calendar-official.*. The default Python user agent received403; a transparent Fingent360 research user agent retrieved the official calendar. Runtime failures must be surfaced, never bypassed or replaced by fabricated schedules.

Observed format: `DTSTART;TZID=US-Eastern`, with explicit VTIMEZONE defining second-Sunday March daylight transition (UTC-5→UTC-4) and first-Sunday November standard transition (UTC-4→UTC-5), from2007. Parse only the observed timezone definition, use America/New_York wall-time validation, reject nonexistent/ambiguous local times, unsupported properties/recurrence/unknown zones and invalid dates. Preserve UID/SEQUENCE/cancelled status, raw capture and revised editions; never treat a schedule as a published numerical vintage.

Implement a BEA/BLS source selector with source-isolated captures/history, scheduled BLS capture, PostgreSQL edition source identity, Mongo raw retention, attribution, empty/loading/retry states and connected/native shared UI. Offline snapshots contain latest downloaded captures per source and never claim older undownloaded capture bodies. Author API/browser/offline and winter/summer/DST-invalid parser cases. Other calendars and original numerical publication vintages remain a separate remaining scope.

## BLS implementation handoff — 2026-09-15

Implemented source-isolated BLS/BEA capture history, strict DST-aware BLS parser, migration062, shared web/connected-Android source/capture selection, BLS schedule control through existing Operations, Mongo raw retention and offline source-keyed snapshots. Actual BLS format/rights evidence is recorded above; no current private input or question is needed. [Detailed implementation and manual acceptance](../development/bls-release-calendar.md).

Authored E2E-API-1070, E2E-WEB-1070 (desktop/mobile), E2E-OFFLINE-1070 and real-fixture/DST parser unit cases. Tests, migration, source jobs and all SDLC commands remain **not run**. Existing BEA records/hashes and old APK bundle format remain compatible. Parent RESEARCH-AUTO-002 stays Partial for remaining calendars and original numerical publication vintages; no broader completion or source activation is claimed.

## FOMC calendar specification and input evidence — 2026-09-15

Implement the next calendar family from the [official Federal Reserve Board meeting calendar](https://www.federalreserve.gov/monetarypolicy/fomccalendars.htm), whose current HTML supplies year headings, meeting month/day ranges, projection asterisks and optional statement links. Day precision is mandatory: do not fabricate announcement timestamps. Parse cross-month ranges and notation votes, preserve statement-linked versus merely scheduled status, and retain source captures before parsing. Known-at is capture retrieval, never original publication. Revised captures stay independently readable. Unknown layouts fail closed; no generated eight-meeting approximation.

[Board reuse terms](https://www.federalreserve.gov/disclaimer.htm) permit copying/distribution of its information unless otherwise marked, with attribution. This adapter extracts calendar facts and links, not logos/third-party imagery. The source request is fixed to the original HTTPS page, bounded, without redirects. API, web/connected Android and downloaded offline projection must share the strict schema, include loading/empty/error/retry and capture history, and author actual storage/API plus keyboard/mobile/offline acceptance. Migration085 stores immutable parsed editions, Mongo stores original captures; the existing manual-enabled worker schedule owns acquisition. Tests and worker activation remain manual.

RBI follow-up research: the official 2025–26 schedule is referenced by https://www.rbi.org.in/scripts/PublicationsView.aspx?id=23139, but exact original 2026–27 source retrieval and reproduction permission remain unverified; third-party claims are not admitted. Generalized original GDP release acquisition beyond the two already retained BEA Q2 2025 vintages remains separate remaining work.

### FOMC implementation handoff

Authored strict `policy-calendar` contracts and verified-layout parser, fixed-source acquisition under the existing research worker, Mongo raw retention (including rejected layouts), immutable PostgreSQL migration085 capture history, source-schedule controls, public `GET /research-calendar/policy?edition=...`, shared web/connected Android reader and downloaded offline handler. No invented exact announcement time, publication timestamp or decision values. Old offline bundles without this optional field show an empty state; downloaded history choices include only their stored body. Original BEA/BLS response contracts remain unchanged.

Authoring evidence: E2E-API-1440 retains original input and independent revised editions, verifies actual API read and immutable storage; E2E-API-1441 rejects invalid dates, mismatched statement dates, duplicate meetings and changed layout without replacing last good capture. E2E-WEB-1440 exercises API-backed calendar/capture navigation, keyboard selection and failure/retry in desktop/mobile projects. E2E-OFFLINE-1440 rejects unavailable history and tampered statement origins. Fixture HTML is explicitly **reconstructed minimal format with real published dates**, not a claimed original byte capture. Full official-page read-only research was temporary and no source job was run.

Manual next actions: PostgreSQL/MongoDB and connected API required; apply `pnpm db:migrate` for085, then `pnpm sdlc "Add retained FOMC policy calendar" --grep "E2E-(API|WEB|OFFLINE)-144[01]"`. Open the dev URL printed by `pnpm dev`, `#research-calendar`; the FOMC section must retain day precision, source attribution, statement links, edition selection, loading/empty/retry states. In Operations automatic research, enable `fomc-calendar` when live acquisition is wanted. Refresh the offline snapshot and rebuild the APK manually to include new captured data; installed offline APKs do not update themselves. Report the failing case ID, run folder and error context. Dependencies unchanged. No tests, format, checks, builds, migration, source acquisition job or commit were run. Last existing commit observed: `a2c53a0`; team changes remain uncommitted until user-run gates.

## Generalized original BEA GDP vintages specification — 2026-09-15

Source research verified the official [current release index](https://www.bea.gov/news/current-releases), original [2025 Q2 second estimate](https://www.bea.gov/news/2025/gross-domestic-product-2nd-quarter-2025-second-estimate-and-corporate-profits-preliminary) and [2026 Q2 second estimate](https://www.bea.gov/news/2026/gdp-second-estimate-and-corporate-profits-2nd-quarter-2026). Both original layouts explicitly provide an embargo date/time with EDT/EST, original quarter, estimate class, and annualized real-GDP headline. The newer title begins GDP rather than Gross Domestic Product; support both verified layouts. Follow only exact official original links whose title identifies GDP estimate and quarter; reject state/county/annual-only/unknown layouts. Fetch original pages, never treat current interactive table links as original vintages.

Contracts first: retain real GDP annualized decimal value, original period, estimate, original publication timestamp parsed from source, retrieval known-at, original URL and source hash. Source acquisition uses the existing bounded fetch+raw retention workflow and publication review, not fabricated numerical data or a fixed quarter list. Existing discovery PostgreSQL versions and Mongo captures supply persistence; no new migration is required. Shared web/Android reader presents provenance and same-quarter published releases; offline uses only retained downloaded publications. A captured revised body is not silently an earlier known-at fact. Source release corrections remain available through existing per-item history. Never label a previous estimate as a market expectation or a vintage revision as a GDP surprise. Author original-format, wrong-quarter/time, API retention/review/withdrawal, desktop/mobile keyboard/retry and offline tests1480–1489. Runtime source activation and all deterministic gates remain manual.

### Generalized BEA GDP implementation handoff

Implemented `bea-gdp-original` acquisition from the official current-release index, bounded discovery of original national GDP estimate pages, original-body retention and strict original URL/year/quarter/estimate/timestamp agreement. The parser supports the verified long and short headline layouts, validates actual New York daylight/standard offset and weekday, and extracts only the explicitly annualized real GDP sentence. It neither follows current interactive tables nor substitutes current values for an original release. Contract provenance says `captured-original-release-page`: original publication time belongs to the source release metadata; the captured value is known to this app only at retrieval. Re-fetch corrections receive new existing immutable discovery versions, with older captures retained; this does not backdate corrected knowledge.

Reuse is concrete: existing Mongo `discovery_raw`, PostgreSQL `discovery_items`/immutable `discovery_versions`, source worker schedules, Operations discovery/publishing review, publication admission/withdrawal, feed/story reading and source evidence. New `GET /discovery/gdp-vintages?period=YYYY-Qn` returns currently published retained releases for comparison; final source-view data is recorded through existing public-view capture. Shared web/connected Android calendar overview and story detail show same-quarter estimates, original publication, capture/review times and original link. Offline derives only from retained downloaded publications and removes withdrawn provenance. No new migration/dependency; reserved087 was not used. Existing automatic-publication permissions are not broadened for numerical GDP source; acquisition makes drafts for review.

Authored E2E-API-1480 (real retained draft/publication review, quarter history, source evidence and withdrawal), E2E-API-1481 (three actual-release factual fixtures, both original layouts, timezone/weekday/period and index-origin rejection), E2E-WEB-1480 (actual API-backed quarter list, keyboard expansion and retry on desktop/mobile) and E2E-OFFLINE-1480 (source-bound snapshot, withdrawal, period tamper). Fixtures explicitly reconstruct minimal markup around primary-source facts; they are not complete original-byte captures or live ingestion evidence.

Manual: with PostgreSQL/MongoDB and API running, use `pnpm sdlc "Add original BEA GDP vintage acquisition" --grep "E2E-(API|WEB|OFFLINE)-148[01]"`; open `#research-calendar` or a reviewed GDP story on the printed dev URL. Enable/refresh `bea-gdp-original` in Operations and review retained drafts; verify original timestamps versus capture time and follow original links. Refresh offline snapshot/rebuild APK to ship new captures. Report failing ID, artifact run path and error context. No execution, dependencies, migration or commit performed; existing commit `a2c53a0`, user-run gates own the next commit.

Scope still pending: this current-release-index adapter accumulates future originals and retains observed edits; it does not claim a complete retrospective archive. Original release archive form/pagination and other numerical families remain research work, alongside RBI schedule capture.

## RBI MPC acquisition specification — 2026-09-15

Primary original verified: https://www.rbi.org.in/Scripts/BS_PressReleaseDisplay.aspx?prid=62422, published March23 2026, FY2026–27 six explicit three-day meetings. Ordinary original HTML retrieval succeeded during format research. Implement fixed-original acquisition, strict table/date/year/press-reference parsing, raw-first Mongo retention and immutable PostgreSQL editions094; day precision only, no inferred decision time, rate or inflation release. Scheduled worker defaults disabled and requires stored operator permission evidence covering caching, display, internal links and offline distribution. Official https://www.rbi.org.in/Scripts/Disclaimer.aspx expressly restricts caching/internal links absent permission; no production permission is recorded. Shared calendar reader shows only captured facts, expiry/refresh/history/error states; offline contains only downloaded capture and no claimed archive. Add actual published date fixtures with reconstructed minimal markup, API/browser/offline1550–1551. No deterministic execution. General retrospective numerical backfill and other RBI releases remain outside this verified MPC-format acquisition.

### RBI MPC acquisition implementation handoff

Authored fixed-original FY2026–27 acquisition in `rbi-calendar.ts`, strict runtime source/date/reference/ordered six-row validation, bounded original fetch, Mongo raw-first retention (including rejected layout), immutable094 capture editions with recorded permission evidence, existing durable worker/schedule configuration, public edition/history API, shared web/connected Android loading/empty/retry/keyboard/earlier-meeting reader, and offline downloaded-edition validation. Worker defaults disabled. Enabling requires actual operator permission evidence covering caching, display, internal linking and offline distribution, recorded atomically with the authorized setting. The original schedule supplies meeting days only; no 10am decision, original decision timestamp, rate or general RBI-release calendar is invented. Current fixed original expires after FY2026–27; onboarding a later exact original requires verified parser/source update, not rolling synthetic dates.

No permission has been provided. Concrete input for the operator: provide the RBI permission reference covering source caching, app display, internal links and offline redistribution before enabling `rbi-mpc-calendar`. Research and code authoring did not need this private input. No request or message was sent to RBI. Official-source date facts were verified via the original; test HTML is explicitly reconstructed and permission text explicitly simulated. Full original research HTML remains a temporary local research file, not a committed article fixture.

Authored E2E-API-1550/1551, E2E-WEB-1550 desktop/mobile and E2E-OFFLINE-1550 cover durable editions/raw retention, no-permission fetch prevention, invalid layout retaining last good edition, actual API-backed navigation/retry and downloaded history/tampering. Not run. Apply094 with user `pnpm db:migrate`, keep PostgreSQL/MongoDB and connected API running, then user `pnpm sdlc "Add permitted RBI MPC calendar acquisition" --grep "E2E-(API|WEB|OFFLINE)-155[01]"`. Open printed dev URL `#research-calendar`; use Operations automatic research only after actual permission. Refresh offline snapshot/rebuild APK manually for newly captured data. Report case ID, artifact run folder and error context on failure. No dependencies added. No test/check/format/build/migration/source job/commit was run. Existing commit a2c53a0; shared work remains awaiting user gates.

Remaining broader implementation: exact historical original-release archive backfill and numerical families beyond retained GDP/CPI, other RBI schedules not covered by the MPC original, and source-specific runtime/worker/device validation. RBI source acquisition code is now authored; permission and activation remain distinct gates.

### India GDP current-day acquisition — 2026-09-15

Authored a default-disabled `india-gdp` source schedule (hourly default) through the existing research worker. The verified English PIB current-day index supplies exact MoSPI quarterly-GDP links; original candidates are bounded, raw-retained and parsed into independently reviewable India macro drafts or quarantined attempts. No auto-publication policy applies. Source permission evidence is separately configured. Original next-release calendar text keeps day precision and never becomes proof of actual release. Missed days and complete historical backfill are not covered by the current-day index; original archived uploads remain available. See SRC-007 for primary evidence, migration109 and cases1770/1771. Authoring is complete for this bounded source workflow; execution/activation and historical coverage validation remain manual.

## 2026-09-15 specification: bounded original PIB GDP archive pickup

Official archive navigation and monthly public ASP.NET callback were inspected from original HTML: `/archive2/erelease.aspx`, ministry55, day0, selected month/year. August2026 returned the original GDP release with legacy relid294102. That identifier is not a modern PRID. Retain archive callback and exact original release HTML, parse its own ministry/date/title markup, retain publication time in IST independently of current retrieval, and keep the existing strict quarterly GDP numerical grammar/base-year reconciliation. Operator chooses one calendar month (bounded date range), explicitly confirms rights, fetches archive candidates and retains each into existing immutable GDP drafts/quarantine; independent publication remains mandatory. Unsupported historical grammar stays quarantined, never a fake complete archive. Shared web/app Ops component must show empty/error/retry/results and open the existing review queue; offline Operations remains denied. No background provider calls during authoring. Existing JSON source store supports archive-proof metadata without schema migration.

Authored archive delivery: `india-gdp-archive-provider.ts` uses the verified public form-state/callback protocol at `https://archive.pib.gov.in/archive2/erelease.aspx`: `1|0|month|year|55` selects all days for the Statistics ministry. One month per request, eight quarterly-title candidates maximum, bounded2MB responses,20second individual fetch timeout, no redirects, no arbitrary URL input. Legacy `/archive2/erelcontent.aspx?relid=...` IDs are preserved rather than converted to unrelated modern PRIDs. Original ministry/date/title markup has its own explicit parser path; numerical validation reuses the existing quarterly release grammar. Publication IST→UTC conversion and source fiscal/base-year validation survive public/offline records. Month mismatch and unsupported numerical originals are retained as attempts, not silently skipped or published. An unsupported discovery response fails visibly rather than claiming an empty complete archive.

POST `/api/v1/ops/india-macro/gdp-archive` requires actual named permission and explicit source rights, retains the original archive response and release inputs in the existing raw store, and creates existing GDP draft/quarantine receipts. Repeat unchanged source/month/rights reuse capture IDs. A different source/month has a distinct request identity. Protected GET `archive-evidence/:hash` exposes retained discovery only to authorized Operations; public projections never expose permission evidence. The new month form announces loading/error/empty/outcomes and supports original discovery inspection; the existing paginated India queue and independent review publish/withdraw workflow are reused. No migration111 or additional dependency was needed. Scheduled current-day polling remains separate; this request does not silently enable a worker or claim every historical layout is supported.

Research evidence: [PIB archive instructions](https://www.pib.gov.in/Aboutarchive.aspx?lang=1&reg=1) identify month/ministry search. Original archive root redirects in its own script to archive2; the inspected monthly callback for August2026 yielded legacy relid294102, and its original release supplied31August2026 16:00IST. Full original source pages stayed temporary outside the repository. Synthetic test markup represents that verified grammar and is not a claimed historical capture.

Authored cases: API1790 actual form-protocol simulated upstream→real retention/idempotent retry/independent review/public timestamp; API1791 source-month mismatch→actual quarantine/evidence; WEB1790 actual month form→retained draft→existing review queue; OFFLINE1790 legacy original time/base separation and connected-Operations denial. No cases were run. User command: `pnpm sdlc "Add bounded PIB GDP archive pickup" --grep 'E2E-(API|WEB|OFFLINE)-179[012]'`. Existing configured databases/API/web and existing India GDP migrations are required; open printed dev URL→Operations→India macro→Recover a missed GDP release. Expected source timestamps remain historical; capture timestamps are current. Report failed ID/project, saved run path and individual assertion. No format/check/build/tests/provider ingestion/services/commit ran; HEAD remains `a2c53a0`, shared work awaits manual gates. Full retrospective discovery, unsupported original layouts and actual source-rights activation remain explicitly pending.

Archive retrieval reconstruction follow-up: responses now include a persisted `captureHash` linking the retrieval timestamp to the stable original-index hash. Authorized discovery inspection resolves and hash-verifies both records; unchanged original inputs remain idempotent. API1790 authors that exact receipt reconstruction assertion. No verification ran.

Archive failure handling: future calendar months are rejected as400 before contacting the source (API1792 authored). Discovery transport failures return safe503. Individual unavailable releases are reported explicitly while successful originals and the stable index/retrieval receipt are retained; an incomplete retrieval does not become an empty successful archive. Retry the same month to recover failed originals. API1790–1792 remain unexecuted.

## Initial scope closure audit — 2026-09-15

The initial request is scheduled evidence research, verified release calendars and historical publication vintages. It is not an unbounded promise to support every world release or archive layout. Reading the implemented source contracts, acquisition workers, API/public/offline projections and authored cases establishes the following initial coverage:

| Acceptance                                  | Implemented evidence                                                                                                                                                                                                                        | Authored acceptance                                             |
| ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------- |
| Scheduled evidence acquisition              | `research-auto.ts` stored source schedules, actual source refresh, deployment-wide lock, interruption recovery, safe retries and durable runs; `research-auto-policy.ts` independently approved publication controls                        | API1050/1056–1058/1060, browser/offline research-auto cases     |
| Verified release calendars and revisions    | BEA and BLS source-isolated ICS captures; exact timezone validation; FOMC day-precision original meetings; permission-gated RBI FY2026–27 MPC original dates                                                                                | 1051–1052,1070,1440–1441,1550–1551 API/browser/offline families |
| Original numerical publications and changes | Original BEA national GDP estimate pages/quarter comparisons; original India quarterly GDP current-day acquisition and selected-month archive retrieval with original publication time, retained attempts/quarantine and independent review | 1480–1481,1770–1771,1790–1792 API/browser/offline families      |
| Dated prior model and actual                | Cleveland Fed historical monthly CPI archive selected vintage with exact first BLS release comparison; model versus consensus and retrospective acquisition remain explicit                                                                 | 1850–1851 API,WEB1850,OFFLINE1850                               |
| Web/native/offline access and recovery      | Research calendar source/edition selection, GDP/CPI reader components, Operations schedules/review, immutable raw/projection receipts; only downloaded evidence offline, connected-only source mutations                                    | Shared UI plus each family browser/offline acceptance above     |

All initial implementation rows are authored. No new dependency/migration/code was added in this reconciliation. Broader RBI releases, additional numerical series, full retrospective archive discovery and all historical source layouts remain explicitly unclaimed expansion. RBI caching/display/offline permission, approved publication policies and worker flags are actual activation gates. The original historical source date never establishes prior app possession; current interactive values never replace original releases.

Manual document acceptance: follow each matrix implementation/case reference, verify no source activation/test pass is implied, and confirm old preserved Partial paragraphs are historical snapshots superseded by this current top. For new runtime validation, use the relevant documented source-specific command rather than rerunning every suite. Minimal existing worker/control sample: `pnpm sdlc "Validate research scheduling" -- --grep "E2E-API-(1050|1056|1057)"`. Existing configured PostgreSQL/MongoDB/API required; Operations automatic research on the printed dev URL shows schedules/run outcomes and explicit publication gates. Report case/project, saved run path and response/assertion evidence. No tests, gates, migrations, services, worker activation or commit were run; local HEAD `a2c53a0` and unrelated concurrent authoring remain preserved.

## Saved full-inventory repair — 2026-09-19

The saved full run1789752953639-97020 includes failed cases tagged to this task. Confirmed causes, scoped authored repairs and remaining verification are recorded in [the full-audit RCA](../development/full-audit-2026-09-19.md). User runs `SDLC_AUTO_REPAIR=0 pnpm sdlc "Repair full audit failures" --story SDLC-REPAIR-016`. No new passing evidence or automatic bug resolution is claimed; this bounded repair does not remove broader source/device/functional requirements recorded above.

<!-- sdlc-validation:start -->

## Automated validation

Failed — unresolved bug. [Evidence](../validation/README.md); [bugs](../bugs/README.md). Latest reconciliation: 1789837057413-22117.
<!-- sdlc-validation:end -->
