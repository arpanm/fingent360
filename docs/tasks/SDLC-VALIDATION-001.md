# SDLC-VALIDATION-001 — Story acceptance and unresolved bugs

- **Status:** Implementation complete; validation pending
- **Request:** The user-run SDLC must update validation against functional stories and retain unresolved failures in a separate bug tracker, including unsuccessful automatic repairs.
- **Specification:** Collect only run-specific reports produced by commands in this invocation, including exact retries and offline tests. Map inherited story tags to existing task IDs. Distinguish passed, failed, skipped, interrupted and flaky outcomes. Maintain stable bug identities per case/project or command stage; close only on a later strict passing result for that identity. Record safe bounded failure details and local evidence links. Never copy credentials, attachments or full API payloads into tracked documentation.
- **Acceptance boundary:** Every story receives observed-result status. Full automated acceptance additionally requires an explicit reviewed case/project matrix and all required cases passing against the same source fingerprint. Checks-only, partial selections, stale passes and unknown coverage cannot complete a story. Functional scope and live/native activation remain separate from automated acceptance.
- **Implementation plan:** Pure validation reducer plus filesystem persistence, command observer in SDLC, unique offline report IDs, acceptance matrix, generated story summaries and separate docs/bugs index. Persist even on failed repair/command exit. Generated results after the gated commit remain uncommitted.
- **Layers:** Developer workflow only; no product database/API migration. Existing API/browser/offline cases remain real. Author reducer and integration unit regressions; do not execute validation as an agent.
- **Reusable prompt:** Implement this specification without expanding test reruns or weakening gates. Preserve partial story scope and existing tracker edits. Add real functional fixes in their own story records. User runs pnpm sdlc; agents only author.
- **Verification:** Pending; no execution authorized. Record unresolved setup failures as bugs, never successful functional acceptance.

## Authored delivery

The user-operated command collects run-specific connected/offline reports after every test attempt and records command failures before repair. Its final step persists docs/validation/results.json, a story index, task validation blocks, TODO's separate validation column and deduplicated docs/bugs records. It runs on success and failure; exact passing receipts resolve matching bugs. Interrupted/failed/flaky/skipped cases cannot certify acceptance. Per-stage ignored checkpoints preserve evidence if the process is forcibly terminated, but SIGKILL cannot run final reconciliation.

The --story option runs a reviewed acceptance matrix independently of Git diff selection. Four initial matrices cover ACCOUNT-001, GOALS-001, XLSX-001 and WORKER-HEALTH-001; missing matrices fail before gates and cannot imply completion. Other known task tags receive observed-result status. Matrix membership also maps shared tests without requiring duplicate tagging. Full automated acceptance requires current-source/configuration fingerprints and a matching successful check gate. Do not promote broader functional, source or device scope merely from this result.

Unit regressions cover incomplete/skip/stale coverage, exact bug resolution and recurrence, inherited tags, flaky results, persistence, scope-preserving task updates, explicit story filters and exact offline repair. App WEB034 and OFFLINE034 independently cover the first functional story's fixes. No new dependencies or database migration.

Manual acceptance: on the fresh installation, run privacy:keys and restart dev, then `pnpm sdlc "Complete account workflow" --story ACCOUNT-001`. Expect API030/031, WEB030/033/034 on desktop/mobile and OFFLINE034, with the full check gate before commit. Review generated validation and bug files. A failure must retain an Open bug; successful exact repair resolves that bug; missing required coverage remains incomplete. Authoring is complete; none of these commands were executed by the authoring agent.

ACCOUNT-001 opts into automatic Done through reviewed completion metadata. It has no remaining external gates within its bounded shared-app scope after fresh setup and successful acceptance. Full passing current coverage closes it; failures reopen it, stale coverage returns it to validation pending. Other stories retain their implementation statuses until their closure metadata is reviewed.
