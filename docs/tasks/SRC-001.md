# SRC-001 — Instrument/security master source onboarding (P0)

- **Status:** Partial
- **Implemented / recorded:** OpenFIGI identity directory plus actual NSE EQUITY_L.csv acquisition, immutable retention/review and v2 full identity fields with dated reconciliation in web/offline. BSE legacy listing now renders verified table columns; its actual CSV download remains inaccessible in research.
- **Pending:** Actual original BSE listing/master export and complete documented layout; dataset rights activation and saved manual acceptance.
- **Next action / inputs:** Developer: use the official Old Website → Corporates → Listed Securities route recorded below; obtain the actual CSV bytes before authoring its parser. If the normal download remains blocked, an original public export or complete official member-file format is the concrete unavailable input. Permission holder: supply actual permitted storage/display/offline scope before activation.
- **Verification:** The preserved evidence below applies only to its recorded scope/revision. This tracker migration did not run validation.

See [current delivery summary](current-delivery.md) for the batch-wide distinction between code, missing functionality and validation.

## Implementation handoff rule

After each change, update the summary above and the matching [TODO row](../../TODO.md). Keep prompts, detailed scope, remaining work, verification evidence and handoff commands in this file. Follow [task maintenance](README.md); a parent stays partial while a child requirement is missing.

## Preserved specification, prompts and history

The entries below are migrated records, not new execution instructions or current test-pass claims. The current summary above takes precedence where older statuses differ.

### SRC-001 — Instrument/security master source onboarding (P0)

- **TEAM-002 delivery:** IDENTITY-001 ingests five real OpenFIGI India/INR common-stock ISIN mappings with permitted open metadata, PostgreSQL editions, MongoDB evidence, public directory/history and a dated offline bundle. See docs/product/security-identities.md for dated source evidence. Complete exchange master/listing status remains open.

- **Implementation:** Partial
- **Verification:** Selected child evidence exists; full parent acceptance is incomplete
- **Dependencies:** DEV-003, DEV-004, DEV-005; Yes
- **Context:** README.md sections 10.6–10.10, source order 1, P0
- **Scope and acceptance:** Onboard Instrument/security master. Starting public candidates: NSE + BSE + ISIN crosswalk. Paid upgrade candidates: NSE/BSE licensed data; Capitaline/ACE. Next action from the accepted plan: Obtain files, document terms, define canonical ISIN/symbol schema. Document actual access/usage rights, effective/retrieval times, units, revisions, fixtures, quarantine and reconciliation. See the implemented bounded child and current source evidence; never mark production without user-provided approval and validation evidence.
- **E2E cases:** Planned scenarios in tests/e2e/plans/product-coverage.md#src-001; add stable executable IDs when implemented
- **Evidence / gaps:** Five actual OpenFIGI identities, evidence/revisions, refresh and directory/offline views. Remaining: Accepted NSE/BSE symbol/ISIN crosswalk, listing/reference reconciliation and source-quality cases; restricted exchange data needs actual agreement. Evidence: docs/product/security-identities.md.
- **Manual next actions:** After implementation, user runs relevant checks and selected E2E cases listed in the handoff; attach evidence before marking verified.

**Codex prompt**

> Read AGENTS.md, README.md, TODO.md task SRC-001, README.md sections 10.6–10.10, source order 1, P0, and the linked E2E coverage plan. Follow the standard task prompt contract in TODO.md. Work on Instrument/security master source onboarding (P0). Scope and acceptance: Onboard Instrument/security master. Starting public candidates: NSE + BSE + ISIN crosswalk. Paid upgrade candidates: NSE/BSE licensed data; Capitaline/ACE. Next action from the accepted plan: Obtain files, document terms, define canonical ISIN/symbol schema. Document actual access/usage rights, effective/retrieval times, units, revisions, fixtures, quarantine and reconciliation. See the implemented bounded child and current source evidence; never mark production without user-provided approval and validation evidence. Dependencies: DEV-003, DEV-004, DEV-005; Yes. Do not invent missing domain/provider fields or claim future functionality. If a product decision is unresolved, record it as a blocker and progress independent authored work. Add/update API and/or browser acceptance cases (or manual document-review scenarios if there is no executable behavior), including prerequisites and meaningful failures. Update this task, test catalogue and relevant README details. Do not run installation, formatting, lint, builds, tests, browser checks, service changes or migrations. Leave the local commit to the user-run pnpm sdlc format/check gate; never push. Give exact manual next actions.

<a id="src-002"></a>

## Reusable task prompt

Read AGENTS.md, the task-maintenance guide and this task’s current summary. Work only on SRC-001 unless the user expands the scope. Treat the preserved specification/history as context; current user instructions take precedence. Implement the listed remaining acceptance end to end, or reconcile recorded completion evidence if no implementation remains. Keep tests, documentation and the root index consistent. Record exact remaining work and who needs to act. Do not execute deterministic validation or commit without the user-authorized gates.

## Input and pickup decision — 2026-09-15

- **Readiness:** Research-ready
- **User input needed now:** No for the independent next step.
- **Decision:** No user input needed for the next step: research primary documentation, record evidence and implement only verified source/domain behavior. Research-ready is not a claim that all inputs or permissions are already available.
- **Recorded answer / authority:** User: use free sources first and have the agent determine regulations, source usage and formats; do not ask the user to discover them.
- **Question status:** None now. Do not ask for a repeat of existing answers.
- **Later input trigger:** Before live redistribution/offline bundling, establish dataset-specific rights. Ask only for an actual agreement or paid decision that research cannot supply.
- **Next action:** Developer: complete remaining source families and document actual coverage.
- **Research/evidence:** See [dated source checks and existing answers](input-research-2026-09-15.md). Source-specific permissions, complete parser layouts and legal classification are not claimed resolved by triage.
- **Completion boundary:** This updates readiness only, not test passes, live activation or full feature completion. On later pickup, refresh saved evidence and update this record plus the TODO row.

## 2026-09-15 implementation specification: retained exchange identity reconciliation

Existing actual NSE `EQUITY_L.csv` fixed-source fetch/parser, Mongo raw retention, immutable PostgreSQL editions/reviews, company history and offline projections already implement source acquisition beyond normalized imports; the migrated summary omits this. Complete its concrete gaps: validate every row width and paid-up/lot fields, retain those fields, preserve distinct exchange/series/symbol records, reject ambiguous same-date symbol-to-ISIN mappings within a source edition, and reconstruct dated symbol/name/face-value conflicts for each ISIN in API/reader/offline. A changed symbol is a historical record, not an automatically inferred corporate action. Absence from one captured file is not proof of delisting. Existing typed APIs and immutable JSON observations require no schema migration.

BSE research remains independent: current official List Of Securities page opens with Equity T+1/status filters and download button, but both all-status and Active requests remained at the source loader and the normal download returned no file. The older full standard-master attachment was unavailable (web403; browser refused attachment). First-ten-field search excerpts do not establish a complete58+column parser. Do not fabricate the missing layout or infer status from missing rows. No licence activation or source jobs during authoring.

## Authored delivery: NSE master v2 and reconciliation

`nse-equity-master-v2` preserves all eight official columns: symbol, company name, series, listing date, paid-up value, market lot, ISIN and face value. Actual fixed source acquisition remains `/ops/equities/fetch`; upload is `/ops/equities/import`, bound to the exact EQUITY_L.csv URL. Old v1 editions remain parseable without silently adding fields to their original receipt. Row widths, positive integer market lot, decimal paid-up/face value, no future listing date relative to capture effective date and same-date symbol/series ambiguity are validated. Source identity uniqueness now distinguishes exchange, series and symbol while keeping ISIN as canonical identity. Independent named publication of v2 reconstructs the retained raw observations before admission. Original raw bytes, parsed immutable observations and review rows reuse existing stores; migration096 is not needed.

API `/equities/:isin` and snapshots return deterministic `identityReconciliation` reconstructed from included records. Distinct names/symbols remain historical, same-date disagreements are explicit, numerically equivalent decimal face values do not create false conflicts, and no record silently becomes a delisting/corporate-action inference. Public contracts reject records for a different ISIN and altered reconciliation. The reader shows retained symbol identities and keyboard-expandable limitations; original identity rows also show paid-up value/market lot. The same React reader and schema validation apply to downloaded Android/offline company snapshots. Existing history/source/withdrawal/empty/retry routes are reused.

Primary research15September2026: [NSE securities available for trading](https://www.nseindia.com/static/market-data/securities-available-for-trading) publishes the exact Equity CSV and separate name-change/symbol-change files; these do not establish permission to redistribute exchange data. [BSE current listing page](https://www.bseindia.com/corporates/list_scrips) was opened through its historical link redirect and visibly offers segment/status/name/group filters plus download. Equity T+1 with all statuses and Active each remained loading; the normal download produced no accessible report. [BSE standardized security master documentation](https://www.bseindia.com/markets/MarketInfo/DownloadAttach.aspx?attachedId=e99e2ca5-95d7-4173-83da-7774412bbb2d&id=20220531-44) is search-indexed but direct retrieval returned403 and browser refused the attachment. Its excerpt describes only an initial subset of fields, so no BSE parser is claimed. Actual BSE source format/acquisition is still pending. NSE change-notice files also remain a distinct unimplemented source family; retained snapshot differences do not substitute for original notices.

Authored API1580 covers actual import/independent review/dated renamed symbol/same-date conflict/withdrawal; API1581 rejects malformed widths, zero lot, impossible listing timing and ambiguous symbol mappings, and preserves v1 format output. WEB1580 covers desktop/mobile reader and keyboard disclosure. OFFLINE1580 covers downloaded typed identity records and rejects forged consensus/cross-ISIN data. Fixtures are synthetic source-layout reconstructions, not real verified market observations. No validation ran.

Manual actions: no dependencies or new migration. Start the existing configured databases/API/web if needed (`pnpm db:up`, `pnpm dev`), use its printed web URL at `#equities` and Operations Indian equity data. Run `pnpm sdlc "Complete NSE master identity reconciliation" -- --grep "E2E-(API|WEB|OFFLINE)-158[01]"`; the user command owns format/check/gated commit and selected E2E. Physical Android validation requires rebuilding the bundled snapshot/APK and opening identity history in airplane mode. Report the saved run ID and individual failure context if a case fails. No tests/builds/migration/provider jobs/format/check/commit were run; current HEAD remains a2c53a0 and unrelated team changes remain uncommitted.

Additional read-only BSE check (2026-09-15): the legacy official listed-scrip page returned403; a candidate public list API URL could not be opened by the research tool. Neither response establishes an export layout or a supported API contract. The earlier current-page loading state therefore remains unresolved; no guessed BSE parser or private API bypass was added. NSE identity reconciliation is authored separately.

## 2026-09-15 bounded BSE original-export research follow-up

The newer [List Of Securities](https://www.bseindia.com/corporates/list_scrips) still stayed at its loader after an ordinary Equity T+1 submission; the browser reported Angular NG0901. Its observed list request was `https://api.bseindia.com/BseIndiaAPI/api/ListofScripData_new/w?Group=&Scripcode=&segment=Equity&status=&scripName=`. This is an observed request URL, not a verified public API contract or response schema.

Following the site's **Old Website** link, then **Corporates → Compliances and Other Info → Listed Securities**, reached [the official legacy listing](https://beta.bseindia.com/corporates/List_Scrips.html). Unlike the earlier legacy attempt, an ordinary Equity T+1 / all-status Submit rendered rows and page 1 of 435. The visible table has nine columns: Security Code, Issuer Name, Security Id, Security Name, Status, Group, Face Value, ISIN No, Market Capitalisation (Rs. Cr.). Its rows include Active and Delisted securities and literal `NA` ISIN values. Those missing identities cannot become canonical ISINs; absence from a page is not delisting evidence. Market capitalisation has no independently established observation timestamp here and must not become a current valuation. The footer directs requests for the full list with four-level industry classification to BSE; no email was sent and no dataset rights were inferred.

The normal download icon (`lnkDownload`) navigated to the exact source-generated URL `https://api.bseindia.com/BseIndiaAPI/api/LitsOfScripCSVDownload/w?Scripcode=&Group=&status=&segment=Equity`. Chrome returned **ERR_BLOCKED_BY_CLIENT**. Opening that observed URL with the web research tool returned a non-retryable safe-open denial. No original CSV body, header quoting, encoding, complete record count or null representation was obtained. No access-control bypass was attempted. The visible HTML table is useful schema evidence but is not proof of the downloaded CSV layout, so no invented BSE parser or synthetic imitation of an unseen original was added. The old standardized-master attachment remains unavailable and its first-ten-field search excerpt remains insufficient for its complete layout.

**Next pickup acceptance:** obtain the source-generated original through normal permitted access; retain its exact bytes/hash and download context; inspect all headers and explicit segment/status/unknown-ISIN semantics before contracts or parser work. Use synthetic reconstructions only after that inspection. Add API1740–1749 as appropriate for actual acquisition, independent review, invalid/unresolved identities, same-date conflicting identities, withdrawal and offline reconstruction. These IDs are reserved, not authored or passed. Do not promote an issuer annual report or an individual listing notice to complete exchange-master coverage; those need a separately bounded source contract.

One subsequent ordinary HTTP GET to the exact observed download URL, without browser cookies, spoofed headers or authentication, returned **HTTP403**, `text/html`, 425 bytes, rather than CSV. The temporary response was inspected outside the repository and was not committed. No retry/bypass followed. This establishes an actual source denial for the plain download in this environment, separately from the earlier browser/tool restrictions.

This follow-up changes research/readiness documentation only. API, UI, database and test code are unchanged because the original-export input remains unavailable. Manual document acceptance: follow the recorded official navigation, confirm the listing and download outcome, and confirm the tracker still calls BSE partial. No dependencies, migration or running services are needed for this document check. Use `pnpm sdlc "Record BSE original export research" --checks-only` for the manual gated commit when desired. No format/check/tests/builds/migrations/services/commit ran. Git inspection still showed extensive shared team changes awaiting manual gates; HEAD was `a2c53a0`.

<!-- sdlc-validation:start -->

## Automated validation

Blocked — workflow failure. [Evidence](../validation/README.md); [bugs](../bugs/README.md). Latest reconciliation: 1789846657524-36112.
<!-- sdlc-validation:end -->
