# SRC-015 — MF scheme master/NAV source onboarding (P1)

- **Status:** Completed initial implementation; validation and source activation pending
- **Implemented / recorded:** Current and legacy AMFI NAV parsing,76 verified AMC choices/three types/All historical acquisition, versioned catalog/source retention, exact date/identity reconciliation, independent review, shared reader and offline history.
- **Pending:** Actual AMFI source permission/activation and saved user-run validation. Future source format/catalog changes require a versioned adapter update; holdings/bond source families remain SRC-016–018.
- **Next action / inputs:** User applies093 and validates1084/1460/1540–1543 through manual SDLC. Operator activates the source only with the applicable permission.
- **Verification:** The preserved evidence below applies only to its recorded scope/revision. This tracker migration did not run validation.

Current implementation detail: [FUNDS-BONDS-001](FUNDS-BONDS-001.md).

See [current delivery summary](current-delivery.md) for the batch-wide distinction between code, missing functionality and validation.

## Implementation handoff rule

After each change, update the summary above and the matching [TODO row](../../TODO.md). Keep prompts, detailed scope, remaining work, verification evidence and handoff commands in this file. Follow [task maintenance](README.md); a parent stays partial while a child requirement is missing.

## Preserved specification, prompts and history

The entries below are migrated records, not new execution instructions or current test-pass claims. The current summary above takes precedence where older statuses differ.

### SRC-015 — MF scheme master/NAV source onboarding (P1)

- **Implementation:** Deferred
- **Verification:** Not run
- **Dependencies:** DEV-003, DEV-004, DEV-005; P1 gate
- **Context:** README.md sections 10.6–10.10, source order 15, P1
- **Scope and acceptance:** Onboard MF scheme master/NAV. Starting public candidates: AMFI. Paid upgrade candidates: Morningstar/CRISIL/Lipper. Next action from the accepted plan: Validate AMFI use terms; map scheme variants and history. Document actual access/usage rights, effective/retrieval times, units, revisions, fixtures, quarantine and reconciliation. Source register currently says Not started; never mark production without user-provided approval and validation evidence.
- **E2E cases:** Planned scenarios in tests/e2e/plans/product-coverage.md#src-015; add stable executable IDs when implemented
- **Evidence / blockers:** No implementation or verification evidence yet; dependencies and required product/source approvals remain open.
- **Manual next actions:** After implementation, user runs relevant checks and selected E2E cases listed in the handoff; attach evidence before marking verified.

**Codex prompt**

> Read AGENTS.md, README.md, TODO.md task SRC-015, README.md sections 10.6–10.10, source order 15, P1, and the linked E2E coverage plan. Follow the standard task prompt contract in TODO.md. Work on MF scheme master/NAV source onboarding (P1). Scope and acceptance: Onboard MF scheme master/NAV. Starting public candidates: AMFI. Paid upgrade candidates: Morningstar/CRISIL/Lipper. Next action from the accepted plan: Validate AMFI use terms; map scheme variants and history. Document actual access/usage rights, effective/retrieval times, units, revisions, fixtures, quarantine and reconciliation. Source register currently says Not started; never mark production without user-provided approval and validation evidence. Dependencies: DEV-003, DEV-004, DEV-005; P1 gate. Do not invent missing domain/provider fields or claim future functionality. If a product decision is unresolved, record it as a blocker and progress independent authored work. Add/update API and/or browser acceptance cases (or manual document-review scenarios if there is no executable behavior), including prerequisites and meaningful failures. Update this task, test catalogue and relevant README details. Do not run installation, formatting, lint, builds, tests, browser checks, service changes or migrations. Leave the local commit to the user-run pnpm sdlc format/check gate; never push. Give exact manual next actions.

<a id="src-016"></a>

## Reusable task prompt

Read AGENTS.md, the task-maintenance guide and this task’s current summary. Work only on SRC-015 unless the user expands the scope. Treat the preserved specification/history as context; current user instructions take precedence. Implement the listed remaining acceptance end to end, or reconcile recorded completion evidence if no implementation remains. Keep tests, documentation and the root index consistent. Record exact remaining work and who needs to act. Do not execute deterministic validation or commit without the user-authorized gates.

## Input and pickup decision — 2026-09-15

- **Readiness:** Research-ready
- **User input needed now:** No for the independent next step.
- **Decision:** No user input needed for the next step: research primary documentation, record evidence and implement only verified source/domain behavior. Research-ready is not a claim that all inputs or permissions are already available.
- **Recorded answer / authority:** User: use free sources first and have the agent determine regulations, source usage and formats; do not ask the user to discover them.
- **Question status:** None now. Do not ask for a repeat of existing answers.
- **Later input trigger:** Before live redistribution/offline bundling, establish dataset-specific rights. Ask only for an actual agreement or paid decision that research cannot supply.
- **Next action:** Developer: complete AMFI live acceptance and scheme reconciliation; operator confirms permission.
- **Research/evidence:** See [dated source checks and existing answers](input-research-2026-09-15.md). Source-specific permissions, complete parser layouts and legal classification are not claimed resolved by triage.
- **Completion boundary:** This updates readiness only, not test passes, live activation or full feature completion. On later pickup, refresh saved evidence and update this record plus the TODO row.

## Retained history reconciliation specification — 2026-09-15

For each exact AMFI scheme code, compare admitted retained editions without merging direct/regular or payout/reinvestment identities. Same-date differing numerical NAVs, conflicting non-null ISIN/plan/option/AMC identities and changed names require review. Decimal-equivalent NAV strings are the same amount. Missing identities, NAV gaps and a truncated result remain explicit limitations. This is retained-record consistency, never proof of complete historical coverage or a return recommendation. API and shared web/app must show the same diagnostics, original editions and dates; the existing downloaded single-observation snapshot must say insufficient history. Reuse immutable NAV editions/reviews; no new database migration. Author API/WEB/OFFLINE1460 and arithmetic/schema goldens without running them. Historical backfill format remains unverified; the official AMFI download page currently limits each history request to90days and announces old-format retirement on30September2026. Its original history bytes were inaccessible in this research, so no historical parser is inferred.

## Retained history reconciliation implemented

Shared contracts now compute exact same-day NAV conflicts, identity/name/plan/option changes, distinct dates and missing/truncated coverage limitations. The real database-backed detail API derives diagnostics only from admitted stored rows and rejects cross-scheme records or forged summaries. The web/Android reader exposes warnings beside original editions; downloaded single-point data explicitly says insufficient history. No record is rewritten and no NAV is selected as the correct revision merely because it arrived later. Existing database editions and review/withdrawal lifecycle are reused; no new migration or dependency. API/WEB/OFFLINE1460 and contract exact-decimal/forged-summary goldens authored, not run.

Primary history access check: [AMFI download page](https://www.amfiindia.com/net-asset-value/nav-download), retrieved2026-09-15, documents90-day history windows and old-format retirement30September2026. The original history endpoint was unavailable to the research reader; third-party guessed columns were not used. Parent remains Partial for a verified original-history parser/backfill.

Manual acceptance: with PostgreSQL/MongoDB and existing API/web running, `pnpm sdlc "Reconcile retained fund NAV history" -- --grep "E2E-(API|WEB)-1460"`; offline `pnpm android:web` then `pnpm android:test --grep E2E-OFFLINE-1460`. Expect two conflicting retained captures to warn, actual withdrawal to remove that conflict, and single-point offline history to remain insufficient. Use printed web URL `/#funds-bonds`. Supply saved case/project/error details if a gate fails. No checks, tests, migration, build or commit run; gated commit remains user-owned.

## 2026-09-15 history implementation specification

Scope: current official AMFI `_Po` historical report, initially verified SBI Mutual Fund (mf=22), Open Ended (tp=1), maximum 90 inclusive calendar days per request. Preserve the original report URL, exact requested interval, retrieved bytes/hash and each scheme/date observation; do not claim every day is present. Exact history header differs from NAVAll. Reuse draft, independent named review, withdrawal, current list and history readers. Expand observation key to edition/scheme/date without deleting old data. Offline snapshots carry bounded admitted history and report truncation. Upload and ordinary fixed-host download require actual written permission covering retention, web and offline redistribution. No source activation during authoring.

Acceptance: repeated scheme across dates accepted; repeated same scheme/date, malformed widths, out-of-range dates, unknown URL/AMC/type and changed header rejected. Existing current/legacy NAV parsing unchanged. New historical editions cannot publish under bootstrap or by their preparer. Retained revisions remain distinguishable and existing latest NAV selection stays date-first. API, browser and offline cases authored, execution manual.

## 2026-09-15 authored historical NAV backfill

Implemented verified current history parser `amfi-history-v1` for SBI/Open Ended, alongside unchanged current/legacy NAVAll parsers. A report URL generated by AMFI's own form is supplied to `/ops/funds/import` or `/ops/funds/fetch` as optional `sourceUrl`. Fixed host/path, exact four query fields, verified AMC/type and 1–90 inclusive dates are enforced. Historical rows must match that interval, AMC and category. Same scheme on distinct dates is accepted; duplicate same-date rows are rejected. Arbitrary URLs, unknown columns and guessed legacy history layouts are rejected. The API retains source bytes/hash before parsing and edition metadata after successful validation. Migration093 expands the immutable observation key without deleting existing records. No new dependency.

Publication requires another named operator and rechecks retained raw hash, parser and exact stored observation reconstruction. Current list stays date-first; backfilled dates and multiple editions remain visible in history, with existing conflict/identity diagnostics. Operations exposes history URL, upload/fetch, retained original, review and withdrawal. Existing responsive fund reader renders all dated rows, keyboard-expandable provenance, back navigation and error/retry states. Shared web code runs in Android; rebuilding the APK/snapshot is required to receive these changes. Offline snapshots include up to5000 history records across included schemes, at most500 per selected detail, explicit truncation and legacy one-record fallback; disconnected devices cannot acquire source data.

### Primary evidence (15 September2026)

[AMFI NAV Download](https://www.amfiindia.com/net-asset-value/nav-download) was inspected through its actual form. Current form selections SBI Mutual Fund/Open Ended produced `mf=22` / `tp=1`. The form states a maximum90day interval and legacy download retirement on30September2026. Its actual [09–11 September history report](https://portal.amfiindia.com/DownloadNAVHistoryReport_Po.aspx?mf=22&frmdt=09-Sep-2026&todt=11-Sep-2026&tp=1) returned HTTP200 text/plain,178999bytes; same scheme appears once on each of the three dates. Exact header: `Scheme Code;NAV Name;Plan;Option;ISIN Div Payout/ISIN Growth;ISIN Div Reinvestment;Net Asset Value;Date`. Source sections carry category then AMC. Actual format differs from NAVAll; parser maps fields by this specific version, without treating the report as a generic normalized import. Files downloaded only to temporary research storage; no original financial rows copied into fixtures. Fixtures explicitly reconstruct this layout using synthetic scheme/value data.

### Remaining and verification

Authoring complete for verified SBI Open Ended history; other AMC/type history IDs and legacy historical formats remain unsupported until verified. This is not universal AMFI backfill or proof of contiguous coverage. Actual written permission covering retention, connected display and offline redistribution remains an activation input; the checkbox records the operator attestation and does not establish a licence. No providers activated, migration applied, APK rebuilt, tests/checks run or commit made. Parent source task remains partial until broader agreed coverage and activation/acceptance are satisfied.

Authored API1540: actual retention/idempotency, independent review, current/history/snapshot reads, original bytes, corrected-date conflict and withdrawal. API1541: changed header, widths, duplicates, date/AMC/type/URL bounds. WEB1540: desktop/mobile history navigation and keyboard provenance. OFFLINE1540: multiple dated records, truncation and legacy fallback. User manual acceptance additionally checks history form loading/error/retry, literal source URL shown with rights scope, text file upload and ordinary download with permitted real data, browser narrow/mobile viewport and Android downloaded snapshot history.

Manual next actions: existing PostgreSQL/MongoDB services and API/web are required; user applies `pnpm db:migrate`, then restarts `pnpm dev` and uses its printed URL (`#funds-bonds`, Operations fund sources). Run `pnpm sdlc "Add verified AMFI history capture" -- --grep "E2E-(API|WEB|OFFLINE)-154[01]"` for scoped format/check/gated commit plus authored cases; use `--checks-only` if postponing E2E. No dependency installation needed. Keep test watch/eye off. On failure, provide artifacts/e2e/latest.md run ID and the single failed case's error/context.

## 2026-09-15 catalog completion (supersedes SBI-only authoring limitation above)

The current historical adapter now supports all76 AMC options actually published by AMFI, every type (Open Ended1, Close Ended2, Interval Fund3), and All. The official form's All-AMC option produces a **single-date** URL with `frmdt` and optional`tp`; named AMC URLs include`mf`, `frmdt`, `todt` and optional`tp`. All types omits`tp`, rather than inventing code0. Actual browser form generation verified each shape; HDFC all-type request confirms `mf=9`. Catalog identifiers/names come from the official page response's `mfId`/`mfName` records, cross-checked against its visible76-option list. Source payload SHA256, URL and observation date are versioned in `amfi-history-catalog.ts`; each new historical edition retains that catalog provenance, which travels through public and offline reads. Names listed by AMFI may be historical/inactive, so listing is not an active-fund assertion.

Specification extension: accept only catalog IDs; permit no duplicated/extra query fields; preserve exact single-date versus range semantics; check each returned AMC against selected/catalog names with whitespace normalization only and each section against selected/catalog type. No fuzzy identity matching. Same eight-column current history grammar applies to the actual all-AMC download (HTTP200 text/plain, with Open Ended/Close Ended/Interval sections). Existing immutable retention/review/withdrawal/reconciliation and bounded offline history are reused. Unknown/new AMC IDs fail closed until documented catalog revision; absence of rows remains explicit and never becomes a fabricated observation. The source's legacy history format remains deliberately unsupported because current form covers the required functionality.

UI now includes `AmfiHistorySelector`: all published AMC/type choices, one-date or range controls, local interval validation, generated URL preview via existing field, keyboard/native selects and catalog provenance. User still explicitly fetches/uploads with rights attestation; selecting a catalog item cannot execute capture or grant permission. Existing API validates generated/manual URLs identically. Backend JSON edition adds catalog version/date/source hash; no additional migration beyond093 required. Shared React selector/reader and history bundle apply to Android; native source ingestion remains unavailable offline.

Authored additional API1542 enumerates every published AMC/type against synthetic reports and rejects unsupported IDs/range leakage; API1543 uses actual API import/independent publication/snapshot with a non-SBI close-ended fixture and retained catalog proof. WEB1542 covers named operator source screen, HDFC/type/date selection, oversize range error and All-AMC single-date transition. Existing OFFLINE1540 covers historical snapshot route/records; its catalog proof now also travels with the edition. No tests run.

Remaining: actual AMFI written permission, provider activation and saved user-run evidence; future new/renamed IDs need a versioned catalog update. There is no remaining initial historical AMC/type coding restriction. The older SBI-only remaining statement above is superseded. The exact user command now is `pnpm sdlc "Complete AMFI history catalog" -- --grep "E2E-(API|WEB|OFFLINE)-154[0-3]"`; migration093 and running database/API/web prerequisites are unchanged. No dependencies added, execution or commit performed.

## Saved full-inventory repair — 2026-09-19

The saved full run1789752953639-97020 includes failed cases tagged to this task. Confirmed causes, scoped authored repairs and remaining verification are recorded in [the full-audit RCA](../development/full-audit-2026-09-19.md). User runs `SDLC_AUTO_REPAIR=0 pnpm sdlc "Repair full audit failures" --story SDLC-REPAIR-016`. No new passing evidence or automatic bug resolution is claimed; this bounded repair does not remove broader source/device/functional requirements recorded above.

## Current acceptance review — 20 September 2026

Reviewed scope: Current and legacy AMFI NAV, exact plan/option/scheme identity, retained observations and conflict/history diagnostics; verified current historical grammar with all76 published AMC choices, three types/All and exact single-date/range URL rules; independent review/raw retention, catalog provenance, shared reader and bounded offline history. No contiguous-coverage, active-fund or full holdings/bond-source claim.

The required API, browser-project and offline case IDs are now explicit in [acceptance.json](acceptance.json). Only actual current receipts plus successful normal gates can close this scope. Existing API/contracts/database/source workflows are reused; a matrix correction itself adds no migration or source permission. Physical-device and deployment claims require their separate evidence. The user authorized this validation/repair run; older manual-only handoff wording is historical for this run.

Remaining gates:

- Actual AMFI permission covering original retention, connected display and offline redistribution, followed by permitted activation.
- Record physical Android downloaded-package acceptance required by the shared app delivery; desktop/mobile Playwright is not native certification.

<!-- sdlc-validation:start -->

## Automated validation

Passed — automated acceptance. [Evidence](../validation/README.md); [bugs](../bugs/README.md). Latest reconciliation: final-scoped-1789857073654.
<!-- sdlc-validation:end -->

## Remaining completion gates — 20 September 2026

The complete current automated matrix passed. The reviewed manifest retains these separate requirements; rerunning passing cases does not satisfy them:

- Actual AMFI permission covering original retention, connected display and offline redistribution, followed by permitted activation.
- Record physical Android downloaded-package acceptance required by the shared app delivery; desktop/mobile Playwright is not native certification.
