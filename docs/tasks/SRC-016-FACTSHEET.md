# SRC-016-FACTSHEET — Reviewed AMC factsheet fees and fund size

- **Status:** Implementation complete; validation pending
- **Parent:** [SRC-016](SRC-016.md).
- **Scope:** Initial original Kotak Multi Asset Omni FOF HTML factsheet adapter. Retain and parse source bytes, effective date, fund-level AUM/AAUM and direct/regular scheme-plus-underlying base expense ratios. Preserve the explicit exclusion of brokerage/transaction costs; never label BER as total expense ratio. Do not project third-party performance/risk figures without their methodology and rights.
- **Evidence:** [Official August2026 factsheet](https://www.kotakmf.com/factsheet/August_2026/kotak/ASSET-ALLOCATOR.html), inspected15September2026. Original table distinguishes Direct and Regular, Scheme/Underlying Funds/Total; a separate footnote identifies Base Expense Ratio. Fund size is INR crore; AUM and average AUM are separate measures. Folio count has a different date and is excluded from this initial projection.
- **Specification:** Strict bounded HTML parser, exact source host/path/month/title, explicit footnote and exact integer decimal reconciliation. Retain immutable original and parsed edition. Independent named reviewer binds actual admitted AMFI plan identities before publication. Reader shows source/effective/retrieval time, plan-specific fees, publication/withdrawal and unknown total costs. Shared web/app view and downloaded snapshot use exact source/identity bindings; offline Operations unavailable. No automatic holdings changes or advice.
- **Acceptance:** API/browser/offline1910–1919 cover original upload or bounded original capture, source reparse, wrong identity/plan, decimal total mismatch, independent review, withdrawal, reader and stale/offline evidence. No tests run. Migration120 reserved if required. No dependencies installed.
- **Reusable prompt:** Implement this bounded original-source workflow end to end, reusing fund NAV identity and existing retention/review conventions. Keep the source caveat attached to every displayed fee. Reject unsupported source semantics rather than inventing total expenses, risk or returns. Update tests, this task, parent SRC016, TODO and README; leave deterministic execution and gated commit to the user.

## Delivered workflow

`fund-factsheet-workflow.ts` defines source capture, explicit Direct/Regular AMFI binding, immutable edition/review and offline snapshot contracts. Migration120 retains parsed disclosures and append-only named-review receipts in PostgreSQL; exact original HTML and hash reuse Mongo `FundsRawStore`. Upload is bounded to1MB UTF8; malformed source is retained as quarantined, cannot publish, and has a downloadable plain-text original. No provider job or service was started.

`FundFactsheetController` publishes `/api/v1/fund-factsheets?schemeCode=...` and `/snapshot`; `OpsFundFactsheetController` provides queue/import/evidence/review. Publication reparses retained source bytes, checks exact decimal fee proof and requires a different named operator. Both AMFI plan identities must already be admitted, with exact scheme name and explicit plan fields. No real AMFI code was invented; missing legacy plan fields are not inferred. Review retains original NAV edition and complete relevant identity. Later source withdrawal or changed current plan/ISIN/category hides the factsheet. Factsheet withdrawal also removes it. History is explicitly bounded100 rather than silently truncated.

Shared web/Android reader `FundFactsheetReader` presents plan-specific BER, dated fund-level AUM versus average AUM, original source/hash/retrieval/review, loading/empty/error/retry. It always explains that BER excludes brokerage/transaction costs/statutory levies and is not established total cost. Operations has file upload, explicit permission covering storage/display/offline, named-plan review, original download, quarantine and withdrawal. Source/permission/session changes clear pending upload consent; retained HTML is never executed.

Offline `handleFundFactsheet` re-admits both original and current NAV identities against the installed fund snapshot, hides missing/withdrawn/changed mappings and rejects Operations writes. A truncated snapshot omitting the original NAV binding deliberately hides that factsheet. Offline copies cannot know later network withdrawals until refreshed; snapshot-time status remains explicit.

Authored API1910–1912 exercise retention, independent actors, swapped plan rejection, retained original, NAV/factsheet withdrawal, quarantine and exact source/cost semantics. Actual Operations→reader WEB1910 runs on desktop/mobile; OFFLINE1910 covers installed receipts, missing NAV and changed ISIN denial. Fixtures reconstruct original grammar and independently transcribe August2026 fee/AUM facts, with explicitly synthetic908001/908002 AMFI test bindings. They are not official original bytes, real scheme-code claims or executed validation.

Manual next actions: user runs `pnpm db:migrate` for120 with local PostgreSQL/MongoDB configured, restarts API/web as needed, then `pnpm sdlc "Add reviewed AMC factsheet fees" -- --grep "1910|1911|1912"`. Open printed dev URL → Operations → Fund factsheets; after independent publication, Funds and bonds → mapped scheme shows its fee plan and dated size. Refresh offline bundle/rebuild Android APK through existing manual workflow. Report failing case/project plus saved SDLC/E2E run directory and error-context. No dependencies changed; no tests, formatting, checks, builds, migrations, provider jobs or commits executed. Existing HEAD remains `a2c53a0`; this and earlier team changes await user-run gates.

Remaining broader parent work: other AMC factsheet layouts, TER/transaction-cost methodology, source rights activation, actual AMFI code admission and independent user-run verification. None is inferred from this bounded fee disclosure.

## Actual AMFI identity compatibility —15September2026

Ordinary read-only HTTPS retrieval of [AMFI current NAVAll.txt](https://portal.amfiindia.com/spages/NAVAll.txt) succeeded. Original complete-file SHA256: `c43c3d5ebcc6805d2792db0d645ec62ff6ea98a84026baf0ec10668d6a9a04c5`. Its rows dated11September2026 identify exact scheme name `Kotak Multi Asset Omni FOF`, AMC `Kotak Mahindra Mutual Fund`, category `Open Ended Schemes(Other Scheme - FoF Domestic)`, explicit `Direct Plan` or `Regular Plan`, and separate option fields. Existing strict matching already accepts these actual fields; no guessed alias or code mapping was needed.

Public reference identities:119777 Direct/Growth (`INF174K01LN3`);119776 Direct/IDCW payout (`INF174K01LO1`, reinvestment`INF174K01LP8`);102574 Regular/Growth (`INF174K01468`);102573 Regular/IDCW payout (`INF174K01484`, reinvestment`INF174K01476`). These are dated public reference facts, not automatically activated application mappings. The operator must still select admitted source rows and another named operator review both mappings. API/Operations workflow fixtures retain separately labelled synthetic908001/908002 codes.

Authored API1913 captures this actual spelling/plan compatibility using a minimal reconstructed official section with source hash; it rejects reversed plans, missing plan, different scheme and different AMC. No parser or test was executed against this source during research; only original text and selected identity rows were inspected. Manual selected validation may include `--grep "1910|1911|1912|1913"`. No API ingestion, provider job, service or commit was run.

## Pagination specification

Static audit found that Operations silently selected its first100 captures; the public reader instead returned an explicit error above100, making old reader editions inaccessible. Add deterministic `(retrievedAt,id)` keyset pages of at most30, validated cursor/limit, current scheme filtering before the bound, next/previous controls, and reset on scheme/session changes. Page continuation is based on scanned candidates so withdrawn NAV mappings cannot strand older admissible factsheets. Snapshots remain complete-or-error: they must never return a first-page fragment as a complete bundle. No new source format or database model is needed. Cases1914–1915 author boundary/continuation/snapshot coverage with browser1914 and offline1910; no execution.

Pagination authored: Operations and public reader now accept `cursor` (validated `ISO timestamp|UUID`) and `limit`1–30, default30. Responses add nullable `nextCursor`; old single-page responses default it to null. Ordering uses retrieval time and UUID descending, with tuple keyset continuation. Public scheme filtering happens before the page limit; expired NAV bindings can produce an empty page with a continuation cursor, so older valid entries remain reachable. Both UIs have Older/Previous controls; scheme/session changes reset traversal. Complete snapshot endpoint retains its explicit100-candidate limit and returns503 above it; an incomplete page with non-null cursor is rejected as an offline snapshot. Offline installed-reader pagination follows the same admitted-edition ordering.

API1914 covers operations/public page continuation, invalid limits/cursors and complete small snapshots; API1915 simulates101 records in the disposable schema from an actual retained/published source binding and asserts snapshot503 plus usable paged reader. WEB1914 reaches the31st actual retained capture and returns; OFFLINE1910 additionally rejects a partial-page snapshot. No new migration/dependency. Manual smallest pagination command: `pnpm sdlc "Paginate reviewed factsheet history" -- --grep "1914|1915|E2E-OFFLINE-1910"` with databases/API/web running. No commands/gates/commit were executed by the agent; HEAD remainsa2c53a0.

## Current acceptance review — 20 September 2026

Reviewed scope: Initial Kotak original HTML factsheet retention, explicit Direct and Regular plan bindings, exact BER and AUM/AAUM semantics, independent publication and withdrawal, source re-admission, paginated Operations and reader history, complete-or-error snapshots, replay authorization after storage waits and offline admission. BER is not total expense ratio. Actual source permission and activation, independent operational original review and physical-device release certification remain separate. This does not complete wider parent scope.

The required API, browser-project and offline case IDs are now explicit in [acceptance.json](acceptance.json). Only actual current receipts plus successful normal gates can close this scope. Existing API/contracts/database/source workflows are reused; a matrix correction itself adds no migration or source permission. Physical-device and deployment claims require their separate evidence. The user authorized this validation/repair run; older manual-only handoff wording is historical for this run.

No additional input is needed for this bounded automated scope. Run the complete required matrix and review attached keyboard/narrow-layout artifacts where applicable before claiming accepted delivery.

<!-- sdlc-validation:start -->

## Automated validation

Stale — rerun required. [Evidence](../validation/README.md); [bugs](../bugs/README.md). Latest reconciliation: 1789923079896-69469.
<!-- sdlc-validation:end -->
