# SRC-017 — India G-sec/yield curve source onboarding (P1)

- **Status:** Partial — indicative yields and sourced historical sovereign settlement authored; broader curve coverage pending
- **Implemented / recorded:** Original CCIL NSS static parameters and101 reported zero-rate points per date (126/1970–1974), plus indicative-yield HTML parser, permission-gated capture/review, immutable evidence/history, shared reader/offline and cases authored. Matched April2026 RBI auction/Gazette terms/NSE identity/RBI conventions original-pack retention, independent exact source-version review, historical clean/accrued/dirty calculator and shared offline reader authored (121;1920–1921).
- **Pending:** Model interpolation/compounding conventions and current secondary-price coverage; actual written deployment permission and manual validation. One historical sovereign auction is not live quotation or corporate credit/liquidity coverage.
- **Next action / inputs:** Developer: research remaining current secondary/zero-curve originals; the matched historical source pack is now authored. Source rights and manual acceptance remain separate.
- **Verification:** The preserved evidence below applies only to its recorded scope/revision. This tracker migration did not run validation.

## Implementation handoff rule

After each change, update the summary above and the matching [TODO row](../../TODO.md). Keep prompts, detailed scope, remaining work, verification evidence and handoff commands in this file. Follow [task maintenance](README.md); a parent stays partial while a child requirement is missing.

## Preserved specification, prompts and history

The entries below are migrated records, not new execution instructions or current test-pass claims. The current summary above takes precedence where older statuses differ.

### SRC-017 — India G-sec/yield curve source onboarding (P1)

- **Implementation:** Deferred
- **Verification:** Not run
- **Dependencies:** DEV-003, DEV-004, DEV-005; P1 gate
- **Context:** README.md sections 10.6–10.10, source order 17, P1
- **Scope and acceptance:** Onboard India G-sec/yield curve. Starting public candidates: RBI/FBIL/CCIL public reports. Paid upgrade candidates: CCIL/Bloomberg/LSEG. Next action from the accepted plan: Validate benchmark rights and bond calculator inputs. Document actual access/usage rights, effective/retrieval times, units, revisions, fixtures, quarantine and reconciliation. Source register currently says Not started; never mark production without user-provided approval and validation evidence.
- **E2E cases:** Planned scenarios in tests/e2e/plans/product-coverage.md#src-017; add stable executable IDs when implemented
- **Evidence / blockers:** No implementation or verification evidence yet; dependencies and required product/source approvals remain open.
- **Manual next actions:** After implementation, user runs relevant checks and selected E2E cases listed in the handoff; attach evidence before marking verified.

**Codex prompt**

> Read AGENTS.md, README.md, TODO.md task SRC-017, README.md sections 10.6–10.10, source order 17, P1, and the linked E2E coverage plan. Follow the standard task prompt contract in TODO.md. Work on India G-sec/yield curve source onboarding (P1). Scope and acceptance: Onboard India G-sec/yield curve. Starting public candidates: RBI/FBIL/CCIL public reports. Paid upgrade candidates: CCIL/Bloomberg/LSEG. Next action from the accepted plan: Validate benchmark rights and bond calculator inputs. Document actual access/usage rights, effective/retrieval times, units, revisions, fixtures, quarantine and reconciliation. Source register currently says Not started; never mark production without user-provided approval and validation evidence. Dependencies: DEV-003, DEV-004, DEV-005; P1 gate. Do not invent missing domain/provider fields or claim future functionality. If a product decision is unresolved, record it as a blocker and progress independent authored work. Add/update API and/or browser acceptance cases (or manual document-review scenarios if there is no executable behavior), including prerequisites and meaningful failures. Update this task, test catalogue and relevant README details. Do not run installation, formatting, lint, builds, tests, browser checks, service changes or migrations. Leave the local commit to the user-run pnpm sdlc format/check gate; never push. Give exact manual next actions.

<a id="src-018"></a>

## Reusable task prompt

Read AGENTS.md, the task-maintenance guide and this task’s current summary. Work only on SRC-017 unless the user expands the scope. Treat the preserved specification/history as context; current user instructions take precedence. Implement the listed remaining acceptance end to end, or reconcile recorded completion evidence if no implementation remains. Keep tests, documentation and the root index consistent. Record exact remaining work and who needs to act. Do not execute deterministic validation or commit without the user-authorized gates.

## Primary source access review — 2026-09-15

[Actual URLs, access results and remaining format/rights evidence](../development/fund-lookthrough-source-research.md). Public disclosure links do not establish a parser schema or redistribution permission. No provider ingestion/test was run and no private input was assumed.

## Original CCIL indicative yield implementation — 2026-09-15

[Specification, researched conventions, complete workflow and manual commands](../development/ccil-yields.md). Implemented original public HTML acquisition/import, strict eleven-tenor table parsing, raw retention/quarantine, independent named publish/withdraw, source history, web/shared Android UI and offline snapshot admission. Source is disabled by default. API1570–1572, WEB1570 and OFFLINE1570 authored; migration095 authored. No tests or live source activation ran. These observations have source security labels, not verified ISIN identities, and no clean prices or ratings; bond pricing/curve parent scope remains incomplete.

Input record: no implementation input was needed for this public-format adapter. Actual written CCIL commercial retention and web/APK distribution permission is required before operator activation through server config. The agent neither obtained nor inferred it. Gated MTM downloads were not bypassed. Next independent step: research an accessible original security/price feed or accept an operator-provided unchanged permitted file; define its genuine units and conventions before code.

Static integration correction: CCIL controller paths now inherit the global `/api/v1` prefix exactly once; public and Operations routes match their reader/client/test paths. Existing API1571–1572 are the authored route regression. No tests run.

2026-09-15 static integration correction: CCIL final named-session admission now follows replay/new edition queries, so expiry during storage waits cannot return protected results or commit new work. API1573 authored for actual blocked replay, expiry and401. Manual gated validation remains pending; source activation inputs and unsupported bond identity/price scope above are unchanged.

## 2026-09-15 auction-to-calculator admission specification

Acquisition research now has matching original RBI auction-result and announcement HTML. This resolves auction-body availability, not security identity. Preserve the exact result/announcement pair and source date; require separately retained, independently reviewed original security terms matched by actual ISIN, issuer, coupon and exact maturity. Before any calculator use, establish coupon dates/frequency, settlement, day-count and clean/dirty quotation basis from the applicable original; cutoff and noncompetitive primary-auction prices remain separate, and neither becomes a current secondary quote. Missing or conflicting terms must deny calculator admission in API and shared web/offline; no approximate cashflow or guessed identifier fallback. Issuance terms for another auction/security cannot satisfy the linkage. Keep revisions, original hashes, withdrawal and source rights separate.

See the dated research section in [fund-lookthrough-source-research.md](../development/fund-lookthrough-source-research.md) for URLs, actual HTTP outcomes and hashes. No new parser/API/DB/UI was authored because the requested linked-price acceptance cannot yet be satisfied; existing indicative-yield reader remains unchanged. Migration111 and cases1790–1799 are reserved only, not authored/passed. Manual research acceptance: confirm both original publications share the chosen auction, inspect an independently obtained security-terms original for actual ISIN and full calendar, and ensure the tracker does not describe an auction price as a secondary quote. No services/dependencies/migration are required for this document review. User may run `pnpm sdlc "Record RBI auction linkage evidence" --checks-only`; no checks/tests/build/provider ingestion/commit were run. HEAD remains `a2c53a0`; shared work remains awaiting manual gates.

## Historical sovereign source pack specification — 2026-09-15

Independently review one matched original pack: RBI10April2026 auction result62540; Gazette271595 reissue terms; NSE31August2026 CompositeGsec factsheet identity; RBI2020 Gsec primer conventions. Preserve exact bytes/hash of each original, fixed original URLs, known publication dates and unknown upload retrieval time. A named independent reviewer checks the original extraction; envelope recognition is not automatic PDF interpretation. Record IN0020250091/6.48%GS2035/6October2035 maturity; auction cutoff96.67 and weighted96.69 are distinct historical price-per100 observations, not current quotes or execution prices. Settlement13April2026; accrued6–12April; 30/360; semiannual6April/6October; repaypar. Registry/API/shared reader/offline admits only released immutable packs; withdrawal removes current admission. Historical calculator takes nominal INR face value and explicit cutoff/weighted selection, computes aggregate clean/accrued/dirty amounts by integer arithmetic with half-up paise rounding. It never rewrites holdings or generates future holiday-adjusted cashflows or trades. Existing user-entered actual-period comparator remains distinct. Raw source store and migration121 edition/review rows; no credentials/provider activation.

### Historical sovereign implementation handoff

Contracts/API/121 tables/shared reader/Operations/offline calculator are authored. Original upload is explicit human transcription review, not PDF semantic parsing: separate named reviewer checks all originals and binds exact source hashes plus terms version; envelope/hash checks alone never publish. Raw evidence remains inspectable and withdrawal removes calculator admission. Uploaded retrieval time is unknown, capture time is separate; NSE identity date is as-of, not publication. Nominal calculation values are transient and are not persisted.

Authored cases: API1920 (independence/exact settlement/withdrawal), API1921 (missing originals/quarantine/replay), WEB1920 (actual reader calculation/price choice), WEB1921 (actual four-file Operations capture), OFFLINE1920 (downloaded version/withdrawal/no private mutation). Fixtures explicitly simulate envelopes and review; they do not claim semantic validation of genuine provider documents. User separately compares the real retained originals against every fixed term and verifies mobile/native file selection and keyboard/error recovery.

Manual next actions: existing PostgreSQL/MongoDB and app prerequisites; run `pnpm db:migrate` for121, then `pnpm sdlc "Add reviewed historical sovereign settlement" -- --grep "E2E-(API|WEB|OFFLINE)-192[012]"`. Open printed web URL `/#funds-bonds`, Operations → Sovereign bonds. Expect independent review then ₹10000 nominal cutoff clean₹9667.00/accrued₹12.60/dirty₹9679.60; weighted dirty₹9681.60; withdrawal prevents fresh calculation. Report run ID, selected project/test and saved error context for failures. No dependencies changed. No deterministic command was run; no commit created; existing HEAD a2c53a0 and shared uncommitted work remain pending manual gates.

WEB1922 covers rejected replacement of an original: the prior kind entry and consent are cleared before reading a replacement, including cancelled selection; oversize/read failures cannot silently reuse the old original.

## CCIL NSS original-parameter specification — 2026-09-15

Add a dedicated, disabled-by-default original CCIL NSS parameter capture. The actual static HTML table `advancedSearch` contains seven columns Date, ß0, ß1, ß2, ß3, tau1, tau2. Preserve exact decimal strings, positive decay parameters, strict dates and unique rows; never confuse NSS with older NS or indicative YTM tables. Retain original HTML hash and capture time, private quarantine, independent named review and withdrawal, explicit history bounds, shared reader and offline snapshot. Parameters describe a fitted sovereign curve, not security prices or portfolio cashflows. No interpolated yields are exposed until maturity/compounding conventions and independent curve reconciliation are verified. Future automatic source acquisition remains off; explicit original fetch/upload requires server-configured written storage/web/offline permission. Authored acceptance1970–1972 covers parser contradictions, real retention and independent review, actual file-upload Operations, reader and offline withdrawal. Source metadata documents missing original publication hour; source midnight date encoding is not publication time.

### NSS and original zero-rate delivery

Original static parameter capture and inert literal-JSON reported-point capture are authored end to end in separate `ccil-zero-curve` files, migration126, dedicated default-disabled gate, immutable Mongo raw/PostgreSQL editions/reviews, named independent release, source-bound reconstruction, shared Funds and bonds reader/Operations and downloaded Android handler. Both original sources were actually downloaded during research; no production ingestion happened. Original points include101displayed maturity labels0.0–50.0, excluding the unused50.5sentinel. The source table's missing maturity-unit and compounding declaration is explicit, so no new valuation/interpolation or security quote is inferred. This completes bounded original parameter/point workflow authoring, not current executable bond prices or live permission.

API1970–1974 cover exact independently read numeric facts, grammar/duplicate/key/date rejection, disabled gate, actual retention/review/withdrawal and source substitution. WEB1970–1971 actual parameter/point upload, independent review and reader; OFFLINE1970–1971 installed published/withdrawn/disabled behavior. Fixtures reconstruct source data and simulate operator permissions. No validation executed.

User next actions: existing PostgreSQL/MongoDB/API/web prerequisites, `pnpm db:migrate` for126; `pnpm sdlc "Add reviewed CCIL curve originals" -- --grep "E2E-(API|WEB|OFFLINE)-197[0-4]"`. Open printed app URL → `/#funds-bonds`; Operations → CCIL NSS. Live activation separately requires genuine `CCIL_ZERO_PERMISSION_REFERENCE` covering storage/web/offline and `CCIL_ZERO_ENABLED`; default stays off. No dependency changed. User manually refreshes the offline bundle/rebuilds APK to include reviewed captures. Expect original coefficients, source points, explicit missing conventions, withdrawal removing fresh snapshot admission; report failing project/case/run directory/error context. No checks/builds/tests/migrations/services/commits run; HEAD `a2c53a0`, shared team changes awaiting user gates.

### Curve history capacity specification and delivery

Replace the100-capture Operations dead end with timestamp/UUID keyset pages (30max), next/previous controls in both source queue and connected reader, strict cursor validation and deterministic tie handling. Withdrawal remains reachable beyond101captures. Snapshots remain complete-or-explicit-error at100admitted editions, separate from connected pagination; offline schemas reject a continuation-bearing fragment and provide local pagination over a complete snapshot. Public date labels now distinguish recent capture from most recent source effective date. API1975 uses an explicit disposable101-edition volume fixture derived from a real retained/reviewed original, asserts both full traversals,400invalidcursor,503incomplete snapshot and successful withdrawal restoring100snapshot. WEB1975 covers actual queue/reader Older/Previous transitions; OFFLINE1976 refuses a partial snapshot. No new migration beyond126 or dependencies. User command adds `pnpm sdlc "Paginate CCIL curve history" -- --grep "E2E-(API|WEB|OFFLINE)-197[5-6]"`; existing services/app/migration prerequisites apply. No gates or commit run.

Capture transport follow-up: API1977 covers decoded500KB original with expanded JSON whitespace and multibyte overflow rejected before retention; dedicated transport3.1MB does not enlarge decoded source limit. See CCIL source documentation. Authored only.

Convention research follow-up: [CCIL source note](../development/ccil-zero-curve.md) now records the bundled Node legacy-reader inventory and a successfully downloaded original2005 NSS methodology PDF. It confirms historical percentage/year discussion but not the current table's explicit compounding contract. Original parameter/point workflow completion is separate from optional curve valuation; no invented labels or new pricing prerequisite added. No execution/gates.

## Saved full-inventory repair — 2026-09-19

The saved full run1789752953639-97020 includes failed cases tagged to this task. Confirmed causes, scoped authored repairs and remaining verification are recorded in [the full-audit RCA](../development/full-audit-2026-09-19.md). User runs `SDLC_AUTO_REPAIR=0 pnpm sdlc "Repair full audit failures" --story SDLC-REPAIR-016`. No new passing evidence or automatic bug resolution is claimed; this bounded repair does not remove broader source/device/functional requirements recorded above.

<!-- sdlc-validation:start -->

## Automated validation

Failed — unresolved bug. [Evidence](../validation/README.md); [bugs](../bugs/README.md). Latest reconciliation: 1789837057413-22117.
<!-- sdlc-validation:end -->
