# UX-002C — Real brief, Explore and contextual reading

2026-09-15 follow-up: [READER-DIAGNOSTICS-001](READER-DIAGNOSTICS-001.md) adds a safe incident/category/phase to non-HTTP reader failures and API120 error output. The original503 cause is still unresolved; mandatory evaluation capture is preserved. API1250 authors isolated missing-table/recovery coverage; no execution claimed.

- **Status:** Implemented; validation pending
- **Implemented / recorded:** - Status/dependencies: Implemented for fixed permitted Fed RSS metadata, existing World Bank annual data and sourced glossary. Broader source onboarding remains open. Parents DEV-003–006/015/016/018/020; relevant SRC-004–012.
- **Pending:** E2E-API-120 public item GET is confirmed HTTP 503; the underlying sanitized transaction cause is still needed before a production repair is safe.
- **Next action / inputs:** Parent/user supplies the underlying sanitized transaction error for the confirmed 503; another unchanged retry alone cannot identify it.
- **Verification:** The preserved evidence below applies only to its recorded scope/revision. This tracker migration did not run validation.

## Implementation handoff rule

After each change, update the summary above and the matching [TODO row](../../TODO.md). Keep prompts, detailed scope, remaining work, verification evidence and handoff commands in this file. Follow [task maintenance](README.md); a parent stays partial while a child requirement is missing.

## Preserved specification, prompts and history

The entries below are migrated records, not new execution instructions or current test-pass claims. The current summary above takes precedence where older statuses differ.

### UX-002C — Real brief, Explore and contextual reading

- **Status/dependencies:** Implemented for fixed permitted Fed RSS metadata, existing World Bank annual data and sourced glossary. Broader source onboarding remains open. Parents DEV-003–006/015/016/018/020; relevant SRC-004–012.
- **Outcome/layers:** Source ingestion/review/publication → versioned feed → typographic Scan/Stories → reader/terms/search → corrections; strict API/data and recorded operator-triggered ingestion runs.
- **Codex prompt:** Select and verify one real news source's ingestion/display/summary rights. Build canonical item/version/topic/evidence contracts, additive Postgres schemas, Mongo raw evidence, deduplicated jobs and operator review/publication/withdrawal. Expose stable cursor feed/search/term APIs and implement the approved readable Scan/Stories collection, contextual detail, hover/focus preview, end-of-brief and exact Back. Keep annual macro labelled annual and refuse generated live news. Define freshness/importance/relevance independently, group duplicate events and retain correction lineage. Include real provider integration, missing/invalid/quarantine cases, keyboard/phone reading and docs; no mock product feed or placeholder pass.
- **Acceptance:** UX-ACC-007–010 and B reader cases; both backend publication and user reading must work before completion.

## Reusable task prompt

Read AGENTS.md, the task-maintenance guide and this task’s current summary. Work only on UX-002C unless the user expands the scope. Treat the preserved specification/history as context; current user instructions take precedence. Implement the listed remaining acceptance end to end, or reconcile recorded completion evidence if no implementation remains. Keep tests, documentation and the root index consistent. Record exact remaining work and who needs to act. Do not execute deterministic validation or commit without the user-authorized gates.

## Input and pickup decision — 2026-09-15

- **Readiness:** Validation only
- **User input needed now:** No for the independent next step.
- **Decision:** No new feature input needed. Implementation is already recorded; do not put this in the implementation queue solely because tests are unrun. Match saved failures to this task before authoring a repair.
- **Recorded answer / authority:** Existing user instruction: agent owns research/implementation decisions within scope; user owns deterministic validation.
- **Question status:** None now. Do not ask for a repeat of existing answers.
- **Later input trigger:** User-owned SDLC/test evidence is needed for verification. The report observed during triage is incomplete; no new full run is requested.
- **Next action:** Await completed saved-run evidence; agent fixes specific failures without rerunning the suite.
- **Research/evidence:** See [dated source checks and existing answers](input-research-2026-09-15.md). Source-specific permissions, complete parser layouts and legal classification are not claimed resolved by triage.
- **Completion boundary:** This updates readiness only, not test passes, live activation or full feature completion. On later pickup, refresh saved evidence and update this record plus the TODO row.

## Scoped failure repair — 2026-09-15

- **Input:** User supplied E2E-API-120 failure at discovery.spec.ts:94 after successful publication and stale-review rejection. No run timestamp, run ID, GET status or server-side cause was supplied. Whole-suite reports were not read, per the current instruction.
- **Status:** Diagnosis blocked; regression assertions authored, validation pending. No production fix claimed.
- **Specification:** Public GET must return HTTP 200 and the exact reviewed edition after a stale review receives 409. Repeating GET must preserve the same edition, including when its evaluation capture already exists. Keep strict FeedItemSchema validation, evidence/feed checks, real Fed ingestion, protected operations and cleanup.
- **Implementation:** Add status-before-schema assertions for the failing GET and a repeated real GET with exact edition equality. This exposes the transport failure without copying arbitrary response bodies into diagnostics.
- **Evidence:** DiscoveryStore.item calls admitPublications, publicEdition and recordPublicView in a transaction. Non-HTTP failures become a generic 503. recordPublicView inserts into evaluation_public_views, introduced by migration056. Missing schema/grants or another storage failure is possible but not established by the supplied ZodError. No SQL exception or HTTP status is available to distinguish those from other causes.
- **Remaining blocker:** Obtain the scoped GET HTTP status and, for 503, the matching sanitized server/database error code and relation name. Review applied migration056 and runtime privileges locally; do not share credentials or source payloads. Do not bypass capture, relax validation or apply speculative schema changes.
- **Layers:** API/contracts reuse existing strict schemas; database/source paths remain unchanged. No new UI, navigation, keyboard/mobile/visual behavior, financial calculations, automation, fixtures, dependencies or migrations. Existing browser acceptance remains applicable; this diagnostic change needs no new browser case.
- **Documentation acceptance:** README, TODO, catalogue and coverage plan distinguish an unresolved runtime failure from authored regression coverage. Parent UX-002 remains partial.
- **Reusable repair prompt:** Inspect only E2E-API-120 and its discovery read/publication/evaluation path using the newly supplied status and sanitized cause. Fix the demonstrated cause, preserve strict schemas and real integration assertions, and update this record. Authoring/read-only inspection only; do not run commands for validation, migrate, commit or delegate.

### Manual handoff

Run only:

```sh
pnpm e2e:run 'tests/e2e/cases/api/discovery\.spec\.ts' --project=api --grep 'E2E-API-120 real feed review, withdrawal and protected operations @UX-002 @external$'
```

Requires the existing current API, PostgreSQL/MongoDB, configured operations key/origin and actual Fed network access. No UI URL applies to this API case. No dependency changes. The parent owns rebuilding/retrying; this attempt ran no validation, services, ingestion or migrations. Expected: HTTP200, exact reviewed edition on both reads,409 for stale review, existing evidence/feed/auth/cleanup assertions pass. On failure report only this case's run ID/time, status/assertion and sanitized cause. No tests passed claim; no commit made. Existing HEAD was09b3b1e; unrelated pre-existing working changes are preserved.

## Scoped 503 follow-up — 2026-09-15

- **Input:** Exact API120 case, exit 1, discovery.spec.ts:96: expected 200, received 503 after successful review and stale-review rejection. Run ID/time and underlying exception were not supplied. The current user prohibited reading whole-suite reports; none were read.
- **Finding:** `DiscoveryStore.transaction` replaces every non-HTTP exception with the same 503 and does not retain/log its cause. `item` admits the selected publication, applies `publicEdition`, records the public evaluation view and commits. For a published item, `publicEdition` returns the admitted edition unchanged. The capture SQL uses `ON CONFLICT DO NOTHING`; migration056 declares its table and matching revision foreign key. Static inspection establishes no failing statement or demonstrated code defect. Missing schema, privileges, timeout, persisted-data validation or other transaction failures cannot be distinguished by this response.
- **Disposition:** Diagnosis blocked. No production change, schema/grant change, capture bypass or speculative fix. The existing real-source status, exact-edition and repeated-read regression assertions are retained; no redundant case was added. API/contracts, UI/UX, database, provider ingestion, automation and dependencies are unchanged. Browser, keyboard, mobile and visual acceptance are unaffected. Parent UX-002 remains partial.
- **Required evidence:** Parent/operator must obtain a sanitized underlying exception category and failing operation from this exact request using local diagnostics. For a database exception, provide only SQLSTATE and a non-sensitive relation/constraint name where available; do not share credentials, SQL parameters, source bodies or private data. The current wrapper discards the cause, so ordinary public response output cannot supply it. No infrastructure action is authorized by this record.
- **Manual documentation acceptance:** README and TODO now acknowledge the confirmed 503 without claiming the root cause or a fix. Catalogue/coverage notes preserve the regression requirements and pending validation.
- **Verification/handoff:** No formatting, checks, builds, tests, services, migrations, ingestion, installs or commits were executed. The exact smallest validation command and prerequisites remain in Manual handoff above; an unchanged retry may reproduce the same blocker. This attempt only updates README, TODO, this task, the E2E catalogue and coverage plan. All pre-existing code/documentation changes and untracked task/archive records remain uncommitted and preserved. HEAD remains `09b3b1e`.

### Repeated supplied 503 — scoped inspection

The next supplied failure is again the HTTP 200 assertion at line 96 receiving 503, without a run timestamp, run ID or underlying exception. Read-only inspection of `DiscoveryStore.item`, its transaction wrapper, publication admission, edition rendering, evaluation recording and migration056 found no demonstrated root cause. The SQL capture already uses `ON CONFLICT DO NOTHING`; removing capture or changing schema/privileges without evidence would be speculative. No runtime repair is claimed.

Existing E2E-API-120 assertions already cover the meaningful regression: strict reviewed-edition equality after a rejected stale mutation and an identical repeated real GET. They are preserved without adding a redundant case. The remaining input is a sanitized failing operation and exception category (SQLSTATE for a database error), obtained by the operator from this exact request. A repeated public status alone cannot distinguish admission, validation, capture or commit failure.

This attempt changes only README, TODO and this blocker record. Manual documentation acceptance: all three must describe the unresolved cause and pending verification consistently; no pass or completed parent is implied. No tests, formatting, checks, builds, services, migrations, ingestion, installation, delegation or commits were performed. The exact case command and service prerequisites above remain applicable; UI and dependency changes are not applicable. All pre-existing changes are retained.

## Current acceptance review — 20 September 2026

Reviewed scope: Original UX-ACC007–010 and referenced B reader scope: real permitted-source capture/review/versioned publication, source/topic/search filtering, stable collection cursors, typographic Scan/Stories, contextual full reader and terms/hover/focus preview, corrections/history, exact return navigation, explicit incoming-reading staging/apply preserving selected identity, no-action end-of-brief and complete keyboard/phone/assistive reading. Broader source catalogue onboarding is separate, but the explicit duplicate-event grouping requirement is not dropped.

The required API, browser-project and offline case IDs are now explicit in [acceptance.json](acceptance.json). Only actual current receipts plus successful normal gates can close this scope. Existing API/contracts/database/source workflows are reused; a matrix correction itself adds no migration or source permission. Physical-device and deployment claims require their separate evidence. The user authorized this validation/repair run; older manual-only handoff wording is historical for this run.

Remaining gates:

- Review and implement/verify explicit duplicate-event grouping required by the task. Current append item-ID deduplication does not establish grouping distinct source releases of one event. Do not substitute URL ingestion deduplication for event grouping. This is agent-actionable behavior, not a permission gate.
- Complete UX-ACC-010 assistive reading order/contrast/large-text and B-reader nested focus/Back restoration acceptance, including UX-ACC-006 iOS Safari/Android Chrome physical keyboard/safe-area scope. WEB135 emulation does not establish physical-device or assistive acceptance.

## Focused acceptance repair — 20 September 2026

WEB137 confirms the public continuation cursor was sent to the private feed decoder, producing400. Keep continuation on the endpoint admitted by page1; authenticated session loss still rejects mixed pages. Regression requires complete33-item collection and no public cursor sent to private feed.

Evidence: connected run2026-09-19T19-40-29-161Z-37323 completed76 passes and10 failures. Repair authored; subsequent validation pending. User explicitly authorized agent execution for this closure pass. Reuse the existing specification and full acceptance matrix; no dependencies or migrations added.

<!-- sdlc-validation:start -->

## Automated validation

Stale — rerun required. [Evidence](../validation/README.md); [bugs](../bugs/README.md). Latest reconciliation: outage-final-1789852687840.
<!-- sdlc-validation:end -->
