# UX-002G — Mobile quality and user-affinity acceptance

- **Status:** Source-flow implementation authored; device/user acceptance pending
- **Implemented / recorded:** Historical integration/viewport evidence is preserved below. Source loading/retry, input-selection integrity, price-history keyboard focus, commodity edition Back/exact retry and narrow-screen styles are authored, not executed.
- **Pending:** Physical iOS/Android, screen-reader, user research and field performance/rollout acceptance are missing.
- **Next action / inputs:** User runs authored source-flow cases and supplies physical-device/screen-reader observations; agent fixes concrete reported gaps.
- **Verification:** The preserved evidence below applies only to its recorded scope/revision. This tracker migration did not run validation.

## Source publishing acceptance repair — 2026-09-17

Saved browser failures show source Operations cases either starting bootstrap mode while requesting named login, or reaching the named sign-in screen instead of the requested source section. Scope: configure the five named review suites explicitly, install only the selected actor’s actual Operations cookie in the browser, and assert the browser session matches the independently authenticated actor before exercising source forms. Preserve all real API/storage calls, permission rejection and independent publication checks; never bypass authentication. No backend/database migration or app behavior change is required for these fixture defects. Validation remains manual; do not close saved bugs without a passing rerun.

## Implementation handoff rule

## Retained-history navigation specification — 2026-09-15

Static inspection found two concrete source-reader issues: price pagination removes its focused button while loading and leaves keyboard focus without a meaningful destination; commodity edition changes clear the selector on failure, expose only refresh-current and ignore same-component hash/back changes. Required behavior: explicit price page/range requests focus the loaded results heading; failure focuses the error; initial automatic loads never steal focus. Commodity edition selection must have a stable URL, browser Back must reload that edition, and retry must preserve the exact failed edition. Long source hashes/links stay within the mobile viewport. Reuse unchanged API/data contracts and existing source admission. Author browser1830–1831 against actual isolated source APIs with only explicit simulated transport failure; no browser execution or physical-device acceptance claim.

Implementation authored: price results/error focus targets and commodity hash-aware edition navigation, selected-edition retry, focus recovery and shared wrapping styles. A narrow commodity API static correction also re-admits the actor after storage reads on idempotent capture/review returns. WEB1830 and WEB1831 exercise actual retained API data, keyboard pagination and same-component Back; only the first selected-edition network failure is simulated. No new database model, migration, dependency or source-data change was needed. Shared Android/iOS reuse the reader code; physical device and screen-reader behavior remain manual acceptance.

Manual next action with configured API/web/PostgreSQL/Mongo and existing source migrations: `pnpm sdlc "Fix retained history navigation and recovery" -- --grep "E2E-WEB-183[01]"`. Open the printed app URL → company Price history / Monthly commodities. Expected: keyboard focus reaches new results or the error, retry requests the same selected edition, browser Back restores that edition, and no horizontal overflow. Report selected ID/project, run directory and failing assertion. No agent format/check/build/E2E/browser run or commit; last inspected HEAD is `a2c53a0`. Root tracker/catalog integration is coordinated separately.

## Source-screen static review scope — 2026-09-15

Acceptance for this pickup: source reader/Operations panels visibly announce loading and expose retry; long hashes, URLs, source JSON and nested fieldsets do not force horizontal page overflow; India macro has an explicit return link; crosswalk source choices are cleared when the company changes; selecting a new/invalid file never silently submits the previous file; asynchronous file reads cannot replace a later selection. Preserve native labels, buttons, details keyboard behavior and existing independent-review API gates. Author browser regression assertions; do not run app/browser validation. Physical-device, assistive-technology and visual acceptance remain pending.

Implementation authored: bounded wrapping/fieldset/preformatted source styles and focus indicators shared by company news, India/global macro, normalization and classification components; announced loading and disabled repeated refresh in source readers/queues; explicit India reader Back to More; clearing old crosswalk source choices on company change; source-file replacement clears both prior bytes and rights confirmation immediately, with stale FileReader/text results rejected by selection generation and unmount. India source-type changes also replace the file input. Changing company-news editorial/citation fields requires confirming the revised content again. Backend/contracts/database reuse unchanged because these defects concern selection and display, not a changed admitted data model.

Authored acceptance: WEB1321/1341 select synthetic file then oversize replacement and verify old bytes cannot remain submittable; WEB1360 verifies company change removes old classification choice; WEB1320 keyboard-activates Back; existing WEB1300/1340/1350 retain source navigation and narrow-width checks. No API test is invented for an unchanged API. Manual next action: `pnpm sdlc "Improve source workflow state and input integrity" -- --grep 'E2E-WEB-(1300|1320|1321|1340|1341|1350|1360)'` using configured DB/API/web, printed dev URL → #ops / #india-macro / #global-macro / company detail. Run desktop and mobile projects deliberately; report failed ID/project plus saved artifact. Dependencies and migrations unchanged. No format/check/test/build/migration/browser app QA/commit/push ran; physical Android, screen-reader and visual acceptance remain pending.

After each change, update the summary above and the matching [TODO row](../../TODO.md). Keep prompts, detailed scope, remaining work, verification evidence and handoff commands in this file. Follow [task maintenance](README.md); a parent stays partial while a child requirement is missing.

## Preserved specification, prompts and history

The entries below are migrated records, not new execution instructions or current test-pass claims. The current summary above takes precedence where older statuses differ.

### UX-002G — Mobile quality and user-affinity acceptance

- **Status/dependencies:** Automated API/browser integration passed for the delivered paths; viewport/media review recorded in status. Physical iOS/Android, screen-reader review, representative user research, field metrics and production rollout remain open release gates. Parents DEV-002/012/017/021.
- **Outcome/layers:** Exhaustive CTA regression, actual-phone interaction/accessibility/performance, representative-user findings, privacy/operations and measured rollout. Reuse implemented APIs/data; add any missing observable evidence.
- **Codex prompt:** Audit every released CTA against A's contract, then verify positive/negative/API/worker/browser paths with authorized execution. Include narrow/landscape/large-text/keyboard/safe-area and real iOS/Android Back/gestures, VoiceOver/TalkBack, reduced motion/data, visible-result and focus/return tests. Measure declared lab performance and field metrics when available; never claim field evidence from emulation. Collect consented usability findings from representative users, fix critical failures and evaluate clarity, useful return visits, saves revisited and reminder helpfulness with guardrail metrics. Confirm new-domain export/deletion, migrations/restore and worker/rights/correction operations. Record exact results, scope and open issues, update TODO/README, gate local commit and never push/deploy automatically.
- **Acceptance:** UX-ACC-026–028 plus all released package cases; user design acceptance remains distinct from automated passes.

## Reusable task prompt

Read AGENTS.md, the task-maintenance guide and this task’s current summary. Work only on UX-002G unless the user expands the scope. Treat the preserved specification/history as context; current user instructions take precedence. Implement the listed remaining acceptance end to end, or reconcile recorded completion evidence if no implementation remains. Keep tests, documentation and the root index consistent. Record exact remaining work and who needs to act. Do not execute deterministic validation or commit without the user-authorized gates.

## Input and pickup decision — 2026-09-15

- **Readiness:** Agent-ready
- **User input needed now:** No for the independent next step.
- **Decision:** No user input is needed for independent implementation/diagnosis. Use documented requirements; do not run deterministic gates under the existing execution boundary.
- **Recorded answer / authority:** Existing user instruction: agent owns research/implementation decisions within scope; user owns deterministic validation.
- **Question status:** None now. Do not ask for a repeat of existing answers.
- **Later input trigger:** Physical device and assistive-technology acceptance cannot be manufactured from code inspection; record real user feedback when available.
- **Next action:** Agent: audit accessibility/gesture code and author acceptance cases; user supplies physical-device observations later.
- **Research/evidence:** See [dated source checks and existing answers](input-research-2026-09-15.md). Source-specific permissions, complete parser layouts and legal classification are not claimed resolved by triage.
- **Completion boundary:** This updates readiness only, not test passes, live activation or full feature completion. On later pickup, refresh saved evidence and update this record plus the TODO row.

## Source Operations static integration correction — 2026-09-15

Audited source Operations request contracts across India macro, company news, governance, institutional flows, positioning, classification and price adjustments. No further fetch-options-to-json mutation mismatch was found after WEB1472/1522 correction. Concrete fixes: India source/JSON/permission edits, positioning permission/review edits and adjustment company/window/coverage/permission edits now invalidate the prior checkbox attestation; switching classification company starts a new mapping instead of accidentally editing the prior company's mapping ID; governance event pagination preserves already selected reviewed events; source401 clears pending file reads and protected local state immediately so a late response cannot restore it before unmount.

Authored WEB1590–1592 submit actual India CPI, institutional-flow and positioning source forms, re-confirm changed permission, then publish/withdraw through a distinct real named reviewer in isolated API/storage. Source fixture numbers/permissions remain explicitly simulated. These cases do not claim all Operations flows or the app visually verified. Existing governance/classification/adjustment API cases remain, and broader native/device acceptance stays pending. Manual: user `pnpm sdlc "Fix source Operations interaction boundaries" --grep "E2E-WEB-159[0-2]"` with databases/API/dev server and source migrations installed. Report failing ID, run folder and error context. No commands/tests/gates executed; a2c53a0 remains existing commit, changes await user gates.

Additional authored WEB1593 exercises the actual governance form, source/citation/company selection, saved simulation, distinct reviewer release and withdrawal. Its source preparation is API-backed, but all governance mutations originate from UI controls. Include it with `--grep "E2E-WEB-159[0-3]"`. This is authored acceptance, not a reported pass.

Remaining source Operations mutation coverage authored as WEB1594 (company report form with two independent source declarations, actual preparation and independent publish/withdraw), WEB1595 (load real admitted classification through UI, save exact mapping and independent publish/withdraw), WEB1596 (company/date/action file/coverage form, changed-permission checkbox reset and independent normalization publish/withdraw). Combined selected command: `pnpm sdlc "Validate source Operations submissions" --grep "E2E-WEB-159[0-6]"`. All use isolated actual APIs and named reviewers; primary source prerequisites are explicitly synthetic fixture admission, not live integration. No execution/pass claim. Crosswalk source selection and generated fixture financial records do not establish provider licensing.

### Daily oil edition navigation — 2026-09-15

Concrete static defect fixed: the new daily oil selector previously held edition only in React state, so browser Back could not restore the selected original. `EiaSpot.tsx` now reads/writes `#daily-oil?edition=<UUID>`, listens to same-component hash changes, retains exact failed target for retry and moves focus after user navigation to the result/error. Initial load does not take focus. WEB1943 authors actual two-edition retention/review plus one simulated network error, retry/Back and narrow viewport acceptance. Manual command and prerequisites are in [EIA handoff](../development/eia-daily-spot.md); tests and physical device acceptance were not run. No new migration/dependency or commit.

<!-- sdlc-validation:start -->

## Automated validation

Failed — unresolved bug. [Evidence](../validation/README.md); [bugs](../bugs/README.md). Latest reconciliation: 1789719520254-79325.
<!-- sdlc-validation:end -->
